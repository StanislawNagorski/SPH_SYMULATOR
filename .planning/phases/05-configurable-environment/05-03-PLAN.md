---
phase: 05-configurable-environment
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - sphsim/cli/output.py
  - sphsim/cli/repl.py
  - tests/test_env.py
autonomous: true
requirements: [ENV-03]
must_haves:
  truths:
    - "Every human-readable run begins with a Markdown config table listing all 9 environment parameters (nU, T, κ, α, K0, K1, φ, ρ, seed)"
    - "format_config_header is a reusable helper Phase 6 can call directly when generating report.md"
    - "REPL run and compare commands print the same config header (using DEFAULT_* values since REPL has no env-override flags)"
    - "JSON env block gains K0, phi, rho, seed, valuation keys (extending v1.0 schema; regression preserved via SKIP_KEYS in Plan 04)"
    - "Existing format_human output (banner, METRYKI, IC, VETO sections) remains intact — header is PREPENDED, not replaced"
  artifacts:
    - path: "sphsim/cli/output.py"
      provides: "format_config_header(args, K0, K1, phi, rho) -> str MD table + format_human calls it at top + format_json env block extended"
      contains: "def format_config_header"
      contains: "## Konfiguracja środowiska"
      exports: ["format_human", "format_json", "format_compare", "format_config_header"]
    - path: "sphsim/cli/repl.py"
      provides: "fake_args Namespace at both do_run and do_compare sites includes phi, rho, K0, valuation defaults"
      contains: "phi=DEFAULT_PHI"
      contains: "K0=DEFAULT_K0"
    - path: "tests/test_env.py"
      provides: "TestConfigHeader (unit) + TestHumanHeader (integration) replace the last 2 Plan 00 skip stubs"
      contains: "class TestConfigHeader"
  key_links:
    - from: "sphsim/cli/output.py:format_human"
      to: "sphsim/cli/output.py:format_config_header"
      via: "lines.insert(0, format_config_header(args, args.K0, K1, args.phi, args.rho))"
      pattern: "format_config_header\\("
    - from: "sphsim/cli/repl.py:do_run"
      to: "sphsim/cli/output.py:format_human → format_config_header"
      via: "fake_args.phi/rho/K0/valuation populated → no AttributeError"
      pattern: "phi=DEFAULT_PHI.*rho=DEFAULT_RHO.*K0=DEFAULT_K0"
---

<objective>
Deliver ENV-03 (SC-4): A reusable `format_config_header(args, K0, K1, phi, rho) -> str` function in `sphsim/cli/output.py` that emits a Polish Markdown table listing all 9 environment parameters. The function is called at the top of `format_human()` output (always-on, no flag), satisfying SC-4 immediately. Phase 6 will reuse the same function when generating `reports/<timestamp>/report.md`. The JSON `env` block is extended with K0, phi, rho, seed, valuation keys (regression will be preserved via SKIP_KEYS in Plan 04).

Critical landmine fix (RESEARCH §F.15 Pitfall 2): `format_config_header` reads `args.phi`, `args.rho`, `args.K0`, `args.valuation`. The REPL's `fake_args` Namespace at `repl.py:219-222` (do_run) and `repl.py:284-287` (do_compare) does NOT currently have these fields — calling `format_human` from the REPL would AttributeError. This plan extends BOTH `fake_args` constructions to include the four missing fields with DEFAULT_* values.

Purpose: Reproducibility — run parameters are visible at the top of every output. Phase 6 reuses the helper without duplicating MD-table logic. SC-4 satisfied today.

Output: `format_config_header` function in `output.py`; `format_human` prepends header; `format_json` extends env block with 5 keys; REPL `fake_args` at both call sites gets phi/rho/K0/valuation; TestConfigHeader + TestHumanHeader replace the last 2 Plan 00 skip stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-configurable-environment/05-RESEARCH.md
@.planning/phases/05-configurable-environment/05-PATTERNS.md
@.planning/phases/05-configurable-environment/05-VALIDATION.md
@.planning/phases/05-configurable-environment/05-02-SUMMARY.md
@sphsim/cli/output.py
@sphsim/cli/repl.py
@sphsim/config.py
@tests/test_env.py

<interfaces>
Existing format_human banner (sphsim/cli/output.py:101-106) — config header inserts BEFORE this block:
```
lines.append(f"\n{'='*62}")
lines.append(f"  SPH SYMULATOR  |  Strategia: {args.strategy.upper()}")
lines.append(f"  nU={args.nU}, nSUS={args.nSUS}, K1={K1}, T={args.T}, κ={args.kappa}, α={args.alpha}")
lines.append(f"{'='*62}")
```

Existing format_json env block (sphsim/cli/output.py:10-11) — to be EXTENDED with 5 keys:
```
'env': {'nU': args.nU, 'nSUS': args.nSUS, 'K1': K1,
        'T': args.T, 'kappa': args.kappa, 'alpha': args.alpha},
```
becomes:
```
'env': {'nU': args.nU, 'nSUS': args.nSUS, 'K0': args.K0, 'K1': K1,
        'T': args.T, 'kappa': args.kappa, 'alpha': args.alpha,
        'phi': args.phi, 'rho': args.rho, 'seed': args.seed, 'valuation': args.valuation},
```

Required format_config_header signature (RESEARCH §D.11 verbatim):
```
def format_config_header(args, K0, K1, phi, rho) -> str:
    """Serializuje konfigurację środowiska do tabeli Markdown (ENV-03, SC-4)."""
```

Required 9-row output (RESEARCH §D.11 verbatim):
```
## Konfiguracja środowiska

| Parametr | Wartość |
|----------|---------|
| nU       | {args.nU} |
| T        | {args.T} |
| κ (kappa) | {args.kappa} |
| α (alpha) | {args.alpha} |
| K0       | {K0} |
| K1       | {k1_display} |   ← '∞' when K1 == float('inf'), else str(K1)
| φ (phi)  | {phi_str} |     ← ', '.join(f'{v:.2f}' for v in phi)
| ρ (rho)  | {rho_str} |     ← ', '.join(f'{v:.2f}' for v in rho)
| seed     | {args.seed} |
```

REPL fake_args at do_run (repl.py:219-222):
```
fake_args = argparse.Namespace(
    strategy=name, nU=DEFAULT_NU, nSUS=DEFAULT_NSUS, T=DEFAULT_T,
    kappa=DEFAULT_KAPPA, alpha=DEFAULT_ALPHA, verbose=False, no_agent=False,
)
```
Must add: phi=DEFAULT_PHI, rho=DEFAULT_RHO, K0=DEFAULT_K0, valuation='window', seed=42

REPL fake_args at do_compare (repl.py:284-287) — identical structure, same fields needed.

DEFAULT_K0, DEFAULT_PHI, DEFAULT_RHO already imported in repl.py:32-35 (no new imports needed).

After Plan 02, args.K0 and args.valuation are guaranteed populated by argparse. After Plan 01, args.phi and args.rho are guaranteed populated. args.seed exists since v1.0.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add format_config_header + extend format_human/format_json in output.py</name>
  <files>sphsim/cli/output.py, tests/test_env.py</files>
  <behavior>
    TestConfigHeader (5 unit cases — direct calls to format_config_header):
    - test_header_contains_section_title: returned string contains literal `## Konfiguracja środowiska`.
    - test_header_contains_md_table_structure: returned string contains the table header row `| Parametr | Wartość |` and the separator row `|----------|---------|`.
    - test_header_contains_all_9_param_labels: returned string contains all 9 row labels: `nU`, `T`, `κ (kappa)`, `α (alpha)`, `K0`, `K1`, `φ (phi)`, `ρ (rho)`, `seed` (one assertIn per label).
    - test_header_renders_phi_and_rho_lists: with phi=[0.1, 0.2, 0.3, 0.4, 1.0] and rho=[0.5, 0.5, 0.7, 1.5, 3.0], returned string contains `0.10, 0.20, 0.30, 0.40, 1.00` and `0.50, 0.50, 0.70, 1.50, 3.00` (2 decimal places per RESEARCH §D.11).
    - test_header_renders_K1_inf_as_unicode: with K1=float('inf'), the K1 row contains the `∞` character (not `inf`).
  </behavior>
  <read_first>
    - sphsim/cli/output.py (full file — 169 lines)
    - .planning/phases/05-configurable-environment/05-RESEARCH.md §D.11 (verbatim format_config_header body)
    - .planning/phases/05-configurable-environment/05-PATTERNS.md (output.py section)
    - tests/test_env.py (TestConfigHeader currently skip stub)
  </read_first>
  <action>
    Step A — Edit `sphsim/cli/output.py`:

    1. Add a new function `format_config_header(args, K0, K1, phi, rho) -> str` BEFORE `format_compare()` (around line 25). Body verbatim from RESEARCH §D.11 (reproduced for traceability):
       ```
       def format_config_header(args, K0, K1, phi, rho) -> str:
           """Serializuje konfigurację środowiska do tabeli Markdown (ENV-03, SC-4).
           Zwracany string to walidna tabela MD — Phase 6 może go wkleić bezpośrednio do report.md.
           """
           phi_str = ', '.join(f'{v:.2f}' for v in phi)
           rho_str = ', '.join(f'{v:.2f}' for v in rho)
           k1_display = '∞' if K1 == float('inf') else str(K1)
           lines = [
               '## Konfiguracja środowiska',
               '',
               '| Parametr | Wartość |',
               '|----------|---------|',
               f'| nU       | {args.nU} |',
               f'| T        | {args.T} |',
               f'| κ (kappa) | {args.kappa} |',
               f'| α (alpha) | {args.alpha} |',
               f'| K0       | {K0} |',
               f'| K1       | {k1_display} |',
               f'| φ (phi)  | {phi_str} |',
               f'| ρ (rho)  | {rho_str} |',
               f'| seed     | {args.seed} |',
           ]
           return '\n'.join(lines)
       ```

    2. In `format_human()` (current line 96), prepend the config header. The cleanest insertion: right after the comparison early-return (line 99) and BEFORE `lines = []` is initialised on line 101. Insert:
       ```
       lines = [format_config_header(args, args.K0, K1, args.phi, args.rho), '']
       sep = '─' * 62
       lines.append(f"\n{'='*62}")
       ```
       (Replace the existing `lines = []` initialisation; `sep = '─' * 62` and subsequent appends remain unchanged. The empty string after the header creates visual breathing room before the SPH SYMULATOR banner.)

    3. In `format_json()` (current line 6), extend the env block to include 5 new keys:
       ```
       'env': {'nU': args.nU, 'nSUS': args.nSUS, 'K0': args.K0, 'K1': K1,
               'T': args.T, 'kappa': args.kappa, 'alpha': args.alpha,
               'phi': args.phi, 'rho': args.rho, 'seed': args.seed,
               'valuation': args.valuation},
       ```
       The order is: original 2 (nU, nSUS) → K0 (NEW) → original K1 → original 3 (T, kappa, alpha) → 4 NEW (phi, rho, seed, valuation). This keeps the original keys' relative order and groups the new keys logically.

    4. Do NOT change `format_compare()` — comparison output already has its own banner; the config header is only added to `format_human` single-run output. Phase 6 will decide whether to add it to compare-mode reports.

    Step B — Replace TestConfigHeader skip stub in tests/test_env.py with the 5 unit test methods specified in `<behavior>`. Use `import argparse` and construct a minimal `argparse.Namespace(nU=250, nSUS=20, T=1000, kappa=0.25, alpha=1, seed=42)` for each test — only the args fields read by format_config_header need to exist. Use `from sphsim.cli.output import format_config_header`. For each assertion, use `self.assertIn(literal_substring, header_str, msg=...)`.

    Leave TestHumanHeader as a skip stub for now — Task 2 owns that.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; python -c "import argparse; from sphsim.cli.output import format_config_header; a = argparse.Namespace(nU=250, nSUS=20, T=1000, kappa=0.25, alpha=1, seed=42); h = format_config_header(a, 100, 120, [0.1,0.2,0.3,0.4,1.0], [0.5,0.5,0.7,1.5,3.0]); assert '## Konfiguracja środowiska' in h; assert '| Parametr | Wartość |' in h; assert '0.10, 0.20, 0.30, 0.40, 1.00' in h; assert '∞' in format_config_header(a, 100, float('inf'), [0.1]*5, [0.5]*5); print('header OK')" &amp;&amp; python -m unittest tests.test_env.TestConfigHeader -v 2>&amp;1 | tail -3 | grep -E "^OK$|^OK \("</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from sphsim.cli.output import format_config_header"` succeeds.
    - `format_config_header(...)` output is a single string containing literal `## Konfiguracja środowiska`, `| Parametr | Wartość |`, `|----------|---------|`, and all 9 row labels.
    - φ list formatting: with phi=[0.1, 0.2, 0.3, 0.4, 1.0] the header contains `0.10, 0.20, 0.30, 0.40, 1.00`.
    - K1=float('inf') renders `∞` (Unicode), not the string `inf`.
    - `python -m unittest tests.test_env.TestConfigHeader -v` exits 0 with ≥5 tests PASS.
    - `python sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 2>&1 | head -3` shows `## Konfiguracja środowiska` as the first non-empty line.
    - `python sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 --json | python -c "import json,sys; d=json.load(sys.stdin); env=d['env']; assert 'K0' in env and 'phi' in env and 'rho' in env and 'seed' in env and 'valuation' in env"` exits 0.
    - Existing format_human sections (SPH SYMULATOR banner, METRYKI, IC tables) still appear AFTER the header (grep `--strategy naive` output for `SPH SYMULATOR` and `METRYKI`).
  </acceptance_criteria>
  <done>
    format_config_header exists, renders the 9-row MD table with Polish labels, handles K1=inf with ∞ glyph; format_human prepends the header; format_json env block extended with 5 keys; TestConfigHeader has ≥5 green tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend REPL fake_args at do_run and do_compare with phi/rho/K0/valuation/seed</name>
  <files>sphsim/cli/repl.py, tests/test_env.py</files>
  <read_first>
    - sphsim/cli/repl.py (lines 32-35 imports, lines 200-225 do_run, lines 260-289 do_compare)
    - .planning/phases/05-configurable-environment/05-RESEARCH.md §F.15 Pitfall 2 (REPL fake_args AttributeError)
    - .planning/phases/05-configurable-environment/05-PATTERNS.md (repl.py section)
    - tests/test_env.py (TestHumanHeader currently skip stub)
  </read_first>
  <action>
    Step A — Edit `sphsim/cli/repl.py`:

    1. In `do_run()` `fake_args` construction (currently lines 219-222), extend the Namespace to include the four new fields PLUS `seed` (currently missing from fake_args but required by format_config_header which reads args.seed). Final shape:
       ```
       fake_args = argparse.Namespace(
           strategy=name, nU=DEFAULT_NU, nSUS=DEFAULT_NSUS, T=DEFAULT_T,
           kappa=DEFAULT_KAPPA, alpha=DEFAULT_ALPHA, verbose=False, no_agent=False,
           phi=DEFAULT_PHI, rho=DEFAULT_RHO, K0=DEFAULT_K0, valuation='window',
           seed=42,
       )
       ```

    2. In `do_compare()` `fake_args` construction (currently lines 284-287), apply the identical extension (same 5 new fields).

    3. DEFAULT_K0, DEFAULT_PHI, DEFAULT_RHO are already imported at repl.py:32-35 (line 32 imports DEFAULT_K0; line 34 imports DEFAULT_PHI, DEFAULT_RHO). No new imports required.

    4. Do NOT extend the SPHSimulator calls inside `do_run` (line 209-214) or `do_compare` (line 258-262 common dict) to use args-derived phi/rho/K0/valuation_preset — RESEARCH §F.15 Open Question 1 confirms the REPL does NOT expose env-override flags (defer to Phase 6 or later). However, since Plan 02 changed SPHSimulator's signature to accept `valuation_preset='window'` as a keyword argument with default, the existing REPL SPHSimulator calls remain VALID — the default 'window' is consumed silently. No change needed there.

    Step B — Replace TestHumanHeader skip stub in tests/test_env.py with integration tests (3 cases):
    - test_human_output_starts_with_config_header: subprocess `python sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42` (no --json) exits 0; the first non-empty line of stdout is `## Konfiguracja środowiska`.
    - test_human_output_contains_full_table: same invocation; stdout contains all 9 row labels (use assertIn for each: `'nU'`, `'T'`, `'κ (kappa)'`, `'α (alpha)'`, `'K0'`, `'K1'`, `'φ (phi)'`, `'ρ (rho)'`, `'seed'`).
    - test_human_output_preserves_legacy_sections: same invocation; stdout contains `SPH SYMULATOR` (the v1.0 banner survives) AND `METRYKI` (the v1.0 metrics section survives). Proves header is PREPENDED, not REPLACING.

    All 3 tests use the existing `_run_sph` helper and `r.stdout` extraction. Each test has a Polish docstring.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; python -m unittest tests.test_env.TestHumanHeader -v 2>&amp;1 | tail -3 | grep -E "^OK$|^OK \(" &amp;&amp; python -m unittest discover tests/ -v 2>&amp;1 | tail -3 | grep -E "^OK$|^OK \(" &amp;&amp; echo "exit\n" | python -c "import sys, subprocess; r = subprocess.run([sys.executable, 'sph_sim.py', '--interactive'], input='run naive zeta=0.5\\nexit\\n', capture_output=True, text=True, timeout=15); assert r.returncode == 0, f'REPL crashed: {r.stderr[:400]}'; assert '## Konfiguracja środowiska' in r.stdout, f'REPL run output missing header: {r.stdout[:400]}'; print('REPL header OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'phi=DEFAULT_PHI' sphsim/cli/repl.py` returns ≥2 (one in do_run fake_args, one in do_compare fake_args).
    - `grep -c 'K0=DEFAULT_K0' sphsim/cli/repl.py` returns ≥2.
    - `grep -c "valuation='window'" sphsim/cli/repl.py` returns ≥2.
    - REPL session `run naive zeta=0.5\nexit\n` exits 0 WITHOUT AttributeError; stdout contains `## Konfiguracja środowiska` (proves fake_args extension defused Pitfall 2).
    - REPL session `compare naive zeta=0.5\nexit\n` exits 0 WITHOUT AttributeError (verify by similar piped subprocess; do_compare path also exercised).
    - `python -m unittest tests.test_env.TestHumanHeader -v` exits 0 with 3 tests PASS.
    - `python -m unittest discover tests/ -v` exits 0 overall (Phase 4 REPL tests still pass, Plan 01/02 tests still pass).
    - `grep -c "skipTest" tests/test_env.py` returns 0 (all 7 test classes now have real implementations).
  </acceptance_criteria>
  <done>
    REPL do_run and do_compare fake_args populated with phi/rho/K0/valuation/seed defaults; REPL `run` and `compare` sessions complete without AttributeError; TestHumanHeader has 3 green tests; all 7 test_env classes have real implementations (zero skipTest calls); full suite OK.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Simulation result → output formatter | No external input; format_config_header reads internal args + already-validated phi/rho lists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-03-INFO | Information disclosure | format_config_header emits seed in output | accept | Seed is intentional reproducibility metadata. SC-4 requires it. Not a secret. |
| T-5-03-INJ | Injection (markdown breakout) | If phi/rho values contained pipe characters or newlines, the MD table would visually break | accept | phi/rho are validated by Plan 01 converters as `list[float]`; floats serialize via `f'{v:.2f}'` producing only `[0-9.\-]` characters. No injection vector. |
| T-5-03-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new packages. |

No new attack surface.
</threat_model>

<verification>
After both tasks complete:

```bash
cd "$(git rev-parse --show-toplevel)"
python -m unittest tests.test_env -v                                       # 7 classes all green
python -m unittest discover tests/ -v                                      # full suite OK
python sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 | head -15      # header at top
python sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 --json | python -c "import json,sys; e=json.load(sys.stdin)['env']; print({k: e[k] for k in ('K0','phi','rho','seed','valuation')})"
printf 'run naive zeta=0.5\nexit\n' | python sph_sim.py --interactive | grep -c 'Konfiguracja środowiska'   # > 0
printf 'compare naive zeta=0.5\nexit\n' | python sph_sim.py --interactive | grep -c 'PORÓWNANIE'   # > 0
```

NOTE: regression_check.py will FAIL at this point because format_json env block has new keys not in baseline fixtures. That is EXPECTED — Plan 04 fixes via SKIP_KEYS extension. Do NOT attempt to patch fixtures; Plan 04 owns regression remediation.
</verification>

<success_criteria>
1. ENV-03 fully delivered: `format_config_header()` exists in output.py and is called at top of format_human (always-on, satisfies SC-4 now).
2. SC-4 satisfied: human-readable output begins with a Polish MD table containing all 9 environment parameters.
3. JSON env block extended with K0, phi, rho, seed, valuation keys.
4. REPL Pitfall 2 (RESEARCH §F.15) defused: fake_args at do_run and do_compare have all 5 new fields; REPL sessions complete without AttributeError.
5. All 7 test_env classes have real implementations (zero skipTest calls remain).
6. format_human existing sections (banner, METRYKI, IC, VETO) preserved — header is PREPENDED, not replacing.
</success_criteria>

<output>
Create `.planning/phases/05-configurable-environment/05-03-SUMMARY.md` when done. Include: files modified (3), exact lines added to output.py (format_config_header + format_human header insertion + format_json env extension), exact fake_args field additions in repl.py (do_run + do_compare), list of new test methods, confirmation all 7 test_env classes have real tests (skipTest count = 0), explicit note that regression_check.py is currently RED pending Plan 04 SKIP_KEYS extension.
</output>
