---
phase: 05-configurable-environment
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_env.py
  - scripts/verify_phase5.sh
autonomous: true
requirements: [ENV-01, ENV-02, ENV-03]
must_haves:
  truths:
    - "tests/test_env.py exists with 7 empty test classes that import without errors"
    - "scripts/verify_phase5.sh exists, is executable, and runs end-to-end producing PASS=0/FAIL=0 summary on an empty body"
    - "Subsequent waves have concrete test class names to GREEN against (no scavenger-hunt)"
  artifacts:
    - path: "tests/test_env.py"
      provides: "Stub container for 7 test classes covering ENV-01/02/03"
      contains: "class TestPhiRhoParsing"
      min_lines: 60
    - path: "scripts/verify_phase5.sh"
      provides: "Phase-5 exit-gate skeleton with check() helper, PASS/FAIL counters, Polish error preamble"
      contains: "PASS=$((PASS + 1))"
      min_lines: 50
  key_links:
    - from: "tests/test_env.py"
      to: "sphsim.cli.args, sphsim.core.model, sphsim.cli.output"
      via: "import statements present (even if classes empty)"
      pattern: "from sphsim"
---

<objective>
Scaffold the Wave 0 test container (`tests/test_env.py`) and the exit-gate skeleton (`scripts/verify_phase5.sh`) so subsequent waves have concrete file paths and class names to write against. NO Phase 5 features are implemented in this plan — only empty test stubs and a runnable verify-script skeleton.

Purpose: Per `05-VALIDATION.md` Wave 0 contract, Nyquist compliance requires the test file to exist before implementation tasks run. Without these stubs, Wave 1+ tests would either (a) be invented ad-hoc by the executor breaking the per-task verification map, or (b) skipped silently. This plan locks the test taxonomy.

Output: `tests/test_env.py` (7 empty test classes, all importable, all `pass`-bodied or `unittest.skip`-bodied), `scripts/verify_phase5.sh` (copy of Phase 4 skeleton with prefix renamed `p4_`→`p5_`, body checks empty/placeholder).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-configurable-environment/05-RESEARCH.md
@.planning/phases/05-configurable-environment/05-PATTERNS.md
@.planning/phases/05-configurable-environment/05-VALIDATION.md
@tests/test_args_agent_flags.py
@tests/test_agent.py
@scripts/verify_phase4.sh

<interfaces>
Test class taxonomy locked by 05-VALIDATION.md per-task verification map:

| Class name                       | Plan | Covers              |
|----------------------------------|------|---------------------|
| TestPhiRhoParsing                | 01   | ENV-01 argparse     |
| TestPhiRhoFlow                   | 01   | ENV-01 plumbing     |
| TestValuationDispatch            | 02   | ENV-02 unit         |
| TestValuationPresets             | 02   | ENV-02 integration  |
| TestPresetDistinguishability     | 02   | ENV-02 SC-3         |
| TestConfigHeader                 | 03   | ENV-03 unit         |
| TestHumanHeader                  | 03   | ENV-03 integration  |

Verify-script PASS/FAIL counter pattern from verify_phase4.sh:1-65 (PASS=0, FAIL=0, check() helper using eval+/tmp/p5_check.log, trap 'rm -f /tmp/p5_*' EXIT).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_env.py with 7 empty test classes</name>
  <files>tests/test_env.py</files>
  <read_first>
    - tests/test_args_agent_flags.py (lines 1-36 — file header + _PROJECT_ROOT + _run_sph helper)
    - tests/test_agent.py (lines 1-36 — subprocess pattern with MONOLITH path)
    - .planning/phases/05-configurable-environment/05-VALIDATION.md (per-task verification map for class names)
  </read_first>
  <action>
    Create `tests/test_env.py` per Polish UX standard D-17. File MUST contain:

    1. Module docstring (Polish): "Unit i integration tests dla Phase 5 (Configurable environment). Pokrywa ENV-01 (--phi/--rho), ENV-02 (--valuation/--K0), ENV-03 (config header). Stdlib only: unittest + subprocess + json + os + sys."
    2. Imports: `json`, `os`, `subprocess`, `sys`, `unittest`, `from pathlib import Path`.
    3. Module-level constants (copy from `tests/test_agent.py:24-35` pattern):
       ```
       _PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), os.pardir))
       if _PROJECT_ROOT not in sys.path:
           sys.path.insert(0, _PROJECT_ROOT)
       PROJECT_ROOT = Path(_PROJECT_ROOT)
       MONOLITH = PROJECT_ROOT / 'sph_sim.py'
       ```
    4. Helper `_run_sph(*args, **kwargs)` mirroring `tests/test_args_agent_flags.py:26-34` (subprocess.run with cwd=_PROJECT_ROOT, capture_output=True, text=True).
    5. Exactly 7 test classes in this order, each subclassing `unittest.TestCase`, each with a docstring in Polish naming the requirement, each containing exactly ONE method `def test_placeholder(self):` with body `self.skipTest("Wave N implementation — class name locked by Plan 00")` where N matches the per-task map:
       - `TestPhiRhoParsing` (ENV-01, Plan 01) — skip reason "Wave 1 implementation"
       - `TestPhiRhoFlow` (ENV-01, Plan 01) — skip reason "Wave 1 implementation"
       - `TestValuationDispatch` (ENV-02, Plan 02) — skip reason "Wave 2 implementation"
       - `TestValuationPresets` (ENV-02, Plan 02) — skip reason "Wave 2 implementation"
       - `TestPresetDistinguishability` (ENV-02 SC-3, Plan 02) — skip reason "Wave 2 implementation"
       - `TestConfigHeader` (ENV-03, Plan 03) — skip reason "Wave 3 implementation"
       - `TestHumanHeader` (ENV-03, Plan 03) — skip reason "Wave 3 implementation"
    6. Standard `if __name__ == '__main__': unittest.main()` footer.

    Skip-only bodies are CORRECT for Wave 0 — they prove the test file is discoverable, importable, and registers the right class names without false-positive PASS. Subsequent waves will REPLACE the `test_placeholder` skip with real test methods.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; python -m unittest tests.test_env -v 2>&amp;1 | grep -c "skipped" | { read n; [ "$n" -ge 7 ] || { echo "expected ≥7 skipped tests, got $n"; exit 1; }; }</automated>
  </verify>
  <acceptance_criteria>
    - `python -m unittest tests.test_env -v` exits 0 with at least 7 "skipped" entries (one per class).
    - `python -c "import tests.test_env"` runs without ImportError.
    - `grep -c '^class Test' tests/test_env.py` returns exactly 7 (header comments stripped naturally — class lines start at column 0 only).
    - All 7 class names from the per-task verification map are present (grep verifies each):
      `grep -E '^class (TestPhiRhoParsing|TestPhiRhoFlow|TestValuationDispatch|TestValuationPresets|TestPresetDistinguishability|TestConfigHeader|TestHumanHeader)\b' tests/test_env.py | wc -l` equals 7.
    - No use of `pytest` or non-stdlib imports.
  </acceptance_criteria>
  <done>
    File `tests/test_env.py` exists; `python -m unittest tests.test_env -v` reports `OK (skipped=7)`; all 7 class names from VALIDATION.md per-task map are grep-discoverable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/verify_phase5.sh skeleton</name>
  <files>scripts/verify_phase5.sh</files>
  <read_first>
    - scripts/verify_phase4.sh (lines 1-65 — full skeleton + check() helper)
    - .planning/ROADMAP.md (Phase 5 Success Criteria 1-4)
  </read_first>
  <action>
    Create `scripts/verify_phase5.sh` as a copy of `scripts/verify_phase4.sh` lines 1-65 SKELETON with the following exact deviations:

    1. Replace all `Phase 4` / `phase4` mentions with `Phase 5` / `phase5` in comments and echo lines.
    2. Replace all `/tmp/p4_*` and `/tmp/p4_check.log` with `/tmp/p5_*` and `/tmp/p5_check.log` (including the `trap 'rm -f /tmp/p4_*' EXIT` line at verify_phase4.sh:45).
    3. Header docstring (Polish, mirror verify_phase4.sh:1-28 style) MUST list 4 Phase 5 SCs verbatim from ROADMAP:
       - SC #1: --phi/--rho override (length=5, range [0,1] for φ, ≥0 for ρ)
       - SC #2: --valuation window|step|linear preset + --K0/--K1 override
       - SC #3: 3 presets give distinguishable KPI (same seed+strategy)
       - SC #4: human/MD report header contains nU, T, κ, α, K0, K1, φ, ρ, seed table
    4. Preserve `set -euo pipefail`, `cd "$(dirname "$0")/.."`, Python interpreter detection (lines 29-43), `trap`, `PASS=0`/`FAIL=0`, and `check()` function (lines 50-64) VERBATIM (except the prefix swap).
    5. After the `check()` definition, add the section banner only (no checks yet):
       ```
       echo "=== Phase 5: Configurable environment — verification ==="
       echo "Interpreter: $PY ($($PY --version 2>&1))"
       echo ""
       echo "── Phase 5 checks will be inserted by Plan 04 (Wave 4) ──"
       echo "(Wave 0 skeleton — no checks implemented yet)"
       echo ""
       ```
    6. Final block (mirror verify_phase4.sh:183-192 with Phase 5 wording):
       ```
       echo ""
       echo "════════════════════════════════════════"
       echo "  Phase 5 verification: PASS=$PASS / FAIL=$FAIL"
       echo "════════════════════════════════════════"
       if [ "$FAIL" -gt 0 ]; then
           echo "✗ Phase 5 NIE jest gotowe — popraw FAIL'e powyżej."
           exit 1
       fi
       echo "✓ Phase 5 ready for /gsd:verify-work"
       exit 0
       ```
    7. After creation, chmod 755 the file: `chmod +x scripts/verify_phase5.sh`.

    DO NOT add any `check "SC #1" "..."` invocations in this plan — Plan 04 (Wave 4) owns SC body implementation. The Wave 0 deliverable is the runnable skeleton that exits 0 with PASS=0/FAIL=0.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; bash scripts/verify_phase5.sh 2>&amp;1 | grep -E "Phase 5 verification: PASS=0 / FAIL=0" &amp;&amp; bash scripts/verify_phase5.sh; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify_phase5.sh` exits 0.
    - Output contains literal line `Phase 5 verification: PASS=0 / FAIL=0`.
    - Output contains literal line `✓ Phase 5 ready for /gsd:verify-work`.
    - File is executable: `test -x scripts/verify_phase5.sh`.
    - `grep -c 'p4_' scripts/verify_phase5.sh` returns 0 (no leftover Phase 4 prefixes).
    - `grep -c 'Phase 5' scripts/verify_phase5.sh` returns ≥ 6 (header docstring + section banner + summary block).
    - `grep -E '^check ' scripts/verify_phase5.sh | wc -l` returns 0 (no real check calls yet — Plan 04 owns those).
  </acceptance_criteria>
  <done>
    Skeleton verify_phase5.sh runs end-to-end, exits 0 with PASS=0/FAIL=0, has Phase 5 branding everywhere, is chmod +x, contains zero `check` calls (those belong to Plan 04).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer→test infra | No external input; test stubs run only locally |
| Developer→verify script | No untrusted input; script runs with developer privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-00-01 | Tampering | `/tmp/p5_check.log` (verify script log file) | accept | Single-user developer machine; `trap 'rm -f /tmp/p5_*' EXIT` cleans up. Low value target — log content is verify output only, no secrets. |
| T-5-00-SC | Tampering | npm/pip installs | mitigate | N/A — Phase 5 introduces zero new packages (stdlib only, per RESEARCH §"Don't Hand-Roll"). No `pip install` lines anywhere. |

Phase 5 introduces zero new attack surface (Wave 0 is test stubs + shell skeleton with no user input). T-5-01 and T-5-02 (argparse input validation) defer to Plan 01 (where the actual validators are introduced).
</threat_model>

<verification>
After both tasks complete:

```bash
cd "$(git rev-parse --show-toplevel)"
python -m unittest tests.test_env -v 2>&1 | tail -5      # ≥7 skipped, OK
bash scripts/verify_phase5.sh 2>&1 | tail -10            # PASS=0/FAIL=0, exit 0
python -m unittest discover tests/ -v 2>&1 | tail -3     # Phase 4 suite still green
python scripts/regression_check.py                       # 8/8 still PASS
```

All four MUST succeed before declaring Plan 00 done.
</verification>

<success_criteria>
1. `tests/test_env.py` exists with 7 named test classes (per Plan 00 taxonomy lock); `python -m unittest tests.test_env` reports 7 skipped, exit 0.
2. `scripts/verify_phase5.sh` exists, is chmod +x, runs to completion with `PASS=0 / FAIL=0` and exits 0.
3. Phase 4 test suite (`python -m unittest discover tests/`) remains green — no collateral damage.
4. Regression check (`scripts/regression_check.py`) remains green — no collateral damage.
</success_criteria>

<output>
Create `.planning/phases/05-configurable-environment/05-00-SUMMARY.md` when done. Include: files created (2), test class names locked (list of 7), confirmation that Phase 4 suite + regression still green.
</output>
