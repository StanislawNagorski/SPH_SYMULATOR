---
phase: 05-configurable-environment
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - scripts/regression_check.py
  - scripts/verify_phase5.sh
autonomous: true
requirements: [ENV-01, ENV-02, ENV-03]
must_haves:
  truths:
    - "scripts/regression_check.py PASS 8/8 after SKIP_KEYS extension — v1.0 baseline contract preserved without regenerating fixtures"
    - "scripts/verify_phase5.sh runs end-to-end and exits 0 with PASS≥21 / FAIL=0, covering all 4 ROADMAP Phase 5 SCs"
    - "verify_phase5.sh check bodies are executable as written (no manual edits required between plan-write and gate-run)"
    - "Exit gate is the single canonical pre-flight before /gsd:verify-work"
  artifacts:
    - path: "scripts/regression_check.py"
      provides: "SKIP_KEYS extended with Phase 5 env keys (K0, phi, rho, seed, valuation)"
      contains: "K0', 'phi', 'rho', 'seed', 'valuation'"
    - path: "scripts/verify_phase5.sh"
      provides: "All 4 ROADMAP SCs covered as check() invocations + regression + full test suite + test_env.py specifically"
      contains: "check \"SC #"
      min_lines: 150
  key_links:
    - from: "scripts/verify_phase5.sh"
      to: "ROADMAP Phase 5 Success Criteria 1-4"
      via: "explicit check() per SC with grep/python-c assertion bodies"
      pattern: "SC #(1|2|3|4)"
    - from: "scripts/regression_check.py:SKIP_KEYS"
      to: "deep_diff env-block tolerance"
      via: "K0/phi/rho/seed/valuation added to filter tuple"
      pattern: "'K0', 'phi', 'rho', 'seed', 'valuation'"
---

<objective>
Close Phase 5 by extending `scripts/regression_check.py` SKIP_KEYS (Phase 4 D-67 strategy) to tolerate the 5 new JSON env keys introduced in Plan 03, then flesh out the `scripts/verify_phase5.sh` skeleton (Plan 00) with all check() invocations for the 4 ROADMAP Success Criteria + regression + test suite. After this plan the v1.0 baseline contract is fully restored AND the exit gate verifies the entire phase end-to-end.

Purpose: Final wave before `/gsd:verify-work`. Two scripts → one decision: green gate means Phase 5 ships.

Output: 1 SKIP_KEYS tuple extension in regression_check.py (with D-style rationale comment); full check() body insertion in verify_phase5.sh (covering SC #1 phi/rho validation + flow, SC #2 valuation preset + K0 override, SC #3 preset distinguishability, SC #4 config header in human + JSON env, plus regression and unittest discover gates).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-configurable-environment/05-RESEARCH.md
@.planning/phases/05-configurable-environment/05-PATTERNS.md
@.planning/phases/05-configurable-environment/05-VALIDATION.md
@.planning/phases/05-configurable-environment/05-03-SUMMARY.md
@scripts/regression_check.py
@scripts/verify_phase5.sh
@scripts/verify_phase4.sh

<interfaces>
regression_check.py SKIP_KEYS state after Phase 4 (line 41):
```
SKIP_KEYS = ('veto_per_phase', 'n_vetoed_total', 'agent_enabled')
```
Plan 04 extends to (D-67-style Strategia B):
```
SKIP_KEYS = (
    'veto_per_phase', 'n_vetoed_total', 'agent_enabled',  # Phase 4 D-67
    'K0', 'phi', 'rho', 'seed', 'valuation',              # Phase 5 D-PH5 SKIP-EXT
)
```
The deep_diff function (regression_check.py:60-61) already strips SKIP_KEYS from BOTH expected and actual key sets, so the 5 new env keys appearing only in actual are silently dropped from comparison. No other code changes needed.

verify_phase5.sh skeleton state after Plan 00:
- Header docstring listing 4 SCs ✓
- set -euo pipefail, cd, PY detection, trap, PASS/FAIL counters, check() function ✓
- Banner echo + placeholder "── Phase 5 checks will be inserted by Plan 04 ──"
- PASS=$PASS / FAIL=$FAIL summary block + exit 0/1 ✓

Plan 04 REPLACES the placeholder banner with full check() invocations.

ROADMAP Phase 5 Success Criteria mapped to check() invocations:

SC #1 — phi/rho overrides + validation:
- accept valid phi: `--phi 0.1,0.2,0.3,0.4,1.0` exits 0
- reject wrong length: `--phi 0.1,0.2,0.3` exits 2 with 'dokładnie 5' in stderr
- reject out-of-range phi: `--phi 0.1,0.2,0.3,0.4,1.5` exits 2 with 'poza zakresem' in stderr
- reject negative rho: `--rho 0.5,0.5,0.7,1.5,-3.0` exits 2 with 'ujemne' in stderr

SC #2 — valuation preset + K0 override:
- `--valuation window` exits 0
- `--valuation step` exits 0
- `--valuation linear` exits 0
- `--valuation foobar` exits 2 (argparse choices)
- `--K0 90 --K1 150` exits 0

SC #3 — three presets distinguishable:
- run window/step/linear with same seed+strategy via inline python -c; assert all 3 KPI values pairwise distinct

SC #4 — config header in human output AND env block in JSON:
- human-readable output contains `## Konfiguracja środowiska` (grep)
- human-readable output contains all 9 row labels (one grep per label, or single multi-grep)
- JSON env block contains K0, phi, rho, seed, valuation (python -c json check)

Plus regression + test suite gates:
- python scripts/regression_check.py exits 0
- python -m unittest discover tests/ exits 0
- python -m unittest tests.test_env -v exits 0 with all 7 classes green

Pattern for check() invocations from verify_phase4.sh:90-130:
```
check "DESC" "CMD || ANNOT"
```
Use $PY (the interpreter detected at script top), not literal `python`.

Pipe-safe grep pattern from verify_phase4.sh:122: `{ cmd || true; } | grep PATTERN > /dev/null`.

JSON assertion via inline python -c: `... | $PY -c \"import json,sys; d=json.load(sys.stdin); assert ...\"`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SKIP_KEYS in regression_check.py with 5 Phase 5 env keys</name>
  <files>scripts/regression_check.py</files>
  <read_first>
    - scripts/regression_check.py (lines 35-42 — current SKIP_KEYS + Phase 4 D-67 comment)
    - .planning/phases/05-configurable-environment/05-RESEARCH.md §F.15 (Pitfall 3: JSON env block extension regression)
    - .planning/phases/05-configurable-environment/05-PATTERNS.md (regression_check.py section)
  </read_first>
  <action>
    Edit `scripts/regression_check.py` at line 41 (the SKIP_KEYS tuple definition):

    1. Replace the current single-line definition:
       ```
       SKIP_KEYS = ('veto_per_phase', 'n_vetoed_total', 'agent_enabled')
       ```
       with a multi-line, commented version that ADDS the 5 Phase 5 env-block keys:
       ```
       SKIP_KEYS = (
           'veto_per_phase', 'n_vetoed_total', 'agent_enabled',  # Phase 4 D-67 Strategia B
           'K0', 'phi', 'rho', 'seed', 'valuation',              # Phase 5 ENV-03 (D-PH5 SKIP-EXT, mirroring D-67)
       )
       ```

    2. Add a Polish rationale comment ABOVE the new line (extend the existing comment block at lines 35-40), mirroring the Phase 4 D-67 explanation style:
       ```
       # Phase 5 (Strategia B, mirror Phase 4 D-67): pięć nowych kluczy w env bloku
       # (K0, phi, rho, seed, valuation) ignorowane przy compare z baseline_v1 fixtures —
       # fixtures są oracle dla zachowania v1.0 i nie zawierają tych pól. Pola obecne
       # tylko w actual output po Plan 03 rozszerzeniu format_json env block.
       ```

    Do NOT touch any other line in `regression_check.py`. Do NOT regenerate fixtures (Strategia B is explicit non-regeneration). Do NOT touch generate_baseline.py.

    Total edit: ~7 lines modified at line 41 area; new comment block ~4 lines above.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; python scripts/regression_check.py 2>&amp;1 | grep -E "^PASS: 8/8$"</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/regression_check.py` exits 0 and stderr contains literal line `PASS: 8/8`.
    - `grep -c "'K0', 'phi', 'rho', 'seed', 'valuation'" scripts/regression_check.py` returns 1 (exact substring present once).
    - `grep -c "'veto_per_phase', 'n_vetoed_total', 'agent_enabled'" scripts/regression_check.py` returns 1 (Phase 4 entries preserved).
    - `python -c "from scripts.regression_check import SKIP_KEYS; assert set(SKIP_KEYS) >= {'K0','phi','rho','seed','valuation','veto_per_phase','n_vetoed_total','agent_enabled'}, SKIP_KEYS"` succeeds (works only if scripts/__init__.py exists; if not, use: `python -c "import sys; sys.path.insert(0,'scripts'); from regression_check import SKIP_KEYS; assert set(SKIP_KEYS) >= {'K0','phi','rho','seed','valuation'}; print(SKIP_KEYS)"`).
    - `python -m unittest discover tests/` still green (no side effects on test infra).
  </acceptance_criteria>
  <done>
    SKIP_KEYS includes both Phase 4 (3 keys) and Phase 5 (5 keys) entries with rationale comments; regression_check.py PASS 8/8 restored.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flesh out scripts/verify_phase5.sh with all check() invocations covering 4 SCs + regression + tests</name>
  <files>scripts/verify_phase5.sh</files>
  <read_first>
    - scripts/verify_phase5.sh (current state — Plan 00 skeleton with placeholder banner)
    - scripts/verify_phase4.sh (full file — analog for check body patterns, especially lines 70-180)
    - .planning/ROADMAP.md (Phase 5 SC #1-4 verbatim wording)
    - .planning/phases/05-configurable-environment/05-RESEARCH.md §E.13 (8 baseline invocations preserved)
  </read_first>
  <action>
    Edit `scripts/verify_phase5.sh`: REPLACE the placeholder banner block (the lines `── Phase 5 checks will be inserted by Plan 04 ──` and `(Wave 0 skeleton — no checks implemented yet)`) with the full check() invocation set below. Keep the file header docstring, set/cd/PY-detect/trap/PASS-FAIL/check() function definition (Plan 00 skeleton) UNCHANGED. Keep the final summary block (PASS=$PASS / FAIL=$FAIL + exit 0/1) UNCHANGED.

    Insert these sections in order (each with its own echo banner). Use `$PY` (not literal `python`) and follow the pipe-safe grep pattern `{ ... || true; } | grep PATTERN > /dev/null`. Quote-escape inline python `-c` snippets per the verify_phase4.sh:93-98 style.

    ── 1. Regression backwards compat (CLI-04 from Phase 1) ──
    ```
    echo ""
    echo "── 1. Regression backwards compat (CLI-04 z Phase 1) ──"
    check "Regression: 8/8 baseline_v1 fixtures (SKIP_KEYS extended dla Phase 5)" \
        "$PY scripts/regression_check.py"
    ```

    ── 2. Full test suite (Phase 2/3/4 invariants + Phase 5 nowe) ──
    ```
    echo ""
    echo "── 2. Full test suite (Phase 2/3/4 invariants + Phase 5 nowe) ──"
    check "Unittest discover tests/ — wszystko zielone" \
        "$PY -m unittest discover tests"
    check "Phase 5 tests/test_env.py specifically — 7 klas green" \
        "$PY -m unittest tests.test_env"
    ```

    ── 3. SC #1 — phi/rho overrides + walidacja (ENV-01) ──
    ```
    echo ""
    echo "── 3. SC #1: --phi/--rho override + walidacja (ENV-01) ──"
    check "SC #1 (accept): --phi 0.1,0.2,0.3,0.4,1.0 + --rho 0.5,0.5,0.7,1.5,3.0 → exit 0" \
        "$PY sph_sim.py --strategy naive --zeta 0.5 --phi 0.1,0.2,0.3,0.4,1.0 --rho 0.5,0.5,0.7,1.5,3.0 --no-agent --seed 42 --json > /dev/null"
    check "SC #1 (reject length): --phi 0.1,0.2,0.3 → exit 2 + 'dokładnie 5' w stderr" \
        "{ $PY sph_sim.py --strategy naive --zeta 0.5 --phi 0.1,0.2,0.3 --no-agent --seed 42 2>&1 || true; } | grep 'dokładnie 5' > /dev/null"
    check "SC #1 (reject range phi>1): --phi 0.1,0.2,0.3,0.4,1.5 → exit 2 + 'poza zakresem' w stderr" \
        "{ $PY sph_sim.py --strategy naive --zeta 0.5 --phi 0.1,0.2,0.3,0.4,1.5 --no-agent --seed 42 2>&1 || true; } | grep 'poza zakresem' > /dev/null"
    check "SC #1 (reject rho<0): --rho 0.5,0.5,0.7,1.5,-3.0 → exit 2 + 'ujemne' w stderr" \
        "{ $PY sph_sim.py --strategy naive --zeta 0.5 --rho 0.5,0.5,0.7,1.5,-3.0 --no-agent --seed 42 2>&1 || true; } | grep 'ujemne' > /dev/null"
    ```

    ── 4. SC #2 — valuation preset + K0 (ENV-02) ──
    ```
    echo ""
    echo "── 4. SC #2: --valuation preset + --K0/--K1 (ENV-02) ──"
    check "SC #2 (window default): --valuation window → exit 0" \
        "$PY sph_sim.py --strategy naive --zeta 0.5 --valuation window --no-agent --seed 42 --json > /dev/null"
    check "SC #2 (step): --valuation step → exit 0" \
        "$PY sph_sim.py --strategy naive --zeta 0.5 --valuation step --no-agent --seed 42 --json > /dev/null"
    check "SC #2 (linear): --valuation linear → exit 0" \
        "$PY sph_sim.py --strategy naive --zeta 0.5 --valuation linear --no-agent --seed 42 --json > /dev/null"
    check "SC #2 (reject preset): --valuation foobar → exit 2 (argparse choices)" \
        "{ $PY sph_sim.py --strategy naive --zeta 0.5 --valuation foobar --no-agent --seed 42 2>&1 || true; } | grep -- '--valuation' > /dev/null"
    check "SC #2 (K0 override): --K0 80 → exit 0 + parsowalny JSON" \
        "$PY sph_sim.py --strategy naive --zeta 0.5 --K0 80 --no-agent --seed 42 --json | $PY -c 'import json,sys; d=json.load(sys.stdin); assert d[\"env\"][\"K0\"]==80.0, d[\"env\"][\"K0\"]'"
    check "SC #2 (K0+K1 override): --K0 90 --K1 150 → exit 0 + parsowalny JSON" \
        "$PY sph_sim.py --strategy naive --zeta 0.5 --K0 90 --K1 150 --no-agent --seed 42 --json > /dev/null"
    ```

    ── 5. SC #3 — 3 presety dają distinguishable KPI (ENV-02 SC-3) ──
    ```
    echo ""
    echo "── 5. SC #3: 3 presety → distinguishable KPI (ENV-02 SC-3) ──"
    check "SC #3: window/step/linear KPI parami różne na seed=42 + naive zeta=0.5" \
        "$PY -c \"
    import json, subprocess, sys
    def run(preset):
        r = subprocess.run([sys.executable, 'sph_sim.py', '--strategy', 'naive', '--zeta', '0.5', '--valuation', preset, '--no-agent', '--seed', '42', '--json'], capture_output=True, text=True)
        assert r.returncode == 0, f'{preset} exit={r.returncode} stderr={r.stderr[:200]}'
        return json.loads(r.stdout)['metrics']['avg_val_last100']
    w, s, l = run('window'), run('step'), run('linear')
    assert w != s, f'window={w} == step={s} (PITFALL 1: sph_stp nie threading preset)'
    assert s != l, f'step={s} == linear={l}'
    assert w != l, f'window={w} == linear={l}'
    print(f'OK distinct: window={w} step={s} linear={l}')
    \""
    ```

    ── 6. SC #4 — config header w human + JSON env block (ENV-03 SC-4) ──
    ```
    echo ""
    echo "── 6. SC #4: nagłówek konfiguracji środowiska (ENV-03 SC-4) ──"
    check "SC #4 (human): output zawiera sekcję '## Konfiguracja środowiska'" \
        "{ $PY sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 || true; } | grep '## Konfiguracja środowiska' > /dev/null"
    check "SC #4 (human): tabela MD zawiera nagłówek '| Parametr | Wartość |'" \
        "{ $PY sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 || true; } | grep '| Parametr | Wartość |' > /dev/null"
    check "SC #4 (human): tabela zawiera wszystkie 9 etykiet (nU, T, kappa, alpha, K0, K1, phi, rho, seed)" \
        "{ $PY sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 || true; } | grep -E 'nU|T|κ \\(kappa\\)|α \\(alpha\\)|K0|K1|φ \\(phi\\)|ρ \\(rho\\)|seed' | wc -l | { read n; [ \"\$n\" -ge 9 ] || exit 1; }"
    check "SC #4 (JSON): env block zawiera 5 nowych kluczy (K0, phi, rho, seed, valuation)" \
        "$PY sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 --json | $PY -c 'import json,sys; e=json.load(sys.stdin)[\"env\"]; [e[k] for k in (\"K0\",\"phi\",\"rho\",\"seed\",\"valuation\")]; print(\"env keys OK\")'"
    check "SC #4 (legacy preserved): output nadal zawiera 'SPH SYMULATOR' banner i 'METRYKI' sekcję" \
        "{ $PY sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 || true; } | grep -E 'SPH SYMULATOR|METRYKI' | wc -l | { read n; [ \"\$n\" -ge 2 ] || exit 1; }"
    ```

    ── 7. REPL Pitfall 2 defused (D-PH5-Pitfall-2) ──
    ```
    echo ""
    echo "── 7. REPL Pitfall 2 (fake_args AttributeError) defused ──"
    check "REPL: 'run naive zeta=0.5' nie crashe na AttributeError; output zawiera nagłówek" \
        "printf 'run naive zeta=0.5\\nexit\\n' | $PY sph_sim.py --interactive 2>&1 | grep 'Konfiguracja środowiska' > /dev/null"
    check "REPL: 'compare naive zeta=0.5' nie crashe na AttributeError; output zawiera PORÓWNANIE" \
        "printf 'compare naive zeta=0.5\\nexit\\n' | $PY sph_sim.py --interactive 2>&1 | grep 'PORÓWNANIE' > /dev/null"
    ```

    After insertion, target total PASS count ≥ 21 (4 SC#1 + 5 SC#2 + 1 SC#3 + 5 SC#4 + 2 REPL + 1 regression + 2 test-suite + 1 buffer for grep heuristics). FAIL MUST equal 0.

    The check() definitions and PASS/FAIL summary tail (Plan 00) remain unchanged — only the inner section between `echo "=== Phase 5: Configurable environment — verification ==="` and the final `if [ "$FAIL" -gt 0 ]` block is replaced.

    After saving, ensure file remains executable: `chmod +x scripts/verify_phase5.sh`.

    NOTE on heredoc-style python -c: use `\"` for inner double-quotes and `\\\"` only if needed for nested escapes; the verify_phase4.sh:131-145 pattern uses single-quote outer Python `-c` strings with `"` JSON keys inside — adopt the same convention. If a particular check is hard to write portably, prefer extracting the python logic into a small `tools/p5_*.py` helper (analogous to verify_phase4.sh:121-127 inline scripts) — but DO NOT add helpers unless absolutely necessary.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; bash scripts/verify_phase5.sh 2>&amp;1 | tee /tmp/p5_verify.out | tail -10 &amp;&amp; grep -E "^  Phase 5 verification: PASS=([2-9][0-9]|[1-9][0-9]{2,}) / FAIL=0$" /tmp/p5_verify.out</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify_phase5.sh` exits 0.
    - Output contains literal `Phase 5 verification: PASS=N / FAIL=0` where N ≥ 20 (regex `PASS=[2-9][0-9] / FAIL=0` or `PASS=[1-9][0-9]{2,} / FAIL=0`).
    - Output contains literal line `✓ Phase 5 ready for /gsd:verify-work`.
    - `grep -cE '^check ' scripts/verify_phase5.sh` returns ≥ 19 (counting only top-level check invocations).
    - `grep -c 'SC #1' scripts/verify_phase5.sh` returns ≥ 4 (one per SC #1 sub-check).
    - `grep -c 'SC #2' scripts/verify_phase5.sh` returns ≥ 5.
    - `grep -c 'SC #3' scripts/verify_phase5.sh` returns ≥ 1.
    - `grep -c 'SC #4' scripts/verify_phase5.sh` returns ≥ 4.
    - `test -x scripts/verify_phase5.sh` (executable bit preserved).
    - `python scripts/regression_check.py` separately exits 0 (independent re-verify of Task 1).
  </acceptance_criteria>
  <done>
    verify_phase5.sh has all 4 ROADMAP SCs + regression + test suite + REPL Pitfall 2 covered as check() invocations; running the script exits 0 with PASS≥20 and FAIL=0; final line states "Phase 5 ready for /gsd:verify-work". File remains chmod +x.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer→verify script | Local exit gate; no external input. Runs developer's own code. |
| Developer→regression_check.py | Same; deep_diff runs only on local fixtures + local subprocess output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-04-FP | Spoofing (false-PASS) | SKIP_KEYS over-relaxation could hide a real regression in K0/phi/rho/seed/valuation values | accept | The 5 skipped keys are NEWLY introduced in Phase 5 — they cannot have a v1.0 baseline to regress against. Plan 04 deliberately scopes regression to "v1.0 pre-existing fields unchanged" (Strategia B). Phase 6+ should add value-level assertions for the new env keys if needed; out of scope for Phase 5. |
| T-5-04-INJ | Injection (shell) | verify_phase5.sh executes user-supplied flags as part of check() commands | accept | All flags in the script are hard-coded literals authored by the developer. No untrusted input crosses any check() boundary. |
| T-5-04-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new packages. |
</threat_model>

<verification>
After both tasks complete, run the full gate:

```bash
cd "$(git rev-parse --show-toplevel)"
python scripts/regression_check.py                    # PASS 8/8
bash scripts/verify_phase5.sh                          # PASS ≥20 / FAIL=0
python -m unittest discover tests/ -v                  # full suite OK
```

If all three exit 0, Phase 5 is ready for `/gsd:verify-work`.
</verification>

<success_criteria>
1. regression_check.py PASS 8/8 after SKIP_KEYS extension (Plan 03's env-block changes no longer trip regression).
2. verify_phase5.sh runs end-to-end and exits 0 with PASS ≥ 20 / FAIL = 0.
3. All 4 ROADMAP Phase 5 SCs explicitly verified by check() invocations in verify_phase5.sh.
4. REPL Pitfall 2 (RESEARCH §F.15) explicitly verified by piped subprocess checks.
5. Phase 4/3/2/1 test suite remains green (no regressions in earlier phases).
</success_criteria>

<output>
Create `.planning/phases/05-configurable-environment/05-04-SUMMARY.md` when done. Include: exact SKIP_KEYS extension diff (5 keys added with Phase 5 comment), count of check() invocations added to verify_phase5.sh, captured PASS/FAIL counts from a real verify_phase5.sh run, the three SC-3 KPI values (window/step/linear) printed from the SC#3 check (proof of distinguishability), confirmation that all 4 ROADMAP SCs + REPL Pitfall 2 are covered.
</output>
