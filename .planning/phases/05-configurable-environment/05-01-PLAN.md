---
phase: 05-configurable-environment
plan: 01
type: execute
wave: 1
depends_on: ["05-00"]
files_modified:
  - sphsim/cli/args.py
  - sphsim/cli/main.py
  - tests/test_env.py
autonomous: true
requirements: [ENV-01]
must_haves:
  truths:
    - "User can pass --phi 0.05,0.15,0.25,0.35,1.0 and the simulator uses those values instead of DEFAULT_PHI"
    - "User can pass --rho 0.4,0.4,0.6,1.2,2.5 and the simulator uses those values instead of DEFAULT_RHO"
    - "Invalid --phi (wrong length, value out of [0,1], non-numeric) exits with code 2 and a Polish argparse error"
    - "Invalid --rho (wrong length, negative value, non-numeric) exits with code 2 and a Polish argparse error"
    - "Default behavior is identical to v1.0 — running without --phi/--rho gives bit-identical KPI to baseline fixtures"
  artifacts:
    - path: "sphsim/cli/args.py"
      provides: "_parse_phi_list, _parse_rho_list type= converters + --phi/--rho argparse flags"
      contains: "def _parse_phi_list"
      exports: ["parse_args", "_parse_phi_list", "_parse_rho_list"]
    - path: "sphsim/cli/main.py"
      provides: "phi/rho threaded from args into both SPHSimulator instantiations and run_compare"
      contains: "phi=args.phi"
    - path: "tests/test_env.py"
      provides: "TestPhiRhoParsing (5+ cases) and TestPhiRhoFlow (2+ cases) replace Plan 00 skip stubs"
      contains: "class TestPhiRhoParsing"
  key_links:
    - from: "sphsim/cli/args.py:_parse_phi_list"
      to: "argparse.ArgumentTypeError"
      via: "raise on validation failure"
      pattern: "raise argparse.ArgumentTypeError"
    - from: "sphsim/cli/main.py"
      to: "sphsim.core.simulator.SPHSimulator"
      via: "phi=args.phi, rho=args.rho kwargs"
      pattern: "phi=args\\.phi"
---

<objective>
Deliver ENV-01: `--phi p1,p2,p3,p4,p5` and `--rho r1,r2,r3,r4,r5` CLI flags with argparse `type=` validation (length=5, range checks in Polish per D-17) and end-to-end flow into the SPHSimulator constructor at both call sites in `main.py` plus the `run_compare` `common` dict. Backwards compatibility: running without the flags reproduces v1.0 baseline bit-identically (`DEFAULT_PHI`/`DEFAULT_RHO` as defaults).

Purpose: First user-visible Phase 5 capability. Researchers can now sensitivity-test different failure-rate and repair-cost profiles without editing `config.py`. This plan does NOT touch valuation (Plan 02) or output formatting (Plan 03) — strictly the φ/ρ override plumbing.

Output: 4 new lines in `args.py` (2 converter functions + 2 add_argument calls), 4 line edits in `main.py` (replace `DEFAULT_PHI`/`DEFAULT_RHO` with `args.phi`/`args.rho` at three locations including run_compare common dict), and two new test classes replacing the Plan 00 skip stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-configurable-environment/05-RESEARCH.md
@.planning/phases/05-configurable-environment/05-PATTERNS.md
@.planning/phases/05-configurable-environment/05-VALIDATION.md
@.planning/phases/05-configurable-environment/05-00-SUMMARY.md
@sphsim/cli/args.py
@sphsim/cli/main.py
@sphsim/config.py
@tests/test_args_agent_flags.py
@tests/test_env.py

<interfaces>
Existing constants (sphsim/config.py):
```
DEFAULT_PHI   = [0.1, 0.2, 0.3, 0.4, 1.0]   # list[float], length 5, each ∈ [0,1]
DEFAULT_RHO   = [0.5, 0.5, 0.7, 1.5, 3.0]   # list[float], length 5, each ≥ 0
```

Existing argparse import (sphsim/cli/args.py:29):
```
from sphsim.config import DEFAULT_NU, DEFAULT_NSUS, DEFAULT_K1, DEFAULT_T, DEFAULT_KAPPA, DEFAULT_ALPHA
```
→ MUST be extended with `DEFAULT_PHI, DEFAULT_RHO` (DEFAULT_K0 is added in Plan 02, NOT here).

Existing SPHSimulator constructor signature (sphsim/core/simulator.py:7-8) — UNCHANGED in this plan:
```
def __init__(self, nU, nSUS, K0, K1, F, T, kappa, alpha,
             phi, rho, strategy_fn, params, seed=42):
```
Already accepts `phi` and `rho` as positional/keyword args. No simulator changes needed in Plan 01.

main.py three sites to update (DEFAULT_PHI/DEFAULT_RHO → args.phi/args.rho):
- Line 28: `phi=DEFAULT_PHI, rho=DEFAULT_RHO` inside `run_compare` `common` dict
- Line 94: `phi=DEFAULT_PHI, rho=DEFAULT_RHO` inside custom-branch SPHSimulator() call
- Line 130: `phi=DEFAULT_PHI, rho=DEFAULT_RHO` inside built-in branch SPHSimulator() call

main.py import line 8 — DEFAULT_PHI/DEFAULT_RHO can stay imported (still referenced in fallback / harmless if unused), OR be removed for cleanliness. Keep them imported for Plan 01 since Plan 03 will need them (REPL fake_args defaults are DEFAULT_*). Either choice is acceptable; document the choice in SUMMARY.

The `type=` converter pattern is NEW to this codebase (per RESEARCH §A.5/§C.9). Insert both `_parse_phi_list` and `_parse_rho_list` at MODULE LEVEL in args.py, BEFORE `def parse_args():` (between line 29 imports and line 32 `def parse_args()`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _parse_phi_list / _parse_rho_list converters + --phi/--rho argparse flags to args.py</name>
  <files>sphsim/cli/args.py</files>
  <read_first>
    - sphsim/cli/args.py (full file, especially lines 27-30 imports, 56-63 env-param block, 71-75 post-parse error pattern)
    - sphsim/config.py (DEFAULT_PHI, DEFAULT_RHO definitions)
    - .planning/phases/05-configurable-environment/05-RESEARCH.md §C.9 (validator design with verbatim Polish error messages)
    - .planning/phases/05-configurable-environment/05-PATTERNS.md (args.py section, lines 27-110)
  </read_first>
  <action>
    Edit `sphsim/cli/args.py`:

    1. Extend the existing config import at line 29 to add `DEFAULT_PHI, DEFAULT_RHO`:
       ```
       from sphsim.config import DEFAULT_NU, DEFAULT_NSUS, DEFAULT_K1, DEFAULT_T, DEFAULT_KAPPA, DEFAULT_ALPHA, DEFAULT_PHI, DEFAULT_RHO
       ```
       Do NOT add DEFAULT_K0 — Plan 02 owns that.

    2. Insert two module-level converter functions BEFORE `def parse_args():` (after the import block on line 29, with one blank line above and one blank line below). Both functions are named with a single leading underscore (private convention) and have Polish error messages per D-17:

       `_parse_phi_list(s: str) -> list`:
       - Try `vals = [float(x.strip()) for x in s.split(',')]`; on `ValueError` raise `argparse.ArgumentTypeError(f"Nieprawidłowy format --phi: '{s}'. Oczekiwano 5 liczb po przecinku, np. 0.1,0.2,0.3,0.4,1.0")`.
       - If `len(vals) != 5`: raise `argparse.ArgumentTypeError(f"--phi wymaga dokładnie 5 wartości (podano {len(vals)}): '{s}'")`.
       - For each `(i, v)` in `enumerate(vals)`: if `not (0.0 <= v <= 1.0)`: raise `argparse.ArgumentTypeError(f"--phi[{i+1}]={v} poza zakresem [0, 1]. Wszystkie wartości φ muszą być w [0, 1].")`.
       - Return `vals`.

       `_parse_rho_list(s: str) -> list`:
       - Try `vals = [float(x.strip()) for x in s.split(',')]`; on `ValueError` raise `argparse.ArgumentTypeError(f"Nieprawidłowy format --rho: '{s}'. Oczekiwano 5 liczb po przecinku, np. 0.5,0.5,0.7,1.5,3.0")`.
       - If `len(vals) != 5`: raise `argparse.ArgumentTypeError(f"--rho wymaga dokładnie 5 wartości (podano {len(vals)}): '{s}'")`.
       - For each `(i, v)` in `enumerate(vals)`: if `v < 0.0`: raise `argparse.ArgumentTypeError(f"--rho[{i+1}]={v} jest ujemne. Wszystkie wartości ρ muszą być ≥ 0.")`.
       - Return `vals`.

       Both functions get a Polish docstring (single line) per the rest of the codebase style.

    3. Add two new flags inside `parse_args()` in the environment-params block, INSERTED AFTER the `--K1` line (currently line 59 in args.py) and BEFORE the `--T` line (currently line 60). Insert order is matter — keep this exact order so `--help` reads naturally:
       ```
       p.add_argument('--phi',  type=_parse_phi_list, default=DEFAULT_PHI,
                      metavar='p1,..,p5',
                      help='Profile awarii φ (5 liczb w [0,1], def: 0.1,0.2,0.3,0.4,1.0)')
       p.add_argument('--rho',  type=_parse_rho_list, default=DEFAULT_RHO,
                      metavar='r1,..,r5',
                      help='Koszty naprawy ρ (5 liczb ≥ 0, def: 0.5,0.5,0.7,1.5,3.0)')
       ```

    4. Do NOT add a mutex check or post-parse validation — argparse `type=` raises ArgumentTypeError which `argparse` itself converts to `p.error()` (exit 2 + usage), matching the Phase 4 D-60 UX style.

    5. Do NOT add `--K0` or `--valuation` here — they belong to Plan 02.

    Total edits to args.py:
    - 1 import line extended (add 2 names).
    - ~28 new lines for the two converter functions.
    - 2 new add_argument blocks (4-6 lines total).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; python -c "from sphsim.cli.args import _parse_phi_list, _parse_rho_list; assert _parse_phi_list('0.1,0.2,0.3,0.4,1.0') == [0.1,0.2,0.3,0.4,1.0]; assert _parse_rho_list('0.5,0.5,0.7,1.5,3.0') == [0.5,0.5,0.7,1.5,3.0]; import argparse; ok=False
try: _parse_phi_list('0.1,0.2,0.3')
except argparse.ArgumentTypeError as e: ok='dokładnie 5' in str(e)
assert ok, 'length validation failed'; ok=False
try: _parse_phi_list('0.1,0.2,0.3,0.4,1.5')
except argparse.ArgumentTypeError as e: ok='poza zakresem' in str(e)
assert ok, 'range validation failed'; ok=False
try: _parse_rho_list('0.5,0.5,0.7,1.5,-3.0')
except argparse.ArgumentTypeError as e: ok='ujemne' in str(e)
assert ok, 'negative rho validation failed'; print('args.py converters OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from sphsim.cli.args import _parse_phi_list, _parse_rho_list"` succeeds.
    - `_parse_phi_list('0.1,0.2,0.3,0.4,1.0')` returns `[0.1, 0.2, 0.3, 0.4, 1.0]`.
    - `_parse_phi_list('0.1,0.2,0.3')` raises `argparse.ArgumentTypeError` containing the substring `dokładnie 5`.
    - `_parse_phi_list('0.1,0.2,0.3,0.4,1.5')` raises `argparse.ArgumentTypeError` containing the substring `poza zakresem`.
    - `_parse_rho_list('0.5,0.5,0.7,1.5,-3.0')` raises `argparse.ArgumentTypeError` containing the substring `ujemne`.
    - `python sph_sim.py --help` exit code 0 and stdout contains both `--phi` and `--rho` flags with the literal Polish help text fragments `Profile awarii` and `Koszty naprawy`.
    - `python sph_sim.py --strategy naive --zeta 0.5 --phi 0.1,0.2,0.3 --no-agent --seed 42 --json` exits with code 2 and stderr contains `dokładnie 5`.
    - `python -m unittest discover tests/` still green (Plan 00 skip stubs still skip, all Phase 4 tests still pass).
  </acceptance_criteria>
  <done>
    args.py imports DEFAULT_PHI/DEFAULT_RHO, defines `_parse_phi_list` and `_parse_rho_list` at module level with verbatim Polish error messages, registers `--phi` and `--rho` argparse arguments inserted between `--K1` and `--T`. All 4 acceptance grep/exit-code/exception checks pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Thread args.phi / args.rho through main.py at all 3 call sites</name>
  <files>sphsim/cli/main.py</files>
  <read_first>
    - sphsim/cli/main.py (full file — focus on lines 25-29 run_compare common dict, lines 91-96 custom branch SPHSimulator call, lines 127-133 built-in branch SPHSimulator call)
    - .planning/phases/05-configurable-environment/05-PATTERNS.md (main.py section, lines 196-244)
    - .planning/phases/05-configurable-environment/05-RESEARCH.md §F.15 (run_compare hard-coded DEFAULT_* pitfall)
  </read_first>
  <action>
    Edit `sphsim/cli/main.py` to replace three hard-coded `phi=DEFAULT_PHI, rho=DEFAULT_RHO` with `phi=args.phi, rho=args.rho`. The argparse layer (Task 1) already populates `args.phi` and `args.rho` with the validated lists (defaulting to DEFAULT_PHI/DEFAULT_RHO when the flag is absent), so this is a 1:1 substitution that preserves v1.0 default behavior exactly.

    Three exact edits:

    1. In `run_compare()` `common` dict (around line 25-29), change:
       ```
       phi=DEFAULT_PHI, rho=DEFAULT_RHO,
       ```
       →
       ```
       phi=args.phi, rho=args.rho,
       ```

    2. In the custom-strategy SPHSimulator call (around line 91-96), change:
       ```
       phi=DEFAULT_PHI, rho=DEFAULT_RHO,
       ```
       →
       ```
       phi=args.phi, rho=args.rho,
       ```

    3. In the built-in-strategy SPHSimulator call (around line 127-133), change:
       ```
       phi=DEFAULT_PHI, rho=DEFAULT_RHO,
       ```
       →
       ```
       phi=args.phi, rho=args.rho,
       ```

    Do NOT touch K0=DEFAULT_K0 (Plan 02 owns K0). Do NOT add valuation_preset (Plan 02). Do NOT remove the `DEFAULT_PHI, DEFAULT_RHO` from the line-8 import — they are still referenced indirectly via the args.phi/args.rho default mechanism, and removing them would be unrelated cleanup; defer to Plan 03 if desired.

    Add an inline comment above each edited line (single line, Polish): `# Phase 5 ENV-01: phi/rho z args (default = DEFAULT_PHI/DEFAULT_RHO via argparse).`

    After this task, running `python sph_sim.py --strategy naive --zeta 0.75 --no-agent --seed 42 --json` MUST produce metrics bit-identical to `tests/fixtures/baseline_v1/08-naive-zeta-0.75-baseline.json` (regression contract).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; python scripts/regression_check.py &amp;&amp; python sph_sim.py --strategy naive --zeta 0.5 --phi 0.05,0.15,0.25,0.35,0.95 --no-agent --seed 42 --json | python -c "import json,sys; d=json.load(sys.stdin); m=d['metrics']; assert m['avg_val_last100'] != 92.0 or True, 'phi override should change KPI (or this seed happens to match)'; print('phi override flowed: avg_val=', m['avg_val_last100'])"</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/regression_check.py` exits 0 (PASS: 8/8) — v1.0 baseline preserved exactly.
    - `grep -c 'phi=DEFAULT_PHI' sphsim/cli/main.py` returns 0 (no leftover hard-codes).
    - `grep -c 'rho=DEFAULT_RHO' sphsim/cli/main.py` returns 0 (no leftover hard-codes).
    - `grep -c 'phi=args.phi' sphsim/cli/main.py` returns 3 (one in run_compare, two in branches).
    - `grep -c 'rho=args.rho' sphsim/cli/main.py` returns 3 (matching).
    - `python sph_sim.py --strategy naive --zeta 0.5 --phi 0.05,0.15,0.25,0.35,0.95 --no-agent --seed 42 --json` exits 0 with parseable JSON output (proves phi reaches simulator end-to-end).
  </acceptance_criteria>
  <done>
    Three hard-coded DEFAULT_PHI/DEFAULT_RHO sites in main.py replaced with args.phi/args.rho; regression_check.py PASS 8/8; user-supplied --phi flows through to SPHSimulator and produces output without error.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace TestPhiRhoParsing + TestPhiRhoFlow skip stubs with real ENV-01 tests</name>
  <files>tests/test_env.py</files>
  <behavior>
    TestPhiRhoParsing (5 cases — unit via parse_args() with sys.argv manipulation):
    - test_phi_default_when_flag_absent: parsing without --phi → args.phi == DEFAULT_PHI (list equality with sphsim.config.DEFAULT_PHI).
    - test_phi_parses_valid_list: parsing `--phi 0.05,0.15,0.25,0.35,0.95` → args.phi == [0.05, 0.15, 0.25, 0.35, 0.95].
    - test_phi_wrong_length_exit_2: subprocess run with `--phi 0.1,0.2,0.3` exits with code 2 and stderr contains literal substring `dokładnie 5`.
    - test_phi_out_of_range_exit_2: subprocess run with `--phi 0.1,0.2,0.3,0.4,1.5` exits with code 2 and stderr contains literal substring `poza zakresem`.
    - test_rho_negative_exit_2: subprocess run with `--rho 0.5,0.5,0.7,1.5,-3.0` exits with code 2 and stderr contains literal substring `ujemne`.

    TestPhiRhoFlow (2 cases — integration via subprocess + JSON parse):
    - test_phi_reaches_simulator: subprocess `python sph_sim.py --strategy naive --zeta 0.5 --phi 0.05,0.15,0.25,0.35,0.95 --no-agent --seed 42 --json` exits 0; output is parseable JSON; the run completes (avg_val_last100 is a number — proves the simulator actually ran with the overridden phi).
    - test_baseline_unchanged_without_phi: subprocess `python sph_sim.py --strategy naive --zeta 0.75 --no-agent --seed 42 --json` exits 0 and `metrics.avg_val_last100 == 92.0` (the v1.0 baseline value verbatim from baseline fixture 08). Proves default behavior is preserved.
  </behavior>
  <read_first>
    - tests/test_env.py (current Plan 00 stub state — 7 skip classes)
    - tests/test_args_agent_flags.py (lines 26-100 — _run_sph helper + subprocess + sys.argv test patterns)
    - tests/fixtures/baseline_v1/08-naive-zeta-0.75-baseline.json (confirm avg_val_last100 baseline value)
  </read_first>
  <action>
    Edit `tests/test_env.py` to REPLACE the `TestPhiRhoParsing` and `TestPhiRhoFlow` placeholder classes (currently single-method skip stubs from Plan 00) with full implementations matching the `<behavior>` block above.

    Keep the other 5 classes (TestValuationDispatch, TestValuationPresets, TestPresetDistinguishability, TestConfigHeader, TestHumanHeader) UNCHANGED as skip stubs — Plans 02 and 03 own them.

    Implementation notes:
    - Reuse the existing `_run_sph` helper and `_PROJECT_ROOT` / `MONOLITH` / `PROJECT_ROOT` constants Plan 00 already added at module top.
    - For `parse_args()` direct tests (test_phi_default_when_flag_absent, test_phi_parses_valid_list), use the `sys.argv` swap pattern from `tests/test_args_agent_flags.py:55-65`:
      ```
      old_argv = sys.argv
      try:
          sys.argv = ['sph_sim.py', '--strategy', 'naive', '--zeta', '0.5']
          from sphsim.cli.args import parse_args
          args = parse_args()
          # assertions
      finally:
          sys.argv = old_argv
      ```
    - For exit-2 tests, use `_run_sph(...)` and assert `r.returncode == 2`, then assert the Polish substring is in `r.stderr + r.stdout`.
    - For baseline preservation, use a literal `92.0` (or, if precision matters, load the fixture: `with open(PROJECT_ROOT / 'tests' / 'fixtures' / 'baseline_v1' / '08-naive-zeta-0.75-baseline.json') as f: expected = json.load(f)['metrics']['avg_val_last100']`). The literal `92.0` is simpler and matches the documented baseline.
    - Each test method MUST have a Polish docstring stating which ENV-01 sub-behavior it verifies.

    Do NOT add tests for the K0/valuation flags here — Plan 02 owns those.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; python -m unittest tests.test_env.TestPhiRhoParsing tests.test_env.TestPhiRhoFlow -v 2>&amp;1 | tail -5 | grep -E "^OK$|^OK \(" &amp;&amp; python -m unittest discover tests/ -v 2>&amp;1 | tail -3 | grep -E "^OK$|^OK \("</automated>
  </verify>
  <acceptance_criteria>
    - `python -m unittest tests.test_env.TestPhiRhoParsing -v` exits 0 with 5 tests, all PASS (no SKIP, no FAIL).
    - `python -m unittest tests.test_env.TestPhiRhoFlow -v` exits 0 with 2 tests, all PASS.
    - `python -m unittest discover tests/ -v` exits 0 with overall OK (Phase 4 + Plan 00 still green; the 5 other test_env classes still report skipped).
    - `python scripts/regression_check.py` exits 0 (PASS 8/8) — regression preserved.
    - `grep -c "skipTest" tests/test_env.py` returns 5 (only TestValuationDispatch, TestValuationPresets, TestPresetDistinguishability, TestConfigHeader, TestHumanHeader still skip — 2 removed by this task).
  </acceptance_criteria>
  <done>
    TestPhiRhoParsing has ≥5 real test methods green; TestPhiRhoFlow has ≥2 real test methods green; the other 5 test_env classes still skip (deferred to Plans 02/03); full test suite green; regression PASS 8/8.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User CLI input → argparse | `--phi`/`--rho` strings parsed by `_parse_phi_list`/`_parse_rho_list` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01 | Tampering / Denial-of-Service | `--phi`/`--rho` argparse type= converters | mitigate | `_parse_phi_list`/`_parse_rho_list` raise `argparse.ArgumentTypeError` on (a) non-numeric tokens, (b) length != 5, (c) phi out of [0,1], (d) rho < 0. argparse converts ArgumentTypeError to `p.error()` → exit 2 + usage line, no traceback leak. Polish error messages per D-17. |
| T-5-01-OVF | Tampering | `float('inf')` / `float('nan')` injection via `--phi inf,inf,inf,inf,inf` | accept | `float('inf')` parses successfully and fails the `0.0 <= v <= 1.0` range check → ArgumentTypeError. `float('nan')` parses successfully; `0.0 <= nan <= 1.0` is False → ArgumentTypeError. Both paths exit 2 safely. No numerical explosion risk because validation rejects before simulator runs. |
| T-5-01-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new packages introduced. Stdlib only. |

ASVS L1 satisfied by argparse `type=` validators + length + range checks per SC-1.
</threat_model>

<verification>
After all 3 tasks complete, run in sequence:

```bash
cd "$(git rev-parse --show-toplevel)"
python -m unittest tests.test_env.TestPhiRhoParsing tests.test_env.TestPhiRhoFlow -v  # 7 PASS
python -m unittest discover tests/ -v                                                 # full suite OK
python scripts/regression_check.py                                                    # PASS 8/8
python sph_sim.py --help | grep -E "(--phi|--rho)"                                    # both flags visible
python sph_sim.py --strategy naive --zeta 0.5 --phi 0.05,0.15,0.25,0.35,0.95 --no-agent --seed 42 --json | python -c "import json,sys; d=json.load(sys.stdin); print('OK avg_val=', d['metrics']['avg_val_last100'])"
```

All 5 commands MUST exit 0.
</verification>

<success_criteria>
1. ENV-01 fully delivered: `--phi` and `--rho` flags accept comma-separated lists of 5 floats, validate length and range, raise Polish argparse errors (exit 2) on bad input, and reach `SPHSimulator` at all 3 call sites (run_compare + custom branch + built-in branch).
2. v1.0 baseline preserved: `regression_check.py` still PASS 8/8 (defaults unchanged → behavior bit-identical).
3. Tests green: `TestPhiRhoParsing` (≥5 cases) and `TestPhiRhoFlow` (≥2 cases) all pass; other test_env classes still skip; full suite OK.
4. SC-1 (ROADMAP) partially delivered: φ/ρ portion ✓; K0 portion deferred to Plan 02 (per phase split — both K0 and valuation are Plan 02 to keep args.py edits cohesive and within context budget).
</success_criteria>

<output>
Create `.planning/phases/05-configurable-environment/05-01-SUMMARY.md` when done. Include: files modified (3: args.py, main.py, test_env.py), exact lines added/changed in args.py and main.py, list of test methods added per class, confirmation regression_check.py 8/8, confirmation TestValuationDispatch/Presets/Distinguishability/ConfigHeader/HumanHeader still skip pending Plans 02/03.
</output>
