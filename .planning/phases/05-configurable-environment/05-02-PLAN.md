---
phase: 05-configurable-environment
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - sphsim/cli/args.py
  - sphsim/core/model.py
  - sphsim/core/simulator.py
  - sphsim/cli/main.py
  - tests/test_env.py
autonomous: true
requirements: [ENV-02]
must_haves:
  truths:
    - "User can pass --valuation step or --valuation linear and the simulator uses the chosen preset for both cycle valuation AND sph_stp optimization"
    - "User can pass --K0 X and --K1 Y for full parametric control (both flags work independently of --valuation)"
    - "Default --valuation = window produces v1.0-identical behavior (bit-identical KPI on regression_check baseline)"
    - "All 3 presets (window, step, linear) produce deterministic and distinguishable KPI on the same seed+strategy (SC-3)"
    - "sph_stp threads the valuation preset through P_of_x closure — no silent fallback to window"
  artifacts:
    - path: "sphsim/cli/args.py"
      provides: "--K0 (float), --valuation (choices=window|step|linear) flags"
      contains: "choices=['window', 'step', 'linear']"
    - path: "sphsim/core/model.py"
      provides: "valuation(u, K0, K1, preset='window') with step/linear branches + sph_stp(..., preset='window') threading"
      contains: "if preset == 'step'"
    - path: "sphsim/core/simulator.py"
      provides: "SPHSimulator __init__ accepts valuation_preset='window'; stored as self.valuation_preset; threaded into run() sph_stp + valuation calls"
      contains: "self.valuation_preset"
    - path: "sphsim/cli/main.py"
      provides: "K0/valuation_preset threaded from args into both SPHSimulator instantiations and run_compare common dict"
      contains: "valuation_preset=args.valuation"
    - path: "tests/test_env.py"
      provides: "TestValuationDispatch + TestValuationPresets + TestPresetDistinguishability replace Plan 00 skip stubs"
      contains: "class TestValuationDispatch"
  key_links:
    - from: "sphsim/cli/args.py"
      to: "sphsim.core.model.valuation"
      via: "args.valuation string → SPHSimulator.valuation_preset → sph_stp(..., preset) → valuation(..., preset)"
      pattern: "preset=self\\.valuation_preset"
    - from: "sphsim/core/simulator.py:run"
      to: "sphsim.core.model.sph_stp"
      via: "self.valuation_preset positional arg"
      pattern: "sph_stp\\(.*self\\.valuation_preset"
---

<objective>
Deliver ENV-02: `--valuation <window|step|linear>` preset selector + `--K0 X` parametric control flag, with full propagation through `args → SPHSimulator → sph_stp → valuation` so the chosen preset affects BOTH the per-cycle `valuation(svc_to_cons, K0, K1, preset)` call AND the inner `P_of_x` lambda inside `sph_stp` (the RESEARCH §F-15 / Pitfall 1 landmine: forgetting to thread the preset into `sph_stp` causes silent fallback to window for STP optimisation).

Purpose: Enables Phase 5 SC-2 + SC-3 — distinguishable KPI across three preset shapes for sensitivity analysis. Backward compat: window preset + DEFAULT_K0=100 produces v1.0-identical KPI (regression_check 8/8 must remain green).

Output: `--K0` and `--valuation` flags in args.py; `valuation()` and `sph_stp()` extended with `preset` parameter in `sphsim/core/model.py`; `SPHSimulator.__init__` accepts `valuation_preset='window'`; main.py threads K0/valuation through both branches and run_compare; 3 new test classes (TestValuationDispatch, TestValuationPresets, TestPresetDistinguishability) replace Plan 00 skip stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-configurable-environment/05-RESEARCH.md
@.planning/phases/05-configurable-environment/05-PATTERNS.md
@.planning/phases/05-configurable-environment/05-VALIDATION.md
@.planning/phases/05-configurable-environment/05-01-SUMMARY.md
@sphsim/cli/args.py
@sphsim/core/model.py
@sphsim/core/simulator.py
@sphsim/cli/main.py
@sphsim/config.py
@tests/test_env.py
@tests/fixtures/baseline_v1/08-naive-zeta-0.75-baseline.json

<interfaces>
Current valuation function (sphsim/core/model.py:7-10) — to be EXTENDED with preset:
```
def valuation(u, K0, K1):
    if K1 == float('inf'):
        return float(K0) if u >= K0 else 0.0
    return float(K0) if K0 <= u <= K1 else 0.0
```

Current sph_stp signature (sphsim/core/model.py:13) — to be EXTENDED with preset, P_of_x closure MUST pass preset to valuation.

Current SPHSimulator constructor (sphsim/core/simulator.py:7-8) — to be EXTENDED with valuation_preset='window' as new keyword argument BEFORE seed=42.

Three call sites inside SPHSimulator.run() (sphsim/core/simulator.py:80, 82, 105) MUST thread the preset:
- Line 80: sph_stp(u, self.s, self.nSUS, self.K0, self.K1) becomes sph_stp(u, self.s, self.nSUS, self.K0, self.K1, self.valuation_preset)
- Line 82: valuation(svc_to_cons, self.K0, self.K1) + z - y becomes valuation(svc_to_cons, self.K0, self.K1, self.valuation_preset) + z - y
- Line 105: val = valuation(svc_to_cons, self.K0, self.K1) becomes val = valuation(svc_to_cons, self.K0, self.K1, self.valuation_preset)

main.py three SPHSimulator instantiations + run_compare common dict — same pattern as Plan 01: add K0=args.K0 and valuation_preset=args.valuation. After Plan 01 the K0 line still reads K0=DEFAULT_K0 (Plan 01 deliberately left K0 alone). Plan 02 replaces it with K0=args.K0.

Presets (functional forms verbatim from 05-RESEARCH.md §B.7):
- window (def, v1.0): K0 if K0 <= u <= K1 else 0  (K1==inf fallback: K0 if u >= K0 else 0)
- step:               K0 if u >= K0 else 0        (no upper bound)
- linear:             K0 * min(u, K1) / K1        (K1==inf or K1<=0 fallback: K0 if u >= K0 else 0)

args.py already imports DEFAULT_K1 (line 29). For Plan 02 we add DEFAULT_K0 to that import too. NOTE: Plan 01 already added DEFAULT_PHI, DEFAULT_RHO to the import; this plan adds DEFAULT_K0.

DEFAULT_K0 = 100 (sphsim/config.py:6).

main.py currently imports DEFAULT_K0 (line 8) — keep it imported.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --K0 and --valuation flags to args.py</name>
  <files>sphsim/cli/args.py</files>
  <read_first>
    - sphsim/cli/args.py (state AFTER Plan 01 — should now have DEFAULT_PHI, DEFAULT_RHO imported plus the two converter functions)
    - sphsim/config.py (DEFAULT_K0 = 100)
    - .planning/phases/05-configurable-environment/05-RESEARCH.md §B.8 / Proposed Design (flag shape)
  </read_first>
  <action>
    Edit `sphsim/cli/args.py`:

    1. Extend the config import line by adding `DEFAULT_K0` to the existing import. Final import line:
       `from sphsim.config import DEFAULT_NU, DEFAULT_NSUS, DEFAULT_K0, DEFAULT_K1, DEFAULT_T, DEFAULT_KAPPA, DEFAULT_ALPHA, DEFAULT_PHI, DEFAULT_RHO`

    2. Insert `--K0` add_argument call IMMEDIATELY AFTER the existing `--K1` line and BEFORE `--phi` (so K1 keeps its current position as v1.0 reference). Verbatim:
       `p.add_argument('--K0', type=float, default=DEFAULT_K0, help=f'Dolny próg waluacji K0 (def {DEFAULT_K0})')`

    3. Insert `--valuation` add_argument call IMMEDIATELY AFTER the existing `--rho` line (Plan 01 addition) and BEFORE `--T`. Verbatim:
       `p.add_argument('--valuation', choices=['window', 'step', 'linear'], default='window', help='Preset funkcji waluacji g(u): window (v1.0 default) | step | linear')`

    4. Do NOT add any post-parse mutex check between `--valuation` and `--K0`/`--K1` — RESEARCH §B.8 confirms they are orthogonal (not mutex). The combination `--valuation step --K0 90 --K1 150` is valid and meaningful.

    5. Do NOT introduce any non-finite-float guard — argparse accepts `float('inf')` and `float('nan')` silently for `--K0` and `--K1`. T-5-02 (threat model) documents this is accepted: K0=inf or K0=nan inside `valuation()` produces 0.0 returns (window/step thresholds never met) or NaN propagation (visible in output) — no crash, no security impact. The phase is single-process CLI without persisted state.

    Final env-param block order: --nU, --nSUS, --K1, --K0 (NEW), --phi, --rho, --valuation (NEW), --T, --kappa, --alpha, --seed.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; python -c "import sys; sys.argv = ['sph_sim.py', '--strategy', 'naive', '--zeta', '0.5', '--K0', '90', '--valuation', 'step']; from sphsim.cli.args import parse_args; a = parse_args(); assert a.K0 == 90.0; assert a.valuation == 'step'; print('args ok')" &amp;&amp; python sph_sim.py --help 2>&amp;1 | grep -cE '(\-\-K0|\-\-valuation)' | { read n; [ "$n" -ge 2 ] || exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from sphsim.cli.args import parse_args"` succeeds (import does not break Plan 01 converters).
    - With `sys.argv` set to `['sph_sim.py', '--strategy', 'naive', '--zeta', '0.5']`, `parse_args()` returns a Namespace where `args.K0 == 100.0` (DEFAULT_K0) and `args.valuation == 'window'`.
    - With `sys.argv` set to include `--K0 90 --valuation step`, returned namespace has `args.K0 == 90.0` and `args.valuation == 'step'`.
    - `python sph_sim.py --strategy naive --zeta 0.5 --valuation foobar` exits 2 (argparse choices rejection); stderr contains the literal string `--valuation`.
    - `python sph_sim.py --help` contains both `--K0` and `--valuation` flags in its output.
    - `python -m unittest tests.test_env.TestPhiRhoParsing tests.test_env.TestPhiRhoFlow -v` still passes (Plan 01 invariants preserved).
  </acceptance_criteria>
  <done>
    args.py registers `--K0` (float, default DEFAULT_K0) and `--valuation` (choices window/step/linear, default window). Both flags visible in --help. Plan 01 tests still green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend valuation() and sph_stp() with preset dispatch in model.py</name>
  <files>sphsim/core/model.py, tests/test_env.py</files>
  <behavior>
    TestValuationDispatch (5 unit cases — direct function calls, no simulator):
    - test_window_default_K0_K1: `valuation(110, 100, 120)` returns `100.0` (in window). Default preset omitted = window.
    - test_window_outside_range: `valuation(80, 100, 120)` returns `0.0`; `valuation(130, 100, 120)` returns `0.0`.
    - test_step_above_threshold: `valuation(130, 100, 120, 'step')` returns `100.0` (no upper bound penalty).
    - test_step_below_threshold: `valuation(80, 100, 120, 'step')` returns `0.0`.
    - test_linear_ramp: `valuation(60, 100, 120, 'linear')` returns approximately `50.0` (= 100 * 60 / 120). Use `assertAlmostEqual` with `places=4`.
    - test_linear_inf_K1_fallback: `valuation(150, 100, float('inf'), 'linear')` returns `100.0` (falls back to step semantics per RESEARCH §B.7).
    - test_sph_stp_threads_preset: call `sph_stp(150, 0, 20, 100, 120, 'step')` and `sph_stp(150, 0, 20, 100, 120, 'window')`; the returned `(z*, y*)` MUST differ OR the resulting `P_of_x(z*-y*)` MUST differ between the two presets — proves preset reached the inner P_of_x closure (Pitfall 1).
  </behavior>
  <read_first>
    - sphsim/core/model.py (full file — only 34 lines)
    - .planning/phases/05-configurable-environment/05-RESEARCH.md §B.7 (preset functional forms verbatim)
    - .planning/phases/05-configurable-environment/05-PATTERNS.md (model.py section)
    - tests/test_env.py (current state — TestValuationDispatch still a Plan 00 skip stub)
  </read_first>
  <action>
    Step A — Extend `sphsim/core/model.py`:

    1. Replace the existing `valuation(u, K0, K1)` function (lines 7-10) with the preset-dispatched version. Add `preset='window'` as the 4th argument with default that preserves v1.0 behavior. New body order: check `preset == 'step'` first, then `preset == 'linear'`, then fall through to window (the existing two-branch body).

       Body for `step` branch: `return float(K0) if u >= K0 else 0.0`
       Body for `linear` branch: guard `if K1 == float('inf') or K1 <= 0: return float(K0) if u >= K0 else 0.0`, otherwise `return float(K0) * min(float(u), float(K1)) / float(K1)`.
       Body for `window` (default fallthrough): preserve existing two-line body verbatim (K1==inf shortcut + bounded check).

    2. Extend `sph_stp(u, s, nSUS, K0, K1)` to `sph_stp(u, s, nSUS, K0, K1, preset='window')`. Inside, update the `P_of_x(x)` closure on line 16 from `return valuation(u - x, K0, K1) + x` to `return valuation(u - x, K0, K1, preset) + x`. Do NOT change the candidate list — RESEARCH §Open Question 2 confirms the existing K0-u/K1-u breakpoint search is acceptable for Phase 5 (linear may not find true optimum but will be close; full optimisation refactor is out of scope).

    3. Add a single-line Polish docstring to the updated `valuation()` function: `"""Funkcja waluacji Konsumentów g(u; K0, K1, preset). preset: 'window' (def, v1.0) | 'step' | 'linear'."""`

    4. Update the docstring on `sph_stp` to mention that it now passes `preset` into the inner `P_of_x`.

    Step B — Replace `TestValuationDispatch` skip stub in `tests/test_env.py` with the 7 test methods specified in `<behavior>` above. Keep the other 4 skip stubs (TestValuationPresets, TestPresetDistinguishability, TestConfigHeader, TestHumanHeader) unchanged — Plan 02 Task 4 will fill TestValuationPresets/Distinguishability; Plan 03 owns the rest.

    Implementation notes for tests:
    - Import directly: `from sphsim.core.model import valuation, sph_stp`.
    - All assertions use `unittest.TestCase.assertEqual` for exact integer comparisons and `assertAlmostEqual(places=4)` for float comparisons involving multiplication/division.
    - Each test method has a Polish docstring naming the preset and behavior verified.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; python -c "from sphsim.core.model import valuation, sph_stp; assert valuation(110, 100, 120) == 100.0; assert valuation(130, 100, 120, 'step') == 100.0; assert valuation(130, 100, 120, 'window') == 0.0; assert abs(valuation(60, 100, 120, 'linear') - 50.0) < 1e-6; print('model.py OK')" &amp;&amp; python -m unittest tests.test_env.TestValuationDispatch -v 2>&amp;1 | tail -3 | grep -E "^OK$|^OK \("</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from sphsim.core.model import valuation; assert valuation(110,100,120)==100.0 and valuation(130,100,120,'step')==100.0 and valuation(130,100,120,'window')==0.0"` succeeds.
    - `python -c "from sphsim.core.model import valuation; print(valuation(60,100,120,'linear'))"` prints a value within 1e-6 of 50.0.
    - `python -c "from sphsim.core.model import sph_stp; r1=sph_stp(150,0,20,100,120,'window'); r2=sph_stp(150,0,20,100,120,'step'); assert r1 != r2 or True, 'preset must reach P_of_x'"` succeeds (the `or True` permits identical returns when both happen to yield same optimum, but the test_sph_stp_threads_preset in TestValuationDispatch asserts P_of_x divergence).
    - `python -m unittest tests.test_env.TestValuationDispatch -v` exits 0 with ≥6 tests PASS (no SKIP, no FAIL).
    - `python -m unittest discover tests/ -v` exits 0 overall (Phase 4 + Plan 01 + new TestValuationDispatch all green; other 4 test_env classes still skip).
  </acceptance_criteria>
  <done>
    `valuation()` accepts preset kwarg with 3 branches (window/step/linear); `sph_stp()` threads preset into P_of_x; TestValuationDispatch has ≥6 real tests green; full suite still OK.
  </done>
</task>

<task type="auto">
  <name>Task 3: Thread valuation_preset through SPHSimulator and main.py</name>
  <files>sphsim/core/simulator.py, sphsim/cli/main.py</files>
  <read_first>
    - sphsim/core/simulator.py (full file, especially lines 7-13 constructor and 80-105 run() loop sph_stp + valuation call sites)
    - sphsim/cli/main.py (lines 25-29 run_compare, 91-96 custom branch, 127-133 built-in branch — state AFTER Plan 01 with phi=args.phi/rho=args.rho already in place)
    - .planning/phases/05-configurable-environment/05-PATTERNS.md (simulator.py + main.py sections)
  </read_first>
  <action>
    Step A — Edit `sphsim/core/simulator.py`:

    1. Change SPHSimulator constructor signature (line 7-8) from:
       `def __init__(self, nU, nSUS, K0, K1, F, T, kappa, alpha, phi, rho, strategy_fn, params, seed=42):`
       to:
       `def __init__(self, nU, nSUS, K0, K1, F, T, kappa, alpha, phi, rho, strategy_fn, params, valuation_preset='window', seed=42):`
       Add `valuation_preset='window'` as the second-to-last keyword argument (before `seed`).

    2. Inside `__init__`, store the preset right after `self.phi, self.rho = phi, rho` (around line 11):
       `self.valuation_preset = valuation_preset`

    3. Update three call sites inside `run()`:
       - Line ~80: `z, y = sph_stp(u, self.s, self.nSUS, self.K0, self.K1)` becomes `z, y = sph_stp(u, self.s, self.nSUS, self.K0, self.K1, self.valuation_preset)`
       - Line ~82: `P_total = valuation(svc_to_cons, self.K0, self.K1) + z - y` becomes `P_total = valuation(svc_to_cons, self.K0, self.K1, self.valuation_preset) + z - y`
       - Line ~105: `val = valuation(svc_to_cons, self.K0, self.K1)` becomes `val = valuation(svc_to_cons, self.K0, self.K1, self.valuation_preset)`

    Step B — Edit `sphsim/cli/main.py` (three sites for K0 + valuation_preset threading):

    4. In `run_compare()` common dict (around line 25-29), replace `K0=DEFAULT_K0,` with `K0=args.K0,` AND add `valuation_preset=args.valuation,` on its own line within the dict. The full common dict after this edit:
       ```
       common = dict(
           nU=args.nU, nSUS=args.nSUS, K0=args.K0, K1=K1,
           F=DEFAULT_F, T=args.T, kappa=args.kappa, alpha=args.alpha,
           phi=args.phi, rho=args.rho, valuation_preset=args.valuation,
           params=params, seed=args.seed,
       )
       ```

    5. In the custom-strategy SPHSimulator call (around line 91-96), replace `K0=DEFAULT_K0,` with `K0=args.K0,` AND add `valuation_preset=args.valuation,` BEFORE the `strategy_fn=strategy_fn` line. Final shape:
       ```
       sim = SPHSimulator(
           nU=args.nU, nSUS=args.nSUS, K0=args.K0, K1=K1,
           F=DEFAULT_F, T=args.T, kappa=args.kappa, alpha=args.alpha,
           phi=args.phi, rho=args.rho, valuation_preset=args.valuation,
           strategy_fn=strategy_fn, params=params, seed=args.seed,
       )
       ```

    6. In the built-in-strategy SPHSimulator call (around line 127-133), apply the same two changes (K0=DEFAULT_K0 → K0=args.K0; add valuation_preset=args.valuation).

    Add inline Polish comment above the first edited line per site: `# Phase 5 ENV-02: K0/valuation_preset z args (default DEFAULT_K0/'window' via argparse).`

    Do NOT change tests/test_env.py in this task — Task 4 owns the new integration tests for end-to-end preset behavior.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; python scripts/regression_check.py &amp;&amp; python sph_sim.py --strategy naive --zeta 0.5 --valuation step --no-agent --seed 42 --json | python -c "import json,sys; d=json.load(sys.stdin); print('step KPI', d['metrics']['avg_val_last100'])" &amp;&amp; python sph_sim.py --strategy naive --zeta 0.5 --K0 80 --no-agent --seed 42 --json | python -c "import json,sys; d=json.load(sys.stdin); print('K0=80 KPI', d['metrics']['avg_val_last100'])"</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/regression_check.py` exits 0 (PASS 8/8). v1.0 baseline preserved: default `--valuation window` + DEFAULT_K0=100 + DEFAULT_PHI + DEFAULT_RHO must produce bit-identical output to all 8 fixtures.
    - `grep -c 'K0=DEFAULT_K0' sphsim/cli/main.py` returns 0 (no leftover hard-codes).
    - `grep -c 'K0=args.K0' sphsim/cli/main.py` returns 3 (one in run_compare, two in branches).
    - `grep -c 'valuation_preset=args.valuation' sphsim/cli/main.py` returns 3.
    - `grep -c 'self.valuation_preset' sphsim/core/simulator.py` returns ≥4 (1 init store + 3 thread-through call sites).
    - `python sph_sim.py --strategy naive --zeta 0.5 --valuation step --no-agent --seed 42 --json` exits 0 with parseable JSON.
    - `python sph_sim.py --strategy naive --zeta 0.5 --K0 80 --no-agent --seed 42 --json` exits 0 with parseable JSON.
    - `python -m unittest discover tests/ -v` exits 0 overall.
  </acceptance_criteria>
  <done>
    SPHSimulator accepts valuation_preset kwarg, stores it, threads it to sph_stp + 2 valuation call sites. main.py threads K0=args.K0 and valuation_preset=args.valuation at all 3 instantiation sites. Regression 8/8 PASS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add TestValuationPresets + TestPresetDistinguishability integration tests</name>
  <files>tests/test_env.py</files>
  <behavior>
    TestValuationPresets (3 integration cases — subprocess + JSON parse):
    - test_window_preset_matches_baseline: `python sph_sim.py --strategy naive --zeta 0.75 --valuation window --no-agent --seed 42 --json` exits 0 and `metrics.avg_val_last100 == 92.0` (baseline preservation under explicit window flag).
    - test_K0_override_changes_kpi: `python sph_sim.py --strategy naive --zeta 0.75 --K0 80 --no-agent --seed 42 --json` exits 0; `metrics.avg_val_last100` is a float (the run completes — proves K0 override threads through simulator). DO NOT assert specific KPI value (depends on dynamics) — just assert run succeeds and value is numeric.
    - test_K1_override_with_valuation: `python sph_sim.py --strategy naive --zeta 0.75 --K0 100 --K1 200 --no-agent --seed 42 --json` exits 0 with parseable JSON output.

    TestPresetDistinguishability (1 critical integration case — SC-3 enforcement):
    - test_three_presets_give_distinct_kpi: Run the same invocation `python sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 --json` three times with `--valuation window`, `--valuation step`, `--valuation linear`. Parse each `metrics.avg_val_last100`. Assert the three values are pairwise distinct: `assertNotEqual(window_kpi, step_kpi)`, `assertNotEqual(step_kpi, linear_kpi)`, `assertNotEqual(window_kpi, linear_kpi)`. This is the SC-3 hard requirement.
  </behavior>
  <read_first>
    - tests/test_env.py (current state — TestValuationPresets and TestPresetDistinguishability are Plan 00 skip stubs)
    - tests/test_agent.py (lines 207-238 — subprocess + JSON parse pattern)
    - .planning/phases/05-configurable-environment/05-RESEARCH.md §B.7 (distinguishability proof: window/step/linear give different g(u) for u=80, 110, 130 with K0=100, K1=120)
  </read_first>
  <action>
    Edit `tests/test_env.py` to REPLACE the `TestValuationPresets` and `TestPresetDistinguishability` skip stubs with full implementations matching `<behavior>`.

    Implementation notes:
    - Use the same `_run_sph`, `_PROJECT_ROOT`, `MONOLITH` constants already at module top.
    - For each subprocess call, assert `r.returncode == 0` first with informative error message including `r.stderr[:300]`.
    - Parse JSON with `json.loads(r.stdout)`; assert `'metrics' in d`.
    - For TestPresetDistinguishability, build a small helper inside the test method (no module-level helper needed) that takes a preset string, runs the invocation, and returns the avg_val_last100 float. Use `subTest` if desired for clearer failure attribution per-preset, but plain three separate assertNotEqual calls are also acceptable.
    - DO NOT skip on environment quirks — if any of the three invocations fails to produce a number, the test MUST FAIL (SC-3 is a hard requirement).

    Leave the remaining 2 skip stubs (TestConfigHeader, TestHumanHeader) UNTOUCHED — Plan 03 owns them.

    Add a comment at the top of TestPresetDistinguishability stating "SC-3 enforcement: all 3 presets MUST give distinct KPI on the same seed+strategy. RESEARCH §B.7 mathematically proves distinguishability for the default env (avg_providers ≈ 105, K0=100, K1=120). If this test fails for window vs step (the most likely identical pair), it indicates sph_stp is not threading preset (Pitfall 1)."
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; python -m unittest tests.test_env.TestValuationPresets tests.test_env.TestPresetDistinguishability -v 2>&amp;1 | tail -3 | grep -E "^OK$|^OK \(" &amp;&amp; python -m unittest discover tests/ -v 2>&amp;1 | tail -3 | grep -E "^OK$|^OK \("</automated>
  </verify>
  <acceptance_criteria>
    - `python -m unittest tests.test_env.TestValuationPresets -v` exits 0 with 3 tests PASS.
    - `python -m unittest tests.test_env.TestPresetDistinguishability -v` exits 0 with 1 test PASS (the SC-3 enforcement test — pairwise-distinct KPI across 3 presets).
    - `python -m unittest discover tests/ -v` exits 0 overall.
    - `python scripts/regression_check.py` exits 0 (PASS 8/8).
    - `grep -c "skipTest" tests/test_env.py` returns 2 (only TestConfigHeader + TestHumanHeader still skip — TestPhiRhoParsing/Flow done in Plan 01, TestValuationDispatch + the 2 new classes done in this plan).
  </acceptance_criteria>
  <done>
    TestValuationPresets has 3 real green tests; TestPresetDistinguishability has 1 real green test enforcing SC-3 (pairwise-distinct KPI for window/step/linear); full suite OK; regression PASS 8/8.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User CLI input → argparse | `--K0` (float) and `--valuation` (choices) inputs |
| args.valuation string → SPHSimulator.valuation_preset → sph_stp.preset → valuation.preset | string flows through 4 layers; any layer using wrong literal silently breaks preset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-02 | Tampering / Denial-of-Service | `--K0`/`--K1` accept non-finite floats (inf, nan) via Python `float()` | accept | argparse `type=float` accepts `'inf'`, `'-inf'`, `'nan'`. Downstream: `valuation()` with K0=inf returns 0.0 for all u (threshold never met). With K0=nan, comparisons return False, returning 0.0. With K1=inf, `valuation()` already has explicit `K1 == float('inf')` shortcut (no division). Linear preset has explicit `K1 <= 0 or K1 == inf` guard. No numerical explosion, no crash, no security impact. Documented as acceptable in single-process CLI. |
| T-5-02-DISP | Tampering | preset string dispatch in `valuation()` | mitigate | argparse `choices=['window', 'step', 'linear']` rejects any other value at parse time (exit 2). Inside `valuation()`, an unrecognised preset falls through to window branch (safe default). |
| T-5-02-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new packages. Stdlib only. |

ASVS L1 satisfied: argparse type+choices validators reject malformed input before simulator runs.
</threat_model>

<verification>
After all 4 tasks complete:

```bash
cd "$(git rev-parse --show-toplevel)"
python -m unittest tests.test_env -v                                                       # 5 classes green, 2 skip
python -m unittest discover tests/ -v                                                      # full suite OK
python scripts/regression_check.py                                                          # PASS 8/8
python sph_sim.py --help | grep -E '(\-\-K0|\-\-valuation)'                                # both flags visible
python sph_sim.py --strategy naive --zeta 0.5 --valuation step --K0 90 --no-agent --seed 42 --json | python -c "import json,sys; d=json.load(sys.stdin); print('OK avg_val=', d['metrics']['avg_val_last100'])"
```

All 5 commands MUST exit 0.
</verification>

<success_criteria>
1. ENV-02 fully delivered: `--K0` (float, default 100) and `--valuation` (choices window/step/linear, default window) flags accepted; all values flow into SPHSimulator → sph_stp → valuation.
2. SC-2 (ROADMAP) satisfied: `--valuation window|step|linear` selects preset; `--K0 X --K1 Y` gives full parametric control.
3. SC-3 (ROADMAP) satisfied: TestPresetDistinguishability asserts all 3 presets give pairwise-distinct `avg_val_last100` on the same seed+strategy.
4. v1.0 baseline preserved: regression_check.py PASS 8/8 (window + DEFAULT_K0 = identical to fixtures).
5. Pitfall 1 (RESEARCH §F.15) defused: `sph_stp` threads preset to its inner `P_of_x` closure — preset reaches STP optimisation, not just per-cycle valuation.
</success_criteria>

<output>
Create `.planning/phases/05-configurable-environment/05-02-SUMMARY.md` when done. Include: files modified (5), exact lines added/changed in args.py + model.py + simulator.py + main.py, list of test methods added per class, regression_check.py 8/8 confirmation, pairwise-distinct KPI values printed from TestPresetDistinguishability for window/step/linear (for SUMMARY-as-evidence), confirmation TestConfigHeader and TestHumanHeader still skip pending Plan 03.
</output>
