---
phase: 08-documentation-interactive-tutorial
plan: 02
type: execute
wave: 1
depends_on:
  - 08-00
files_modified:
  - sphsim/cli/args.py
  - sphsim/cli/main.py
autonomous: true
requirements:
  - TUT-05

must_haves:
  truths:
    - "`python sph_sim.py --tutorial` parses without error and sets `args.tutorial = True`"
    - "`python sph_sim.py --tutorial --strategy naive` errors with Polish message `Flaga --tutorial nie działa z --strategy ...`"
    - "`python sph_sim.py --tutorial --custom <path>` errors with Polish message `Flaga --tutorial nie działa z --custom.`"
    - "`python sph_sim.py --tutorial --batch --seeds 5` errors with Polish message `Flagi --tutorial i --batch są wzajemnie wykluczające.`"
    - "`python sph_sim.py --tutorial --interactive` errors with Polish message `Flagi --tutorial i --interactive są wzajemnie wykluczające.`"
    - "`python sph_sim.py --tutorial --compare-agent` errors with Polish message `Flagi --tutorial i --compare-agent są wzajemnie wykluczające.`"
    - "When `args.tutorial=True`, main.py dispatches to `run_repl(start_in_tutorial=True)` and returns before any other branch"
    - "Existing 4-way mutex (--strategy / --custom / --batch / --compare-agent / --interactive) is unchanged for non-tutorial invocations — backwards-compat preserved"
  artifacts:
    - path: "sphsim/cli/args.py"
      provides: "--tutorial flag + 5-way mutex extension via post-parse Polish errors"
      contains: "args.tutorial and args.interactive"
    - path: "sphsim/cli/main.py"
      provides: "4th early-branch dispatching --tutorial to run_repl(start_in_tutorial=True)"
      contains: "if args.tutorial:"
  key_links:
    - from: "sphsim/cli/args.py::parse_args"
      to: "argparse post-parse mutex block"
      via: "5 p.error() calls for tutorial combinations"
      pattern: "args.tutorial and args"
    - from: "sphsim/cli/main.py::main"
      to: "sphsim.cli.repl.run_repl"
      via: "deferred import + early return"
      pattern: "if args.tutorial:"
---

<objective>
Add `--tutorial` CLI flag (D-02) and wire it as a 5th mutex member via the Phase-7-style post-parse Polish error pattern (NOT inside `add_mutually_exclusive_group` — keeps argparse English errors from leaking). Add the 4th early branch in `main()` that routes `--tutorial` to `run_repl(start_in_tutorial=True)`. The `run_repl` signature change itself is owned by Plan 04; Plan 02 writes the call site to the future signature and leaves the end-to-end "actually enters tutorial mode" subprocess test skipped until Plan 04 lands.

Purpose: Make `--tutorial` parseable and reject conflicting combinations with friendly Polish errors so the test surface for TUT-05 is verifiable independently at Wave 1.
Output: 2 files modified. tests/test_tutorial.py::TestTutorialCLI gets its `@unittest.skip` flipped for the argparse-layer assertions (parsing + mutex errors); the end-to-end "tutorial banner prints" subprocess test stays skipped until Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-documentation-interactive-tutorial/08-CONTEXT.md
@.planning/phases/08-documentation-interactive-tutorial/08-RESEARCH.md
@.planning/phases/08-documentation-interactive-tutorial/08-PATTERNS.md

<interfaces>
sphsim/cli/args.py existing post-parse mutex block (lines 190-205): after `args = p.parse_args()` there are 6 `p.error(...)` calls covering compare_agent/no_agent, compare_agent/interactive, batch/compare_agent, batch/interactive, batch/seeds missing, seeds without batch. The block ends with `return args`. Reference signatures: `args.compare_agent`, `args.no_agent`, `args.interactive`, `args.batch`, `args.seeds`, `args.custom`, `args.strategy`.

sphsim/cli/args.py existing free-standing flag declaration pattern (lines 172-176): `--batch` and `--seeds` are declared OUTSIDE `add_mutually_exclusive_group` with a comment block: "INTENTIONALLY free-standing (NOT in any add_mutually_exclusive_group) so the Phase-7 post-parse Polish p.error fires BEFORE argparse's English fallback."

sphsim/cli/main.py existing --interactive early branch (lines 58-63):
```python
def main():
    args = parse_args()
    if args.interactive:
        from sphsim.cli.repl import run_repl
        run_repl()
        return
```

Forward dependency on Plan 04: Plan 04 will change `run_repl()` to `run_repl(start_in_tutorial: bool = False)` and inject `'tutorial'` into `shell.cmdqueue` when start_in_tutorial=True (RESEARCH lines 594-612). Plan 02 writes the call site `run_repl(start_in_tutorial=True)` to match.

Critical fact about argparse + `required=True` mutex group (args.py line 141): argparse enforces the group's `required=True` BEFORE post-parse code runs. So `python sph_sim.py --tutorial` alone would trip the English "one of the arguments --interactive --strategy --custom is required" error. Resolution in Task 1 below: change the group to `required=False` AND add a Polish post-parse check that exactly one mode is selected.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add --tutorial flag + 5-way post-parse mutex in args.py</name>
  <files>sphsim/cli/args.py, tests/test_tutorial.py</files>

  <read_first>
    - sphsim/cli/args.py (full — 206 lines; locate the mutex group declaration around line 141, the free-standing `--batch` block around lines 172-176, the post-parse mutex block around lines 190-205, and the `parse_args` return point)
    - .planning/phases/08-documentation-interactive-tutorial/08-PATTERNS.md (lines 151-207 — exact patch points for args.py, locked Polish error strings)
    - .planning/phases/08-documentation-interactive-tutorial/08-RESEARCH.md (lines 534-573 — --tutorial Flag Wiring Proposal; Assumption A2: post-parse pattern preferred over mutex group for Polish-error consistency)
    - tests/test_args_agent_flags.py (full — analog for argparse exit-code + stderr assertion pattern using subprocess.run; CRITICAL: this file exercises the existing 4-way mutex contract — verify the `required=True` → `required=False` change in Step B below does NOT regress any case here, especially the "no mode flag" required-mode error which now becomes a Polish post-parse `p.error` instead of argparse's English fallback)
  </read_first>

  <behavior>
    - Test 1: `subprocess.run([python, 'sph_sim.py', '--tutorial', '--help'])` exits 0 (--tutorial parseable; --help short-circuits before mutex checks).
    - Test 2: `subprocess.run([python, 'sph_sim.py', '--tutorial', '--interactive'])` exits non-zero with stderr containing `Flagi --tutorial i --interactive są wzajemnie wykluczające.`
    - Test 3: `subprocess.run([python, 'sph_sim.py', '--tutorial', '--strategy', 'naive'])` exits non-zero with stderr containing `Flaga --tutorial nie działa z --strategy`
    - Test 4: `subprocess.run([python, 'sph_sim.py', '--tutorial', '--custom', 'examples/custom_strategy_template.py'])` exits non-zero with stderr containing `Flaga --tutorial nie działa z --custom.`
    - Test 5: `subprocess.run([python, 'sph_sim.py', '--tutorial', '--batch', '--seeds', '5'])` exits non-zero with stderr containing `Flagi --tutorial i --batch są wzajemnie wykluczające.`
    - Test 6: `subprocess.run([python, 'sph_sim.py', '--tutorial', '--compare-agent'])` exits non-zero with stderr containing `Flagi --tutorial i --compare-agent są wzajemnie wykluczające.`
    - Test 7: `subprocess.run([python, 'sph_sim.py', '--strategy', 'naive', '--seed', '42', '--json', '--no-agent'])` STILL prints `avg_val_last100` (existing CLI unchanged).
    - Test 8: `subprocess.run([python, 'sph_sim.py'])` (no mode flag) exits non-zero with stderr containing `Musisz podać jeden z trybów:` (Polish required-mode replacement of argparse English error).
  </behavior>

  <action>
Edit `sphsim/cli/args.py` in 3 steps.

**Step A — declaration.** Just BELOW the existing free-standing `--batch` declaration (line ~173-176, in the same "INTENTIONALLY free-standing" comment block) add the new flag declaration. Use store_true with Polish help text:

`p.add_argument('--tutorial', action='store_true', help='Uruchom interaktywny tutorial v1.1 (≤15 min)')`

Keep the existing "INTENTIONALLY free-standing (NOT in any add_mutually_exclusive_group) so the Phase-7 post-parse Polish p.error fires BEFORE argparse's English fallback." comment applying to both `--batch` and the new `--tutorial`. Per RESEARCH Assumption A2.

**Step B — relax the mutex group.** Locate the mutex group declaration `mutex = p.add_mutually_exclusive_group(required=True)` (around line 141). Change `required=True` to `required=False`. This is necessary because argparse enforces `required=True` BEFORE post-parse code runs — leaving it True would trip the English "one of the arguments ... is required" error when invoking `python sph_sim.py --tutorial` alone (since `--tutorial` is intentionally NOT in the group).

**Step C — post-parse Polish errors.** Inside `parse_args()` after `args = p.parse_args()`, BEFORE the existing `if args.compare_agent and args.no_agent:` line, prepend the new required-mode check (replaces argparse's English mandatory-group error with a Polish one):

`if not (args.interactive or args.strategy or args.custom or args.batch or args.tutorial):`
`    p.error("Musisz podać jeden z trybów: --interactive, --strategy, --custom, --batch lub --tutorial.")`

Then, just BEFORE the existing `if args.batch and args.seeds is None:` line, insert the 5 tutorial-conflict checks (the order matches PATTERNS.md lines 193-201):

`if args.tutorial and args.interactive:`
`    p.error("Flagi --tutorial i --interactive są wzajemnie wykluczające.")`
`if args.tutorial and getattr(args, 'strategy', None):`
`    p.error("Flaga --tutorial nie działa z --strategy (użyj trybu tutorial interaktywnie).")`
`if args.tutorial and args.custom:`
`    p.error("Flaga --tutorial nie działa z --custom.")`
`if args.tutorial and args.batch:`
`    p.error("Flagi --tutorial i --batch są wzajemnie wykluczające.")`
`if args.tutorial and args.compare_agent:`
`    p.error("Flagi --tutorial i --compare-agent są wzajemnie wykluczające.")`

`getattr(args, 'strategy', None)` is defensive in case the choices=arg evaluates to non-Falsy when None — direct `args.strategy` works in practice but `getattr` matches PATTERNS line 207.

All Polish strings VERBATIM from PATTERNS.md lines 193-201 — do NOT alter wording, punctuation, or trailing periods. Tests grep stderr for these exact substrings.

**Step D — test flip.** In `tests/test_tutorial.py::TestTutorialCLI` remove the broad `@unittest.skip` and split into 8 test methods matching the 8 behavior items above. Use the `_run_sph` helper pattern from `tests/test_batch.py` lines 38-48 (subprocess.run + SPHSIM_NO_REPORT=1 default). Method names:

- `test_tutorial_flag_parses_without_error` (Test 1)
- `test_tutorial_plus_interactive_errors_polish` (Test 2)
- `test_tutorial_plus_strategy_errors_polish` (Test 3)
- `test_tutorial_plus_custom_errors_polish` (Test 4)
- `test_tutorial_plus_batch_errors_polish` (Test 5)
- `test_tutorial_plus_compare_agent_errors_polish` (Test 6)
- `test_existing_cli_unchanged_baseline_works` (Test 7)
- `test_no_mode_errors_polish` (Test 8)

Keep an additional method `test_tutorial_flag_enters_tutorial_mode` STILL `@unittest.skip("Wave 2 — plan 08-04 wires run_repl(start_in_tutorial=True)")` — that subprocess end-to-end test pipes `printf 'skip\nskip\nskip\nskip\nskip\nskip\nskip\nskip\nexit\n' | python sph_sim.py --tutorial` and would fail at runtime until Plan 04 lands the run_repl signature change.

For Test 1 use `--tutorial --help` (--help short-circuits inside argparse before mutex checks).
For Test 7 use `--no-agent` flag so deterministic baseline matches `avg_val_last100=92.0` (RESEARCH §Shape-Match Strategy Worked Example 1).
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; SPHSIM_NO_REPORT=1 python -m unittest tests.test_tutorial.TestTutorialCLI 2>&amp;1 | tail -3 | grep -E 'OK|skipped' &amp;&amp; SPHSIM_NO_REPORT=1 python -m unittest tests.test_args_agent_flags 2>&amp;1 | tail -3 | grep OK &amp;&amp; SPHSIM_NO_REPORT=1 python -m unittest discover tests 2>&amp;1 | tail -3 | grep -E 'OK|skipped' &amp;&amp; python scripts/regression_check.py 2>&amp;1 | tail -3 | grep -E 'PASS=8|OK'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "'--tutorial'" sphsim/cli/args.py` outputs at least 1 (declaration)
    - `grep -c 'args.tutorial and args.interactive' sphsim/cli/args.py` outputs 1
    - `grep -c 'args.tutorial and args.batch' sphsim/cli/args.py` outputs 1
    - `grep -c 'args.tutorial and args.custom' sphsim/cli/args.py` outputs 1
    - `grep -c 'args.tutorial and args.compare_agent' sphsim/cli/args.py` outputs 1
    - `grep -c "Flagi --tutorial i --interactive" sphsim/cli/args.py` outputs 1
    - `grep -c "Flagi --tutorial i --batch" sphsim/cli/args.py` outputs 1
    - `grep -c "Flagi --tutorial i --compare-agent" sphsim/cli/args.py` outputs 1
    - `grep -c "Flaga --tutorial nie działa z --strategy" sphsim/cli/args.py` outputs 1
    - `grep -c "Flaga --tutorial nie działa z --custom" sphsim/cli/args.py` outputs 1
    - `grep -c "Musisz podać jeden z trybów" sphsim/cli/args.py` outputs 1
    - `grep -c 'required=True' sphsim/cli/args.py` outputs 0 OR documented elsewhere — verify by reading line 141; required=False after this plan
    - Behavior — argparse layer: `python sph_sim.py --tutorial --help 2>&amp;1 | grep -F -- '--tutorial'` exits 0
    - Behavior — mutex: `python sph_sim.py --tutorial --interactive 2>&amp;1 | grep -F 'Flagi --tutorial i --interactive są wzajemnie wykluczające'` exits 0
    - Behavior — mutex: `python sph_sim.py --tutorial --strategy naive 2>&amp;1 | grep -F 'Flaga --tutorial nie działa z --strategy'` exits 0
    - Behavior — required-mode: `python sph_sim.py 2>&amp;1 | grep -F 'Musisz podać jeden z trybów'` exits 0
    - Behavior — regression: `SPHSIM_NO_REPORT=1 python sph_sim.py --strategy naive --seed 42 --json --no-agent 2>&amp;1 | grep -F 'avg_val_last100'` exits 0
    - `SPHSIM_NO_REPORT=1 python -m unittest tests.test_tutorial.TestTutorialCLI` exits 0 (8 tests pass; 1 still skipped pointing at Plan 04)
    - `python -m unittest discover tests` exits 0 (no regressions across all modules)
    - `python scripts/regression_check.py` exits 0 OR PASS=8/8 (CLI-04 backwards-compat preserved)
  </acceptance_criteria>

  <done>
    --tutorial flag is parseable. All 5 conflict combinations produce Polish-localized errors verbatim per PATTERNS.md. Required-mode check converted from argparse English to Polish post-parse error. Existing CLI invocations unchanged (regression_check PASS=8/8). 8 tests in TestTutorialCLI pass + 1 end-to-end test still skipped pointing at Plan 04.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 4th early branch in main.py for --tutorial</name>
  <files>sphsim/cli/main.py</files>

  <read_first>
    - sphsim/cli/main.py (full — 194 lines; locate the `--interactive` early branch around lines 58-63, the `--batch` and `--compare-agent` branches similarly)
    - .planning/phases/08-documentation-interactive-tutorial/08-PATTERNS.md (lines 210-242 — main.py 4th early branch exact pattern)
    - .planning/phases/08-documentation-interactive-tutorial/08-RESEARCH.md (lines 575-612 — main.py changes, run_repl signature change, cmdqueue injection)
  </read_first>

  <behavior>
    - Test 1: When the user passes `--tutorial`, main() calls `run_repl(start_in_tutorial=True)` and returns BEFORE the existing `--interactive` branch runs.
    - Test 2: When `--tutorial` is not passed AND `--interactive` is, main() calls `run_repl()` (no kwarg — backwards compat unchanged).
    - Test 3: When neither flag is passed, the one-shot CLI path runs as before.
  </behavior>

  <action>
Edit `sphsim/cli/main.py`. Locate the existing `args.interactive` early branch (around lines 58-63). Insert a NEW branch IMMEDIATELY BEFORE it so `--tutorial` is checked first:

`if args.tutorial:`
`    from sphsim.cli.repl import run_repl`
`    run_repl(start_in_tutorial=True)`
`    return`

The deferred `from sphsim.cli.repl import run_repl` mirrors the existing `--interactive` pattern — do NOT hoist to module level (PATTERNS line 239-241).

`run_repl(start_in_tutorial=True)` is a FORWARD REFERENCE to Plan 04's signature change. At Plan 02 time, `run_repl()` has no `start_in_tutorial` kwarg — invoking `--tutorial` at this wave will raise `TypeError: run_repl() got an unexpected keyword argument 'start_in_tutorial'` AT RUNTIME (not import time). This is acceptable because: (a) the args.py + main.py wiring is independently testable via subprocess argparse tests (Task 1), (b) Plan 04 lands the run_repl signature change in Wave 2 before any test invokes `--tutorial` end-to-end, (c) tests/test_tutorial.py::TestTutorialCLI's end-to-end test stays `@unittest.skip` until Plan 04 lands.

Do NOT add a no-op stub `def run_repl(start_in_tutorial=False)` in repl.py at this wave — that would be premature; Plan 04 owns the repl.py changes entirely.

No test file flip needed for this task — the args.py tests (Task 1) already cover the parse layer; the end-to-end flag test is owned by Plan 04. Verification here relies on grep + `python -m unittest discover tests` exiting 0 (no import-time regressions from main.py edits).
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; grep -B1 -A3 'if args.tutorial:' sphsim/cli/main.py | grep -F 'run_repl(start_in_tutorial=True)' &amp;&amp; SPHSIM_NO_REPORT=1 python -m unittest discover tests 2>&amp;1 | tail -3 | grep -E 'OK|skipped' &amp;&amp; python scripts/regression_check.py 2>&amp;1 | tail -3 | grep -E 'PASS=8|OK'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'if args.tutorial:' sphsim/cli/main.py` outputs 1
    - `grep -c 'run_repl(start_in_tutorial=True)' sphsim/cli/main.py` outputs 1
    - `grep -c 'from sphsim.cli.repl import run_repl' sphsim/cli/main.py` outputs at least 2 (one for --tutorial branch, one for --interactive branch)
    - The `if args.tutorial:` block appears in source BEFORE the `if args.interactive:` block (verify by `grep -n` line ordering)
    - `python -c "import sphsim.cli.main"` exits 0 (no syntax errors)
    - `python -m unittest discover tests` exits 0 (no regressions)
    - `python scripts/regression_check.py` exits 0 OR PASS=8/8 (CLI-04 preserved)
  </acceptance_criteria>

  <done>
    main.py has the 4th early branch wired to `run_repl(start_in_tutorial=True)` with the deferred import pattern matching the existing `--interactive` branch. Branch order: tutorial → interactive → batch → compare-agent → one-shot. Full test suite still green. Regression check PASS=8/8.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user shell → argparse | --tutorial flag is store_true (no user-supplied value); no injection surface |
| argparse → main.py | args.tutorial is bool; deferred import of run_repl is a hardcoded module path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | Existing 4-way mutex invariant for non-tutorial invocations | mitigate | Test 7 (regression_check.py PASS=8/8) + existing tests/test_args_agent_flags.py covers all prior mutex combinations |
| T-08-02-02 | Information Disclosure | argparse English error leakage for required-mode case | mitigate | Required-mode check converted from `required=True` (English) to post-parse Polish `Musisz podać jeden z trybów:` (Test 8) |
| T-08-02-03 | Denial of Service | TypeError at runtime when --tutorial invoked before Plan 04 lands | accept | Forward-reference is documented in action; tests/test_tutorial.py::TestTutorialCLI end-to-end test stays @unittest.skip until Plan 04; Wave 1 → Wave 2 dependency closed by Plan 04 |
| T-08-SC | Tampering | npm/pip installs | n/a | Plan 08-02 installs no packages |
</threat_model>

<verification>
- --tutorial flag parses without error and integrates into the 5-way post-parse mutex with Polish errors verbatim per PATTERNS.md.
- main.py dispatches --tutorial to `run_repl(start_in_tutorial=True)` BEFORE the --interactive branch.
- All existing CLI invocations work unchanged — regression_check PASS=8/8.
- 8 new tests in tests/test_tutorial.py::TestTutorialCLI pass; 1 end-to-end test still skipped (Plan 04 dependency).
</verification>

<success_criteria>
- [x] `--tutorial` declared as free-standing store_true flag with Polish help
- [x] Mutex group changed from required=True to required=False
- [x] Polish required-mode check `Musisz podać jeden z trybów:` lands in post-parse
- [x] 5 tutorial-conflict Polish errors land in post-parse, verbatim per PATTERNS.md
- [x] main.py has 4th early branch for `--tutorial` calling `run_repl(start_in_tutorial=True)`
- [x] 8 new test methods in TestTutorialCLI all pass; end-to-end test stays skipped
- [x] regression_check.py PASS=8/8 (CLI-04 preserved)
</success_criteria>

<output>
Create `.planning/phases/08-documentation-interactive-tutorial/08-02-SUMMARY.md` when done. List: (a) args.py declaration + 5 polish errors + required-mode check + group-required change, (b) main.py 4th early branch, (c) 8 TestTutorialCLI tests passing, 1 still skipped pointing at Plan 04, (d) regression_check PASS=8/8 confirmation, (e) any subtlety encountered during the `required=True → required=False` change.
</output>
