---
phase: 08-documentation-interactive-tutorial
plan: 01
type: execute
wave: 1
depends_on:
  - 08-00
files_modified:
  - sphsim/report/__init__.py
autonomous: true
requirements:
  - TUT-06

must_haves:
  truths:
    - "write_report() accepts new keyword-only kwarg report_dir_override=None (D-10)"
    - "write_batch_report() accepts new keyword-only kwarg report_dir_override=None (D-10)"
    - "When report_dir_override is set, write_report uses it directly via mkdir(parents=True, exist_ok=True), bypassing _resolve_report_dir entirely (per RESEARCH §Anti-Patterns)"
    - "When report_dir_override is set, write_batch_report uses it directly, bypassing the inline batch_<ts>/ mkdir block"
    - "SPHSIM_NO_REPORT=1 opt-out still fires BEFORE report_dir_override logic (Pitfall 4: env-var check stays at line 99 / line ~189)"
    - "Default behavior (override=None) is byte-identical to v1.1.7 — backwards-compat verified by regression_check.py PASS=8/8"
  artifacts:
    - path: "sphsim/report/__init__.py"
      provides: "write_report + write_batch_report with report_dir_override kwarg"
      contains: "report_dir_override=None"
      contains: "report_dir.mkdir(parents=True, exist_ok=True)"
  key_links:
    - from: "sphsim/report/__init__.py::write_report"
      to: "Path(report_dir_override).mkdir"
      via: "early-return branch before _resolve_report_dir"
      pattern: "if report_dir_override is not None"
    - from: "sphsim/report/__init__.py::write_batch_report"
      to: "Path(report_dir_override).mkdir"
      via: "early-return branch before inline batch_ts mkdir"
      pattern: "if report_dir_override is not None"
---

<objective>
Implement D-10: extend `write_report()` and `write_batch_report()` in `sphsim/report/__init__.py` with optional keyword-only `report_dir_override=None`. When provided, the caller's path is used directly (with `mkdir(parents=True, exist_ok=True)`) — bypassing the default timestamped `./reports/<ts>/` and `./reports/batch_<ts>/` paths. Default behavior is byte-identical to current — backwards-compat is hard-locked.

Purpose: Tutorial mode (Plan 04) needs to land reports at `./reports/tutorial-<ts>/step-N-<topic>/` instead of polluting the normal `./reports/<ts>/` namespace. This plan provides the wiring; Plan 04 calls it.
Output: One file modified. Existing 21 test modules still green (regression_check.py PASS=8/8 invariant preserved). New stub `tests/test_tutorial.py::TestTutorialReports` is flipped from `@unittest.skip` to a real assertion verifying override end-to-end via direct function call (subprocess wiring lands in Plan 04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-documentation-interactive-tutorial/08-CONTEXT.md
@.planning/phases/08-documentation-interactive-tutorial/08-RESEARCH.md
@.planning/phases/08-documentation-interactive-tutorial/08-PATTERNS.md

<interfaces>
<!-- write_report current signature (sphsim/report/__init__.py line 79): -->
```python
def write_report(args, res, params, K1, *, mode='single'):
    if os.environ.get('SPHSIM_NO_REPORT') == '1':
        return None
    try:
        try:
            report_dir = _resolve_report_dir()
        except OSError as e:
            print(f'[OSTRZEŻENIE] Nie udało się utworzyć katalogu raportu: {e}. '
                  f'Raport pominięty.', file=sys.stderr)
            return None
        # ... write report.md + PNGs ...
        return report_dir
```

<!-- write_batch_report inline mkdir (lines ~190-203): -->
```python
def write_batch_report(args, aggregate, per_seed_results, K1):
    if os.environ.get('SPHSIM_NO_REPORT') == '1':
        return None
    try:
        ts = _timestamp()
        base = Path('reports') / f'batch_{ts}'
        n = 1
        while base.exists():
            n += 1
            base = Path('reports') / f'batch_{ts}-{n}'
        base.mkdir(parents=True, exist_ok=False)
        report_dir = base
    except OSError as e:
        ...
```

<!-- Target signature (RESEARCH §Pattern 6, lines 341-352): -->
```python
def write_report(args, res, params, K1, *, mode='single', report_dir_override=None):
def write_batch_report(args, aggregate, per_seed_results, K1, *, report_dir_override=None):
```

<!-- TutorialFlow.step_report_dir (created in Plan 03): returns Path('reports') / f'tutorial-{ts}' / f'step-{N}-{topic}'. write_report does NOT need to know about TutorialFlow — it just accepts a Path. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add report_dir_override to write_report</name>
  <files>sphsim/report/__init__.py, tests/test_tutorial.py</files>

  <read_first>
    - sphsim/report/__init__.py (full — 230 lines; locate write_report signature line 79, opt-out check line 99, _resolve_report_dir() call site)
    - .planning/phases/08-documentation-interactive-tutorial/08-PATTERNS.md (lines 245-320 — exact patch points for write_report)
    - .planning/phases/08-documentation-interactive-tutorial/08-RESEARCH.md (lines 333-365 — Pattern 6 D-10 override; lines 855-859 — Pitfall 4 SPHSIM_NO_REPORT precedence)
    - tests/test_report.py (full — analog test class for write_report tests; uses tempfile.mkdtemp + os.chdir setUp/tearDown pattern)
  </read_first>

  <behavior>
    - Test 1: `write_report(args, res, params, K1, mode='single', report_dir_override=Path('reports/tutorial-X/step-1-baseline'))` returns the override path and creates report.md + PNGs at that path (no `./reports/<ts>/`).
    - Test 2: `write_report(args, res, params, K1, mode='single', report_dir_override=None)` produces identical output to v1.1.7 — file structure under `./reports/<ts>/` unchanged.
    - Test 3: With `SPHSIM_NO_REPORT=1`, `write_report(..., report_dir_override=Path('/tmp/should_not_exist'))` returns None and does NOT create the override directory (env-var check fires first).
    - Test 4: `report_dir_override` is keyword-only (positional call raises TypeError) — same convention as existing `mode=` kwarg.
  </behavior>

  <action>
Edit `sphsim/report/__init__.py` to extend `write_report` signature to `def write_report(args, res, params, K1, *, mode='single', report_dir_override=None):` (one keyword added after the existing `*` keyword-only marker).

Inside `write_report`, AFTER the existing `if os.environ.get('SPHSIM_NO_REPORT') == '1': return None` opt-out (line ~99 — Pitfall 4 mandates this stays FIRST), and replacing the existing `try: report_dir = _resolve_report_dir()` block:

```
if report_dir_override is not None:
    report_dir = Path(report_dir_override)
    report_dir.mkdir(parents=True, exist_ok=True)
else:
    try:
        report_dir = _resolve_report_dir()
    except OSError as e:
        print(f'[OSTRZEŻENIE] Nie udało się utworzyć katalogu raportu: {e}. '
              f'Raport pominięty.', file=sys.stderr)
        return None
```

Notable: the override branch uses `exist_ok=True` (not `exist_ok=False`) because the tutorial caller may call it multiple times during a session for the same step on hint retries. The default branch keeps `exist_ok=False` via `_resolve_report_dir`'s collision-retry behavior (unchanged).

Polish error message (kept verbatim — `[OSTRZEŻENIE] Nie udało się utworzyć katalogu raportu: {e}. Raport pominięty.`) matches existing style — do NOT alter it.

In `tests/test_tutorial.py`, flip the TestTutorialReports class: remove the `@unittest.skip` decorator on `test_tutorial_reports_go_to_dedicated_dir` and add a new method `test_report_dir_override_creates_path_and_writes_files`. Test body uses the existing `tests/test_report.py` setUp/tearDown pattern (tempfile.mkdtemp + os.chdir before/after; `SPHSIM_NO_REPORT=''`); imports `write_report` from `sphsim.report`; calls it with `report_dir_override=Path('reports/tutorial-test/step-1-baseline')` and asserts that path exists with `report.md` + `decision_distribution.png` + `kpi_timeseries.png` files inside.

Build `args` for the test using the existing `fixtures` directory or by importing `argparse.Namespace` directly with the same shape as `repl.py` `fake_args` (line ~225 reference — strategy='naive', nU=DEFAULT_NU, etc.). Use seed=42 for determinism.

Keep the existing tests `test_tutorial_reports_go_to_dedicated_dir` as a placeholder `@unittest.skip("Wave 2 — plan 08-04 wires _tutorial_state into do_run")` for now; Plan 04 will flip it.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; SPHSIM_NO_REPORT=1 python -m unittest tests.test_report 2>&amp;1 | tail -3 | grep OK &amp;&amp; SPHSIM_NO_REPORT='' python -m unittest tests.test_tutorial.TestTutorialReports.test_report_dir_override_creates_path_and_writes_files 2>&amp;1 | tail -3 | grep OK &amp;&amp; SPHSIM_NO_REPORT=1 python -m unittest discover tests 2>&amp;1 | tail -3 | grep -E 'OK|skipped'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'report_dir_override=None' sphsim/report/__init__.py` outputs at least 1 (write_report signature)
    - `grep -c 'if report_dir_override is not None:' sphsim/report/__init__.py` outputs at least 1 (write_report body)
    - `grep -c 'report_dir.mkdir(parents=True, exist_ok=True)' sphsim/report/__init__.py` outputs at least 1
    - `python -c "import inspect; from sphsim.report import write_report; sig = inspect.signature(write_report); assert 'report_dir_override' in sig.parameters; assert sig.parameters['report_dir_override'].kind == inspect.Parameter.KEYWORD_ONLY"` exits 0
    - `python -c "import inspect; from sphsim.report import write_report; sig = inspect.signature(write_report); assert sig.parameters['report_dir_override'].default is None"` exits 0
    - `SPHSIM_NO_REPORT=1 python -m unittest tests.test_report` exits 0 (existing tests still pass; opt-out branch unchanged)
    - `SPHSIM_NO_REPORT='' python -m unittest tests.test_tutorial.TestTutorialReports.test_report_dir_override_creates_path_and_writes_files` exits 0
    - `python -m unittest discover tests` exits 0 (no regressions across all 22 modules; only the new test added; original `test_tutorial_reports_go_to_dedicated_dir` still skipped pointing at Plan 04)
    - `python scripts/regression_check.py` exits 0 OR PASS=8/8 (8 baseline JSON fixtures still byte-identical for `--seed 42` — D-10 default branch unchanged)
  </acceptance_criteria>

  <done>
    write_report accepts report_dir_override; default branch unchanged (backwards-compat verified by regression_check + existing test_report); override branch writes report.md + PNGs to the supplied path. New test in TestTutorialReports passes; original TUT-06 subprocess test still skipped pointing at Plan 04.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add report_dir_override to write_batch_report</name>
  <files>sphsim/report/__init__.py, tests/test_tutorial.py</files>

  <read_first>
    - sphsim/report/__init__.py (lines 156-230 — write_batch_report; INDEPENDENT mkdir block at lines ~190-203, does NOT call _resolve_report_dir)
    - .planning/phases/08-documentation-interactive-tutorial/08-PATTERNS.md (lines 282-320 — write_batch_report patch; Pitfall 5 lines 861-865)
    - .planning/phases/08-documentation-interactive-tutorial/08-RESEARCH.md (lines 861-865 — Pitfall 5 write_batch_report independent mkdir)
    - tests/test_batch_report.py (full — analog test class for write_batch_report tests)
  </read_first>

  <behavior>
    - Test 1: `write_batch_report(args, aggregate, per_seed_results, K1, report_dir_override=Path('reports/tutorial-X/step-8-batch'))` returns the override path and creates batch_report.md + batch_aggregate.png at that path.
    - Test 2: `write_batch_report(...)` with `report_dir_override=None` is byte-identical to v1.1.7 — `./reports/batch_<ts>/` structure unchanged.
    - Test 3: With `SPHSIM_NO_REPORT=1`, `write_batch_report(..., report_dir_override=Path('/tmp/should_not_exist'))` returns None and does NOT create the override directory.
    - Test 4: `report_dir_override` is keyword-only.
  </behavior>

  <action>
Edit `sphsim/report/__init__.py` to extend `write_batch_report` signature to `def write_batch_report(args, aggregate, per_seed_results, K1, *, report_dir_override=None):` (add the `*` keyword-only marker if not present; add the new kwarg after).

Inside `write_batch_report`, AFTER the existing `if os.environ.get('SPHSIM_NO_REPORT') == '1': return None` opt-out check, replace the existing inline mkdir block (lines ~190-203 — `ts = _timestamp(); base = Path('reports') / f'batch_{ts}'; ...`):

```
if report_dir_override is not None:
    report_dir = Path(report_dir_override)
    report_dir.mkdir(parents=True, exist_ok=True)
else:
    try:
        ts = _timestamp()
        base = Path('reports') / f'batch_{ts}'
        n = 1
        while base.exists():
            n += 1
            base = Path('reports') / f'batch_{ts}-{n}'
        base.mkdir(parents=True, exist_ok=False)
        report_dir = base
    except OSError as e:
        print(f'[OSTRZEŻENIE] Nie udało się utworzyć katalogu raportu batchowego: {e}. '
              f'Raport pominięty.', file=sys.stderr)
        return None
```

Polish error message (`[OSTRZEŻENIE] Nie udało się utworzyć katalogu raportu batchowego:`) matches existing style for batch — preserve verbatim.

In `tests/test_tutorial.py::TestTutorialReports`, add a second test method `test_batch_report_dir_override_creates_path_and_writes_files`. Use the `tests/test_batch_report.py` setUp/tearDown pattern (tempfile.mkdtemp + os.chdir). Build args + aggregate + per_seed_results using fixtures from `tests/test_batch_report.py` (or duplicate inline if simpler). Call `write_batch_report(..., report_dir_override=Path('reports/tutorial-test/step-8-batch'))` and assert the override path exists with `report.md` (or whatever filename `write_batch_report` produces — verify in existing test_batch_report) + `batch_aggregate.png` files inside.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; SPHSIM_NO_REPORT=1 python -m unittest tests.test_batch_report 2>&amp;1 | tail -3 | grep OK &amp;&amp; SPHSIM_NO_REPORT='' python -m unittest tests.test_tutorial.TestTutorialReports.test_batch_report_dir_override_creates_path_and_writes_files 2>&amp;1 | tail -3 | grep OK &amp;&amp; SPHSIM_NO_REPORT=1 python -m unittest discover tests 2>&amp;1 | tail -3 | grep -E 'OK|skipped'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'def write_batch_report.*report_dir_override' sphsim/report/__init__.py` outputs 1
    - `python -c "import inspect; from sphsim.report import write_batch_report; sig = inspect.signature(write_batch_report); assert 'report_dir_override' in sig.parameters; assert sig.parameters['report_dir_override'].kind == inspect.Parameter.KEYWORD_ONLY; assert sig.parameters['report_dir_override'].default is None"` exits 0
    - `SPHSIM_NO_REPORT=1 python -m unittest tests.test_batch_report` exits 0 (existing batch tests still pass; default branch unchanged)
    - `SPHSIM_NO_REPORT='' python -m unittest tests.test_tutorial.TestTutorialReports.test_batch_report_dir_override_creates_path_and_writes_files` exits 0
    - `python -m unittest discover tests` exits 0 (no regressions)
    - `python scripts/regression_check.py` exits 0 OR PASS=8/8 (regression invariant preserved)
  </acceptance_criteria>

  <done>
    write_batch_report accepts report_dir_override; default branch unchanged; override branch writes batch report files to the supplied path. Test passes; full unittest discover green; regression check green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → write_report/write_batch_report | report_dir_override is a Path constructed by tutorial code (TutorialFlow.step_report_dir) — not user input, so no path-traversal vector |
| filesystem → ./reports/ | mkdir(parents=True, exist_ok=True) on override may create directories outside the expected tutorial-<ts>/ subtree if a malicious tutorial caller passed an arbitrary path — but tutorial caller is internal code, not user-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Information Disclosure | report_dir_override path traversal | accept | Path is constructed internally by TutorialFlow (Plan 03) from a timestamp + step number + topic slug — not user input. RESEARCH §Security Domain V5 confirms no attack surface (line 1001). |
| T-08-01-02 | Tampering | Backwards-compat for existing callers (default branch) | mitigate | `report_dir_override=None` default + existing test_report.py + test_batch_report.py + regression_check.py PASS=8/8 invariant lock the default behavior byte-for-byte |
| T-08-01-03 | DoS | SPHSIM_NO_REPORT=1 bypassed by override | mitigate | Env-var check stays FIRST in both functions (line ~99 / ~189) — RESEARCH Pitfall 4 explicitly verified; override branch is AFTER the env check |
| T-08-SC | Tampering | npm/pip installs | n/a | Plan 08-01 installs no packages |
</threat_model>

<verification>
- write_report + write_batch_report both expose `report_dir_override` keyword-only kwarg with `None` default.
- Override path produces files at exactly that path; default branch unchanged.
- SPHSIM_NO_REPORT=1 still wins over override (env check first).
- All 22 test modules green (+ 2 new tests in test_tutorial.py for D-10 wiring).
- regression_check.py PASS=8/8 (D-10 default branch byte-identical to v1.0).
</verification>

<success_criteria>
- [x] `report_dir_override` kwarg on write_report (keyword-only, default None)
- [x] `report_dir_override` kwarg on write_batch_report (keyword-only, default None)
- [x] Override branch uses `mkdir(parents=True, exist_ok=True)` per RESEARCH §Pattern 6
- [x] Env-var opt-out check stays first (Pitfall 4 mitigation verified)
- [x] regression_check.py PASS=8/8 (backwards compat hard-locked)
- [x] 2 new tests in tests/test_tutorial.py::TestTutorialReports flipped from skip to passing
</success_criteria>

<output>
Create `.planning/phases/08-documentation-interactive-tutorial/08-01-SUMMARY.md` when done. List: (a) write_report new signature, (b) write_batch_report new signature, (c) 2 new tests added, (d) regression_check.py PASS=8/8 confirmation, (e) confirm SPHSIM_NO_REPORT=1 opt-out still wins.
</output>
