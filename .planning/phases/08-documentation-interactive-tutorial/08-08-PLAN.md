---
phase: 08-documentation-interactive-tutorial
plan: 08
type: execute
wave: 5
depends_on:
  - 08-04
files_modified:
  - sphsim/cli/repl.py
autonomous: true
gap_closure: true
requirements:
  - TUT-01
  - TUT-02

must_haves:
  truths:
    - "After booting `python sph_sim.py --interactive`, the INTRO banner mentions the `tutorial` command between the `help` and `exit` pointer lines (Gap 1 — discoverability of the ≤15-min onboarding path)"
    - "Inside the tutorial, every step display (after the bottom rule of _show_tutorial_step) prints the controls line `Sterowanie: skip | back | repeat | exit` so the user is reminded of available verbs on every krok (Gap 2 — controls visibility throughout flow)"
    - "The controls line `Sterowanie: skip | back | repeat | exit` is defined exactly ONCE in repl.py as a module-level constant `_TUTORIAL_CONTROLS_LINE` and reused by both the entry banner (do_tutorial) and the per-step display (_show_tutorial_step) — DRY"
    - "check_step rejects steps whose REPL output was `Nieznana komenda` — typos do NOT auto-pass soft-pass steps 6/7 (Gap 4)"
    - "Existing tutorial behavior unchanged: banner shows once on entry, step 1/8 description block prints, skip/back/repeat/exit still work, ✓ zaliczone fires for real commands, regression_check.py PASS=8/8"
  artifacts:
    - path: "sphsim/cli/repl.py"
      provides: "INTRO with tutorial pointer + _TUTORIAL_CONTROLS_LINE module-level constant + per-step controls footer + _last_command_unknown flag plumbed through __init__/default/precmd/postcmd"
      contains: "_TUTORIAL_CONTROLS_LINE"
      contains: "tutorial` żeby uruchomić"
      contains: "_last_command_unknown"
  key_links:
    - from: "sphsim/cli/repl.py INTRO constant"
      to: "user reading first banner of --interactive boot"
      via: "cmd.Cmd.intro emitted by cmdloop()"
      pattern: "tutorial.*żeby uruchomić interaktywny tutorial"
    - from: "sphsim/cli/repl.py _show_tutorial_step"
      to: "user seeing controls reminder on every step display"
      via: "print(_TUTORIAL_CONTROLS_LINE) after bottom rule"
      pattern: "Sterowanie: skip \\| back \\| repeat \\| exit"
    - from: "sphsim/cli/repl.py SPHShell.default"
      to: "sphsim/cli/repl.py SPHShell.postcmd"
      via: "self._last_command_unknown flag short-circuits check_step invocation"
      pattern: "self._last_command_unknown"
---

<objective>
Close UAT Gaps 1, 2, and 4 — three small, related repl.py edits that together fix
tutorial discoverability and rejection of garbage input on soft-pass steps. All
three gaps touch ONLY sphsim/cli/repl.py so they ship in a single plan to
minimize merge surface and commit once.

Purpose: Restore the contract that
  (a) new users discover `tutorial` from the INTRO banner of --interactive,
  (b) the four control verbs (skip|back|repeat|exit) remain visible on every
      step display (not just the one-time entry banner),
  (c) soft-pass steps 6/7 reject typos that SPHShell.default() rejects as
      `Nieznana komenda` (current bug: bool(line) accepts garbage).

Output: Updated sphsim/cli/repl.py + green tests/test_tutorial.py (all existing
TestTutorial* classes still pass; we add no new assertions in this plan — the
existing TestTutorialEntry banner test plus the proposed new
test_soft_pass_step_rejects_unknown_command from UAT Gap 4 `missing` block
cover the surface).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-documentation-interactive-tutorial/08-UAT.md
@.planning/phases/08-documentation-interactive-tutorial/08-PATTERNS.md
@.planning/phases/08-documentation-interactive-tutorial/08-04-SUMMARY.md
@sphsim/cli/repl.py
@sphsim/cli/tutorial.py
@tests/test_tutorial.py

<interfaces>
<!-- Key contracts the executor needs. Extracted from repl.py + tutorial.py. -->

From sphsim/cli/repl.py:
- INTRO is a module-level str constant (lines 52–62), bound to `cmd.Cmd.intro` at line 68.
  Current shape: 7 banner lines (separators + header + pointers for `help` and `exit`).
  Gap 1 insertion point: between line 59 (`Wpisz \`help\` ...`) and line 60 (`Wpisz \`exit\` ...`).
- SPHShell.__init__ (lines 74–77) currently sets `self._tutorial_state = None` and
  `self._last_sim_result = None`. Gap 4 requires adding `self._last_command_unknown = False`.
- SPHShell.precmd (lines 94–133) returns `''` for control verbs and `line` otherwise.
  Gap 4: add `self._last_command_unknown = False` reset BEFORE returning `line`
  (the "we are about to dispatch a real command" point) — avoids editing every do_*.
- SPHShell.postcmd (lines 138–172) currently dispatches check_step on non-empty lines
  when _tutorial_state is active. Gap 4 requires early-return BEFORE check_step:
  `if self._last_command_unknown: self._last_command_unknown = False; return stop`.
- SPHShell.default (lines 616–621) prints `Nieznana komenda: '<text>'. ...`.
  Gap 4: set `self._last_command_unknown = True` at the top BEFORE the print.
- SPHShell._show_tutorial_step (lines 567–584) prints title + top rule + description
  + bottom rule. Gap 2 insertion: one `print(_TUTORIAL_CONTROLS_LINE)` after line 581
  (the bottom rule print) and BEFORE the inline `# Note: step 6 ...` comment.
- SPHShell.do_tutorial (lines 596–613) prints a 7-line banner including the inline
  literal `'  Sterowanie: skip | back | repeat | exit\\n'` at line 608. Gap 2 requires
  replacing that literal with the new constant — banner must render identically.

From sphsim/cli/tutorial.py:
- No changes in this plan. tutorial.py is decoupled from repl.py via dependency
  injection (STRATEGIES + BUILTIN_STRATEGIES passed as args to check_step).

From tests/test_tutorial.py:
- TestTutorialEntry.test_tutorial_banner_and_step1_shown asserts `INTERAKTYWNY TUTORIAL`
  + `[krok 1/8` + `Tutorial opuszczony` are in stdout. Adding controls footer does NOT
  break these assertions (Tutorial opuszczony only fires after exit, separate event).
- TestTutorialExit.test_cr01_tutorial_banner_shown_exactly_once asserts
  `INTERAKTYWNY TUTORIAL SPH SYMULATORA` appears EXACTLY ONCE for
  `tutorial\nexit\nexit\n`. The banner constant is unchanged in shape — only the
  Sterowanie line literal is replaced by the constant containing the same string.
- No existing assertion checks `Sterowanie:` line count or position — adding the
  controls footer to _show_tutorial_step is safe.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: INTRO banner tutorial pointer + _TUTORIAL_CONTROLS_LINE constant + controls footer + _last_command_unknown plumbing</name>
  <files>sphsim/cli/repl.py</files>
  <action>
    Apply four small edits to sphsim/cli/repl.py — all in one task, one commit.

    EDIT 1 (Gap 1 — INTRO banner pointer):
    In the INTRO constant (currently lines 52–62), insert one new Polish line
    BETWEEN the existing `Wpisz \`help\` żeby zobaczyć dostępne komendy.\n` line
    and the existing `Wpisz \`exit\` lub Ctrl+D żeby zakończyć.\n` line.
    The new line must read EXACTLY (verbatim per UAT Gap 1 missing block):
        "  Wpisz `tutorial` żeby uruchomić interaktywny tutorial v1.1 (≤15 min).\n"
    Indentation: 2 spaces (matches existing pointer lines). Backticks around
    `tutorial`. Trailing `\n` inside the string literal (consistent with surrounding lines).

    EDIT 2 (Gap 2 — extract controls constant + reuse in banner):
    Just AFTER the INTRO constant definition (before `class SPHShell(cmd.Cmd):`),
    add a new module-level constant:
        _TUTORIAL_CONTROLS_LINE = "Sterowanie: skip | back | repeat | exit"
    Then in do_tutorial (currently line 596), replace the inline banner literal
    that today reads `'  Sterowanie: skip | back | repeat | exit\n'` with an
    f-string that uses the constant:
        f'  {_TUTORIAL_CONTROLS_LINE}\n'
    The banner must render IDENTICALLY to today — only the literal source changes.

    EDIT 3 (Gap 2 — per-step controls footer):
    In _show_tutorial_step (currently lines 567–584), AFTER the existing
    `print('══════════════════════════════════════════════════════════')`
    line that prints the bottom rule (currently line 581) and BEFORE the inline
    `# Note: step 6 jest soft-pass ...` comment block, add one new line:
        print(_TUTORIAL_CONTROLS_LINE)
    No leading newline, no decorations — just the constant. Visible on every
    step display (entry via do_tutorial, repeat, skip-advance, back, postcmd auto-advance).

    EDIT 4 (Gap 4 — _last_command_unknown flag plumbing — 4 sub-edits):

    EDIT 4a (in SPHShell.__init__ around line 77, after `self._last_sim_result = None`):
        # Phase 8 gap-closure (UAT Gap 4): set by default() when cmd.Cmd cannot
        # parse the line. postcmd reads + resets to short-circuit check_step
        # BEFORE soft-pass steps 6/7 falsely accept the garbage input.
        self._last_command_unknown = False

    EDIT 4b (in SPHShell.precmd, IMMEDIATELY BEFORE the final `return line` on
    line 133 — the "we are about to dispatch a real command" exit point):
        # Phase 8 gap-closure (UAT Gap 4): reset BEFORE dispatch so default()
        # is the only path that sets _last_command_unknown=True. Single reset
        # point avoids editing every do_* method.
        self._last_command_unknown = False
    Important: place this BEFORE the `return line` statement on line 133, NOT
    before each of the `return ''` short-circuit branches above (those represent
    control verbs that don't reach dispatch).

    EDIT 4c (in SPHShell.postcmd, IMMEDIATELY AFTER the existing top guard
    `if not line.strip() or self._tutorial_state is None or stop: return stop`
    on line 139 — BEFORE the `ts = self._tutorial_state` line on line 141):
        # Phase 8 gap-closure (UAT Gap 4): if default() flagged the previous
        # line as Nieznana komenda, do NOT advance the tutorial — soft-pass
        # steps 6/7 must not accept garbage that cmd.Cmd refused to parse.
        if self._last_command_unknown:
            self._last_command_unknown = False
            return stop

    EDIT 4d (in SPHShell.default at line 616, BEFORE the existing
    `text = line.strip() if isinstance(line, str) else str(line)` line):
        # Phase 8 gap-closure (UAT Gap 4): record that cmd.Cmd refused to
        # dispatch this line. postcmd reads + resets the flag to short-circuit
        # check_step (otherwise soft-pass steps 6/7 falsely advance on typos).
        self._last_command_unknown = True

    Verification after edits:
      - INTRO renders 8 lines now (was 7 content lines + 2 separators); the new
        pointer line falls between help and exit pointers.
      - _TUTORIAL_CONTROLS_LINE appears in exactly 3 source locations: the
        definition, the do_tutorial banner f-string, and _show_tutorial_step.
      - _last_command_unknown appears in exactly 4 source locations: __init__
        init, precmd reset, postcmd guard, default set.

    Source references (existing patterns to mirror — do NOT change):
      - SPHShell.__init__ block at lines 74–77 (the existing precedent for adding
        new instance state attributes).
      - SPHShell.precmd structure at lines 94–133 (read carefully — only the
        final `return line` path is the dispatch point; the `return ''` short-
        circuit paths must NOT reset the flag).
      - SPHShell.postcmd top-of-method guard at line 139 (early-return is the
        existing pattern; we add another one in the same style).
      - SPHShell.default at lines 616–621 (the EXACT polish error message
        wording is unchanged — we only set the flag BEFORE the print).

    Anti-regression notes:
      - Do NOT alter the `Tutorial opuszczony` message in precmd (line 127–129).
      - Do NOT change the cmd.Cmd intro binding `intro = INTRO` at line 68.
      - Do NOT extract the Polish text in the new tutorial pointer line to a
        constant — keep it inline in INTRO (matches existing style for the
        help/exit pointer lines).
      - Do NOT touch the `# Note: step 6 jest soft-pass informational step.`
        comment block in _show_tutorial_step — append the controls footer
        BEFORE this comment (so the comment continues to describe the
        soft-pass behavior of check_step, unaffected by display changes).

    Verbatim per UAT Gap 4 missing block: the `_last_command_unknown` flag is
    the single source of truth — postcmd does NOT need to inspect the printed
    output, it just reads the flag.
  </action>
  <verify>
    <automated>SPHSIM_NO_REPORT=1 python -m unittest tests.test_tutorial -v 2>&amp;1 | grep -E '^OK|^FAILED'</automated>
    Plus three structural greps (must all match):
      grep -c "tutorial\` żeby uruchomić interaktywny tutorial" sphsim/cli/repl.py  → 1
      grep -c "_TUTORIAL_CONTROLS_LINE" sphsim/cli/repl.py  → 3
      grep -c "_last_command_unknown" sphsim/cli/repl.py  → 4
    Plus one behavioral smoke test:
      printf 'help\nexit\n' | SPHSIM_NO_REPORT=1 python sph_sim.py --interactive 2>&amp;1 | grep -F 'tutorial' &gt; /dev/null
      (the do_help line already exists; this just confirms no regression)
    Plus banner smoke:
      printf 'exit\nexit\n' | SPHSIM_NO_REPORT=1 python sph_sim.py --interactive 2>&amp;1 | grep -F 'Wpisz `tutorial`' &gt; /dev/null
  </verify>
  <done>
    - All four edits applied; all 3 _TUTORIAL_CONTROLS_LINE references resolve.
    - tests/test_tutorial.py — all TestTutorial* classes still pass (TestTutorialEntry
      banner test still asserts banner present, TestTutorialExit.test_cr01 still
      asserts banner appears EXACTLY ONCE).
    - INTRO banner now includes the tutorial pointer line between help and exit.
    - _show_tutorial_step prints the controls line after every bottom rule.
    - scripts/regression_check.py PASS=8/8 (no impact on --json contract).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add test_soft_pass_step_rejects_unknown_command + green</name>
  <files>tests/test_tutorial.py</files>
  <behavior>
    - New test in TestTutorialControls (or new class TestTutorialUnknownCommand
      — choose TestTutorialControls for cohesion with skip/back navigation).
    - Subprocess: drive sph_sim.py --interactive through stdin: `tutorial`,
      then 5 skips to reach step 6 (the first soft-pass step), then a typo
      like `tojesttypo`, then exit, exit.
    - Assert that `Nieznana komenda` appears in stdout (default() fired).
    - Assert that `✓ zaliczone — krok 6/8` does NOT appear in stdout (the typo
      did NOT advance the tutorial).
    - Assert that `[krok 6/8` still appears (we are still on step 6 — repeat-
      style behavior; no auto-advance triggered).
  </behavior>
  <action>
    Add ONE new subprocess behavior test to tests/test_tutorial.py covering
    UAT Gap 4. Place inside the existing class TestTutorialControls (mirrors
    the skip/back navigation tests).

    Test method name (verbatim per UAT Gap 4 missing optional bullet):
        test_soft_pass_step_rejects_unknown_command

    Test body outline (apply existing _run_repl_interactive helper from this file):
        def test_soft_pass_step_rejects_unknown_command(self):
            """UAT Gap 4: typo on step 6 (soft-pass) must NOT auto-advance —
            `Nieznana komenda` short-circuits check_step via _last_command_unknown."""
            # Reach step 6 via 5 skips (steps 1→6).
            r = _run_repl_interactive(
                'tutorial\nskip\nskip\nskip\nskip\nskip\ntojesttypo\nexit\nexit\n'
            )
            self.assertEqual(r.returncode, 0,
                             msg=f'REPL crashed rc={r.returncode}, stderr={r.stderr[:600]}')
            self.assertIn('Nieznana komenda', r.stdout,
                          msg=f'default() nie zarejestrował typo: {r.stdout[:2000]}')
            self.assertNotIn('✓ zaliczone — krok 6/8', r.stdout,
                             msg=f'typo nie powinien advance step 6: {r.stdout[:2000]}')
            # Still on step 6 after typo (no advance, no implicit repeat needed).
            self.assertIn('[krok 6/8', r.stdout,
                          msg=f'step 6 banner nie widoczny: {r.stdout[:2000]}')

    No imports needed — argparse, subprocess, sys, os already imported at top
    of file. _run_repl_interactive helper already defined at module scope.

    RED→GREEN order: Task 1 already made the source change. After Task 1, run
    the new test FIRST to confirm it passes (proving Task 1 closed Gap 4).
    If the test fails, fix Task 1's edits before commit — do NOT rewrite the
    test to match buggy behavior.

    Anti-regression notes:
      - SPHSIM_NO_REPORT=1 is the default in _run_repl_interactive (no reports
        written, no filesystem pollution).
      - 5 skips advance step 1→6 (each `pominięto — krok N/8` increments; the
        6th input is the typo).
      - The 2nd `exit\n` is needed because the 1st `exit` after a typo on step 6
        is still in tutorial mode (no auto-advance, tutorial still active);
        the 1st `exit` exits tutorial back to REPL, the 2nd exits the REPL.
  </action>
  <verify>
    <automated>SPHSIM_NO_REPORT=1 python -m unittest tests.test_tutorial.TestTutorialControls.test_soft_pass_step_rejects_unknown_command -v</automated>
    Plus a full Phase 8 test run to confirm no regression:
      SPHSIM_NO_REPORT=1 python -m unittest tests.test_tutorial tests.test_docs -v 2>&amp;1 | grep -E '^OK|^FAILED'
  </verify>
  <done>
    - test_soft_pass_step_rejects_unknown_command passes after Task 1's edits.
    - All other TestTutorial* tests still green.
    - regression_check.py PASS=8/8 unchanged.
    - scripts/verify_phase8.sh exit 0 (no assertion changes needed in 08-08 —
      08-09 / 08-10 will update step-count and required-mode literals).
  </done>
</task>

</tasks>

<verification>
- All TestTutorial* classes in tests/test_tutorial.py pass (4 classes + the new
  test_soft_pass_step_rejects_unknown_command).
- All TestTutorialFlow tests still pass (no change to sphsim/cli/tutorial.py
  in this plan — 09 will update that file).
- scripts/regression_check.py reports PASS: 8/8 (CLI-04 byte-identical
  invariant preserved).
- scripts/verify_phase8.sh exits 0 with the existing D1–D7 controls assertions
  (this plan does not change step counts; 08-09 will).
- Manual smoke (developer): `python sph_sim.py --interactive` then read INTRO —
  three pointer lines now visible (help, tutorial, exit). Then `tutorial` —
  banner shows controls + every subsequent step display also shows controls.
</verification>

<success_criteria>
- All three gaps observable as closed via the verify block above.
- Single git commit covering Task 1 + Task 2 (small surface, single file
  source change, single file test change — easy review).
- No changes to sphsim/cli/tutorial.py (decoupled from this plan).
- No changes to sphsim/cli/args.py or sphsim/cli/main.py (decoupled).
- No changes to scripts/verify_phase8.sh, scripts/verify_phase3.sh, or
  docs/PRZEWODNIK.md (decoupled — Plans 08-09 and 08-10 own those edits).
</success_criteria>

<output>
Create `.planning/phases/08-documentation-interactive-tutorial/08-08-SUMMARY.md` when done.
</output>
