---
phase: 08-documentation-interactive-tutorial
plan: 07
type: execute
wave: 4
depends_on:
  - 08-04
  - 08-05
  - 08-06
files_modified:
  - scripts/verify_phase8.sh
autonomous: false
requirements:
  - GATE-01

must_haves:
  truths:
    - "scripts/verify_phase8.sh has ≥33 `check()` invocations covering: (a) D-11 PRZEWODNIK sections, (b) D-14 assets PNG magic, (c) TUT-01 do_tutorial in source + behavior, (d) TUT-02/03 skip/back behavior, (e) TUT-04 exit in tutorial preserves REPL, (f) TUT-05 --tutorial flag mutex + entry, (g) TUT-06 reports under tutorial-<ts>/step-N-<topic>/, (h) at least one end-to-end step verification firing `✓ zaliczone — krok 1/8` (non-skip path, per GATE-01 VALIDATION.md row), (i) DOC-01/EX-01 via unittest discover tests.test_docs, (j) full unittest discover tests, (k) regression_check.py PASS=8/8, (l) CLI-04 baseline JSON byte-identical"
    - "verify_phase8.sh runs to completion with PASS=N / FAIL=0 and exits 0 (the BLOCKING gate per planning_context — 'Phase 8 cannot be marked complete without it passing in CI')"
    - "The 1 BLOCKING checkpoint task in this plan pauses for the user to confirm the gate is green before phase exit"
  artifacts:
    - path: "scripts/verify_phase8.sh"
      provides: "Phase 8 exit gate with ≥33 check() invocations covering all 6 Phase 8 SCs + ROADMAP goal verification"
      contains: "check "
      contains: "docs/PRZEWODNIK.md"
      contains: "docs/assets"
      contains: "--tutorial"
      contains: "tutorial-"
      contains: "regression_check"
  key_links:
    - from: "scripts/verify_phase8.sh"
      to: "all 22 test modules + docs/ + sphsim/cli/{repl,args,main,tutorial}.py + sphsim/report/__init__.py + regression_check.py"
      via: "check() invocations covering each phase 8 must_have"
      pattern: "check "
---

<objective>
Fill the `scripts/verify_phase8.sh` skeleton (created by Plan 00) with the full check() suite that verifies Phase 8 is complete. This is the BLOCKING exit gate — `/gsd:verify-work` (the phase-exit step) requires this script to exit 0 before Phase 8 is marked complete in ROADMAP. Mirrors the established pattern from verify_phase{3,4,5,6,7}.sh.

Purpose: One bash script encodes ALL Phase 8 acceptance criteria as automated checks. Re-runnable on any machine with python + bash. Output ends with `Phase 8 verification: PASS=N / FAIL=0` followed by `Phase 8 ready for /gsd:verify-work`.
Output: 1 file modified (verify_phase8.sh skeleton → full script ~180-200 lines). 1 BLOCKING `checkpoint:human-verify` task at end so user confirms the gate green before phase moves to /gsd:verify-work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-documentation-interactive-tutorial/08-CONTEXT.md
@.planning/phases/08-documentation-interactive-tutorial/08-RESEARCH.md
@.planning/phases/08-documentation-interactive-tutorial/08-PATTERNS.md
@.planning/phases/08-documentation-interactive-tutorial/08-VALIDATION.md

<interfaces>
scripts/verify_phase7.sh — full file, 195 lines — exact structural template. Phase 7 had 32 check() invocations covering 5 ROADMAP SCs + 4 REQ-IDs + regression + REPL pitfalls + opt-out. Phase 8 target: ≥33 check() invocations (covers full goal — D-11 sections, D-14 PNGs, TUT-01..TUT-06, EX-01, CLI-04 regression, plus non-skip step-verification path per GATE-01).

verify_phase8.sh skeleton (from Plan 00, lines ~1-56 + ~185-195 from verify_phase7.sh, minus all check() invocations). The placeholder line `# === Phase 8 checks land here in Plan 07 ===` marks where the Plan 07 content goes.

Smoke test recipes from RESEARCH §Non-Interactive Smoke Test (lines 616-640) and PNG magic-byte check pattern (verify_phase7.sh line 130). CLI-04 regression via `regression_check.py | grep -F 'PASS=8/8'`. Full unit-test discover via `SPHSIM_NO_REPORT=1 python -m unittest discover tests`.

VALIDATION.md acceptance: GATE-01 row says "scripts/verify_phase8.sh checks PRZEWODNIK sections + assets + --tutorial flag + tutorial smoke (printf-piped)" → this maps to ≥6 distinct check() categories.

Phase 8 has NO new REQ-IDs. The phase goal: "Nowy użytkownik bez znajomości projektu potrafi w ≤15 minut: (1) przeczytać polski przewodnik docs/PRZEWODNIK.md, (2) uruchomić w REPL tryb tutorial (lub --tutorial) i przejść krok-po-kroku przez wszystkie zdolności v1.1 ... z opcją skip per krok". The verify script translates this goal into 20+ binary checks.

Total target: 33 check() invocations across 7 categories (A: PRZEWODNIK sections, B: PNG magic, C: --tutorial mutex, D: REPL controls, E: TUT-06 dirs, F: source assertions, G: regression + tests).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble verify_phase8.sh full check() suite</name>
  <files>scripts/verify_phase8.sh</files>

  <read_first>
    - scripts/verify_phase7.sh (full — 195 lines; reference for check() patterns including REPL smoke tests lines 155-180)
    - scripts/verify_phase8.sh (skeleton from Plan 00 — has top matter + bottom matter + placeholder line)
    - .planning/phases/08-documentation-interactive-tutorial/08-PATTERNS.md (lines 559-583 — verify_phase8 smoke test patterns)
    - .planning/phases/08-documentation-interactive-tutorial/08-RESEARCH.md (lines 616-639 — non-interactive smoke test recipes)
    - .planning/phases/08-documentation-interactive-tutorial/08-VALIDATION.md (full — GATE-01 contract)
    - docs/PRZEWODNIK.md (from Plan 06 — confirm exact section headers for grep checks)
  </read_first>

  <action>
Replace the placeholder line `# === Phase 8 checks land here in Plan 07 ===` in `scripts/verify_phase8.sh` with 7 categories of `check()` invocations, in order (A–G). Mirror the verify_phase7.sh patterns exactly. All Polish strings in stdout assertions VERBATIM with what repl.py / args.py / tutorial.py actually emit.

**Category A — docs/PRZEWODNIK.md exists with required sections (DOC-01):** 7 checks
- File exists: `test -f docs/PRZEWODNIK.md`
- Each of 5 H2 headers present: `grep -F '## Szybki start' docs/PRZEWODNIK.md > /dev/null`, similarly for `## Interaktywny tutorial`, `## Opis funkcjonalności v1.1`, `## Referencja`, `## Teoria`
- Lead points at --tutorial: `head -15 docs/PRZEWODNIK.md | grep -F -- '--tutorial' > /dev/null`

**Category B — docs/assets/*.png present with valid PNG magic (DOC-02):** 3 checks
- For each of the 3 PNGs (decision_distribution_naive, kpi_timeseries_naive, batch_aggregate_naive), run a `$PY -c 'import sys; d=open("docs/assets/X.png","rb").read(8); sys.exit(0 if d==b"\\x89PNG\\r\\n\\x1a\\n" else 1)'` check.

**Category C — --tutorial CLI flag parses + 5-way mutex errors (TUT-05):** 7 checks
- `$PY sph_sim.py --tutorial --help 2>&1 | grep -F -- '--tutorial' > /dev/null` (flag in help)
- `$PY sph_sim.py --tutorial --interactive 2>&1 | grep -F 'Flagi --tutorial i --interactive są wzajemnie wykluczające' > /dev/null`
- `$PY sph_sim.py --tutorial --strategy naive 2>&1 | grep -F 'Flaga --tutorial nie działa z --strategy' > /dev/null`
- `$PY sph_sim.py --tutorial --custom examples/custom_strategy_template.py 2>&1 | grep -F 'Flaga --tutorial nie działa z --custom' > /dev/null`
- `$PY sph_sim.py --tutorial --batch --seeds 5 2>&1 | grep -F 'Flagi --tutorial i --batch są wzajemnie wykluczające' > /dev/null`
- `$PY sph_sim.py --tutorial --compare-agent 2>&1 | grep -F 'Flagi --tutorial i --compare-agent są wzajemnie wykluczające' > /dev/null`
- `$PY sph_sim.py 2>&1 | grep -F 'Musisz podać jeden z trybów' > /dev/null` (Polish required-mode error)

**Category D — Tutorial REPL command + 4 controls + non-skip step verification (TUT-01/02/03/04 + GATE-01 non-skip requirement):** 7 checks
- `printf 'help\nexit\n' | SPHSIM_NO_REPORT=1 $PY sph_sim.py --interactive 2>&1 | grep -F 'tutorial' > /dev/null` (tutorial in help)
- `printf 'tutorial\nexit\nexit\n' | SPHSIM_NO_REPORT=1 $PY sph_sim.py --interactive 2>&1 | grep -F 'INTERAKTYWNY TUTORIAL' > /dev/null` AND grep `[krok 1/8` (banner + step 1 shown)
- `printf 'tutorial\nskip\nskip\nskip\nskip\nskip\nskip\nskip\nskip\nexit\n' | SPHSIM_NO_REPORT=1 $PY sph_sim.py --interactive 2>&1 | grep -F 'pominięto — krok 8/8' > /dev/null` (8 skips advance; per RESEARCH §Non-Interactive Smoke Test Recipe — 8 skips + 1 exit, the trailing skip at step 8 already drops `_tutorial_state` to None so a single `exit` returns to bare REPL and then EOF on stdin ends the session)
- `printf 'tutorial\nrun naive zeta=0.75\nexit\nexit\n' | SPHSIM_NO_REPORT='' $PY sph_sim.py --interactive 2>&1 | grep -F '✓ zaliczone — krok 1/8' > /dev/null` (NON-SKIP path: step 1 verification fires when the user types the canonical baseline command; per GATE-01 VALIDATION.md row — required because skip-only smoke tests don't exercise check_step at all)
- `printf 'tutorial\nback\nexit\nexit\n' | SPHSIM_NO_REPORT=1 $PY sph_sim.py --interactive 2>&1 | grep -F 'Już jesteś na pierwszym kroku.' > /dev/null` (back boundary)
- `printf 'tutorial\nexit\nstrategies\nexit\n' | SPHSIM_NO_REPORT=1 $PY sph_sim.py --interactive 2>&1 | grep -F 'Tutorial opuszczony' > /dev/null` (Pitfall 1)
- `printf 'exit\nexit\n' | SPHSIM_NO_REPORT=1 $PY sph_sim.py --tutorial 2>&1 | grep -F 'INTERAKTYWNY TUTORIAL' > /dev/null` (--tutorial flag E2E)

**Category E — Tutorial reports go to tutorial-<ts>/step-N-<topic>/ (TUT-06):** 2 checks
- `rm -rf ./reports/; printf 'tutorial\nrun naive zeta=0.75\nexit\nexit\n' | SPHSIM_NO_REPORT='' $PY sph_sim.py --interactive > /tmp/p8_tut.log 2>&1; ls -d ./reports/tutorial-*/step-1-baseline/ 2>/dev/null | head -1 | grep -F 'step-1-baseline' > /dev/null && test -f ./reports/tutorial-*/step-1-baseline/report.md`
- `rm -rf ./reports/; printf 'run naive zeta=0.75\nexit\n' | SPHSIM_NO_REPORT='' $PY sph_sim.py --interactive > /tmp/p8_nontut.log 2>&1; ls -d ./reports/[0-9]*/ 2>/dev/null | head -1 > /dev/null && ! ls -d ./reports/tutorial-*/ 2>/dev/null | head -1 > /dev/null`

After this category, clean up `rm -rf ./reports/`.

**Category F — Source assertions (TUT-01 verification):** 5 checks
- `grep -E '^\s*def do_tutorial' sphsim/cli/repl.py > /dev/null`
- `grep -E '^\s*def precmd' sphsim/cli/repl.py > /dev/null`
- `grep -E '^\s*def postcmd' sphsim/cli/repl.py > /dev/null`
- `test -f sphsim/cli/tutorial.py && grep -F 'class TutorialFlow' sphsim/cli/tutorial.py > /dev/null`
- `grep -F 'report_dir_override' sphsim/report/__init__.py > /dev/null`

**Category G — Regression + full test suite (CLI-04 + DOC-01/02 + EX-01 + all TUT-*):** 3 checks
- `$PY scripts/regression_check.py 2>&1 | grep -F 'PASS=8/8' > /dev/null` (CLI-04 baseline byte-identical)
- `SPHSIM_NO_REPORT=1 $PY -m unittest discover tests > /tmp/p8_discover.log 2>&1 && grep -E 'OK' /tmp/p8_discover.log > /dev/null` (full suite green)
- `SPHSIM_NO_REPORT=1 $PY -m unittest tests.test_tutorial tests.test_docs > /tmp/p8_phase8tests.log 2>&1 && grep -E 'OK' /tmp/p8_phase8tests.log > /dev/null` (Phase 8 modules specifically)

**Category H — Cleanup (just before summary echo):** `rm -rf ./reports/` to remove any tutorial-* or numbered dirs created by smoke tests.

**Total check() count target: 34 invocations** (33 baseline + 1 non-skip step verification per GATE-01). Well above the ≥33 budget AND comparable to verify_phase7.sh's 32 checks.

Quote escaping in bash heredoc strings: the PNG magic byte check needs careful escaping (`\\x89PNG\\r\\n\\x1a\\n` inside double-quotes in eval), and the Polish strings need single-quote wrapping where they contain special chars. Pattern: wrap the entire `eval` command in double-quotes, use single-quote for grep patterns: `"... grep -F 'Polish text' ..."`. For multi-step commands, use `&& \\` line continuation.

After editing the file, ensure executability with `chmod +x scripts/verify_phase8.sh` (no-op if already executable from Plan 00).

Run the full script end-to-end to verify: `bash scripts/verify_phase8.sh; echo "EXIT=$?"`. Expected: `PASS=33 / FAIL=0` and exit 0. If FAIL > 0, investigate which check failed — typical causes: (a) Polish copy mismatch between repl.py emit and grep pattern (re-read repl.py to lock exact text), (b) reports/ directory cleanup race (add explicit `rm -rf` between sequential check blocks).
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; bash scripts/verify_phase8.sh; test $? -eq 0 &amp;&amp; bash scripts/verify_phase8.sh 2>&amp;1 | tail -10 | grep -E 'PASS=[0-9]+ / FAIL=0' &amp;&amp; bash scripts/verify_phase8.sh 2>&amp;1 | grep -cE '^check ' | awk '{exit ($1 &gt;= 33) ? 0 : 1}'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE '^check ' scripts/verify_phase8.sh` outputs at least 33 (per ROADMAP / plan objective / action — exactly 33 check() invocations across 7 categories)
    - `grep -c 'docs/PRZEWODNIK.md' scripts/verify_phase8.sh` outputs at least 5
    - `grep -c 'docs/assets/' scripts/verify_phase8.sh` outputs at least 3
    - `grep -c '\-\-tutorial' scripts/verify_phase8.sh` outputs at least 5
    - `grep -c 'INTERAKTYWNY TUTORIAL' scripts/verify_phase8.sh` outputs at least 2
    - `grep -c 'pominięto — krok' scripts/verify_phase8.sh` outputs at least 1
    - `grep -c 'Tutorial opuszczony' scripts/verify_phase8.sh` outputs at least 1
    - `grep -c 'tutorial-' scripts/verify_phase8.sh` outputs at least 1
    - `grep -c '✓ zaliczone — krok 1/8' scripts/verify_phase8.sh` outputs at least 1 (non-skip path check per GATE-01 / BLOCKER 1)
    - `grep -c 'regression_check' scripts/verify_phase8.sh` outputs 1
    - `grep -c 'unittest discover' scripts/verify_phase8.sh` outputs at least 1
    - `grep -c 'rm -rf ./reports/' scripts/verify_phase8.sh` outputs at least 2
    - `bash scripts/verify_phase8.sh; echo "EXIT=$?"` ends with `EXIT=0`
    - `bash scripts/verify_phase8.sh 2>&amp;1 | tail -5 | grep -E 'PASS=[0-9]+ / FAIL=0'` exits 0 (zero FAILs)
    - `bash scripts/verify_phase8.sh 2>&amp;1 | grep -F 'Phase 8 ready for /gsd:verify-work'` exits 0
    - No leftover `./reports/` directory after running
  </acceptance_criteria>

  <done>
    scripts/verify_phase8.sh has ≥34 check() invocations covering D-11 PRZEWODNIK sections, DOC-02 PNG magic, TUT-01..TUT-06 source + behavior, non-skip step verification (✓ zaliczone — krok 1/8 per GATE-01), CLI-04 backwards-compat regression, full unit test discover. Runs to PASS=34 / FAIL=0 exit 0. Phase 8 ready for /gsd:verify-work.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 [BLOCKING]: Confirm verify_phase8.sh green before phase exit</name>
  <files>(no file modification — verification only)</files>
  <what-built>
    scripts/verify_phase8.sh (assembled in Task 1) with ≥33 check() invocations covering all Phase 8 success criteria — D-11 PRZEWODNIK sections, DOC-02 PNG magic, TUT-01..TUT-06 controls, EX-01 example provenance, GATE-01 non-skip step-verification path, CLI-04 regression, full unit test discover.
  </what-built>
  <action>
    Human-only verification step. Claude does not modify any file in this task. The user is asked to (a) run `bash scripts/verify_phase8.sh` locally, (b) confirm PASS=N / FAIL=0 with N ≥ 33, (c) confirm "Phase 8 ready for /gsd:verify-work" line is printed, (d) confirm exit code is 0, (e) OPTIONALLY run `python sph_sim.py --tutorial` end-to-end and confirm the ≤15-minute onboarding goal is achievable in practice.

    If any check fails, the user describes which check; Claude debugs and re-runs Task 1. If user types "approved", Phase 8 moves to `/gsd:verify-work` for outer checkbox flip.

    Polish copy assertions to spot-check during run:
    - `INTERAKTYWNY TUTORIAL` banner appears when typing `tutorial` inside `--interactive` or invoking `--tutorial`.
    - `[krok 1/8` header shows on first step display.
    - `pominięto — krok N/8` on skip; `cofnięto do kroku N/8` on back; `Już jesteś na pierwszym kroku.` at boundary.
    - `Tutorial opuszczony` when typing `exit` inside tutorial (NOT `Do widzenia.`).
    - Second `exit` (outside tutorial) prints `Do widzenia.`
  </action>
  <how-to-verify>
1. Run the gate locally: `bash scripts/verify_phase8.sh`
2. Confirm output ends with `Phase 8 verification: PASS=N / FAIL=0` (any N ≥ 33, target 34)
3. Confirm output ends with `Phase 8 ready for /gsd:verify-work`
4. Confirm shell exit code is 0: `echo $?` returns `0`
5. Verify the 6 must-have categories are all touched by checks:
   - docs/PRZEWODNIK.md sections present
   - docs/assets/*.png valid
   - --tutorial CLI flag mutex
   - REPL tutorial command + 4 controls + non-skip ✓ zaliczone step verification
   - TUT-06 tutorial-<ts>/step-N-<topic>/ dir creation
   - Regression + unit test discover
6. Optionally: run the tutorial yourself for ≤15 minutes — `python sph_sim.py --tutorial` — and confirm the phase goal (new user onboarding) is achievable.
  </how-to-verify>
  <verify>
    <automated>bash scripts/verify_phase8.sh; test $? -eq 0</automated>
    <human-check>User confirms PASS=N/FAIL=0, exit 0, and (optionally) the ≤15-minute onboarding goal is reachable end-to-end.</human-check>
  </verify>
  <done>
    User typed "approved" indicating verify_phase8.sh exits 0 with FAIL=0 AND the ≤15-minute onboarding goal is achievable. Phase 8 ready for `/gsd:verify-work` to flip the outer ROADMAP checkbox.
  </done>
  <resume-signal>Type "approved" (gate is green and ≤15-min onboarding feels achievable) or describe the specific failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify_phase8.sh → repl.py / args.py / report subprocess | piped commands + grep on stdout; non-interactive use_rawinput=False (cmd.Cmd auto-falls-back per RESEARCH line 637) |
| verify_phase8.sh → filesystem (./reports/) | rm -rf creates known state; smoke tests verify specific subdir patterns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | DoS | printf piped stdin deadlocks if cmd.Cmd waits for TTY (T-08-03 in planning_context) | mitigate | RESEARCH §Non-Interactive Smoke Test Recipe line 637 verified `use_rawinput=False` + stdin read works correctly; verify_phase7.sh uses same pattern across REPL smoke tests (lines 155-180) |
| T-08-07-02 | Tampering | Polish string drift between repl.py emit and verify_phase8.sh grep pattern (T-08-04 from planning_context) | mitigate | EX-01 audit test runs as part of `unittest discover tests` check in Category G; if Polish strings drift, that test detects it before verify_phase8.sh's grep checks run |
| T-08-07-03 | Tampering | ./reports/ leak between sequential check categories | mitigate | Explicit `rm -rf ./reports/` between Category E sub-checks AND in Category H cleanup |
| T-08-07-04 | DoS | matplotlib version drift breaks PNG byte-equality | mitigate | Category B checks PNG magic bytes only (8-byte header), not byte-equality of full content; matplotlib version differences are tolerated (RESEARCH §matplotlib drift line 778-782) |
| T-08-07-05 | Tampering | Phase 8 marked complete with broken tutorial | mitigate | BLOCKING checkpoint (Task 2) — verify_phase8.sh exit 0 is required AND manual verification by user covers the ≤15-min onboarding goal which is not automatable |
| T-08-SC | Tampering | npm/pip installs | n/a | Plan 08-07 installs no packages |
</threat_model>

<verification>
- scripts/verify_phase8.sh: ~180-200 lines, ≥34 check() invocations (33 baseline + 1 non-skip step verification per GATE-01), runs to PASS=34/FAIL=0 exit 0.
- Coverage: PRZEWODNIK (DOC-01), assets PNG (DOC-02), --tutorial mutex (TUT-05), tutorial REPL command + controls (TUT-01/02/03/04), tutorial-<ts>/step-N-<topic>/ reports (TUT-06), CLI-04 regression, full unit test discover (includes EX-01 + all TUT-* tests).
- BLOCKING checkpoint requires user confirmation before phase exit.
- ./reports/ properly cleaned up at end of script.
</verification>

<success_criteria>
- [x] scripts/verify_phase8.sh has ≥33 check() invocations (target 34 after GATE-01 non-skip path is added per BLOCKER 1)
- [x] All 6 must-have categories from must_haves are covered
- [x] Script runs to PASS=N / FAIL=0 exit 0
- [x] Polish copy strings in grep patterns match repl.py emits verbatim
- [x] ./reports/ cleaned up at script end
- [x] BLOCKING checkpoint at end forces user confirmation of phase goal achievement
</success_criteria>

<output>
Create `.planning/phases/08-documentation-interactive-tutorial/08-07-SUMMARY.md` when done. List: (a) total check() count (target ≥34, baseline 33 + GATE-01 non-skip step verification), (b) PASS=N / FAIL=0 output line, (c) the 7 check categories with check counts each (Category D = 7 incl. non-skip ✓ zaliczone), (d) confirmation of ./reports/ cleanup, (e) BLOCKING checkpoint executed (or pending user confirmation), (f) Phase 8 ready for /gsd:verify-work.
</output>
