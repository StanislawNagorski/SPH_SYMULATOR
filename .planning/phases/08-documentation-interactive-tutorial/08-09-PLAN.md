---
phase: 08-documentation-interactive-tutorial
plan: 09
type: execute
wave: 6
depends_on:
  - 08-08
files_modified:
  - sphsim/cli/tutorial.py
  - sphsim/cli/repl.py
  - tests/test_tutorial.py
  - scripts/verify_phase8.sh
  - docs/PRZEWODNIK.md
autonomous: true
gap_closure: true
requirements:
  - TUT-01
  - TUT-02
  - TUT-03

must_haves:
  truths:
    - "Tutorial step 2 (current dual-purpose `strategies` + `strategy <name>`) is split into TWO distinct steps: new step 2 ('Lista strategii') accepts ONLY `strategies`; new step 3 ('Szczegóły strategii') accepts `strategy <name>`"
    - "Total tutorial step count is 9 (was 8). TutorialFlow.total defaults to 9. STEP_TOPICS has 9 keys (1..9). STEP_TASKS has 9 TutorialStep entries with matching step_num + topic slugs"
    - "check_step branches renumbered: old steps 3..8 become 4..9; the new step 3 verifier (Szczegóły strategii) accepts `strategy <name>`; step 2 verifier (Lista strategii) accepts ONLY `strategies`"
    - "All `8 kroków` / `1/8` / `8/8` literals in repl.py source + scripts/verify_phase8.sh + tests/test_tutorial.py + docs/PRZEWODNIK.md sample heading updated to `9 kroków` / `1/9` / `9/9` accordingly"
    - "Tests cover the split: test_check_step2_strategies asserts ONLY `strategies` passes (not `strategy <name>`); new test_check_step3_strategy_details asserts `strategy <name>` passes"
    - "scripts/verify_phase8.sh D3 now uses 9 skip inputs and asserts `pominięto — krok 9/9`; D2/D4 grep for `1/9`; everything else still PASS"
    - "scripts/regression_check.py reports PASS: 8/8 (CLI-04 byte-identical — Phase 8 changes never touch the --json contract)"
  artifacts:
    - path: "sphsim/cli/tutorial.py"
      provides: "9-step STEP_TOPICS + STEP_TASKS + renumbered check_step + TutorialFlow.total=9"
      contains: "TutorialStep("
      contains: "3: 'strategy-details'"
      contains: "total: int = 9"
    - path: "sphsim/cli/repl.py"
      provides: "9-step hint set + ~9 kroków literals"
      contains: "~9 kroków"
    - path: "tests/test_tutorial.py"
      provides: "9-step assertions + new test_check_step3_strategy_details + adjusted test_check_step2_strategies"
      contains: "/9"
      contains: "test_check_step3_strategy_details"
    - path: "scripts/verify_phase8.sh"
      provides: "D3 9 skips → pominięto — krok 9/9 + D2/D4 [krok 1/9 + zaliczone krok 1/9"
      contains: "krok 9/9"
      contains: "krok 1/9"
    - path: "docs/PRZEWODNIK.md"
      provides: "sample step heading updated to [krok 1/9 — Baseline]"
      contains: "[krok 1/9 — Baseline]"
  key_links:
    - from: "sphsim/cli/tutorial.py STEP_TOPICS"
      to: "sphsim/cli/tutorial.py STEP_TASKS"
      via: "key parity — every int key 1..9 in TOPICS appears in TASKS with matching topic slug"
      pattern: "STEP_TOPICS"
    - from: "sphsim/cli/tutorial.py check_step"
      to: "sphsim/cli/repl.py SPHShell.postcmd"
      via: "step_n routes to per-step branch, where steps 4..9 mirror the old 3..8 logic"
      pattern: "if step_n == "
    - from: "sphsim/cli/repl.py do_tutorial banner"
      to: "user reading `~N kroków` literal"
      via: "f-string composition + new TutorialFlow.total=9 default"
      pattern: "~9 kroków"
---

<objective>
Close UAT Gap 3 — split tutorial step 2 into two distinct steps (list strategies
via `strategies`, view details via `strategy <name>`), raising total step count
from 8 to 9. Cascading changes across 5 files: tutorial.py (state machine),
repl.py (hint set + ~N kroków literals), tests/test_tutorial.py (every `/8`
literal + step-2 test restructure + new step-3 test), scripts/verify_phase8.sh
(D2/D3/D4 step count assertions), docs/PRZEWODNIK.md (sample step heading).

Purpose: Tighten pedagogical sequencing — user requested two separate steps
because conflating `strategies` listing and `strategy <name>` details in one
step is dydaktically muddled. This is a scope change (not a bug), so all
artifacts that reference the step count or step 2/3 boundary must be updated
in lock-step to avoid drift.

Output: 5 modified files; all 18+ TestTutorial* + TestTutorialFlow assertions
green with `/9` literals; verify_phase8.sh exits 0 with `pominięto — krok 9/9`
on the 9-skip check; regression_check.py PASS=8/8 unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-documentation-interactive-tutorial/08-UAT.md
@.planning/phases/08-documentation-interactive-tutorial/08-PATTERNS.md
@.planning/phases/08-documentation-interactive-tutorial/08-CONTEXT.md
@.planning/phases/08-documentation-interactive-tutorial/08-03-SUMMARY.md
@.planning/phases/08-documentation-interactive-tutorial/08-04-SUMMARY.md
@sphsim/cli/tutorial.py
@sphsim/cli/repl.py
@tests/test_tutorial.py
@scripts/verify_phase8.sh
@docs/PRZEWODNIK.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase. -->

From sphsim/cli/tutorial.py:
- STEP_TOPICS (lines 35–44): dict[int, str] currently 8 keys (1..8) with values
  baseline, strategies, run-strategy, custom, compare, env, report, batch.
  New key 3 is 'strategy-details'. Old 3..8 shift to 4..9.
- STEP_TASKS (lines 64–187): dict[int, TutorialStep] with 8 entries. After
  split, 9 entries. Each TutorialStep has step_num (must match the dict key)
  and topic (must match STEP_TOPICS[step_n]).
- TutorialFlow.total (line 207): currently `total: int = 8`. Update to 9.
- check_step (lines 226–327): step_n routes through branches.
    * Step 1: unchanged.
    * Step 2: currently accepts `line == 'strategies' or line.startswith('strategy ')`.
      After split, accept ONLY `line == 'strategies'`.
    * NEW Step 3 (strategy-details): accept `len(tokens) >= 2 and tokens[0] == 'strategy'`.
    * Old Step 3 (run-strategy) → new Step 4: same logic.
    * Old Step 4 (custom) → new Step 5.
    * Old Step 5 (compare) → new Step 6.
    * Old Step 6 (env, soft-pass) → new Step 7.
    * Old Step 7 (report, soft-pass) → new Step 8.
    * Old Step 8 (batch) → new Step 9.

From sphsim/cli/repl.py:
- Hint set in postcmd (line 164): `if result is not None or ts.step in (2, 4, 7):`.
  After renumber: soft-pass steps shift. Old (2, 4, 7) maps to:
    - Old 2 (display-only `strategies`) → new 2 + new 3 BOTH are display-only.
    - Old 4 (custom — display-only, no sim) → new 5 (custom).
    - Old 7 (report soft-pass) → new 8 (report soft-pass).
  New hint set: `(2, 3, 5, 8)`.
- do_tutorial docstring (line 597): `"""Uruchom interaktywny tutorial v1.1 (~8 kroków, ≤15 min)."""`
  → change `~8` to `~9`.
- do_tutorial banner string (line 607): `'  ~8 kroków, ≤15 minut\n'`
  → change `~8` to `~9`.
- All other f-strings render `/{ts.total}` dynamically — auto-update when
  TutorialFlow.total = 9.

From tests/test_tutorial.py:
- TestTutorialEntry.test_tutorial_banner_and_step1_shown: asserts `[krok 1/8`
  → change to `[krok 1/9`.
- TestTutorialControls.test_skip_advances_counter: asserts `pominięto — krok 1/8`
  and `[krok 2/8` → change to `1/9` and `2/9`.
- TestTutorialControls.test_back_decrements_counter: asserts `cofnięto do kroku 1/8`
  → change to `1/9`.
- TestTutorialControls.test_back_at_step_one_boundary: no /N literal — unchanged.
- TestTutorialControls (new test_soft_pass_step_rejects_unknown_command from 08-08):
  uses 5 skips to reach step 6, asserts `[krok 6/8` → after split, step 6 (old)
  is now step 7 (env). Need 6 skips (steps 1→7) and assert `[krok 7/9`. Also
  update the assertion `✓ zaliczone — krok 6/8` → `7/9`. ALTERNATIVELY: keep
  the test reaching the FIRST soft-pass step (which is now step 7) — adjust skip
  count and step number consistently.
- TestTutorialExit.test_cr01_tutorial_banner_shown_exactly_once: asserts
  `[krok 1/8` → change to `[krok 1/9`.
- TestTutorialCLI.test_tutorial_flag_enters_tutorial_mode: asserts `[krok 1/8`
  → `[krok 1/9`.
- TestTutorialReports.test_tutorial_step_verification_advances: asserts
  `✓ zaliczone — krok 1/8` and `[krok 2/8` → `1/9` and `2/9`.
- TestTutorialFlow.test_tutorialflow_defaults: asserts `tf.total == 8` → `9`.
- TestTutorialFlow.test_step_topics_keys_and_slugs: expected dict updated to
  include new key 3 'strategy-details' and renumber 3..8 → 4..9.
- TestTutorialFlow.test_step_tasks_have_tutorialstep_instances: range(1, 9) → range(1, 10).
- TestTutorialFlow.test_check_step2_strategies: currently asserts BOTH
  `strategies` AND `strategy incentive` pass. After split, this test must
  assert `strategies` passes AND `strategy incentive` FAILS for step 2.
- NEW TestTutorialFlow.test_check_step3_strategy_details: assert `strategy
  incentive` passes for step 3, bare `strategies` fails (must be 2 tokens with
  tokens[0]=='strategy').
- TestTutorialFlow.test_check_step3_any_builtin → rename to
  test_check_step4_any_builtin (step 3 is now strategy-details; old step 3
  becomes step 4). Update step_n arg from 3 to 4.
- TestTutorialFlow.test_check_step4_custom → test_check_step5_custom (step_n=5).
- TestTutorialFlow.test_check_step5_compare → test_check_step6_compare (step_n=6).
- TestTutorialFlow.test_check_step6_soft_pass → test_check_step7_soft_pass (step_n=7).
- TestTutorialFlow.test_check_step7_soft_pass → test_check_step8_soft_pass (step_n=8).
- TestTutorialFlow.test_check_step8_batch → test_check_step9_batch (step_n=9).
- TestTutorialFlow.test_step6_open_question_2_resolution: this checks STEP_TASKS[6]
  for `--phi` + `informacyjny`. After renumber, env step is at key 7. Update to
  STEP_TASKS[7] and rename test to test_step7_open_question_2_resolution.

From scripts/verify_phase8.sh:
- D2 (line 120): asserts `[krok 1/8` → change to `[krok 1/9`.
- D3 (line 122): currently has 8 skip lines and asserts `pominięto — krok 8/8`.
  After split: 9 skip lines and assertion `pominięto — krok 9/9`.
- D4 (line 124): asserts `✓ zaliczone — krok 1/8` → `1/9`.

From docs/PRZEWODNIK.md:
- Line 59: `[krok 1/8 — Baseline]` → `[krok 1/9 — Baseline]`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split STEP_TOPICS/STEP_TASKS + renumber check_step + TutorialFlow.total=9 in sphsim/cli/tutorial.py</name>
  <files>sphsim/cli/tutorial.py</files>
  <action>
    Apply three structural edits to sphsim/cli/tutorial.py to implement the
    9-step state machine.

    EDIT 1 — STEP_TOPICS (lines 35–44):
    Insert a new key 3 'strategy-details' and renumber old 3..8 → 4..9. The
    final dict (preserve dict-literal style):
        STEP_TOPICS = {
            1: 'baseline',
            2: 'strategies',
            3: 'strategy-details',
            4: 'run-strategy',
            5: 'custom',
            6: 'compare',
            7: 'env',
            8: 'report',
            9: 'batch',
        }

    EDIT 2 — STEP_TASKS (lines 64–187):
    Apply three sub-edits.

    (2a) Modify the existing key 2 entry (currently title 'Lista strategii',
    expected_command_hint 'strategies'). The current description teaches BOTH
    `strategies` AND `strategy incentive`. Rewrite the description to teach
    ONLY `strategies` (Polish copy below). Keep step_num=2, topic='strategies',
    title='Lista strategii'.

    New description for step 2 (verbatim Polish):
        "Wyświetl listę wbudowanych i custom strategii:\n"
        "\n"
        "  strategies\n"
        "\n"
        "Zobaczysz nazwę, krótki opis i sufiks `[custom]` dla niewbudowanych."

    Keep `expected_command_hint='strategies'`.

    (2b) INSERT a new key 3 entry IMMEDIATELY AFTER the (now slimmed) key 2:
        3: TutorialStep(
            step_num=3,
            topic='strategy-details',
            title='Szczegóły strategii',
            description=(
                "Wyświetl szczegóły jednej strategii, np.:\n"
                "\n"
                "  strategy incentive\n"
                "\n"
                "Zobaczysz opis, parametry (z typami i defaultami) oraz\n"
                "baseline KPI (jeśli zdefiniowany w STRATEGY_META)."
            ),
            expected_command_hint='strategy incentive',
        ),

    (2c) Renumber old keys 3..8 to 4..9. For each entry:
      - Update the dict key.
      - Update the inner `step_num=N` field to match the new key.
      - topic, title, description, expected_command_hint UNCHANGED (the slugs
        are just renumbered, not relabeled).

    Mapping after the renumber:
      - Old 3 (run-strategy / 'Inna strategia') → new 4. step_num=4.
      - Old 4 (custom / 'Custom strategia') → new 5. step_num=5.
      - Old 5 (compare / 'Porównanie z agentem') → new 6. step_num=6.
      - Old 6 (env / 'Override środowiska (informacyjny)') → new 7. step_num=7.
      - Old 7 (report / 'Inspekcja raportu') → new 8. step_num=8.
      - Old 8 (batch / 'Batch + agregat') → new 9. step_num=9.

    EDIT 3 — TutorialFlow.total (line 207):
    Change `total: int = 8` to `total: int = 9`. Update the docstring at
    line 199 from `total of 8 for Phase 8` to `total of 9 for Phase 8 (post UAT Gap 3)`.

    EDIT 4 — check_step branches (lines 260–327):
    Reshape the per-step verifier dispatch.

    (4a) Step 1: UNCHANGED — `run naive ...` AND avg_val_last100 >= 80.0.

    (4b) Step 2 (currently lines 273–275): TIGHTEN.
        # Step 2 (strategies — list-only). UAT Gap 3 split: previously this
        # branch also accepted `strategy <name>`; that detail step is now
        # step 3 (its own branch). step 2 accepts ONLY `strategies`.
        if step_n == 2:
            return line == 'strategies'

    (4c) NEW Step 3 (strategy-details) — insert between step 2 and old step 3
    (which becomes new step 4):
        # Step 3 (strategy-details — UAT Gap 3 split). Accept any `strategy
        # <name>` invocation. No simulator dependency. The user is expected
        # to type a specific strategy after the verb.
        if step_n == 3:
            return len(tokens) >= 2 and tokens[0] == 'strategy'

    (4d) Old step 3 (run-strategy) → new step 4. Change `if step_n == 3:` to
    `if step_n == 4:`. Body UNCHANGED.

    (4e) Old step 4 (custom) → new step 5. Change `if step_n == 4:` to
    `if step_n == 5:`. Body UNCHANGED.

    (4f) Old step 5 (compare) → new step 6. Change `if step_n == 5:` to
    `if step_n == 6:`. Body UNCHANGED.

    (4g) Old step 6 (env soft-pass) → new step 7. Change `if step_n == 6:` to
    `if step_n == 7:`. Body UNCHANGED.

    (4h) Old step 7 (report soft-pass) → new step 8. Change `if step_n == 7:` to
    `if step_n == 8:`. Body UNCHANGED.

    (4i) Old step 8 (batch) → new step 9. Change `if step_n == 8:` to
    `if step_n == 9:`. Body UNCHANGED.

    Anti-regression: do NOT touch the docstring at module top (lines 1–26)
    other than the optional TutorialFlow comment update — the contract of
    `strategies_keys` / `builtin_strategies` injection is unchanged.

    Reference existing code patterns:
      - The TutorialStep dataclass definition (lines 47–58) is unchanged.
      - The STEP_TASKS dict-literal style (key: TutorialStep(...)) is preserved
        across the insertion of key 3.
      - `tokens = line.split()` at line 258 of check_step is reused by new
        step 3 (tokens[0] check).
  </action>
  <verify>
    <automated>SPHSIM_NO_REPORT=1 python -m unittest tests.test_tutorial.TestTutorialFlow -v 2>&amp;1 | grep -E '^OK|^FAILED'</automated>
    NOTE: After Task 1 alone (before Task 3 updates tests), TestTutorialFlow
    will FAIL — that's expected (the test expectations encode old 8-step shape).
    Task 1's verify is structural-only:
      grep -F "3: 'strategy-details'" sphsim/cli/tutorial.py
      grep -F "9: 'batch'" sphsim/cli/tutorial.py
      grep -F "total: int = 9" sphsim/cli/tutorial.py
      grep -cE 'if step_n == [1-9]:' sphsim/cli/tutorial.py  # must be 9
  </verify>
  <done>
    - sphsim/cli/tutorial.py contains 9-key STEP_TOPICS (1..9) with new key 3
      'strategy-details'.
    - STEP_TASKS has 9 entries with matching step_num and topic fields.
    - TutorialFlow.total default is 9.
    - check_step has 9 distinct `if step_n == N:` branches (1 through 9).
    - Step 2 branch returns `line == 'strategies'` (no `startswith('strategy ')`
      fallback).
    - New step 3 branch returns `len(tokens) >= 2 and tokens[0] == 'strategy'`.
    - tests will FAIL after this task alone — proceed to Task 2 immediately.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update sphsim/cli/repl.py hint set + ~9 kroków literals</name>
  <files>sphsim/cli/repl.py</files>
  <action>
    Apply three small edits to sphsim/cli/repl.py to mirror the 9-step
    contract.

    EDIT 1 — postcmd hint set (line 164):
    Change `if result is not None or ts.step in (2, 4, 7):` to:
        if result is not None or ts.step in (2, 3, 5, 8):
    Comment context (verbatim per UAT Gap 3 artifacts block):
        - Step 2 (list strategies) — display-only.
        - Step 3 (strategy details) — display-only, NEW after split.
        - Step 5 (custom — display-only after load, no sim).
        - Step 8 (report soft-pass — no sim dependency).

    EDIT 2 — do_tutorial docstring (line 597):
    Change `"""Uruchom interaktywny tutorial v1.1 (~8 kroków, ≤15 min)."""`
    to `"""Uruchom interaktywny tutorial v1.1 (~9 kroków, ≤15 min)."""`.

    EDIT 3 — do_tutorial banner string (line 607):
    Change `'  ~8 kroków, ≤15 minut\n'` to `'  ~9 kroków, ≤15 minut\n'`.

    Anti-regression notes:
      - Do NOT change the f-strings that render `{ts.total}` — they auto-update
        from TutorialFlow.total = 9 (set in Task 1).
      - Do NOT touch the docstring at module top (lines 1–24) which references
        `~8 kroków` in prose — wait, the module docstring at line 20 says
        "kroków. Pitfall 1: ..." — no `~8` literal there, leave it alone.
      - The kroków literal at line 3 of the module docstring is `8 z dodatkiem
        \`tutorial\`)` (referring to REPL command count, not tutorial step
        count). LEAVE THIS ALONE — it refers to the 8 REPL commands
        (`help`, `exit`, `strategies`, ..., `tutorial`), not tutorial steps.

    Verification of "no other `8`-literal regressions":
      grep -n '8 kroków\|krok 1/8\|krok 8/8' sphsim/cli/repl.py
      → after this edit, should print zero matches (excluding the module
      docstring at the top, which says "8 z dodatkiem \`tutorial\`)" and is
      about REPL command count not steps).
  </action>
  <verify>
    <automated>SPHSIM_NO_REPORT=1 python -m unittest tests.test_tutorial.TestTutorialEntry tests.test_tutorial.TestTutorialControls -v 2>&amp;1 | grep -E '^OK|^FAILED'</automated>
    NOTE: After Task 2 alone (before Task 3), most behavior tests will FAIL
    because they still assert `/8` literals. Task 2 structural verify:
      grep -c '~9 kroków' sphsim/cli/repl.py  → 2 (docstring + banner)
      grep -c 'ts.step in (2, 3, 5, 8)' sphsim/cli/repl.py  → 1
      grep -c '~8 kroków' sphsim/cli/repl.py  → 0
  </verify>
  <done>
    - repl.py contains `~9 kroków` in exactly 2 locations (do_tutorial docstring + banner).
    - postcmd hint set is `(2, 3, 5, 8)`.
    - No `~8 kroków` substring remains in the file.
    - tests still failing — proceed to Task 3.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update tests/test_tutorial.py — all /9 literals + restructure step-2 + new step-3 test + renumber check_stepN tests</name>
  <files>tests/test_tutorial.py</files>
  <behavior>
    - All `/8` literal step counters in subprocess behavior assertions → `/9`.
    - test_tutorialflow_defaults: tf.total == 9 (was 8).
    - test_step_topics_keys_and_slugs: expected dict has 9 entries; new key 3 'strategy-details'.
    - test_step_tasks_have_tutorialstep_instances: range(1, 10) (was range(1, 9)).
    - test_step6_open_question_2_resolution renamed to test_step7_open_question_2_resolution; reads STEP_TASKS[7].
    - test_check_step2_strategies tightened: `strategies` passes; `strategy incentive` FAILS for step 2.
    - NEW test_check_step3_strategy_details: `strategy incentive` passes for step 3; bare `strategies` fails (no tokens[0]=='strategy' match).
    - test_check_step3_any_builtin → test_check_step4_any_builtin (step_n=4).
    - test_check_step4_custom → test_check_step5_custom (step_n=5).
    - test_check_step5_compare → test_check_step6_compare (step_n=6).
    - test_check_step6_soft_pass → test_check_step7_soft_pass (step_n=7).
    - test_check_step7_soft_pass → test_check_step8_soft_pass (step_n=8).
    - test_check_step8_batch → test_check_step9_batch (step_n=9).
    - test_soft_pass_step_rejects_unknown_command (added in 08-08): adjust to reach the FIRST soft-pass step under new numbering (step 7 env), via 6 skips; assert `[krok 7/9` and absence of `✓ zaliczone — krok 7/9`.
  </behavior>
  <action>
    Apply targeted edits to tests/test_tutorial.py to align all assertions
    with the 9-step contract. Most edits are mechanical /8 → /9 substitutions;
    one new test method is added; one existing test is tightened; six
    `test_check_stepN_*` methods are renamed + their step_n argument bumped by 1.

    EDIT A — Behavior tests (subprocess /8 → /9 substitutions):

    File-wide find/replace inside subprocess assertions ONLY (NOT inside test
    method names or docstrings unless they refer to /8 step counts):
      - `[krok 1/8` → `[krok 1/9`
      - `[krok 2/8` → `[krok 2/9`
      - `[krok 6/8` → `[krok 7/9` (test_soft_pass_step_rejects_unknown_command:
        the FIRST soft-pass step shifted from old 6 (env) to new 7 (env, post split))
      - `[krok 7/8` → `[krok 8/9` (defensive — but no current assertion uses
        this; safe to skip if grep finds zero hits)
      - `pominięto — krok 1/8` → `pominięto — krok 1/9`
      - `pominięto — krok 8/8` → `pominięto — krok 9/9`
      - `cofnięto do kroku 1/8` → `cofnięto do kroku 1/9`
      - `✓ zaliczone — krok 1/8` → `✓ zaliczone — krok 1/9`
      - `krok 2/8` → `krok 2/9` (in test_tutorial_step_verification_advances)
      - `krok 6/8` → `krok 7/9` (in test_soft_pass_step_rejects_unknown_command
        assertion `assertNotIn('✓ zaliczone — krok 6/8', ...)`)

    EDIT B — test_soft_pass_step_rejects_unknown_command (introduced in 08-08):
    Reach step 7 (env, first soft-pass post-split) instead of old step 6. Body:
        r = _run_repl_interactive(
            'tutorial\nskip\nskip\nskip\nskip\nskip\nskip\ntojesttypo\nexit\nexit\n'
        )
    That's 6 skips (steps 1→7), then the typo on step 7, then 2 exits. Update
    assertion strings: `[krok 7/9` and `✓ zaliczone — krok 7/9`.

    EDIT C — TestTutorialFlow.test_tutorialflow_defaults:
    Change `self.assertEqual(tf.total, 8)` → `self.assertEqual(tf.total, 9)`.

    EDIT D — TestTutorialFlow.test_step_topics_keys_and_slugs:
    Replace the expected dict body with:
        expected = {
            1: 'baseline',
            2: 'strategies',
            3: 'strategy-details',
            4: 'run-strategy',
            5: 'custom',
            6: 'compare',
            7: 'env',
            8: 'report',
            9: 'batch',
        }

    EDIT E — TestTutorialFlow.test_step_tasks_have_tutorialstep_instances:
    Change `set(range(1, 9))` → `set(range(1, 10))` and the for-loop
    `for step_n in range(1, 9):` → `for step_n in range(1, 10):`.

    EDIT F — TestTutorialFlow.test_step6_open_question_2_resolution → rename
    to test_step7_open_question_2_resolution:
    Change method name AND the STEP_TASKS subscript from `STEP_TASKS[6]` to
    `STEP_TASKS[7]`. The two assertions (`assertIn('--phi', desc)`,
    `assertIn('informacyjny', desc)`) are unchanged.

    EDIT G — TestTutorialFlow.test_check_step2_strategies — TIGHTEN:
    Current body asserts BOTH `strategies` and `strategy incentive` pass.
    Rewrite to assert step 2 accepts ONLY `strategies` (new semantics post-
    split):
        def test_check_step2_strategies(self):
            """Test 3 (step 2 strategies — list-only post UAT Gap 3 split):
            line=='strategies' passes; 'strategy <name>' goes to step 3."""
            from sphsim.cli.tutorial import check_step
            self.assertTrue(check_step(2, 'strategies', None, set(), frozenset()))
            self.assertFalse(check_step(2, 'strategy incentive', None, set(), frozenset()),
                             msg="step 2 must NOT accept `strategy <name>` after split")
            self.assertFalse(check_step(2, 'run naive', None, set(), frozenset()))

    EDIT H — NEW TestTutorialFlow.test_check_step3_strategy_details (insert
    between test_check_step2_strategies and the renamed
    test_check_step4_any_builtin):
        def test_check_step3_strategy_details(self):
            """Test 3b (step 3 strategy details — UAT Gap 3 split): `strategy
            <name>` passes; bare `strategies` fails (tokens[0] must be 'strategy'
            AND len(tokens) >= 2)."""
            from sphsim.cli.tutorial import check_step
            self.assertTrue(check_step(3, 'strategy incentive', None, set(), frozenset()))
            self.assertTrue(check_step(3, 'strategy adaptive', None, set(), frozenset()))
            self.assertFalse(check_step(3, 'strategies', None, set(), frozenset()),
                             msg="step 3 requires `strategy <name>`, bare `strategies` is step 2")
            self.assertFalse(check_step(3, 'strategy', None, set(), frozenset()),
                             msg="step 3 requires a name after `strategy`")

    EDIT I — Rename + step_n bump for the remaining check_stepN tests:
      - test_check_step3_any_builtin → test_check_step4_any_builtin
        Change `check_step(3, ...)` to `check_step(4, ...)` in both assertTrue
        and assertFalse calls.
      - test_check_step4_custom → test_check_step5_custom
        Change `check_step(4, ...)` → `check_step(5, ...)`.
      - test_check_step5_compare → test_check_step6_compare
        Change `check_step(5, ...)` → `check_step(6, ...)`.
      - test_check_step6_soft_pass → test_check_step7_soft_pass
        Change `check_step(6, ...)` → `check_step(7, ...)`.
      - test_check_step7_soft_pass → test_check_step8_soft_pass
        Change `check_step(7, ...)` → `check_step(8, ...)`.
      - test_check_step8_batch → test_check_step9_batch
        Change `check_step(8, ...)` → `check_step(9, ...)`.

    Anti-regression: do NOT change unrelated tests (e.g.,
    test_report_dir_override_*, test_wr06_*). The D-10 override unit tests
    are independent of the step count.

    The test `test_check_step1_baseline_pass_and_fail` and
    `test_check_step1_low_kpi_fails` are step-1 only — UNCHANGED.

    Note: the test `test_step1_polish_copy_contains_run_naive_and_kpi` reads
    STEP_TASKS[1] — UNCHANGED.
  </action>
  <verify>
    <automated>SPHSIM_NO_REPORT=1 python -m unittest tests.test_tutorial -v 2>&amp;1 | grep -E '^OK|^FAILED'</automated>
    Should report OK with the new test count (was N, now N+1 from the added
    test_check_step3_strategy_details).
    Plus structural check:
      grep -c '/8' tests/test_tutorial.py  # should drop sharply (only PATTERNS-doc references in module docstring remain, ideally 0 in subprocess assertions)
      grep -c 'test_check_step3_strategy_details' tests/test_tutorial.py  → 1
  </verify>
  <done>
    - All TestTutorial* + TestTutorialFlow tests pass.
    - test_check_step3_strategy_details is the new test (asserts step 3 details).
    - test_check_step2_strategies asserts the tightened contract (only `strategies` passes).
    - Six existing check_stepN_* tests renamed + step_n bumped.
    - test_step7_open_question_2_resolution reads STEP_TASKS[7].
    - No /8 literals remain in subprocess assertions.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update scripts/verify_phase8.sh D2/D3/D4 step-count literals + 9 skips</name>
  <files>scripts/verify_phase8.sh</files>
  <action>
    Apply three edits to scripts/verify_phase8.sh (Category D — REPL tutorial
    command checks).

    EDIT 1 — D2 (line 120):
    Change `grep -F '[krok 1/8'` to `grep -F '[krok 1/9'` (both occurrences in
    the compound command — there are two grep invocations on the same line,
    one for INTERAKTYWNY TUTORIAL and one for [krok 1/8; only the latter changes).

    EDIT 2 — D3 (line 122):
    Two sub-changes on the same check line:
      (a) Increase skip-input count from 8 skips to 9 skips. Current input
          uses `printf 'tutorial\\nskip\\nskip\\nskip\\nskip\\nskip\\nskip\\nskip\\nskip\\nexit\\n'`
          — count of `\\nskip` substrings is 8 (one was already in the literal
          per the existing 8/8 path). After fix: ADD ONE more `skip\\n` so total
          skip lines = 9.
          New input: `printf 'tutorial\\nskip\\nskip\\nskip\\nskip\\nskip\\nskip\\nskip\\nskip\\nskip\\nexit\\n'`.
      (b) Update the assertion `grep -F 'pominięto — krok 8/8'` to
          `grep -F 'pominięto — krok 9/9'`.
    Also update the [PASS] label in `check "D3 (TUT-02 skip): 8 skipów ..."`
    to `check "D3 (TUT-02 skip): 9 skipów dochodzi do 'pominięto — krok 9/9'"`.

    EDIT 3 — D4 (line 124):
    Change `grep -F '✓ zaliczone — krok 1/8'` to `grep -F '✓ zaliczone — krok 1/9'`.

    Anti-regression notes:
      - Do NOT change any other category (A/B/C/E/F/G) — they don't reference
        step counts.
      - C7 (line 112–113) is the bare-sph_sim required-mode check. UNCHANGED
        in this plan — Plan 08-10 owns that flip.
      - The PASS count in the summary banner is computed by the runner; do NOT
        hardcode the new total in the script.
  </action>
  <verify>
    <automated>bash scripts/verify_phase8.sh 2>&amp;1 | tail -20</automated>
    Should exit 0 with `Phase 8 verification: PASS=&lt;N&gt; / FAIL=0`. The PASS
    count should be unchanged vs the pre-08-09 baseline (we didn't add or
    remove checks — just edited their content).
    Plus structural greps:
      grep -c "krok 1/9" scripts/verify_phase8.sh  → 2 (D2 + D4)
      grep -c "krok 9/9" scripts/verify_phase8.sh  → 1 (D3)
      grep -c "krok 1/8\|krok 8/8" scripts/verify_phase8.sh  → 0
  </verify>
  <done>
    - verify_phase8.sh D2 asserts `[krok 1/9`.
    - D3 has 9 skip lines and asserts `pominięto — krok 9/9`.
    - D4 asserts `✓ zaliczone — krok 1/9`.
    - bash scripts/verify_phase8.sh exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 5: Update docs/PRZEWODNIK.md sample step heading to [krok 1/9 — Baseline]</name>
  <files>docs/PRZEWODNIK.md</files>
  <action>
    Apply one trivial edit to docs/PRZEWODNIK.md.

    EDIT: Line 59 currently reads `[krok 1/8 — Baseline]` (inside a fenced code
    block sample showing the tutorial step display). Change to:
        [krok 1/9 — Baseline]

    Anti-regression note: This is a sample heading inside an illustrative
    code block. There are NO other `/8` literals in PRZEWODNIK.md that
    reference tutorial step counts (verified via grep prior to writing this
    plan). The number `8` may appear elsewhere as REPL command count or in
    invocation examples (`--seeds 8` etc.) — leave those alone.

    Verification grep:
      grep -n '\[krok ' docs/PRZEWODNIK.md
      → should print exactly one line: `59:[krok 1/9 — Baseline]`.
  </action>
  <verify>
    <automated>grep -c '\[krok 1/9 — Baseline\]' docs/PRZEWODNIK.md</automated>
    Must return 1. Also:
      grep -c '\[krok 1/8' docs/PRZEWODNIK.md  → 0
    Plus end-to-end:
      bash scripts/verify_phase8.sh 2>&amp;1 | tail -5  # still exit 0
  </verify>
  <done>
    - docs/PRZEWODNIK.md sample step heading reads [krok 1/9 — Baseline].
    - No `[krok 1/8` literal remains in PRZEWODNIK.md.
  </done>
</task>

</tasks>

<verification>
- SPHSIM_NO_REPORT=1 python -m unittest discover tests reports OK (all
  modules green).
- bash scripts/verify_phase8.sh exits 0 with no FAIL lines.
- scripts/regression_check.py reports PASS: 8/8.
- Manual smoke (developer): `python sph_sim.py --tutorial`, type `skip` 8 times,
  observe `pominięto — krok 9/9. Tutorial zakończony.` on the 9th skip.
- Manual smoke: on tutorial step 2, type `strategy incentive` — observe HINT
  (not auto-advance), confirming step 2 rejects details-style input.
- Manual smoke: on tutorial step 3 (after `strategies` advances), type
  `strategy incentive` — observe `✓ zaliczone — krok 3/9` auto-advance.
</verification>

<success_criteria>
- All 5 modified files reflect the 9-step contract consistently.
- No mixed /8 + /9 references across source, tests, scripts, docs.
- Test count INCREASED by exactly 1 (the new test_check_step3_strategy_details).
- Test names renumbered consistently (3..8 → 4..9) — no orphan test_check_stepN
  methods.
- regression_check.py PASS=8/8 preserved (CLI-04 invariant — Phase 8 never
  changes --json output for built-in strategies).
</success_criteria>

<output>
Create `.planning/phases/08-documentation-interactive-tutorial/08-09-SUMMARY.md` when done.
</output>
