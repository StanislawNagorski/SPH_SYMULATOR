---
phase: 01-refactoring-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-03
files_modified:
  - sphsim/core/simulator.py
  - sphsim/core/__init__.py
  - sphsim/cli/__init__.py
  - sphsim/cli/args.py
  - sphsim/cli/output.py
  - sphsim/cli/main.py
  - sphsim/__init__.py
  - sphsim/__main__.py
  - sph_sim.py
autonomous: true
requirements:
  - CLI-04
requirements_addressed:
  - CLI-04

must_haves:
  truths:
    - "sphsim/core/simulator.py (trzeci plik w core/ po model.py i device.py z Planu 02) zawiera klasę SPHSimulator z identyczną logiką (i identyczną kolejnością wywołań random.*) jak v1.0 (D-12)"
    - "sphsim/cli/ rozbite na 3 pliki: args.py (parse_args), output.py (format_human + format_json), main.py (orchestration) — Phase 2 doda repl.py obok (D-15)"
    - "sph_sim.py w root jest thin shimem — dokładnie 4 linie kodu (plus shebang i docstring 1:1 z v1.0), tylko CLI dispatch do sphsim.cli.main.main() (D-05)"
    - "sphsim/__init__.py eksportuje SPHSimulator, Device, STRATEGIES jako publiczne API (D-16)"
    - "sphsim/__main__.py wywołuje cli.main.main()"
    - "Cutover: regression_check.py NA REFAKTOROWANYM kodzie zwraca exit 0 — wszystkie 8 inwokacji produkują bit-identyczne wyniki"
  artifacts:
    - path: "sphsim/core/simulator.py"
      provides: "SPHSimulator class — pełna logika z sph_sim.py:181-326"
      contains: "class SPHSimulator"
    - path: "sphsim/cli/args.py"
      provides: "parse_args() — argparse setup z sph_sim.py:331-356"
      contains: "def parse_args"
    - path: "sphsim/cli/output.py"
      provides: "format_human(args, res) i format_json(args, res, params, K1)"
      contains: "def format_human"
    - path: "sphsim/cli/main.py"
      provides: "main() orchestration — buduje SPHSimulator z args, run, formatuje wynik"
      contains: "def main"
    - path: "sph_sim.py"
      provides: "thin shim dispatch do sphsim.cli.main.main()"
      contains: "from sphsim.cli.main import main"
      max_lines: 30
    - path: "sphsim/__init__.py"
      provides: "publiczne API: SPHSimulator, Device, STRATEGIES"
      contains: "SPHSimulator"
  key_links:
    - from: "sphsim/cli/main.py"
      to: "sphsim.core.simulator.SPHSimulator + sphsim.strategies.STRATEGIES"
      via: "import + instancjonowanie + run()"
      pattern: "SPHSimulator"
    - from: "sph_sim.py"
      to: "sphsim.cli.main.main"
      via: "thin shim import + dispatch w __main__"
      pattern: "from sphsim.cli.main import main"
    - from: "sphsim/core/simulator.py"
      to: "random.seed(seed) w __init__"
      via: "preserves v1.0 invocation order (kluczowy invariant)"
      pattern: "random.seed"
---

<objective>
Wykonać CUTOVER — wyciągnąć `SPHSimulator` z monolitu, zbudować warstwę CLI (`args.py`, `output.py`, `main.py`), przepisać `sph_sim.py` na thin shim i sfinalizować publiczne API w `sphsim/__init__.py`. Po tym planie pakiet `sphsim/` JEST źródłem prawdy; monolit znika.

Purpose: To jest atomic cutover dla CLI-04 (backwards compat). Najryzykowniejszy krok fazy — jakakolwiek zmiana w kolejności `random.*` wywołań w `SPHSimulator.__init__` (`random.seed` na linii 188, potem `random.random()` w pętli inicjalizującej devices na liniach 193–198) zmieni seekwencję losowań i wszystkie 8 fixtures regresji się rozjadą. Per CONTEXT.md `code_context`: "random.seed(seed) raz w `SPHSimulator.__init__` — zapewnia reprodukowalność. Refactor MUSI zachować tę kolejność wywołań random.* — jakakolwiek zmiana kolejności może zmienić sekwencję losowań i zepsuć snapshot test. To krytyczny invariant."
Output: 6 nowych plików w `sphsim/`, przepisany `sph_sim.py` (433 → ~10 linii), `sphsim/__init__.py` z publicznym API. Regression check zwraca exit 0 — to jest moment prawdy fazy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-refactoring-foundation/01-CONTEXT.md
@sph_sim.py

<interfaces>
<!-- Source-of-truth specifications for SPHSimulator + CLI layer. -->
<!-- Verbatim line-for-line copy required for random.* call ordering. -->

From sph_sim.py:181–326 (SPHSimulator → sphsim/core/simulator.py):
```python
class SPHSimulator:
    def __init__(self, nU, nSUS, K0, K1, F, T, kappa, alpha,
                 phi, rho, strategy_fn, params, seed=42):
        # Line 188: random.seed(seed)         <-- CRITICAL FIRST random call
        # Lines 193–198: device init loop calls random.random() and random.randint()
        # in EXACT order: for each id in range(nU): random.random() THEN possibly random.randint(1, F-1)
    def run(self):
        # Lines 209+: per-cycle loop, calls strategy_fn which may invoke random.* internally
        # Lines 232: random.random() < fp  <-- failure check
        # Returns dict matching sph_sim.py:316-326 keys
```

INVARIANT: SPHSimulator.__init__ kolejność operacji MUSI być:
1. self.<assignments> (nU, nSUS, ...)
2. random.seed(seed)               <-- pierwsza linia random.*
3. self.h = lambda
4. for did in range(nU): r1 = random.random(); if r1 < 0.3: Device(DOWN) else: random.randint → Device(UP, phase=ph)
5. self.s = nSUS // 2
6. self.history = {...}

ŻADNA zmiana porządku tych operacji nie może być wprowadzona — w tym restrukturyzacja do "cleaniejszego" __init__ przez wydzielenie helpera. random.* wywołania MUSZĄ pozostać w głównym ciele __init__ w tej samej kolejności.

From sph_sim.py:331–356 (parse_args → sphsim/cli/args.py):
```python
def parse_args():
    p = argparse.ArgumentParser(...)
    # 13 add_argument calls in EXACT order
    # epilog=__doc__ <-- używa docstring modułu, zostanie w cli/args.py jako dedykowany docstring
    return p.parse_args()
```

From sph_sim.py:358–430 (main → split between cli/main.py and cli/output.py):
- cli/main.py: parse args → build params dict → instantiate SPHSimulator → sim.run() → dispatch to output
- cli/output.py: format_human(args, res) handles lines 384-430 (human-readable table + IC analysis + verbose)
- cli/output.py: format_json(args, res, params, K1) handles lines 375-383 (JSON serialization)

NOTE on docstring: sph_sim.py:1–27 contains module docstring z 7 example invocations + 5 strategy descriptions. argparse used `epilog=__doc__` w v1.0 — po refactorze docstring trafia do `sphsim/cli/args.py` jako MODULE docstring (żeby `__doc__` w epilog działało po przeniesieniu).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wyciągnąć SPHSimulator do sphsim/core/simulator.py preserving random.* call order</name>
  <files>sphsim/core/simulator.py, sphsim/core/__init__.py</files>
  <read_first>sph_sim.py (linie 181–326 — pełna klasa SPHSimulator); sphsim/core/device.py (Device dataclass z Planu 02); sphsim/core/model.py (valuation, sph_stp z Planu 02); .planning/phases/01-refactoring-foundation/01-CONTEXT.md (sekcja D-12 + code_context invariant about random.seed ordering)</read_first>
  <action>
    Stworzyć `sphsim/core/simulator.py`:

    1. Importy (kolejność: stdlib → pakiet, identyczna konwencja jak v1.0):
       ```python
       import random
       from sphsim.core.device import Device
       from sphsim.core.model import valuation, sph_stp
       ```

    2. Polski komentarz nagłówkowy: `# Symulator — orchestruje T-cyklowy bieg, deleguje decyzje do strategy_fn.`

    3. Skopiować VERBATIM klasę `SPHSimulator` z sph_sim.py:181–326. ŻADNYCH zmian poza:
       - Usunięcie nagłówkowego komentarza `# SYMULATOR ──...──` (jest już w nagłówku pliku)
       - Importy są na górze pliku, nie wewnątrz klasy

    4. **LOC budget (RAW reality check — zero headroom):**
       - Verbatim klasa SPHSimulator z sph_sim.py:181–326 = **146 linii** (sprawdzone wc -l).
       - 3 importy + 1 komentarz nagłówkowy = **~4 dodatkowe linie**.
       - **Łącznie ~146–150 LOC — pełne wykorzystanie budżetu 150 LOC (Success Criterion #2).**
       - Executor MUSI uważać: jakiekolwiek dodatkowe puste linie ponad oryginał, dodatkowe komentarze tłumaczące, albo formattery (`black`, `autopep8`) mogą przekroczyć cap.
       - Po napisaniu uruchomić `wc -l sphsim/core/simulator.py` i jeśli wynik > 150 — usunąć puste linie wewnątrz metod (oryginał ma wciętą gęstą strukturę), NIE refaktorować logiki.
       - **NIE pre-splittać do `simulator_helpers.py`** — verbatim extract klasy jest semantycznie spójny i pre-split tylko zaciemnia random.* invariant. Jeśli rzeczywiście przekroczy 150 LOC po usunięciu pustych linii — Plan 05 ma reaktywny fallback (split simulator.py + simulator_helpers.py), ale nie aktywuj go pre-emptywnie.

    5. **NIE WOLNO**:
       - Zmieniać porządku linii w `__init__` (random.seed PRZED pętlą device init)
       - Wyciągać `_init_devices` lub `_init_buffer` jako helpery (zmieni kolejność lub call stack random.*)
       - Refaktorować `for did in range(nU)` pętli — `random.random()` musi być pierwsze, `random.randint(1, F-1)` w gałęzi `else`
       - Zmieniać struktury `self.history` ani kluczy
       - Modyfikować return dict w `run()` — klucze MUSZĄ pasować do sph_sim.py:316–326 (`avg_val_last100`, `cum_val_total`, `avg_net_profit`, `delivery_ratio`, `avg_providers_l100`, `sus_final`, `ic_per_phase`, `history`, `devices`)
       - Zmieniać precyzji `round(..., 4)` lub `round(..., 2)` — wartości są bit-identycznie porównywane przez regression_check.py

    6. Zaktualizować `sphsim/core/__init__.py` (z Planu 02, było puste) — dodać re-eksporty:
       ```python
       from sphsim.core.simulator import SPHSimulator
       from sphsim.core.device import Device
       from sphsim.core.model import valuation, sph_stp
       ```
       (Te eksporty będą używane przez `sphsim/__init__.py` w Task 3 i przez user-facing import dla Phase 3+.)

    7. Po napisaniu uruchomić smoke test bezpośrednio (`python -c "..."`):
       ```python
       from sphsim.core.simulator import SPHSimulator
       from sphsim.strategies import STRATEGIES
       from sphsim.config import DEFAULT_K0, DEFAULT_F, DEFAULT_PHI, DEFAULT_RHO
       sim = SPHSimulator(nU=250, nSUS=20, K0=DEFAULT_K0, K1=120, F=DEFAULT_F, T=100, kappa=0.25, alpha=1,
                          phi=DEFAULT_PHI, rho=DEFAULT_RHO, strategy_fn=STRATEGIES['naive'],
                          params={'zeta': 0.5}, seed=42)
       res = sim.run()
       assert 'avg_val_last100' in res
       assert 'history' in res
       print('OK SPHSimulator runs from package')
       ```

    Monolit `sph_sim.py` NADAL nietknięty w tym tasku (zostanie przepisany w Task 3). To pozwala iterować — jeśli simulator.py ma bug, regression_check vs monolit nadal działa jako bezpiecznik.
  </action>
  <verify>
    <automated>
test -f sphsim/core/simulator.py \
  && python -c "
from sphsim.core.simulator import SPHSimulator
from sphsim.core.device import Device
from sphsim.core.model import valuation, sph_stp
from sphsim.strategies import STRATEGIES
from sphsim.config import DEFAULT_NU, DEFAULT_NSUS, DEFAULT_K0, DEFAULT_K1, DEFAULT_F, DEFAULT_T, DEFAULT_KAPPA, DEFAULT_ALPHA, DEFAULT_PHI, DEFAULT_RHO

# Smoke: SPHSimulator instancjonuje się i .run() zwraca expected keys
sim = SPHSimulator(nU=DEFAULT_NU, nSUS=DEFAULT_NSUS, K0=DEFAULT_K0, K1=DEFAULT_K1,
                   F=DEFAULT_F, T=DEFAULT_T, kappa=DEFAULT_KAPPA, alpha=DEFAULT_ALPHA,
                   phi=DEFAULT_PHI, rho=DEFAULT_RHO, strategy_fn=STRATEGIES['naive'],
                   params={'zeta': 0.5}, seed=42)
res = sim.run()
expected_keys = {'avg_val_last100', 'cum_val_total', 'avg_net_profit', 'delivery_ratio', 'avg_providers_l100', 'sus_final', 'ic_per_phase', 'history', 'devices'}
assert expected_keys.issubset(set(res.keys())), set(res.keys()) - expected_keys
print('OK pakiet-side SPHSimulator runs; returns all expected keys')

# Cross-check: pakiet-side SPHSimulator z naive zeta=0.5 seed=42 zwraca avg_val_last100 = JSON output z fixture
import json
fix = json.load(open('tests/fixtures/baseline_v1/01-naive-zeta-0.5.json'))
expected_avg_val = fix['metrics']['avg_val_last100']
got_avg_val = round(sum(res['history']['val'][-100:]) / 100, 4)
assert got_avg_val == expected_avg_val, f'avg_val_last100: expected={expected_avg_val}, got={got_avg_val}'
print(f'OK CROSS-CHECK: pakiet zwraca avg_val_last100={got_avg_val} identyczne z fixture')
" \
  && python scripts/regression_check.py \
  && echo "PASS: simulator.py extracted, package-side smoke OK, cross-check vs fixture OK, monolit nadal działa"
    </automated>
  </verify>
  <done>sphsim/core/simulator.py istnieje, package-side SPHSimulator z `naive --zeta 0.5 --seed 42` produkuje avg_val_last100 IDENTYCZNE z fixture #1, monolit nadal działa (regression_check exit 0).</done>
  <acceptance_criteria>
    - Plik `sphsim/core/simulator.py` istnieje i definiuje `class SPHSimulator`
    - Smoke test (instancjowanie + run) w verify przechodzi
    - **CROSS-CHECK krytyczny**: pakiet-side `SPHSimulator(...).run()` dla `naive zeta=0.5 seed=42` zwraca `avg_val_last100` bit-identyczne z `tests/fixtures/baseline_v1/01-naive-zeta-0.5.json` → potwierdza że random.seed ordering preserved
    - `sphsim/core/__init__.py` re-eksportuje SPHSimulator, Device, valuation, sph_stp
    - `python scripts/regression_check.py` zwraca exit code 0 (monolit nadal działa, fixtures intact)
    - `sph_sim.py` nie został zmodyfikowany W TYM TASKU
    - `wc -l sphsim/core/simulator.py` raportuje ≤ 150 linii (Success Criterion #2; **spodziewane ~146–150 LOC — zero headroom, raw verbatim extract zużywa cały budżet**). Jeśli wynik > 150: usuń puste linie wewnątrz metod (preserve random.* ordering); jeśli nadal > 150 → eskaluj Plan 05 reactive split fallback.
    - `grep -c "random\." sphsim/core/simulator.py` zwraca ≥ 3 (random.seed + random.random + random.randint preserved)
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Stworzyć sphsim/cli/ — args.py, output.py, main.py</name>
  <files>sphsim/cli/__init__.py, sphsim/cli/args.py, sphsim/cli/output.py, sphsim/cli/main.py</files>
  <read_first>sph_sim.py (linie 1–27 docstring, 331–356 parse_args, 358–430 main); sphsim/core/simulator.py (SPHSimulator zbudowany w Task 1); sphsim/strategies/__init__.py (STRATEGIES dict); .planning/phases/01-refactoring-foundation/01-CONTEXT.md (sekcja D-15)</read_first>
  <action>
    Utworzyć katalog `sphsim/cli/` z 4 plikami:

    **1. `sphsim/cli/__init__.py`** — pusty plik (package marker).

    **2. `sphsim/cli/args.py`**:
    - Module docstring: SKOPIOWAĆ VERBATIM sph_sim.py:1–27 (cały docstring z 7 example invocations + 5 strategy descriptions). Po refactorze `epilog=__doc__` w argparse będzie wskazywał na docstring TEGO pliku.
    - Importy: `import argparse`, `from sphsim.strategies import STRATEGIES`, `from sphsim.config import DEFAULT_NU, DEFAULT_NSUS, DEFAULT_K1, DEFAULT_T, DEFAULT_KAPPA, DEFAULT_ALPHA`
    - Skopiować VERBATIM `def parse_args()` z sph_sim.py:331–356. Identyczna kolejność `add_argument` calls (13 argumentów), identyczne defaulty, identyczne help strings (polskie). `epilog=__doc__` zostaje — będzie używał docstring tego modułu.
    - NIE wprowadzać sub-parserów, NIE dzielić na funkcje (args.py to deliberately mechanical extract).

    **3. `sphsim/cli/output.py`**:
    - Polski komentarz nagłówkowy: `# Formatowanie wyniku symulacji — human-readable + JSON.`
    - Importy: `import json`
    - Funkcja `format_json(args, res, params, K1) -> str`:
      ```python
      def format_json(args, res, params, K1):
          out = {
              'strategy': args.strategy,
              'strategy_params': params,
              'env': {'nU': args.nU, 'nSUS': args.nSUS, 'K1': K1,
                      'T': args.T, 'kappa': args.kappa, 'alpha': args.alpha},
              'metrics': {k: v for k, v in res.items() if k not in ('history', 'devices')},
          }
          return json.dumps(out, indent=2)
      ```
      To VERBATIM odpowiednik sph_sim.py:375–383. **KRYTYCZNE**: `json.dumps(out, indent=2)` BEZ `sort_keys=True` — sph_sim.py NIE używa sort_keys, więc kolejność kluczy w outputcie zależy od insertion order dict'a. Fixtures w `tests/fixtures/baseline_v1/` mają sort_keys=True (generate_baseline.py je sortuje), ale regression_check.py parsuje przez `json.loads` zanim porówna — więc kolejność kluczy w stringu output nie matter dla regression check (porównywanie dict-level).

    - Funkcja `format_human(args, res, K1, verbose) -> str`:
      Skopiować VERBATIM print loop z sph_sim.py:384–430. Zmienić `print(...)` calls na akumulację do `lines: List[str]` i return `"\n".join(lines)`. Zachować identyczne format strings, padding (`>10.2f`, `>10`), separator `'─' * 62`, polskie nagłówki, IC per-phase table (linie 405–421), verbose sampling block (423–429).

    **4. `sphsim/cli/main.py`**:
    - Polski komentarz nagłówkowy: `# Entry point CLI — parse args, buduje SPHSimulator, run, formatuje wynik.`
    - Importy:
      ```python
      from sphsim.cli.args import parse_args
      from sphsim.cli.output import format_human, format_json
      from sphsim.core.simulator import SPHSimulator
      from sphsim.strategies import STRATEGIES
      from sphsim.config import DEFAULT_K0, DEFAULT_F, DEFAULT_PHI, DEFAULT_RHO
      ```
    - Funkcja `def main()`:
      Skopiować VERBATIM `def main()` z sph_sim.py:358–430, ale:
      - Zastąpić wbudowany `parse_args()` call na importowany.
      - Zastąpić instancjonowanie używając importowanych defaultów z `sphsim.config` zamiast lokalnych `DEFAULT_*` (były inline w monolicie).
      - Zastąpić `print(json.dumps(out, indent=2))` na `print(format_json(args, res, params, K1))`.
      - Zastąpić cały blok else (human-readable) na `print(format_human(args, res, K1, args.verbose))`.
      - Zachować identyczną logikę `K1 = float('inf') if args.K1 < 0 else args.K1` i build params dict.

    Per CONTEXT.md `code_context` integration points: `cli/main.py` ma "łatwo wyodrębniony 'run simulation' path żeby Phase 2 (REPL) mógł go wywoływać bez duplikacji" — w v1.1 Phase 1 nie wydzielamy tego helpera (YAGNI per D-02), ale piszemy main() tak żeby `parse_args` był wymienialny — Phase 2 podmieni `args = parse_args()` na własny args object z REPL.

    **UWAGA o `sphsim/__main__.py`:** Plan 02 Task 1 stworzył `sphsim/__main__.py` jako placeholder z `NotImplementedError`. Ten task **NIE** modyfikuje `__main__.py` (nie ma go w `<files>`). Wiring `__main__.py` → `cli.main.main()` jest robione w Task 3 (cutover step) razem z thin shim dla `sph_sim.py`. Dlatego verify tego tasku NIE używa `python -m sphsim` — testujemy `main()` przez bezpośredni call z monkeypatched `sys.argv` (subprocess + bezpośrednie wywołanie funkcji).

    Monolit `sph_sim.py` NADAL nietknięty (przepisywany w Task 3).
  </action>
  <verify>
    <automated>
test -f sphsim/cli/__init__.py \
  && test -f sphsim/cli/args.py \
  && test -f sphsim/cli/output.py \
  && test -f sphsim/cli/main.py \
  && python -c "
from sphsim.cli.args import parse_args
from sphsim.cli.output import format_human, format_json
from sphsim.cli.main import main
import inspect

# Sprawdź sygnatury
assert callable(parse_args)
assert callable(format_human) and callable(format_json) and callable(main)

# format_json kontrakt
fake_res = {'avg_val_last100': 92.0, 'history': {'val':[]}, 'devices': []}
class A: strategy='naive'; nU=250; nSUS=20; T=1000; kappa=0.25; alpha=1
import json
out = json.loads(format_json(A(), fake_res, {'zeta': 0.75}, 120))
assert out['strategy'] == 'naive'
assert 'history' not in out['metrics'], 'history powinno być wyfiltrowane'
assert 'devices' not in out['metrics']
assert out['metrics']['avg_val_last100'] == 92.0
print('OK cli/ layer: parse_args + format_json + format_human importowalne, kontrakt JSON spełniony')
" \
  && python -c "
# Test main() przez bezpośredni call z monkeypatched sys.argv (NIE python -m sphsim — __main__.py
# jest jeszcze placeholderem z Planu 02; wiring odbywa się w Task 3 cutover).
import sys, io, json, contextlib
sys.argv = ['sphsim', '--strategy', 'naive', '--zeta', '0.5', '--seed', '42', '--json']
from sphsim.cli.main import main
buf = io.StringIO()
with contextlib.redirect_stdout(buf):
    main()
pkg = json.loads(buf.getvalue())
fix = json.load(open('tests/fixtures/baseline_v1/01-naive-zeta-0.5.json'))
assert pkg['strategy'] == fix['strategy']
assert pkg['metrics']['avg_val_last100'] == fix['metrics']['avg_val_last100'], (pkg['metrics']['avg_val_last100'], fix['metrics']['avg_val_last100'])
assert pkg['metrics'] == fix['metrics'], 'metrics dict nie identyczne'
print('OK main() direct call produkuje JSON identyczny z fixture #1 (sys.argv monkeypatched)')
" \
  && python scripts/regression_check.py \
  && echo "PASS: cli/ layer extracted, main() direct call matches fixture #1, monolit nadal działa"
    </automated>
  </verify>
  <done>4 pliki w sphsim/cli/ istnieją. `main()` wywołane bezpośrednio z `sys.argv = ['sphsim', '--strategy', 'naive', '--zeta', '0.5', '--seed', '42', '--json']` produkuje JSON bit-identyczny z fixture #1. regression_check.py (na monolicie) nadal exit 0. `sphsim/__main__.py` NIE jest jeszcze wpięty — to robi Task 3.</done>
  <acceptance_criteria>
    - 4 pliki istnieją: `sphsim/cli/__init__.py`, `args.py`, `output.py`, `main.py`
    - `from sphsim.cli.args import parse_args` działa
    - `from sphsim.cli.output import format_human, format_json` działa
    - `from sphsim.cli.main import main` działa
    - `main()` wywołane bezpośrednio (z monkeypatched `sys.argv`) dla `naive --zeta 0.5 --seed 42 --json` ZWRACA JSON który po parsowaniu jest dict-equal z `tests/fixtures/baseline_v1/01-naive-zeta-0.5.json`
    - format_json filtruje `history` i `devices` z metrics (kontrakt z sph_sim.py:381)
    - `wc -l sphsim/cli/args.py` ≤ 50 linii (spodziewane ~40)
    - `wc -l sphsim/cli/output.py` ≤ 80 linii (spodziewane ~70)
    - `wc -l sphsim/cli/main.py` ≤ 50 linii (spodziewane ~30)
    - `python scripts/regression_check.py` nadal zwraca 0 (monolit dalej działa — to ostatni task w którym to jest prawda; Task 3 robi cutover)
    - **`sphsim/__main__.py` NIE jest modyfikowany w tym tasku** — pozostaje placeholderem z Planu 02 (NotImplementedError). Wiring odbywa się dopiero w Task 3, gdzie `__main__.py` jest w `<files>` i `<action>` explicite go nadpisuje.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Cutover — przepisać sph_sim.py jako thin shim + zaktualizować sphsim/__main__.py + sphsim/__init__.py z publicznym API</name>
  <files>sph_sim.py, sphsim/__init__.py, sphsim/__main__.py</files>
  <read_first>sphsim/cli/main.py (Task 2); sph_sim.py (cały — żeby wiedzieć co zastępujemy); .planning/phases/01-refactoring-foundation/01-CONTEXT.md (sekcja D-05, D-06, D-16)</read_first>
  <action>
    **TO JEST CUTOVER POINT.** Po tym tasku monolit zniknie i fixtures będą walidować TYLKO pakiet `sphsim/`.

    **1. Przepisać `sph_sim.py` w project root** (433 linie → ~10 linii):
    ```python
    #!/usr/bin/env python3
    """SPH Mediation Simulator — thin shim, full code in sphsim/ package.

    UŻYCIE:
      python sph_sim.py --strategy <NAZWA> [opcje]
      python -m sphsim --strategy <NAZWA> [opcje]   # alternatywny entry point

    Pełna dokumentacja: sphsim/cli/args.py docstring (visible via --help).
    """
    from sphsim.cli.main import main

    if __name__ == '__main__':
        main()
    ```

    Per D-05: BEZ re-eksportów programmatic API w sph_sim.py (`from sph_sim import SPHSimulator` nie był używany w v1.0 user-facing scripts — pakiet `sphsim` jest jedynym programmatic entry point). Krótki polski docstring zachowuje informację że to thin shim — pozostaje semantic backwards-compat dla `python sph_sim.py ...`.

    **2. Przepisać `sphsim/__main__.py`** (z Planu 02 było placeholder z NotImplementedError — Task 2 też go nie tknął; ten task wpina go finalnie):
    ```python
    """Entry point dla `python -m sphsim`."""
    from sphsim.cli.main import main

    if __name__ == '__main__':
        main()
    ```
    Per D-06. Ten wpis usuwa `NotImplementedError` placeholder z Planu 02 i wpina entry point — od tego momentu `python -m sphsim ...` działa.

    **3. Sfinalizować `sphsim/__init__.py`** (z Planu 02 było pustym placeholder) — dodać publiczne API per D-16:
    ```python
    """SPH Mediation Simulator — pakiet refactoringu monolitu sph_sim.py v1.0."""
    from sphsim.core.simulator import SPHSimulator
    from sphsim.core.device import Device
    from sphsim.strategies import STRATEGIES

    __all__ = ['SPHSimulator', 'Device', 'STRATEGIES']
    ```
    Per D-16: `from sphsim import SPHSimulator, STRATEGIES, Device` musi działać.

    **4. Uruchomić MOMENT PRAWDY**: `python scripts/regression_check.py`.

    Skrypt re-uruchamia 8 inwokacji przez `python sph_sim.py ...` — teraz sph_sim.py jest thinem, więc cały kod jest w pakiecie `sphsim/`. Jeśli regression_check.py zwraca exit 0 — refactor jest sukcesem i CLI-04 jest spełniony. Jeśli != 0 — bug w jednym z planów 02/03/04, debug zaczynając od failing slug i porównując diff.

    Jeśli regression check FAILS:
    - NIE COFAĆ commitów — fixtures są bezpieczne (committed w Planie 01)
    - Debug: który slug failuje? np. fixture #1 (naive --zeta 0.5) — porównaj package-side run vs fixture w pythonie (cross-check pattern z Task 1)
    - Najczęstsza przyczyna: zmiana porządku `random.*` calls w simulator.py
    - Druga najczęstsza: nieidentyczna wartość w `sphsim/config.py` (np. `DEFAULT_KAPPA = 0.25` vs `0.250` — to PEP 8 może je zaproponować ale literalna reprezentacja MUSI być identyczna)

    Po sukcesie regression check — wykonać dodatkowe smoke testy backward-compat (Success Criterion #3 ROADMAP):
    ```
    python sph_sim.py --strategy naive --zeta 0.5 --seed 42 --json  → identyczne z fixture #1
    python sph_sim.py --strategy naive                              → human-readable output działa (nie crashuje, drukuje tabelę)
    python -m sphsim --strategy threshold --max_phase 3 --json      → JSON dict-equal z fixture #2
    ```
  </action>
  <verify>
    <automated>
test -f sph_sim.py \
  && test "$(wc -l < sph_sim.py | tr -d ' ')" -le 20 \
  && grep -q "from sphsim.cli.main import main" sph_sim.py \
  && python -c "from sphsim import SPHSimulator, Device, STRATEGIES; print('OK publiczne API:', list(STRATEGIES.keys()))" \
  && python -c "
# Sprawdź że sphsim/__main__.py NIE rzuca już NotImplementedError + dispatch działa
import subprocess, sys
result = subprocess.run([sys.executable, '-m', 'sphsim', '--strategy', 'naive', '--seed', '42', '--json'], capture_output=True, text=True)
assert result.returncode == 0, f'__main__.py failed: {result.stderr}'
import json
out = json.loads(result.stdout)
assert out['strategy'] == 'naive'
print('OK python -m sphsim działa, __main__.py wpięty')
" \
  && python -m sphsim --strategy naive --zeta 0.5 --seed 42 --json > /tmp/pkg_m_out.json 2>&1 \
  && python -c "
# Cross-check: python -m sphsim ścieżka (moved from Plan 04 Task 2 — działa dopiero PO wpięciu __main__.py w tym tasku)
import json
pkg = json.load(open('/tmp/pkg_m_out.json'))
fix = json.load(open('tests/fixtures/baseline_v1/01-naive-zeta-0.5.json'))
assert pkg['metrics'] == fix['metrics'], f'python -m sphsim ścieżka != fixture #1: {pkg[\"metrics\"]} vs {fix[\"metrics\"]}'
print('OK python -m sphsim produkuje JSON identyczny z fixture #1 (D-06 spełnione)')
" \
  && python scripts/regression_check.py \
  && python sph_sim.py --strategy naive --zeta 0.5 --seed 42 --json > /tmp/cutover_test.json \
  && python -c "
import json
got = json.load(open('/tmp/cutover_test.json'))
expected = json.load(open('tests/fixtures/baseline_v1/01-naive-zeta-0.5.json'))
assert got['metrics'] == expected['metrics'], f'CUTOVER FAILED: metrics differ\nGot: {got[\"metrics\"]}\nExpected: {expected[\"metrics\"]}'
print('OK CUTOVER SUCCESS: python sph_sim.py produces bit-identical output to fixture')
" \
  && echo "PASS: thin shim sph_sim.py + publiczne API w sphsim/__init__.py + python -m sphsim match fixture + regression check exit 0 — CUTOVER OK"
    </automated>
  </verify>
  <done>sph_sim.py jest thin shimem (≤20 linii, dispatch do sphsim.cli.main.main). sphsim/__main__.py wpięty (NotImplementedError zastąpiony). sphsim/__init__.py eksportuje SPHSimulator/Device/STRATEGIES. regression_check.py zwraca exit 0 — wszystkie 8 inwokacji produkują bit-identyczne wyniki. Obie ścieżki entry (`python sph_sim.py` i `python -m sphsim`) produkują JSON identyczny z fixture #1. CLI-04 spełnione.</done>
  <acceptance_criteria>
    - `sph_sim.py` zawiera ≤ 20 linii (kod, docstring, shebang razem)
    - `sph_sim.py` zawiera `from sphsim.cli.main import main`
    - `sph_sim.py` NIE definiuje już żadnych funkcji / klas / DEFAULT_* / STRATEGIES
    - `from sphsim import SPHSimulator, Device, STRATEGIES` działa (D-16 compliance)
    - `python -m sphsim --strategy naive --seed 42 --json` zwraca exit 0 z poprawnym JSON (D-06 compliance)
    - **`python -m sphsim --strategy naive --zeta 0.5 --seed 42 --json` produces output dict-equal z fixture #1** (cross-check przeniesiony z Planu 04 Task 2 — działa dopiero PO wpięciu __main__.py)
    - `python scripts/regression_check.py` zwraca exit code 0 — **moment prawdy** wszystkie 8 inwokacji identyczne
    - `python sph_sim.py --strategy naive --zeta 0.5 --seed 42 --json` produces output dict-equal z fixture #1
    - sphsim/__main__.py NIE zawiera `NotImplementedError` ani `TODO` (placeholder z Planu 02 zastąpiony)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sph_sim.py shim → sphsim.cli.main | jedyne miejsce shim — dispatch jeden-do-jednego |
| sphsim/__init__.py → publiczne API | exports SPHSimulator/Device/STRATEGIES — kontrakt dla Phase 3+ |
| SPHSimulator.__init__ → random.* | krytyczny invariant — kolejność wywołań determinuje wszystkie fixtures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Tampering | random.* call ordering in SPHSimulator.__init__ | mitigate | Task 1 verify cross-checks pakiet-side run vs fixture #1 PRZED cutover — failure tutaj zatrzymuje plan przed dotknięciem sph_sim.py |
| T-01-10 | Tampering | sph_sim.py shim contents | mitigate | Task 3 verify wymusza ≤ 20 LOC + brak definicji funkcji/klas + dispatch do sphsim.cli.main |
| T-01-11 | Spoofing | publiczne API (D-16) | accept | from sphsim import X jest kontraktem Phase 3 (custom loader) — verify command wymusza obecność wszystkich 3 nazw |
| T-01-12 | Denial of Service | regression_check.py timeout | accept | 8 inwokacji × ~5s = ~40s — w pełni akceptowalne dla developer feedback loop |
| T-01-SC | Tampering | Package installs | n/a | Phase 1 stdlib only, no installs |
</threat_model>

<verification>
- sph_sim.py ≤ 20 linii thin shim
- sphsim/__init__.py eksportuje SPHSimulator, Device, STRATEGIES
- sphsim/__main__.py wpięty (NIE NotImplementedError)
- python sph_sim.py i python -m sphsim oba działają i produkują identyczne wyniki
- python scripts/regression_check.py exit 0 — CUTOVER SUCCESS
- Wszystkie 8 fixtures match
</verification>

<success_criteria>
- ROADMAP Success Criterion #1: 5 baseline'owych inwokacji zwracają identyczne wyniki numeryczne ✓ (zweryfikowane przez regression_check.py)
- ROADMAP Success Criterion #3: `python sph_sim.py ...` pozostaje uruchamialny jako entry point ✓
- ROADMAP Success Criterion #4: Output `--json` zawiera dokładnie te same klucze i wartości co v1.0 ✓ (8/8 fixtures match)
- D-05 thin shim ✓
- D-06 `python -m sphsim` ✓
- D-16 publiczne API ✓
</success_criteria>

<output>
Create `.planning/phases/01-refactoring-foundation/01-04-SUMMARY.md` when done.
</output>
