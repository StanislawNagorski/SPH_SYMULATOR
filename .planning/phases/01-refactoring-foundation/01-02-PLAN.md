---
phase: 01-refactoring-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - sphsim/__init__.py
  - sphsim/__main__.py
  - sphsim/config.py
  - sphsim/core/__init__.py
  - sphsim/core/model.py
  - sphsim/core/device.py
autonomous: true
requirements:
  - CLI-04
requirements_addressed:
  - CLI-04

must_haves:
  truths:
    - "Pakiet `sphsim/` (nazwa krótka, bez kolizji z `sph_sim.py` w root) istnieje z poprawną strukturą katalogów: sphsim/, sphsim/core/ — bez pustych stubów dla agent/report/batch (YAGNI) (D-01, D-02)"
    - "sphsim/config.py centralizuje wszystkie 10 stałych DEFAULT_* (NU, NSUS, K0, K1, F, T, KAPPA, ALPHA, PHI, RHO) zgodne 1:1 z sph_sim.py:39-48 (D-04)"
    - "sphsim/core/model.py zawiera funkcje valuation() i sph_stp() — pure functions, bez side effects (D-12: model.py jest pierwszym z trzech plików core/)"
    - "sphsim/core/device.py zawiera Device dataclass z polami + property net_profit + metody record_*"
    - "Monolit sph_sim.py wciąż działa bez zmian — regression_check.py nadal zwraca 0"
  artifacts:
    - path: "sphsim/__init__.py"
      provides: "package marker (puste lub minimalna treść — pełne re-exporty w Planie 04)"
      contains: "sphsim"
    - path: "sphsim/__main__.py"
      provides: "stub dla python -m sphsim (treść w Planie 04, w tym planie tylko placeholder który drukuje TODO)"
      contains: "main"
    - path: "sphsim/config.py"
      provides: "DEFAULT_NU, DEFAULT_NSUS, DEFAULT_K0, DEFAULT_K1, DEFAULT_F, DEFAULT_T, DEFAULT_KAPPA, DEFAULT_ALPHA, DEFAULT_PHI, DEFAULT_RHO"
      contains: "DEFAULT_NU"
    - path: "sphsim/core/model.py"
      provides: "valuation(u, K0, K1) i sph_stp(u, s, nSUS, K0, K1)"
      contains: "def valuation"
    - path: "sphsim/core/device.py"
      provides: "Device dataclass + record_commit/record_delivery/record_failure + net_profit property"
      contains: "class Device"
  key_links:
    - from: "sphsim/core/model.py"
      to: "stdlib only"
      via: "no imports beyond stdlib"
      pattern: "import"
    - from: "sphsim/core/device.py"
      to: "dataclasses.dataclass"
      via: "stdlib import"
      pattern: "from dataclasses"
---

<objective>
Założyć szkielet pakietu `sphsim/` i wyciągnąć z monolitu `sph_sim.py` 3 zestawy odpowiedzialności wolne od side-effectów: stałe domyślne (`config.py`), pure model functions (`core/model.py`), dataclass urządzenia (`core/device.py`).

Purpose: To są najłatwiejsze do wyciągnięcia kawałki — nie zależą od stanu globalnego, nie wywołują `random.*`, nie mają obowiązku zachować kolejność losowań. Ten plan zamyka wave 2 (po Plan 01 baseline fixtures); Plan 03 (strategies) jest w wave 3 ponieważ `sphsim/strategies/incentive.py` importuje `DEFAULT_K0` z `sphsim/config.py` tworzonego tutaj. Monolit nadal działa bez zmian, bo nie importuje z `sphsim/` w tym kroku — refactor jest non-destructive.
Output: 6 nowych plików w `sphsim/`, regression_check.py nadal exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-refactoring-foundation/01-CONTEXT.md
@sph_sim.py

<interfaces>
<!-- Source-of-truth implementations being extracted, copied verbatim from sph_sim.py. -->
<!-- Each module preserves identifier names and signatures exactly — no renames, no API changes. -->

From sph_sim.py:39–48 (constants → sphsim/config.py):
```python
DEFAULT_NU    = 250
DEFAULT_NSUS  = 20
DEFAULT_K0    = 100
DEFAULT_K1    = 120
DEFAULT_F     = 5
DEFAULT_T     = 1000
DEFAULT_KAPPA = 0.25
DEFAULT_ALPHA = 1
DEFAULT_PHI   = [0.1, 0.2, 0.3, 0.4, 1.0]
DEFAULT_RHO   = [0.5, 0.5, 0.7, 1.5, 3.0]
```

From sph_sim.py:53–79 (model → sphsim/core/model.py):
```python
def valuation(u, K0, K1) -> float
def sph_stp(u, s, nSUS, K0, K1) -> Tuple[int, int]   # zwraca (z*, y*)
```

From sph_sim.py:84–118 (Device → sphsim/core/device.py):
```python
@dataclass
class Device:
    id: int
    phase: int
    status: str
    down_left: int = 0
    earnings: float = 0.0
    costs: float = 0.0
    n_commit: int = 0
    n_abstain: int = 0
    n_delivered: int = 0
    n_failed: int = 0
    # __post_init__ adds self.phase_stats = {}
    # methods: record_commit(phase, cost), record_delivery(phase, payment), record_failure(phase, repair_cost)
    # property: net_profit (earnings - costs)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Stworzyć szkielet sphsim/ + config.py z DEFAULT_* stałymi</name>
  <files>sphsim/__init__.py, sphsim/__main__.py, sphsim/config.py, sphsim/core/__init__.py</files>
  <read_first>sph_sim.py (linie 29–48 — imports + DEFAULT_* stałe); .planning/phases/01-refactoring-foundation/01-CONTEXT.md (sekcja D-01, D-02, D-04)</read_first>
  <action>
    Utworzyć katalog `sphsim/` w project root z 4 plikami:

    1. `sphsim/__init__.py` — pusty plik (placeholder; pełne re-exporty per D-16 zostaną dodane w Planie 04 po stworzeniu simulator.py). Krótki polski komentarz nagłówkowy: `"""Pakiet SPH Symulator — refactor v1.0 monolitu sph_sim.py."""`.

    2. `sphsim/__main__.py` — placeholder z polskim komentarzem `# TODO Plan 04: from sphsim.cli.main import main; main()` i tymczasowym `raise NotImplementedError("Entry point będzie wpięty w Planie 04")`. Plan 04 nadpisze go pełną implementacją (D-06).

    3. `sphsim/config.py` — zawiera DOKŁADNIE 10 stałych z sph_sim.py:39–48 (verbatim, te same wartości i wartości literalne: integers vs floats — zachować typy z oryginału: `DEFAULT_NU = 250` integer, `DEFAULT_KAPPA = 0.25` float, `DEFAULT_PHI = [0.1, 0.2, 0.3, 0.4, 1.0]` lista floatów). Polski komentarz nagłówkowy zgodny z v1.0: `# Parametry domyślne (z dokumentu PROMPT_DLA_AGENTA.txt)`. Per D-04 ten plik jest pojedynczym punktem nadpisywania dla Phase 5 (configurable environment).

    4. `sphsim/core/__init__.py` — pusty plik (package marker). Plan 04 doda re-exporty SPHSimulator i Device.

    Monolit `sph_sim.py` NIE jest modyfikowany — pakiet `sphsim/` istnieje obok niego i nie jest jeszcze nigdzie importowany. Per D-02 nie tworzymy `sphsim/strategies/`, `sphsim/cli/`, `sphsim/agent/`, `sphsim/report/`, `sphsim/batch/` w tym tasku (strategies tworzy Plan 03 w wave 3; cli tworzy Plan 04; agent/report/batch — fazy 4/6/7).

    Po napisaniu plików potwierdzić że `python -c "import sphsim, sphsim.core, sphsim.config; print(sphsim.config.DEFAULT_NU)"` drukuje `250`.
  </action>
  <verify>
    <automated>
test -f sphsim/__init__.py \
  && test -f sphsim/__main__.py \
  && test -f sphsim/config.py \
  && test -f sphsim/core/__init__.py \
  && python -c "import sphsim, sphsim.core, sphsim.config as c; assert c.DEFAULT_NU==250 and c.DEFAULT_NSUS==20 and c.DEFAULT_K0==100 and c.DEFAULT_K1==120 and c.DEFAULT_F==5 and c.DEFAULT_T==1000 and c.DEFAULT_KAPPA==0.25 and c.DEFAULT_ALPHA==1 and c.DEFAULT_PHI==[0.1,0.2,0.3,0.4,1.0] and c.DEFAULT_RHO==[0.5,0.5,0.7,1.5,3.0]; print('OK config.py — wszystkie 10 stałych identyczne z v1.0')" \
  && python scripts/regression_check.py \
  && echo "PASS: szkielet sphsim/ + config.py OK; monolit nietknięty (regression 0)"
    </automated>
  </verify>
  <done>4 pliki istnieją, `import sphsim.config` działa, wszystkie 10 DEFAULT_* mają wartości 1:1 z sph_sim.py:39–48, regression_check.py nadal exit 0.</done>
  <acceptance_criteria>
    - Pliki `sphsim/__init__.py`, `sphsim/__main__.py`, `sphsim/config.py`, `sphsim/core/__init__.py` istnieją
    - `python -c "import sphsim.config as c; print(c.DEFAULT_NU)"` drukuje `250`
    - Wszystkie 10 stałych mają identyczne typy i wartości jak w sph_sim.py:39–48 (NU=250 int, KAPPA=0.25 float, PHI=[0.1,0.2,0.3,0.4,1.0] lista floatów itd.)
    - `python scripts/regression_check.py` nadal zwraca exit code 0
    - Plik `sph_sim.py` nie został zmodyfikowany (git diff sph_sim.py pusty)
    - W `sphsim/` nie istnieją katalogi `strategies/`, `cli/`, `agent/`, `report/`, `batch/` (per D-02 — YAGNI, tworzone w innych planach/fazach)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wyciągnąć model functions (valuation, sph_stp) i Device dataclass do sphsim/core/</name>
  <files>sphsim/core/model.py, sphsim/core/device.py</files>
  <read_first>sph_sim.py (linie 53–79 dla model functions + linie 84–118 dla Device dataclass + record_* metod + net_profit property); .planning/phases/01-refactoring-foundation/01-CONTEXT.md (sekcja D-12)</read_first>
  <behavior>
    - `valuation(50, 100, 120)` → `0.0` (poza zakresem [K0, K1])
    - `valuation(110, 100, 120)` → `100.0` (w zakresie, zwraca K0)
    - `valuation(150, 100, 120)` → `0.0` (powyżej K1)
    - `valuation(110, 100, float('inf'))` → `100.0` (K1=inf, u >= K0)
    - `sph_stp(10, 5, 20, 100, 120)` → krotka `(z, y)` z `z >= 0` i `y >= 0` i `(z == 0 or y == 0)` (jeden zawsze zerowy)
    - `Device(id=1, phase=2, status='UP')` ma `down_left==0`, `earnings==0.0`, `net_profit==0.0`, `phase_stats=={}`
    - `Device.record_commit(phase=2, cost=0.25)` wstawia klucz 2 do `phase_stats` z `commits=1, costs=0.25`
    - `Device.record_delivery(phase=2, payment=10.0)` ustawia `phase_stats[2]['deliveries']=1, earnings=10.0`
    - `Device(...).earnings=10; .costs=3; .net_profit` → `7.0` (property = earnings - costs)
  </behavior>
  <action>
    Stworzyć `sphsim/core/model.py`:
    1. Importy: tylko `from typing import Tuple` (sygnatura zwracana `Tuple[int, int]` dla sph_stp). UWAGA: NIE importować `math` (sph_sim.py importuje math:32 ale używa tylko w komentarzu — sprawdzić; jeśli nieużywane w lines 53–79, nie kopiować importu).
    2. Skopiować VERBATIM `def valuation(u, K0, K1)` z sph_sim.py:53–56 — zachować identyczne ciało, sygnaturę, type handling `float('inf')`.
    3. Skopiować VERBATIM `def sph_stp(u, s, nSUS, K0, K1)` z sph_sim.py:58–79 — wraz z polskim docstring `"""Zwraca (z*, y*) max-izujące P = g(u-x)+x, x=z-y."""` i wewnętrznym `def P_of_x(x)`. NIE zmieniać kolejności candidates list ani algorytmu (kolejność może wpłynąć na tie-breaking w `if p > best_P`).
    4. Polski komentarz nagłówkowy: `# Funkcje modelu — pure (bez side effects, bez random.*).`

    Stworzyć `sphsim/core/device.py`:
    1. Importy: `from dataclasses import dataclass`.
    2. Skopiować VERBATIM klasę `Device` z sph_sim.py:84–118 — wszystkie 10 pól z defaultami, `__post_init__` inicjalizujący `phase_stats = {}`, 3 metody record_* (`record_commit`, `record_delivery`, `record_failure`) z identycznym `setdefault` schema, property `net_profit`. NIE zmieniać sygnatur metod ani struktury słownika `phase_stats` (klucze: commits, deliveries, failures, earnings, costs).
    3. Polski komentarz nagłówkowy: `# Device — autonomiczne urządzenie z mutowalnym stanem (fazy 1..F-1).`

    Napisać testy inline w jednym pliku skryptu pomocniczym do verify (nie commituj do tests/ — to one-shot smoke test): `python -c "<asserts>"`. Asserts pokrywają każdą linię z `<behavior>` block.

    Monolit `sph_sim.py` NIE jest modyfikowany.
  </action>
  <verify>
    <automated>
test -f sphsim/core/model.py \
  && test -f sphsim/core/device.py \
  && python -c "
from sphsim.core.model import valuation, sph_stp
from sphsim.core.device import Device

# valuation behavior
assert valuation(50, 100, 120) == 0.0, valuation(50,100,120)
assert valuation(110, 100, 120) == 100.0, valuation(110,100,120)
assert valuation(150, 100, 120) == 0.0, valuation(150,100,120)
assert valuation(110, 100, float('inf')) == 100.0
assert valuation(50, 100, float('inf')) == 0.0

# sph_stp behavior (signature + invariants)
z, y = sph_stp(10, 5, 20, 100, 120)
assert z >= 0 and y >= 0 and (z == 0 or y == 0), (z, y)
z2, y2 = sph_stp(0, 0, 20, 100, 120)
assert z2 == 0 and y2 == 0, (z2, y2)

# Device behavior
d = Device(id=1, phase=2, status='UP')
assert d.down_left == 0 and d.earnings == 0.0 and d.net_profit == 0.0 and d.phase_stats == {}
d.record_commit(phase=2, cost=0.25)
assert d.phase_stats[2]['commits'] == 1 and d.phase_stats[2]['costs'] == 0.25
d.record_delivery(phase=2, payment=10.0)
assert d.phase_stats[2]['deliveries'] == 1 and d.phase_stats[2]['earnings'] == 10.0
d.earnings = 10.0; d.costs = 3.0
assert d.net_profit == 7.0
print('OK model + device extracted correctly')
" \
  && python scripts/regression_check.py \
  && echo "PASS: model.py + device.py extracted; behavior tests pass; monolit regression 0"
    </automated>
  </verify>
  <done>sphsim/core/model.py i sphsim/core/device.py istnieją, behavior tests przechodzą, regression_check.py zwraca 0.</done>
  <acceptance_criteria>
    - `sphsim/core/model.py` zawiera `def valuation(` i `def sph_stp(` (identyczne sygnatury z sph_sim.py)
    - `sphsim/core/device.py` zawiera `class Device` z 10 polami i 3 metodami record_*
    - Wszystkie asserty behavior pass (inline w verify command)
    - `python scripts/regression_check.py` nadal zwraca exit code 0
    - Plik `sph_sim.py` nie został zmodyfikowany
    - `sphsim/core/model.py` nie importuje niczego spoza stdlib (sprawdź `grep -E "^import|^from" sphsim/core/model.py` → tylko `typing`)
    - `sphsim/core/device.py` nie importuje niczego spoza stdlib (tylko `dataclasses`)
    - `wc -l sphsim/core/model.py` raportuje ≤ 80 linii (pod limit ≤150)
    - `wc -l sphsim/core/device.py` raportuje ≤ 80 linii (pod limit ≤150)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sphsim/* → stdlib | Pure modules, no external dependencies |
| sph_sim.py → sphsim/ | NONE in this plan — package exists parallel to monolith, not yet imported |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | sphsim/config.py constants | mitigate | Verify command asserts every constant matches v1.0 byte-for-byte; behavior tests check types (int vs float distinction) |
| T-01-05 | Tampering | sphsim/core/model.py algorithm | mitigate | sph_stp candidate list order preserved verbatim — tie-breaking in `p > best_P` deterministic; regression_check still passes |
| T-01-SC | Tampering | Package installs | n/a | Phase 1 stdlib only, no installs |
</threat_model>

<verification>
- Wszystkie 6 plików istnieje
- `python -c "import sphsim, sphsim.config, sphsim.core, sphsim.core.model, sphsim.core.device"` działa
- Behavior asserts w verify pass
- `python scripts/regression_check.py` exit 0
- sph_sim.py nietknięty
</verification>

<success_criteria>
- Pakiet `sphsim/` ma minimalny szkielet (D-02 compliance: tylko `core/`, `config.py`, top-level files; brak stubów dla strategies/cli/agent/report/batch)
- `sphsim/config.py` zawiera wszystkie 10 DEFAULT_* zgodnie z sph_sim.py:39–48
- `sphsim/core/model.py` i `sphsim/core/device.py` zachowują VERBATIM implementacje z monolitu
- Każdy moduł ≤ 150 LOC (Success Criterion #2 ROADMAP)
- Regression check nadal exit 0 (monolit działa, fixtures intact)
</success_criteria>

<output>
Create `.planning/phases/01-refactoring-foundation/01-02-SUMMARY.md` when done.
</output>
