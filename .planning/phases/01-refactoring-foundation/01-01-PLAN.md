---
phase: 01-refactoring-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_baseline.py
  - scripts/regression_check.py
  - tests/fixtures/baseline_v1/01-naive-zeta-0.5.json
  - tests/fixtures/baseline_v1/02-threshold-max-phase-3.json
  - tests/fixtures/baseline_v1/03-phase-prob-default.json
  - tests/fixtures/baseline_v1/04-incentive-expected-P-100.json
  - tests/fixtures/baseline_v1/05-adaptive-s-target-10.json
  - tests/fixtures/baseline_v1/06-naive-zeta-0.4-custom-env.json
  - tests/fixtures/baseline_v1/07-phase-prob-custom-kappa-alpha.json
  - tests/fixtures/baseline_v1/08-naive-zeta-0.75-baseline.json
  - tests/fixtures/baseline_v1/MANIFEST.txt
autonomous: true
requirements:
  - CLI-04
requirements_addressed:
  - CLI-04

must_haves:
  truths:
    - "8 JSON fixtures istnieją w tests/fixtures/baseline_v1/, każdy reprezentuje --json output v1.0 dla --seed 42 (D-10)"
    - "scripts/generate_baseline.py uruchamia dokładnie 8 inwokacji (D-09) na obecnym sph_sim.py PRZED jakimkolwiek refactorem i zapisuje wyniki (D-11)"
    - "scripts/regression_check.py (plain stdlib, bez pytest) re-uruchamia te same 8 inwokacji i diff'uje wynik względem fixtures; exit code != 0 przy jakiejkolwiek różnicy (D-08)"
    - "regression_check.py uruchomiony PRZED refactorem zwraca exit code 0 (sanity check że runner i fixtures są spójne)"
  artifacts:
    - path: "scripts/generate_baseline.py"
      provides: "generator JSON fixtures z monolitu v1.0"
      contains: "INVOCATIONS"
    - path: "scripts/regression_check.py"
      provides: "regression diff runner używany przez plany 02-05"
      contains: "INVOCATIONS"
    - path: "tests/fixtures/baseline_v1/08-naive-zeta-0.75-baseline.json"
      provides: "baseline benchmark (D-09 invocation #8); zawiera avg_val_last100 ≈ 92"
      contains: "avg_val_last100"
    - path: "tests/fixtures/baseline_v1/MANIFEST.txt"
      provides: "lista slug → pełna komenda dla 8 inwokacji"
      contains: "naive --zeta 0.75"
  key_links:
    - from: "scripts/generate_baseline.py"
      to: "sph_sim.py"
      via: "subprocess.run([sys.executable, 'sph_sim.py', ...])"
      pattern: "subprocess"
    - from: "scripts/regression_check.py"
      to: "tests/fixtures/baseline_v1/*.json"
      via: "json.load + dict diff"
      pattern: "fixtures"
---

<objective>
Wygenerować snapshot JSON fixtures z OBECNEGO monolitu `sph_sim.py` (v1.0, 433 linie) ZANIM rozpocznie się jakikolwiek refactor. Zbudować skrypt regresji który będzie używany jako oracle prawdy przez kolejne plany (02–05) po każdej zmianie w kodzie.

Purpose: D-11 jest non-negotiable — fixtures wygenerowane PO refactorze są bezwartościowe (validate'owałyby już zmieniony kod sam ze sobą). Bez tego planu reszta fazy nie ma punktu odniesienia dla CLI-04 (backwards compatibility hard requirement).
Output: 2 skrypty w `scripts/`, 8 plików fixture JSON w `tests/fixtures/baseline_v1/`, plus manifest. Plik `sph_sim.py` NIE jest modyfikowany przez ten plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-refactoring-foundation/01-CONTEXT.md
@sph_sim.py

<interfaces>
<!-- Snapshot of v1.0 JSON output structure — what fixtures must capture. -->
<!-- Source: sph_sim.py:375–383 (main() JSON branch). -->

The JSON output for `--json` flag is structured as:
```
{
  "strategy": "<name>",
  "strategy_params": { "zeta": 0.5, "max_phase": 3, "probs": "0.9,...", "s_target": 10, "expected_P": 100.0 },
  "env": { "nU": 250, "nSUS": 20, "K1": 120, "T": 1000, "kappa": 0.25, "alpha": 1 },
  "metrics": {
    "avg_val_last100": <float>,
    "cum_val_total": <float>,
    "avg_net_profit": <float>,
    "delivery_ratio": <float>,
    "avg_providers_l100": <float>,
    "sus_final": <int>,
    "ic_per_phase": { "<phase>": { "commits": int, "deliveries": int, "failures": int,
                                   "avg_earning_per_commit": float, "avg_cost_per_commit": float,
                                   "avg_net_per_commit": float, "delivery_rate": float,
                                   "ic_satisfied": bool } }
  }
}
```
Note: `history` and `devices` are filtered out of metrics — sph_sim.py:381.

The 8 invocations from D-09 (verbatim):
1. python sph_sim.py --strategy naive --zeta 0.5 --seed 42 --json
2. python sph_sim.py --strategy threshold --max_phase 3 --seed 42 --json
3. python sph_sim.py --strategy phase_prob --probs 0.9,0.7,0.5,0.3,0.0 --seed 42 --json
4. python sph_sim.py --strategy incentive --expected_P 100 --seed 42 --json
5. python sph_sim.py --strategy adaptive --s_target 10 --seed 42 --json
6. python sph_sim.py --strategy naive --zeta 0.4 --nU 200 --nSUS 20 --K1 120 --T 1000 --seed 42 --json
7. python sph_sim.py --strategy phase_prob --probs 1.0,0.8,0.6,0.2,0.0 --kappa 0.5 --alpha 0 --seed 42 --json
8. python sph_sim.py --strategy naive --zeta 0.75 --seed 42 --json
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stworzyć generate_baseline.py + uruchomić go na v1.0 monolicie i zapisać 8 fixtures</name>
  <files>scripts/generate_baseline.py, tests/fixtures/baseline_v1/01-naive-zeta-0.5.json, tests/fixtures/baseline_v1/02-threshold-max-phase-3.json, tests/fixtures/baseline_v1/03-phase-prob-default.json, tests/fixtures/baseline_v1/04-incentive-expected-P-100.json, tests/fixtures/baseline_v1/05-adaptive-s-target-10.json, tests/fixtures/baseline_v1/06-naive-zeta-0.4-custom-env.json, tests/fixtures/baseline_v1/07-phase-prob-custom-kappa-alpha.json, tests/fixtures/baseline_v1/08-naive-zeta-0.75-baseline.json, tests/fixtures/baseline_v1/MANIFEST.txt</files>
  <read_first>sph_sim.py (cały — szczególnie linie 375–383 dla JSON shape i linie 1–27 dla docstring example invocations); .planning/phases/01-refactoring-foundation/01-CONTEXT.md (sekcja decisions D-08, D-09, D-10, D-11)</read_first>
  <action>
    Stworzyć `scripts/generate_baseline.py` (Python 3.7+ stdlib only, polski komentarz nagłówkowy zgodny z v1.0 stylem). Skrypt definiuje stałą `INVOCATIONS` jako listę krotek `(slug, [args_list])` z DOKŁADNIE 8 pozycjami z CONTEXT.md D-09 (per slug naming konwencja: zero-padded sequence + dash-separated parametry — patrz lista plików w `<files>`). Skrypt dla każdej inwokacji:
    1. Wywołuje `subprocess.run([sys.executable, 'sph_sim.py', *args, '--seed', '42', '--json'], capture_output=True, text=True, check=True)` z `cwd` ustawionym na project root.
    2. Parsuje stdout jako JSON (`json.loads`).
    3. Zapisuje JSON do `tests/fixtures/baseline_v1/<slug>.json` pretty-printed `indent=2, sort_keys=True` (sort_keys żeby diff był deterministyczny niezależnie od porządku kluczy w dict).
    4. Drukuje na stderr postęp: `[1/8] naive-zeta-0.5 -> OK (avg_val_last100=...)`.

    UWAGA: invocations #1, 2, 4, 5 nie mają w D-09 jawnej flagi `--seed 42` ani `--json` w komendzie human-readable, ale `--seed 42 --json` MUSI być zawsze dodane (D-09 explicitly podaje to dla każdej z 8 inwokacji). Inwokacje #6, 7, 8 mają już dodatkowe flagi — zachować je 1:1.

    Po napisaniu skryptu uruchomić go: `python scripts/generate_baseline.py`. Sprawdzić że powstały wszystkie 8 plików JSON. Następnie stworzyć `tests/fixtures/baseline_v1/MANIFEST.txt` z 8 linijkami w formacie `<slug>.json | <pełna komenda>` — dla peer review widoczność.

    Skrypt MUSI być deterministyczny: każdy ponowny run produkuje bit-identyczne pliki. Slug konwencja: zero-padded index + slug-case (zgodnie z listą w `<files>`).

    Plik `sph_sim.py` w katalogu głównym NIE jest modyfikowany w tym tasku. Plik `scripts/__init__.py` NIE jest tworzony (scripts/ to nie pakiet).
  </action>
  <verify>
    <automated>
ls scripts/generate_baseline.py tests/fixtures/baseline_v1/*.json tests/fixtures/baseline_v1/MANIFEST.txt 2>&1 | grep -v '^#' | wc -l | xargs -I{} test {} -eq 10 \
  && test $(ls tests/fixtures/baseline_v1/*.json 2>/dev/null | wc -l | tr -d ' ') -eq 8 \
  && python -c "import json; d=json.load(open('tests/fixtures/baseline_v1/08-naive-zeta-0.75-baseline.json')); assert d['strategy']=='naive', d; assert 'avg_val_last100' in d['metrics'], d; assert d['strategy_params']['zeta']==0.75, d; print('OK baseline fixture #8 well-formed')" \
  && python scripts/generate_baseline.py > /tmp/gen_run2.log 2>&1 \
  && diff <(python -c "import json; print(json.dumps(json.load(open('tests/fixtures/baseline_v1/08-naive-zeta-0.75-baseline.json')), indent=2, sort_keys=True))") <(python -c "import json; print(json.dumps(json.load(open('tests/fixtures/baseline_v1/08-naive-zeta-0.75-baseline.json')), indent=2, sort_keys=True))") \
  && echo "PASS: 10 artifacts present, baseline fixture well-formed, regen is deterministic"
    </automated>
  </verify>
  <done>8 plików JSON fixture istnieje w tests/fixtures/baseline_v1/, każdy zgodny ze shape z `<interfaces>` block, MANIFEST.txt zawiera 8 wierszy. Plik scripts/generate_baseline.py uruchomiony 2× produkuje bit-identyczne pliki. sph_sim.py nietknięty.</done>
  <acceptance_criteria>
    - Plik `scripts/generate_baseline.py` istnieje i jest uruchamialny przez `python scripts/generate_baseline.py`
    - Dokładnie 8 plików `.json` istnieje w `tests/fixtures/baseline_v1/` (nie 7, nie 9)
    - Każdy fixture zawiera klucze `strategy`, `strategy_params`, `env`, `metrics` (top-level)
    - Każdy fixture zawiera w `metrics`: `avg_val_last100`, `cum_val_total`, `avg_net_profit`, `delivery_ratio`, `avg_providers_l100`, `sus_final`, `ic_per_phase`
    - Fixture `08-naive-zeta-0.75-baseline.json` ma `strategy_params.zeta == 0.75`
    - `tests/fixtures/baseline_v1/MANIFEST.txt` istnieje i zawiera 8 linii (jedna per fixture)
    - `python scripts/generate_baseline.py` uruchomiony dwa razy daje bit-identyczne fixtures (regen idempotency)
    - Plik `sph_sim.py` nie został zmodyfikowany (git diff sph_sim.py jest pusty)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Stworzyć regression_check.py i potwierdzić exit code 0 na obecnym monolicie</name>
  <files>scripts/regression_check.py</files>
  <read_first>scripts/generate_baseline.py (struktura INVOCATIONS, słownik slugów); tests/fixtures/baseline_v1/MANIFEST.txt; sph_sim.py (linie 375–383 dla JSON shape żeby wiedzieć co diff'ować)</read_first>
  <action>
    Stworzyć `scripts/regression_check.py` (Python 3.7+ stdlib only). Skrypt:
    1. Importuje lub re-definiuje tę samą listę `INVOCATIONS` co `generate_baseline.py` (preferuj DRY: `from generate_baseline import INVOCATIONS` z dodanym `sys.path.insert(0, str(Path(__file__).parent))`).
    2. Dla każdej inwokacji:
       a. Uruchamia `subprocess.run([sys.executable, 'sph_sim.py', *args, '--seed', '42', '--json'], capture_output=True, text=True, check=True)`.
       b. Parsuje stdout jako JSON (current_output).
       c. Wczytuje fixture `tests/fixtures/baseline_v1/<slug>.json` (expected_output).
       d. Porównuje przez głęboki dict equality. Jeśli różnica — drukuje czytelny diff na stderr (`pretty diff: pole X: expected=Y, got=Z`) i odnotowuje failure.
    3. Na końcu drukuje podsumowanie `PASS: 8/8` lub `FAIL: 5/8 (failed slugs: ...)`.
    4. Exit code: 0 jeśli wszystkie 8 pasują, 1 jeśli choć jedna różnica, 2 jeśli błąd uruchomienia (np. subprocess nie zwrócił JSON).

    Diff implementacja: napisać prostą rekurencyjną funkcję `deep_diff(a, b, path='')` która zwraca listę stringów typu `metrics.avg_val_last100: expected=92.0, got=91.84`. Dla float'ów porównuj exact equality (nie tolerance) — fixtures są generowane przez ten sam interpreter z tego samego seeda, więc bit-identyczne wyniki są wymagane (CLI-04 hard requirement, D-11 ordering invariant uzasadnia ten poziom rygoru).

    Polski komentarz nagłówkowy: "Skrypt regresji: re-uruchamia 8 inwokacji v1.0 i porównuje z committed fixtures". Argument `--verbose` (opcjonalny) drukuje per-invocation status zamiast tylko summary.

    Po napisaniu skryptu URUCHOMIĆ go na obecnym monolicie: `python scripts/regression_check.py`. MUSI zwrócić exit code 0 (fixtures właśnie wygenerowane z tego samego kodu — sanity check że runner i fixtures są spójne). Jeśli zwraca != 0 — bug w skrypcie lub w `INVOCATIONS` mismatch, fix przed merge.

    Plik `sph_sim.py` NIE jest modyfikowany.
  </action>
  <verify>
    <automated>
test -f scripts/regression_check.py \
  && python scripts/regression_check.py \
  && echo "PASS: regression_check.py exists and returns 0 on pre-refactor monolith"
    </automated>
  </verify>
  <done>scripts/regression_check.py istnieje. Uruchomiony na obecnym monolicie sph_sim.py zwraca exit code 0 (potwierdza że fixtures i runner są spójne — Plan 02+ mają wiarygodny oracle).</done>
  <acceptance_criteria>
    - Plik `scripts/regression_check.py` istnieje
    - `python scripts/regression_check.py` zwraca exit code 0 na obecnym (pre-refactor) monolicie
    - `python scripts/regression_check.py --verbose` drukuje 8 wierszy per-invocation status (każdy oznaczony OK/FAIL)
    - Skrypt importuje INVOCATIONS z generate_baseline.py (DRY — jedna lista, jedno źródło prawdy)
    - Plik `sph_sim.py` nie został zmodyfikowany (git diff sph_sim.py pusty)
    - Skrypt używa exact equality dla porównań (nie math.isclose / tolerance)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS shell → sph_sim.py | subprocess invocation, fixtures will encode whatever monolith outputs |
| disk → fixtures | fixtures are committed authoritative oracle; tampering breaks regression guarantee |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | tests/fixtures/baseline_v1/*.json | mitigate | Fixtures committed atomically w jednym commicie z generatorem; każda przyszła zmiana fixture widoczna w git diff przy peer review |
| T-01-02 | Repudiation | scripts/generate_baseline.py determinism | mitigate | Verify task wymusza 2× run + diff = pusty; sort_keys=True w json.dumps gwarantuje deterministic key order |
| T-01-03 | Information Disclosure | subprocess capture_output | accept | Output to projekt akademicki single-author, brak sekretów w stdout |
| T-01-SC | Tampering | Package installs (npm/pip/cargo) | n/a | Phase 1 stdlib only — żadnych instalacji pakietów (D-07, PROJECT.md constraint) |
</threat_model>

<verification>
Po wszystkich taskach: `python scripts/regression_check.py` zwraca 0. Plik `sph_sim.py` nietknięty (git diff czysty). Drzewo `tests/fixtures/baseline_v1/` zawiera 8 plików JSON + MANIFEST.txt.
</verification>

<success_criteria>
- 8 plików JSON fixture istnieje, każdy reprezentuje --json output v1.0 monolitu dla --seed 42
- `scripts/generate_baseline.py` jest deterministyczny (2× run → bit-identyczne pliki)
- `scripts/regression_check.py` zwraca exit code 0 na obecnym monolicie
- `sph_sim.py` nie został zmodyfikowany przez ten plan
- Wszystkie artefakty committable jako atomic snapshot pre-refactor
</success_criteria>

<output>
Create `.planning/phases/01-refactoring-foundation/01-01-SUMMARY.md` when done.
</output>
