---
phase: 01-refactoring-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - sphsim/strategies/__init__.py
  - sphsim/strategies/naive.py
  - sphsim/strategies/threshold.py
  - sphsim/strategies/phase_prob.py
  - sphsim/strategies/incentive.py
  - sphsim/strategies/adaptive.py
autonomous: true
requirements:
  - CLI-04
requirements_addressed:
  - CLI-04

must_haves:
  truths:
    - "Każda z 5 strategii v1.0 istnieje w osobnym pliku w sphsim/strategies/ (naive.py, threshold.py, phase_prob.py, incentive.py, adaptive.py) z identyczną sygnaturą plain function `def strategy_X(dev, l, s, phi, kappa, rho, h, p) -> str` — bez Protocol/ABC/dziedziczenia (D-03, D-13)"
    - "sphsim/strategies/__init__.py eksportuje STRATEGIES dict jako mutable global z 5 wpisami (naive, threshold, phase_prob, incentive, adaptive) — Phase 3 custom loader dorzuca klucze runtime'owo (D-14)"
    - "Monolit sph_sim.py wciąż używa własnych funkcji strategy_* — pakiet sphsim/strategies/ istnieje równolegle, nie jest jeszcze importowany przez monolit"
    - "regression_check.py nadal zwraca exit 0"
  artifacts:
    - path: "sphsim/strategies/__init__.py"
      provides: "STRATEGIES registry dict + type alias StrategyFn"
      contains: "STRATEGIES"
    - path: "sphsim/strategies/naive.py"
      provides: "strategy_naive(dev, l, s, phi, kappa, rho, h, p)"
      contains: "def strategy_naive"
    - path: "sphsim/strategies/threshold.py"
      provides: "strategy_threshold(dev, l, s, phi, kappa, rho, h, p)"
      contains: "def strategy_threshold"
    - path: "sphsim/strategies/phase_prob.py"
      provides: "strategy_phase_prob(dev, l, s, phi, kappa, rho, h, p)"
      contains: "def strategy_phase_prob"
    - path: "sphsim/strategies/incentive.py"
      provides: "strategy_incentive(dev, l, s, phi, kappa, rho, h, p)"
      contains: "def strategy_incentive"
    - path: "sphsim/strategies/adaptive.py"
      provides: "strategy_adaptive(dev, l, s, phi, kappa, rho, h, p)"
      contains: "def strategy_adaptive"
  key_links:
    - from: "sphsim/strategies/__init__.py"
      to: "5 strategy modules"
      via: "from sphsim.strategies.X import strategy_X"
      pattern: "from sphsim.strategies"
    - from: "sphsim/strategies/incentive.py"
      to: "sphsim.config.DEFAULT_K0"
      via: "import dla default expected_P (wymaga Planu 02)"
      pattern: "DEFAULT_K0"
---

<objective>
Wyciągnąć 5 funkcji `strategy_*` z monolitu `sph_sim.py:104–157` do osobnych plików w `sphsim/strategies/`, plus zbudować `STRATEGIES` registry dict w `sphsim/strategies/__init__.py`.

Purpose: Per D-03 każda strategia w osobnym pliku ułatwia Phase 3 (custom strategy loader) — będzie wzorcem dla użytkowniczych plików `.py`. Per D-13 strategie są plain functions (bez Protocol/ABC) — zachowana sygnatura v1.0. STRATEGIES dict per D-14 jest mutable global żeby Phase 3 mógł dorzucać klucze runtime'owo. Plan zależy od Planu 02 (`sphsim/config.py` musi istnieć — `strategy_incentive` importuje `DEFAULT_K0` per sph_sim.py:150).
Output: 6 nowych plików w `sphsim/strategies/`, regression_check.py nadal exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-refactoring-foundation/01-CONTEXT.md
@sph_sim.py

<interfaces>
<!-- Strategy function signature contract (D-13): identical for all 5, preserved verbatim from v1.0. -->
<!-- This signature is the contract Phase 3 (custom loader) will validate against. -->

```python
def strategy_<name>(
    dev,        # Device instance
    l,          # List[int] — provider counts per phase from previous cycle
    s,          # int — current SUS buffer occupancy
    phi,        # List[float] — failure probabilities per phase
    kappa,      # float — delivery cost
    rho,        # List[float] — repair costs per phase
    h,          # Callable[[int], float] — phase weighting function
    p,          # dict — strategy-specific params (zeta, max_phase, probs, expected_P, s_target)
) -> str       # 'COMMIT' or 'ABSTAIN'
```

From sph_sim.py:123–168 (5 functions verbatim — copy line-by-line, no logic changes):
- strategy_naive (lines 123–126): random.random() < zeta
- strategy_threshold (lines 128–131): dev.phase <= max_phase
- strategy_phase_prob (lines 133–139): per-phase prob list
- strategy_incentive (lines 141–153): expected net profit computation; references DEFAULT_K0 for default expected_P
- strategy_adaptive (lines 155–168): SUS buffer-based probability

STRATEGIES registry shape (sph_sim.py:170–176):
```python
STRATEGIES = {
    'naive': strategy_naive,
    'threshold': strategy_threshold,
    'phase_prob': strategy_phase_prob,
    'incentive': strategy_incentive,
    'adaptive': strategy_adaptive,
}
```

Pre-requisite from Plan 02 (must exist before this plan starts):
- `sphsim/config.py` zawiera `DEFAULT_K0 = 100` (literal int)
- `sphsim/__init__.py` istnieje jako package marker (Plan 02 stworzył go jako placeholder)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Stworzyć 5 plików strategii (osobny plik per strategia) z verbatim implementacją</name>
  <files>sphsim/strategies/naive.py, sphsim/strategies/threshold.py, sphsim/strategies/phase_prob.py, sphsim/strategies/incentive.py, sphsim/strategies/adaptive.py</files>
  <read_first>sph_sim.py (linie 123–168 — pełne ciała wszystkich 5 funkcji strategii); sphsim/config.py (Plan 02, dla DEFAULT_K0); .planning/phases/01-refactoring-foundation/01-CONTEXT.md (sekcja D-03, D-13)</read_first>
  <action>
    Utworzyć katalog `sphsim/strategies/` i 5 plików, każdy zawiera DOKŁADNIE jedną funkcję skopiowaną VERBATIM z sph_sim.py (zachować polskie komentarze jeśli są w oryginale, identyczną sygnaturę z 8 argumentami):

    1. `sphsim/strategies/naive.py` — funkcja `strategy_naive` z sph_sim.py:123–126. Import: `import random`. Polski komentarz: `# Strategia naive: COMMIT z prawdopodobieństwem zeta.`

    2. `sphsim/strategies/threshold.py` — funkcja `strategy_threshold` z sph_sim.py:128–131. Import: brak (nie używa random ani innych). Polski komentarz: `# Strategia threshold: COMMIT tylko jeśli dev.phase <= max_phase.`

    3. `sphsim/strategies/phase_prob.py` — funkcja `strategy_phase_prob` z sph_sim.py:133–139. Import: `import random`. Polski komentarz: `# Strategia phase_prob: COMMIT z prawdopodobieństwem zależnym od fazy (lista probs).`

    4. `sphsim/strategies/incentive.py` — funkcja `strategy_incentive` z sph_sim.py:141–153. **KRYTYCZNE**: ta funkcja referuje `DEFAULT_K0` (sph_sim.py:150: `exp_P = float(p.get('expected_P', DEFAULT_K0))`). Import: `from sphsim.config import DEFAULT_K0`. Polski komentarz: `# Strategia incentive: COMMIT gdy E[zysk_netto] > 0 (zgodna motywacyjnie).`

    5. `sphsim/strategies/adaptive.py` — funkcja `strategy_adaptive` z sph_sim.py:155–168. Import: `import random`. Polski komentarz: `# Strategia adaptive: prawdopodobieństwo COMMIT zależne od poziomu bufora SUS.`

    DOKŁADNIE zachować:
    - Sygnatury (kolejność i nazwy argumentów: `dev, l, s, phi, kappa, rho, h, p`)
    - Wartości default w `p.get('zeta', 0.5)` itp. — identyczne stringi/floaty jak v1.0
    - Polskie komentarze jeśli są (np. linia 110 w v1.0)
    - Strukturę kontroli (`if dev.status != 'UP': return 'ABSTAIN'` jako pierwsza linia każdej)
    - Logikę bit-by-bit: w `strategy_incentive` kolejność operatorów i tie-breaking matter dla deterministyki

    NIE wprowadzać:
    - Type hints (v1.0 ich nie ma w sygnaturach strategii)
    - Klas/Protocol/ABC (D-13)
    - Walidacji argumentów (Phase 3 będzie walidować sygnaturę custom strategii, nie tych)
    - Wspólnego `_check_up()` helpera (DRY tutaj zaszkodziłoby — każda strategia to mały plik, czytelność wygrywa)

    Plan 02 stworzył `sphsim/config.py` z `DEFAULT_K0 = 100` — `from sphsim.config import DEFAULT_K0` w `incentive.py` MUSI działać od razu. Jeśli nie działa — Plan 02 nie został w pełni wykonany przed startem tego planu (depends_on naruszone), zatrzymaj się i wróć do Planu 02.

    Monolit `sph_sim.py` NIE jest modyfikowany.
  </action>
  <verify>
    <automated>
test -f sphsim/strategies/naive.py \
  && test -f sphsim/strategies/threshold.py \
  && test -f sphsim/strategies/phase_prob.py \
  && test -f sphsim/strategies/incentive.py \
  && test -f sphsim/strategies/adaptive.py \
  && python -c "from sphsim.config import DEFAULT_K0; assert DEFAULT_K0 == 100, DEFAULT_K0; print('OK Plan 02 dependency satisfied: DEFAULT_K0=100')" \
  && python -c "
import random
from sphsim.strategies.naive import strategy_naive
from sphsim.strategies.threshold import strategy_threshold
from sphsim.strategies.phase_prob import strategy_phase_prob
from sphsim.strategies.incentive import strategy_incentive
from sphsim.strategies.adaptive import strategy_adaptive
from sphsim.core.device import Device

# Sygnatura: każda przyjmuje 8 argumentów i zwraca string
import inspect
for fn in [strategy_naive, strategy_threshold, strategy_phase_prob, strategy_incentive, strategy_adaptive]:
    sig = inspect.signature(fn)
    params = list(sig.parameters.keys())
    assert params == ['dev','l','s','phi','kappa','rho','h','p'], (fn.__name__, params)

# Behavior smoke: DOWN device → ABSTAIN dla każdej
d_down = Device(id=1, phase=-1, status='DOWN')
for fn in [strategy_naive, strategy_threshold, strategy_phase_prob, strategy_incentive, strategy_adaptive]:
    assert fn(d_down, [0]*4, 5, [0.1,0.2,0.3,0.4,1.0], 0.25, [0.5,0.5,0.7,1.5,3.0], lambda i: i, {}) == 'ABSTAIN', fn.__name__

# threshold: dev.phase <= 3 → COMMIT
d_up = Device(id=2, phase=2, status='UP')
assert strategy_threshold(d_up, [0]*4, 5, [0.1,0.2,0.3,0.4,1.0], 0.25, [0.5,0.5,0.7,1.5,3.0], lambda i: i, {'max_phase': 3}) == 'COMMIT'

# naive: deterministic z seed
random.seed(42)
d_up = Device(id=3, phase=1, status='UP')
result1 = strategy_naive(d_up, [0]*4, 5, [0.1,0.2,0.3,0.4,1.0], 0.25, [0.5,0.5,0.7,1.5,3.0], lambda i: i, {'zeta': 0.5})
random.seed(42)
result2 = strategy_naive(d_up, [0]*4, 5, [0.1,0.2,0.3,0.4,1.0], 0.25, [0.5,0.5,0.7,1.5,3.0], lambda i: i, {'zeta': 0.5})
assert result1 == result2, (result1, result2)
print('OK 5 strategii — sygnatury i smoke behavior identyczne')
" \
  && python scripts/regression_check.py \
  && echo "PASS: 5 plików strategii utworzonych; behavior preserved; monolit regression 0"
    </automated>
  </verify>
  <done>5 plików strategii istnieje, każdy zawiera dokładnie jedną funkcję, sygnatury identyczne z v1.0, behavior smoke testy pass, regression_check.py exit 0.</done>
  <acceptance_criteria>
    - 5 plików istnieje w `sphsim/strategies/` (naive.py, threshold.py, phase_prob.py, incentive.py, adaptive.py)
    - Każdy plik definiuje DOKŁADNIE jedną funkcję `strategy_<name>` z sygnaturą `(dev, l, s, phi, kappa, rho, h, p)`
    - `sphsim/strategies/incentive.py` importuje `DEFAULT_K0` z `sphsim.config` (NIE z literala)
    - Smoke testy w verify command pass (sygnatury, DOWN→ABSTAIN, threshold logic, naive determinism)
    - `python scripts/regression_check.py` nadal zwraca exit code 0
    - Plik `sph_sim.py` nie został zmodyfikowany
    - Żaden z 5 plików nie definiuje klasy ani nie używa `Protocol`/ABC (D-13)
    - `wc -l sphsim/strategies/*.py` — żaden plik strategy nie > 30 linii (sanity: per-strategy file jest mały)
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Zbudować STRATEGIES registry w sphsim/strategies/__init__.py</name>
  <files>sphsim/strategies/__init__.py</files>
  <read_first>sph_sim.py (linie 170–176 — STRATEGIES dict v1.0); sphsim/strategies/*.py (5 plików utworzonych w Task 1); .planning/phases/01-refactoring-foundation/01-CONTEXT.md (sekcja D-14)</read_first>
  <action>
    Stworzyć `sphsim/strategies/__init__.py`:

    1. Polski komentarz nagłówkowy: `# Registry strategii — mutable global; Phase 3 (custom loader) dodaje klucze runtime'owo.`

    2. Importy:
       ```python
       from typing import Callable
       from sphsim.strategies.naive import strategy_naive
       from sphsim.strategies.threshold import strategy_threshold
       from sphsim.strategies.phase_prob import strategy_phase_prob
       from sphsim.strategies.incentive import strategy_incentive
       from sphsim.strategies.adaptive import strategy_adaptive
       ```

    3. Type alias (per D-13):
       ```python
       StrategyFn = Callable[..., str]
       ```

    4. STRATEGIES dict — VERBATIM kolejność z sph_sim.py:170–176 (TA SAMA kolejność kluczy: naive, threshold, phase_prob, incentive, adaptive — kolejność może być widoczna w `--help` output z argparse `choices=`):
       ```python
       STRATEGIES = {
           'naive': strategy_naive,
           'threshold': strategy_threshold,
           'phase_prob': strategy_phase_prob,
           'incentive': strategy_incentive,
           'adaptive': strategy_adaptive,
       }
       ```

    5. NIE eksportować `__all__` (v1.0 też nie ma). Lista 5 strategii w STRATEGIES jest jedynym kontraktem publicznym tego modułu.

    Per D-14 STRATEGIES jest mutable — Phase 3 doda klucze przez `STRATEGIES['custom_name'] = loaded_fn`. NIE używać `MappingProxyType` ani `frozen` dataclass.

    Monolit `sph_sim.py` NIE jest modyfikowany — wciąż używa swoich własnych funkcji strategy_* zdefiniowanych w monolicie. Plan 04 wprowadzi mapowanie monolit → pakiet (cutover).
  </action>
  <verify>
    <automated>
test -f sphsim/strategies/__init__.py \
  && python -c "
from sphsim.strategies import STRATEGIES, StrategyFn
assert isinstance(STRATEGIES, dict), type(STRATEGIES)
assert list(STRATEGIES.keys()) == ['naive', 'threshold', 'phase_prob', 'incentive', 'adaptive'], list(STRATEGIES.keys())
# Każdy wpis to callable z sygnaturą strategii
import inspect
for name, fn in STRATEGIES.items():
    assert callable(fn), name
    sig = inspect.signature(fn)
    assert list(sig.parameters.keys()) == ['dev','l','s','phi','kappa','rho','h','p'], (name, list(sig.parameters.keys()))
# Test mutability (D-14): można dodać klucz
STRATEGIES['test_custom'] = lambda dev,l,s,phi,kappa,rho,h,p: 'COMMIT'
assert 'test_custom' in STRATEGIES
del STRATEGIES['test_custom']
# Type alias istnieje
assert StrategyFn is not None
print('OK STRATEGIES registry: 5 wpisów, mutable, sygnatura zgodna z v1.0')
" \
  && python scripts/regression_check.py \
  && echo "PASS: STRATEGIES registry built; monolit nietknięty; regression 0"
    </automated>
  </verify>
  <done>sphsim/strategies/__init__.py istnieje, eksportuje STRATEGIES dict z dokładnie 5 wpisami w identycznej kolejności jak v1.0, STRATEGIES jest mutable (Phase 3 ready), regression_check.py exit 0.</done>
  <acceptance_criteria>
    - Plik `sphsim/strategies/__init__.py` istnieje
    - `from sphsim.strategies import STRATEGIES` działa
    - `list(STRATEGIES.keys()) == ['naive', 'threshold', 'phase_prob', 'incentive', 'adaptive']` (identyczna kolejność z sph_sim.py:170–176)
    - Każda wartość w dict jest callable z sygnaturą `(dev, l, s, phi, kappa, rho, h, p)`
    - STRATEGIES dict jest mutable (test: `STRATEGIES['x'] = fn` nie rzuca błędu) — Phase 3 prerequisite
    - Type alias `StrategyFn` jest exportowany (`from sphsim.strategies import StrategyFn` działa)
    - `python scripts/regression_check.py` zwraca exit code 0
    - Plik `sph_sim.py` nie został zmodyfikowany
    - `wc -l sphsim/strategies/__init__.py` raportuje ≤ 30 linii
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sphsim/strategies/* → random module | Strategies invoke random.random() — random state ownership stays with caller (SPHSimulator.__init__) |
| sphsim/strategies/__init__.py → sphsim.config | Cross-package import for DEFAULT_K0 (transitive via incentive.py) |
| Phase 3 future loader → STRATEGIES dict | Mutable registry — design intention per D-14 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | strategy function signatures | mitigate | Verify command introspects sygnaturę via inspect — wymusza identyczność z v1.0 (Phase 3 custom loader będzie polegał na tym kontrakcie) |
| T-01-07 | Tampering | random.random() ordering | accept | Strategie SAME wywołują random.random() — ale to monolit (lub w przyszłości SPHSimulator) ustawia seed; w tym planie monolit dalej używa swoich własnych strategii (nie pakietu), więc ordering nie zmieniony |
| T-01-08 | Elevation of Privilege | STRATEGIES mutability | accept | Mutable jest intencjonalne (D-14, Phase 3 custom loader); ryzyko że ktoś nadpisze 'naive' runtime'owo jest pomijalne dla projektu akademickiego |
| T-01-SC | Tampering | Package installs | n/a | Phase 1 stdlib only, no installs |
</threat_model>

<verification>
- 6 plików w sphsim/strategies/ istnieje
- `from sphsim.strategies import STRATEGIES` daje dict z 5 wpisami w v1.0 kolejności
- Każda strategia ma poprawną sygnaturę (8 args)
- `python scripts/regression_check.py` exit 0
- sph_sim.py nietknięty
</verification>

<success_criteria>
- 5 strategii w osobnych plikach (D-03 compliance)
- STRATEGIES registry mutable, gotowy na Phase 3 custom loader (D-14)
- Każdy plik strategii ≤ 30 linii (ROADMAP Success Criterion #2: każdy moduł ≤ 150 linii)
- Sygnatura `(dev, l, s, phi, kappa, rho, h, p)` zachowana — Phase 3 custom loader polega na tym kontrakcie
- Regression check nadal exit 0
</success_criteria>

<output>
Create `.planning/phases/01-refactoring-foundation/01-03-SUMMARY.md` when done.
</output>
