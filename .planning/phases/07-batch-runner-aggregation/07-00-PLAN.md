---
phase: 07-batch-runner-aggregation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_batch.py
  - tests/test_batch_stats.py
  - tests/test_batch_report.py
  - requirements.txt
autonomous: true
requirements: [BATCH-01, BATCH-02, BATCH-03, PLOT-04]
threat_refs: [T-7-00-01, T-7-00-SC]
must_haves:
  truths:
    - "tests/test_batch.py exists with TestSeedsParser, TestArgsMutex, TestReplBatch, TestDeterminism, TestCliReplParity as skip-stubs (imports clean)"
    - "tests/test_batch_stats.py exists with TestAggregateKpis, TestCIComputation, TestN1Degenerate, TestEmptyInput, TestStatsDeterminism as skip-stubs (imports clean)"
    - "tests/test_batch_report.py exists with TestBatchReport, TestBatchPlots as skip-stubs (imports clean)"
    - "requirements.txt exists pinning matplotlib + numpy + scipy + at-least version per RESEARCH §Environment Availability"
    - "Subsequent Waves 1-5 have concrete class names + filenames to GREEN against — zero scavenger-hunt"
    - "Phase 6 test suite (172 tests) remains green — zero collateral damage from skip-stubs"
  artifacts:
    - path: "tests/test_batch.py"
      provides: "Stub container for CLI/REPL/parser tests (TestSeedsParser, TestArgsMutex, TestReplBatch, TestDeterminism, TestCliReplParity)"
      contains: "class TestSeedsParser"
      min_lines: 80
    - path: "tests/test_batch_stats.py"
      provides: "Stub container for pure-unit stats tests (TestAggregateKpis, TestCIComputation, TestN1Degenerate, TestEmptyInput, TestStatsDeterminism)"
      contains: "class TestAggregateKpis"
      min_lines: 60
    - path: "tests/test_batch_report.py"
      provides: "Stub container for batch report + plot tests (TestBatchReport, TestBatchPlots)"
      contains: "class TestBatchReport"
      min_lines: 50
    - path: "requirements.txt"
      provides: "Pinned dependencies for matplotlib (Phase 6) + numpy + scipy (Phase 7 BATCH-02)"
      contains: "scipy"
  key_links:
    - from: "tests/test_batch.py"
      to: "tests/test_env.py:1-30 subprocess helper boilerplate"
      via: "verbatim copy of _PROJECT_ROOT + _run_sph helper"
      pattern: "_run_sph"
    - from: "tests/test_batch_stats.py"
      to: "tests/test_agent.py:1-37 pure-unit boilerplate"
      via: "verbatim copy of _PROJECT_ROOT setup + sys.path insert"
      pattern: "_PROJECT_ROOT"
    - from: "requirements.txt"
      to: "Phase 7 RESEARCH §Environment Availability (scipy 1.16.3, numpy 2.3.5, matplotlib 3.10.7)"
      via: "pip-installable manifest"
      pattern: "matplotlib"
---

<objective>
Scaffold Wave 0 test containers (`tests/test_batch.py`, `tests/test_batch_stats.py`, `tests/test_batch_report.py`) as skip-bodied stubs with locked class taxonomy, plus create `requirements.txt` pinning the matplotlib/numpy/scipy stack that Phase 7 formalizes. NO Phase 7 feature code lands in this plan — only stubs that import cleanly, skip with explicit Wave-N reasons, and the dependency manifest that documents what `pip install -r requirements.txt` provides.

Purpose: Per `07-VALIDATION.md` Wave 0 contract + `07-PATTERNS.md` §1 (sub-package layout) + `07-RESEARCH.md` §Environment Availability, Nyquist compliance requires the test scaffolding to exist BEFORE implementation waves run. Two extra discipline points specific to Phase 7 (vs Phase 6 W0):
  (a) Three test files (not two) because the test-class taxonomy splits cleanly into three buckets — CLI/REPL-side (`test_batch.py`), pure-stats (`test_batch_stats.py`), report-side (`test_batch_report.py`). This lets Waves 1-4 land in parallel without merge conflicts (Plan 01 → `test_batch_stats.py`; Plan 02 → `test_batch.py::TestSeedsParser`/`TestArgsMutex`; Plan 04 → `test_batch_report.py`; Plan 05 → `test_batch.py::TestReplBatch`/`TestCliReplParity`).
  (b) `requirements.txt` is genuinely NEW (Phase 1-6 had no manifest — VERIFIED via `ls requirements.txt` returns no-such-file). Researcher (RESEARCH §Open Questions #2 + §Environment Availability) recommends adding now since scipy is a new dep beyond what Phase 6 implicitly pulled.

Output: 4 files created (3 new tests/* + 1 new requirements.txt). Zero production code. `python -m unittest discover tests/` still passes (full Phase 6 suite green + new skip-stubs counted). `pip install -r requirements.txt --dry-run` resolves cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-batch-runner-aggregation/07-RESEARCH.md
@.planning/phases/07-batch-runner-aggregation/07-PATTERNS.md
@.planning/phases/07-batch-runner-aggregation/07-VALIDATION.md
@tests/test_env.py
@tests/test_agent.py
@tests/test_report.py

<interfaces>
Test-class taxonomy locked by 07-VALIDATION.md per-task verification map AND the wave-parallelism split decision:

| Class name              | File                       | Plan that GREENs it | Covers                                |
|-------------------------|----------------------------|---------------------|---------------------------------------|
| TestSeedsParser         | tests/test_batch.py        | 02                  | BATCH-01 parser (10 grammar cases)    |
| TestArgsMutex           | tests/test_batch.py        | 02                  | BATCH-01 CLI mutex (--batch/--seeds, --batch+--compare-agent, --seeds without --batch) |
| TestReplBatch           | tests/test_batch.py        | 05                  | BATCH-01 REPL e2e + REPL error paths   |
| TestDeterminism         | tests/test_batch.py        | 03                  | BATCH-01 byte-identical re-run         |
| TestCliReplParity       | tests/test_batch.py        | 05                  | BATCH-01 CLI vs REPL produce identical reports |
| TestAggregateKpis       | tests/test_batch_stats.py  | 01                  | BATCH-02 mean/std/min/max correctness  |
| TestCIComputation       | tests/test_batch_stats.py  | 01                  | BATCH-02 95% CI matches scipy hand-calc + synthetic coverage |
| TestN1Degenerate        | tests/test_batch_stats.py  | 01                  | BATCH-02 N=1 std=0, CI=None graceful   |
| TestEmptyInput          | tests/test_batch_stats.py  | 01                  | BATCH-02 N=0 raises ValueError         |
| TestStatsDeterminism    | tests/test_batch_stats.py  | 01                  | BATCH-02 same input → byte-identical AggregateStat |
| TestBatchReport         | tests/test_batch_report.py | 04                  | BATCH-03 sections (per-seed table, aggregate table, baseline verdict, png link) |
| TestBatchPlots          | tests/test_batch_report.py | 04                  | PLOT-04 batch_aggregate.png exists + 5 subplot panels |

Subprocess-test helper pattern (tests/test_env.py:1-30 — `_PROJECT_ROOT`, `MONOLITH`, `_run_sph`) — re-used verbatim in `tests/test_batch.py` and `tests/test_batch_report.py`.

Pure-unit pattern (tests/test_agent.py:1-37 — `_PROJECT_ROOT` + `sys.path.insert` only, no subprocess) — re-used verbatim in `tests/test_batch_stats.py`.

requirements.txt format per RESEARCH §Environment Availability — minimum versions to support t.interval signature (scipy ≥1.10), ddof argument (numpy ≥1.20), `subplots(1, 5)` shape (matplotlib ≥3.0). VERIFIED locally: scipy 1.16.3, numpy 2.3.5, matplotlib 3.10.7. Recommended pin: `>=` for minimum, no upper bound (Phase 7 has no known incompatibilities with future major versions).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_batch.py + tests/test_batch_stats.py + tests/test_batch_report.py stub containers</name>
  <files>tests/test_batch.py, tests/test_batch_stats.py, tests/test_batch_report.py</files>
  <read_first>
    - tests/test_env.py (lines 1-30 — subprocess header: imports, _PROJECT_ROOT, MONOLITH, _run_sph helper) — analog for test_batch.py + test_batch_report.py
    - tests/test_agent.py (lines 1-37 — pure-unit header: imports + _PROJECT_ROOT + sys.path.insert, NO subprocess) — analog for test_batch_stats.py
    - tests/test_report.py (lines 1-50 — Phase 6 stub-container shape) — analog for skip-body style
    - .planning/phases/07-batch-runner-aggregation/07-VALIDATION.md (Per-Task Verification Map — class names + test method names locked there)
    - .planning/phases/07-batch-runner-aggregation/07-PATTERNS.md §2j/§2k/§2l (test analogs for stats / batch / batch_report)
    - .planning/phases/06-report-plots-generator/06-00-PLAN.md (Phase 6 W0 stub-container task — exact shape to mirror)
  </read_first>
  <action>
    Create three new test files. All three are unittest.TestCase containers with skip-body stub methods naming the Wave-N + Plan-ID that will green them.

    Common header for `tests/test_batch.py` and `tests/test_batch_report.py` (subprocess-capable — verbatim copy from `tests/test_env.py:1-30` with file-specific docstring):

    ```python
    """
    <FILE-SPECIFIC DOCSTRING>
    Stdlib only: unittest + subprocess + json + os + sys + tempfile + pathlib.
    """
    import json, os, subprocess, sys, unittest
    from pathlib import Path

    _PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), os.pardir))
    if _PROJECT_ROOT not in sys.path:
        sys.path.insert(0, _PROJECT_ROOT)

    PROJECT_ROOT = Path(_PROJECT_ROOT)
    MONOLITH = PROJECT_ROOT / 'sph_sim.py'


    def _run_sph(*args, **kwargs):
        """Subprocess helper — uruchamia sph_sim.py z cwd=_PROJECT_ROOT (mirror tests/test_env.py)."""
        env = {**os.environ, 'SPHSIM_NO_REPORT': kwargs.pop('SPHSIM_NO_REPORT', '1')}
        return subprocess.run(
            [sys.executable, 'sph_sim.py', *args],
            cwd=_PROJECT_ROOT,
            capture_output=True,
            text=True,
            env=env,
            **kwargs,
        )
    ```

    Header for `tests/test_batch_stats.py` (pure-unit — verbatim from `tests/test_agent.py:1-37`, NO subprocess imports):

    ```python
    """Unit tests dla sphsim.batch.stats (Phase 7, BATCH-02). Pure-unit — no subprocess."""
    import unittest
    import os, sys

    _PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), os.pardir))
    if _PROJECT_ROOT not in sys.path:
        sys.path.insert(0, _PROJECT_ROOT)
    ```

    ── File 1: tests/test_batch.py ──

    File-specific docstring: `"Unit i integration tests dla Phase 7 — CLI/REPL/parser side (BATCH-01). Klasy: TestSeedsParser, TestArgsMutex, TestReplBatch, TestDeterminism, TestCliReplParity."`

    After the common header, define exactly FIVE classes, each `unittest.TestCase` subclass, each with one `test_placeholder(self)` method whose body is `self.skipTest("Wave N — Plan 07-XX — <one-line scope>")`. Class-level docstrings in Polish.

    Classes in order:

    1. `class TestSeedsParser(unittest.TestCase):` — covers BATCH-01 parser.
       Docstring: `"BATCH-01: _parse_seeds_list grammar — single N → range, lista → dedup, reject 0/ujemne/range-syntax/empty."`
       `def test_placeholder(self): self.skipTest("Wave 1 — Plan 07-02 — _parse_seeds_list converter w args.py")`

    2. `class TestArgsMutex(unittest.TestCase):` — covers BATCH-01 CLI mutex.
       Docstring: `"BATCH-01: --batch wymaga --seeds; --batch + --compare-agent mutex; --seeds bez --batch mutex; --batch + --interactive mutex."`
       `def test_placeholder(self): self.skipTest("Wave 1 — Plan 07-02 — post-parse mutex w args.py")`

    3. `class TestReplBatch(unittest.TestCase):` — covers BATCH-01 REPL e2e.
       Docstring: `"BATCH-01: REPL komenda 'batch <strategia> --seeds N|lista [k=v]' produkuje raport + polski error dla --seeds 0."`
       `def test_placeholder(self): self.skipTest("Wave 4 — Plan 07-05 — SPHShell.do_batch w repl.py")`

    4. `class TestDeterminism(unittest.TestCase):` — covers BATCH-01 determinism contract.
       Docstring: `"BATCH-01: ta sama lista seedów dwa razy → byte-identical per-seed KPI (random.seed(S) reset w SPHSimulator.__init__)."`
       `def test_placeholder(self): self.skipTest("Wave 2 — Plan 07-03 — run_batch orchestrator deterministic loop")`

    5. `class TestCliReplParity(unittest.TestCase):` — covers BATCH-01 CLI/REPL parity.
       Docstring: `"BATCH-01: CLI '--batch --seeds 3' i REPL 'batch <name> --seeds 3' produkują identyczne tabele KPI (single source of truth dla seedów + aggregate)."`
       `def test_placeholder(self): self.skipTest("Wave 4 — Plan 07-05 — REPL fake_args + run_batch reuse")`

    Footer: `if __name__ == '__main__': unittest.main()`.

    ── File 2: tests/test_batch_stats.py ──

    File-specific docstring: `"Unit tests dla sphsim.batch.stats (Phase 7, BATCH-02). Pure-unit — no subprocess. Klasy: TestAggregateKpis, TestCIComputation, TestN1Degenerate, TestEmptyInput, TestStatsDeterminism."`

    After the pure-unit header, define exactly FIVE classes. Each with one `test_placeholder(self)` skipping to Plan 07-01.

    1. `class TestAggregateKpis(unittest.TestCase):` — BATCH-02 mean/std/min/max correctness.
       Docstring: `"BATCH-02: mean/std/min/max poprawne dla znanych próbek; std używa ddof=1 (sample, nie population)."`
       `def test_placeholder(self): self.skipTest("Wave 1 — Plan 07-01 — sphsim/batch/stats.py::aggregate_kpis")`

    2. `class TestCIComputation(unittest.TestCase):` — BATCH-02 95% CI.
       Docstring: `"BATCH-02: 95% CI dla N≥2 zgodne z scipy.stats.t.interval (hand-calc dla N=10); synthetic coverage dla N=100 normals."`
       `def test_placeholder(self): self.skipTest("Wave 1 — Plan 07-01 — scipy.stats.t.interval w aggregate_kpis")`

    3. `class TestN1Degenerate(unittest.TestCase):` — BATCH-02 N=1 graceful.
       Docstring: `"BATCH-02: N=1 nie crashuje — std=0.0, ci_lower=None, ci_upper=None; mean=min=max=jedyna wartość."`
       `def test_placeholder(self): self.skipTest("Wave 1 — Plan 07-01 — N=1 guard PRZED values.std(ddof=1)")`

    4. `class TestEmptyInput(unittest.TestCase):` — BATCH-02 N=0 raises.
       Docstring: `"BATCH-02: aggregate_kpis([]) rzuca ValueError z polskim komunikatem (orkiestrator filtruje pusty input wcześniej)."`
       `def test_placeholder(self): self.skipTest("Wave 1 — Plan 07-01 — ValueError dla empty input")`

    5. `class TestStatsDeterminism(unittest.TestCase):` — BATCH-02 byte-identical aggregate.
       Docstring: `"BATCH-02: ten sam input list[dict] → byte-identical dict[AggregateStat] (no random, no global state)."`
       `def test_placeholder(self): self.skipTest("Wave 1 — Plan 07-01 — pure-function aggregate_kpis")`

    Footer: `if __name__ == '__main__': unittest.main()`.

    ── File 3: tests/test_batch_report.py ──

    File-specific docstring: `"Unit i integration tests dla Phase 7 — batch markdown + plot (BATCH-03, PLOT-04). Klasy: TestBatchReport, TestBatchPlots."`

    After the common subprocess header, define exactly TWO classes:

    1. `class TestBatchReport(unittest.TestCase):` — BATCH-03 raport MD.
       Docstring: `"BATCH-03: render_batch_report składa Title + Konfiguracja + Strategia + Per-seed table + Aggregate + Wykresy + Werdykt. Linki ![](batch_aggregate.png)."`
       `def test_placeholder(self): self.skipTest("Wave 3 — Plan 07-04 — sphsim/report/batch_markdown.py")`

    2. `class TestBatchPlots(unittest.TestCase):` — PLOT-04 boxplot PNG.
       Docstring: `"PLOT-04: plot_batch_aggregate generuje batch_aggregate.png — 5 subplotów (1×5), PNG signature, non-zero size, plt.close-in-finally."`
       `def test_placeholder(self): self.skipTest("Wave 3 — Plan 07-04 — sphsim/report/plots.py::plot_batch_aggregate")`

    Footer: `if __name__ == '__main__': unittest.main()`.
  </action>
  <acceptance_criteria>
    - All three files exist at the paths above.
    - `python -c "import tests.test_batch; import tests.test_batch_stats; import tests.test_batch_report"` succeeds (imports clean, no syntax errors).
    - `SPHSIM_NO_REPORT=1 python -m unittest tests.test_batch tests.test_batch_stats tests.test_batch_report -v` reports exactly 12 tests (5 + 5 + 2), all SKIPPED.
    - `grep -c '^class Test' tests/test_batch.py` returns 5.
    - `grep -c '^class Test' tests/test_batch_stats.py` returns 5.
    - `grep -c '^class Test' tests/test_batch_report.py` returns 2.
    - Skip messages contain "Wave N — Plan 07-XX" pattern (verifiable: `grep -E 'Wave [1-4] — Plan 07-0[1-5]' tests/test_batch*.py | wc -l` ≥ 12).
    - Phase 6 test suite (`SPHSIM_NO_REPORT=1 python -m unittest discover tests/`) still passes — total count is 172_prior + 12_new_skipped.
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && SPHSIM_NO_REPORT=1 python -m unittest tests.test_batch tests.test_batch_stats tests.test_batch_report -v 2>&1 | tail -5 && SPHSIM_NO_REPORT=1 python -m unittest discover tests/ 2>&1 | tail -3</automated>
  </verify>
  <done>
    Three stub-container files exist, import cleanly, contribute exactly 12 skipped tests, and Phase 6 suite remains green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create requirements.txt pinning scipy + numpy + matplotlib</name>
  <files>requirements.txt</files>
  <read_first>
    - .planning/phases/07-batch-runner-aggregation/07-RESEARCH.md (Section: Environment Availability — verified versions: scipy 1.16.3, numpy 2.3.5, matplotlib 3.10.7)
    - .planning/phases/07-batch-runner-aggregation/07-RESEARCH.md (Section: Open Questions #2 — recommendation: ADD in Phase 7)
    - .planning/phases/07-batch-runner-aggregation/07-PATTERNS.md §8 "No Analog Found" — `requirements.txt` is genuinely NEW (no prior file in repo)
  </read_first>
  <action>
    Create `requirements.txt` at repo root with exactly three production dependencies — matplotlib, numpy, scipy — each with a minimum-version pin (`>=`) at the LOCALLY VERIFIED version (RESEARCH §Environment Availability). Use Polish-aware comment header explaining what Phase formalized the dep.

    Content shape (literal text — write this file exactly):

    ```
    # Zależności produkcyjne SPH Mediation Simulator (Milestone v1.1 Agent CLI).
    #
    # Phase 6 (REPORT-01..03 + PLOT-01..03) wprowadził matplotlib (numpy przychodzi tranzytywnie).
    # Phase 7 (BATCH-01..03 + PLOT-04) wprowadził scipy — wszystkie trzy są formalizowane tutaj.
    #
    # Minimum versions matche locally-verified state (2026-05-28).
    # No upper bound — pakiety mają stabilne API dla naszych użyć (Pattern: scipy.stats.t.interval,
    # numpy.std(ddof=1), matplotlib.pyplot.boxplot — wszystkie istnieją od >5 lat bez breaking change).

    matplotlib>=3.10.0
    numpy>=2.3.0
    scipy>=1.16.0
    ```

    Notes for executor:
    - No upper bound — research §J ("Don't Hand-Roll") + §Assumption A1 confirms API stability.
    - DO NOT add unittest, json, os, sys (stdlib).
    - DO NOT pin patch versions (`==1.16.3`) — over-constrains CI; minor-version pins are sufficient for Phase 7 invariants.
    - File MUST end with a trailing newline (POSIX text file convention; some tools error without it).
  </action>
  <acceptance_criteria>
    - `requirements.txt` exists at repo root.
    - `grep -c '^matplotlib' requirements.txt` returns 1.
    - `grep -c '^numpy' requirements.txt` returns 1.
    - `grep -c '^scipy' requirements.txt` returns 1.
    - `grep -cE '^[a-z]' requirements.txt` returns exactly 3 (no other non-comment lines).
    - File header comment mentions both Phase 6 (matplotlib) and Phase 7 (scipy) provenance.
    - `pip install --dry-run -r requirements.txt` resolves without error (do NOT actually install — `--dry-run` is sufficient for CI sanity; if `--dry-run` is unsupported on the local pip, `pip install --no-deps --dry-run` is acceptable fallback).
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -f requirements.txt && [ "$(grep -cE '^(matplotlib|numpy|scipy)' requirements.txt)" -eq 3 ] && grep -q 'Phase 7' requirements.txt && echo OK</automated>
  </verify>
  <done>
    `requirements.txt` exists with exactly the three deps + comment header, all minimum-version pinned, file ends with newline, grep checks pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer→test infra | No external input; stubs run only locally; subprocess helper inherits dev shell env |
| Developer→requirements.txt | Local-only manifest read by `pip install`; supply-chain concern below |
| pip install→PyPI | Phase 7 formalizes 3 transitive deps that Phase 6 already brought in; surface is unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-00-01 | Tampering | `tests/test_batch*.py` stub files | accept | Skip-body only; no executable runtime side effects. Zero attack surface — pure test scaffolds. |
| T-7-00-SC | Tampering | `pip install -r requirements.txt` (supply chain) | mitigate | All three packages (matplotlib, numpy, scipy) are PyPI core scientific packages with cryptographic signing, 10M+ downloads/month, no `[SLOP]`/`[SUS]` flags from researcher. Phase 6 already pulled them transitively — Phase 7 only adds an explicit manifest line. **No blocking checkpoint required** (analog: Phase 6 Plan 03 introduced matplotlib without a slop checkpoint and `verify_phase6.sh` PASS=40/FAIL=0). RESEARCH §Security Domain confirms ASVS N/A (academic local-only). |

Phase 7 W0 introduces zero new attack surface beyond Phase 6 reality. T-7-01 through T-7-05 (path traversal in `./reports/batch_<ts>/`, seed-list resource exhaustion, REPL state leakage, matplotlib FD leak, Polish encoding in filenames) defer to Plans 01-05 where the actual filesystem writes + parsers + REPL state changes are introduced.
</threat_model>

<verification>
After both tasks complete:

```bash
cd "$(git rev-parse --show-toplevel)"
# 1. Stubs import + are correctly skipped
SPHSIM_NO_REPORT=1 python -m unittest tests.test_batch tests.test_batch_stats tests.test_batch_report -v 2>&1 | tail -5
# 2. Phase 6 suite still green (~172 prior + 12 new skipped)
SPHSIM_NO_REPORT=1 python -m unittest discover tests/ 2>&1 | tail -3
# 3. Regression unchanged
SPHSIM_NO_REPORT=1 python scripts/regression_check.py 2>&1 | tail -3   # PASS=8/8
# 4. Class-count gates
grep -c '^class Test' tests/test_batch.py        # 5
grep -c '^class Test' tests/test_batch_stats.py  # 5
grep -c '^class Test' tests/test_batch_report.py # 2
# 5. requirements.txt presence + 3 deps
[ "$(grep -cE '^(matplotlib|numpy|scipy)' requirements.txt)" -eq 3 ] && echo "OK requirements"
# 6. requirements.txt has Phase 7 provenance comment
grep -q 'Phase 7' requirements.txt && echo "OK provenance"
```

All six MUST succeed before declaring Plan 00 done.
</verification>

<success_criteria>
1. `tests/test_batch.py` exists with 5 named test classes (TestSeedsParser, TestArgsMutex, TestReplBatch, TestDeterminism, TestCliReplParity), all skip-bodied with explicit "Wave N — Plan 07-XX" markers.
2. `tests/test_batch_stats.py` exists with 5 named test classes (TestAggregateKpis, TestCIComputation, TestN1Degenerate, TestEmptyInput, TestStatsDeterminism), all skip-bodied.
3. `tests/test_batch_report.py` exists with 2 named test classes (TestBatchReport, TestBatchPlots), all skip-bodied.
4. `requirements.txt` exists at repo root with exactly 3 dep lines (matplotlib, numpy, scipy), each minimum-version pinned, and a header comment that names Phase 6 + Phase 7 provenance.
5. Phase 6 test suite (`python -m unittest discover tests/`) remains green — zero collateral damage.
6. Regression check (`python scripts/regression_check.py`) remains green PASS=8/8 — zero collateral damage.
7. The new test files contribute exactly 12 SKIPPED tests when run together.
</success_criteria>

<output>
Create `.planning/phases/07-batch-runner-aggregation/07-00-SUMMARY.md` when done. Include:
- Files created (4): list paths and 1-line role each
- Test class taxonomy locked: 12 classes total across 3 files, ready for Plans 01-05 to GREEN
- Confirmation that `requirements.txt` resolves cleanly via `pip install --dry-run`
- Confirmation Phase 6 suite + regression_check.py still pass
- Suggested commit message: `chore(07-00): wave 0 scaffolding — test stubs (test_batch + test_batch_stats + test_batch_report) + requirements.txt (matplotlib/numpy/scipy)`
</output>
