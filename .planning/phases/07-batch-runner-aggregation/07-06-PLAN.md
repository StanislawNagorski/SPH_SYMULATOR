---
phase: 07-batch-runner-aggregation
plan: 06
type: execute
wave: 5
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05"]
files_modified:
  - scripts/verify_phase7.sh
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [BATCH-01, BATCH-02, BATCH-03, PLOT-04]
threat_refs: [T-7-06-01]
must_haves:
  truths:
    - "scripts/verify_phase7.sh exists, is chmod +x, runs end-to-end, exits 0 with PASS≥30 / FAIL=0"
    - "verify_phase7.sh has section banners covering all 5 ROADMAP SCs + regression + full test suite + REPL Pitfall checks + opt-out"
    - "Pre-flight cleanup `rm -rf ./reports/` ensures deterministic artifact paths for SC checks"
    - "All 4 REQ-IDs (BATCH-01, BATCH-02, BATCH-03, PLOT-04) covered by ≥1 check() invocation each"
    - "All 5 ROADMAP SCs covered as labeled check() invocations"
    - ".planning/ROADMAP.md Phase 7 entry has Plans count (already \"7 plans\" from planner-annotated state — do NOT regress to 6), Plan list (07-00..07-06), and `[ ]` checkboxes correctly marked for each plan; at closeout the progress table flips \"0/7 Executing\" → \"7/7 Complete\""
    - ".planning/STATE.md reflects Phase 7 completion: progress.completed_phases=7, total_plans=32 (25+7), completed_plans=32, percent=100 (32/32)"
    - "Exit gate is the single canonical pre-flight before /gsd:verify-work"
  artifacts:
    - path: "scripts/verify_phase7.sh"
      provides: "Phase 7 exit gate — all 5 ROADMAP SCs + 4 REQ-IDs + regression + tests + REPL + opt-out as check() invocations"
      contains: "check \"SC #"
      min_lines: 200
    - path: ".planning/ROADMAP.md"
      provides: "Phase 7 section updated: plan list complete, status checkbox final"
      contains: "07-06-PLAN.md"
    - path: ".planning/STATE.md"
      provides: "Updated progress metrics + recent plans + current position"
      contains: "Phase 7"
  key_links:
    - from: "scripts/verify_phase7.sh"
      to: "scripts/verify_phase6.sh framework (verbatim copy with p6_→p7_ + Phase 6→Phase 7 swap)"
      via: "check() helper, set -euo pipefail, trap cleanup"
      pattern: "/tmp/p7_"
    - from: "scripts/verify_phase7.sh"
      to: "ROADMAP Phase 7 Success Criteria 1-5"
      via: "explicit check() per SC with grep/python-c assertion bodies"
      pattern: "SC #(1|2|3|4|5)"
---

<objective>
Close Phase 7 by creating `scripts/verify_phase7.sh` (the canonical exit gate that runs all Phase 7 SC checks + regression + full test suite + REPL + opt-out) and updating `.planning/ROADMAP.md` and `.planning/STATE.md` with final phase markup. After this plan, `bash scripts/verify_phase7.sh` is the single decision: PASS≥30 / FAIL=0 means Phase 7 ships.

Purpose: Mirrors Phase 6 Plan 05 structure exactly. Phase 6's `verify_phase6.sh` has 39 check() invocations covering 6 SCs + regression + tests + opt-out + REPL Pitfalls — Phase 7 targets ≥30 (RESEARCH spec) and aims for ~35-40 to match Phase 6's coverage. The script swaps Phase 6 tokens (`p6_`, `Phase 6`, `decision_distribution.png`, `reports/<ts>/`) for Phase 7 equivalents (`p7_`, `Phase 7`, `batch_aggregate.png`, `reports/batch_<ts>/`) and adds Phase 7-specific SC checks (seed parser + per-seed table + 5-panel boxplot + werdykt baseline-beating + --no-agent parallel run).

Output: 1 new file (`scripts/verify_phase7.sh`, chmod +x) + 2 modified files (`.planning/ROADMAP.md`, `.planning/STATE.md`). After this plan, `gsd:verify-work` can be invoked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-batch-runner-aggregation/07-RESEARCH.md
@.planning/phases/07-batch-runner-aggregation/07-PATTERNS.md
@.planning/phases/07-batch-runner-aggregation/07-VALIDATION.md
@.planning/phases/07-batch-runner-aggregation/07-01-SUMMARY.md
@.planning/phases/07-batch-runner-aggregation/07-02-SUMMARY.md
@.planning/phases/07-batch-runner-aggregation/07-03-SUMMARY.md
@.planning/phases/07-batch-runner-aggregation/07-04-SUMMARY.md
@.planning/phases/07-batch-runner-aggregation/07-05-SUMMARY.md
@scripts/verify_phase6.sh
@scripts/regression_check.py

<interfaces>
verify_phase6.sh is the verbatim template — read its entire 212-line body before writing verify_phase7.sh. Key blocks to preserve verbatim with Phase 7 token swaps:

| Block | Phase 6 location | Phase 7 swap |
|-------|------------------|--------------|
| Shebang + header banner | lines 1-30 | "Phase 6" → "Phase 7", "REPORT-01..03 + PLOT-01..03" → "BATCH-01..03 + PLOT-04" |
| Python detection | lines 31-40 | unchanged |
| `trap` cleanup | line 42 | `p6_` → `p7_` |
| PASS=0, FAIL=0 counters | lines 44-45 | unchanged |
| `check()` helper | lines 47-58 | `/tmp/p6_check.log` → `/tmp/p7_check.log` |
| Section 0: Pre-flight cleanup | rm -rf ./reports/ | unchanged |
| Section 1: Regression | regression_check.py PASS=8/8 | unchanged (Phase 7 has no SKIP_KEYS change per PATTERNS §6) |
| Section 2: Test suite | unittest discover tests/ | swap module names: test_batch, test_batch_stats, test_batch_report |
| Section 2a: Pre-flight artifact | sph_sim.py --strategy naive --no-agent --seed 42 producing reports/<ts>/ | swap: --batch --seeds 5 producing reports/batch_<ts>/ |
| Section 3-7: SC checks | each Phase 6 SC | swap to each Phase 7 SC (see SC table below) |
| Section 8: JSON cleanliness | --json + reports | SKIP (Phase 7 defers --json + --batch per RESEARCH §Open Questions #1) |
| Section 9: REPL Pitfall | printf 'run naive ...\nexit\n' | swap: printf 'batch naive --seeds 5\nexit\n' |
| Section 10: Opt-out | SPHSIM_NO_REPORT=1 ... && [ ! -d reports ] | unchanged framework, add --batch invocation |
| Summary block | lines 202-211 | "Phase 6" → "Phase 7" |

Phase 7 SCs (verbatim from ROADMAP):

| SC | Description | check() body |
|----|-------------|--------------|
| SC #1 | `--batch --seeds N` (CLI) + `/batch --seeds N` (REPL) work | (a) `_parse_seeds_list('5') == [1..5]`; (b) `_parse_seeds_list('1,5,42') == [1,5,42]`; (c) CLI `--batch --seeds 5 --no-agent` produces report; (d) REPL `batch naive --seeds 5` produces report |
| SC #2 | Report MD has per-seed table + aggregate stats | (a) `^\| [0-9]+ \|` row count ≥ 5 in report.md; (b) `^\| (avg_val_last100\|cum_val_total\|...)` row count ≥ 5; (c) 95% CI column present |
| SC #3 | `batch_aggregate.png` generated + linked | (a) PNG signature byte check; (b) PNG size ≥ 10KB; (c) `(batch_aggregate.png)` literal in report.md |
| SC #4 | Batch works with RationalAgent (default) AND --no-agent | (a) `--batch --seeds 3 --no-agent` → report; (b) `--batch --seeds 3` (with agent) → report; (c) both produce non-empty PNG |
| SC #5 | Werdykt baseline-beating in report | (a) `## Werdykt: bije baseline` literal in report.md; (b) ✓ TAK or ✗ NIE glyph present |

Additional check() coverage beyond SCs (Phase 6 had 39 total — Phase 7 targets ~35-40):

- REQ-ID specific checks (4): one explicit per REQ-ID with traceable reference (e.g., `# BATCH-02 — aggregate_kpis MathContract`)
- Mutex checks (3): `--batch --compare-agent`, `--batch` without `--seeds`, `--seeds` without `--batch` all exit 2 with Polish error
- Test count (3): 9 stats green + 17 batch green (8 parser + 4 mutex + 2 determinism + 2 REPL + 1 parity) + 7 report green (5 TestBatchReport + 2 TestBatchPlots) = 33 Phase 7 tests
- REPL pitfall (2): `batch --seeds 0` no crash; `batch unknown --seeds 3` no crash
- Opt-out (1): `SPHSIM_NO_REPORT=1 --batch` → no reports dir
- Determinism (1): two identical `--batch --seeds 1,2,3` produce identical stdout

Total: 5 SC × ~3 = 15 + 4 REQ + 3 mutex + 3 test-count + 2 REPL + 1 opt-out + 1 determinism = ~29-32 check() invocations. Hit ≥30.

ROADMAP.md update — the Phase 7 entry is ALREADY in the planner-annotated state (`**Plans**: 7 plans`, `0/7 Executing`). Plan 06's role is ONLY the closeout flip (executing → Complete, all `[ ]` → `[x]`). The structural shape stays:

```markdown
- [x] **Phase 7: Batch runner + aggregation** - Wiele seedów, agregacja statystyczna, box-ploty (completed 2026-05-28)
```

And under "Phase Details" → "### Phase 7: Batch runner + aggregation":
```markdown
**Plans**: 7 plans
Plans:
**Wave 0** *(scaffolding)*

- [ ] 07-00-PLAN.md — Wave 0 scaffolding: test stubs (test_batch + test_batch_stats + test_batch_report) + requirements.txt

**Wave 1** *(blocked on Wave 0 completion)*

- [ ] 07-01-PLAN.md — BATCH-02: sphsim/batch/stats.py (aggregate_kpis + AggregateStat + KPIS) + TestAggregate green
- [ ] 07-02-PLAN.md — BATCH-01 CLI: args.py _parse_seeds_list + --batch/--seeds + 4-way mutex + TestSeedsParser (8) + TestArgsMutex (4) green (12 tests)

**Wave 2** *(blocked on Wave 1 completion)*

- [ ] 07-03-PLAN.md — BATCH-01 orchestrator: sphsim/batch/runner.py + main.py batch branches + format_batch_summary + TestDeterminism green

**Wave 3** *(blocked on Wave 2 completion)*

- [ ] 07-04-PLAN.md — BATCH-03 + PLOT-04: batch_markdown.py + plot_batch_aggregate + write_batch_report + TestBatchReport/TestBatchPlots green

**Wave 4** *(blocked on Wave 3 completion)*

- [ ] 07-05-PLAN.md — BATCH-01 REPL: SPHShell.do_batch + do_help + CLI/REPL parity + TestReplBatch/TestCliReplParity green

**Wave 5** *(blocked on Wave 4 completion)*

- [ ] 07-06-PLAN.md — verify_phase7.sh exit gate (5 ROADMAP SCs + 4 REQ-IDs + regression + REPL + opt-out) + STATE/ROADMAP closeout
```

Note: this plan does NOT mark the checkboxes [x] — Plan 06 is itself one of the plans being executed. The `[x]` flips happen as each plan completes its own SUMMARY (executor's responsibility). Plan 06 ONLY writes the plan-list shape, leaves all `[ ]` boxes unchecked.

The OUTER Phase 7 status `[x]` (line 27) gets flipped at /gsd:verify-work time, not by this plan.

STATE.md update — fields to update:
- `progress.completed_phases: 6` → 7 at closeout (Phase 7 ships when Plan 06 commits its SUMMARY). The outer ROADMAP `### Phase 7:` checkbox flip (`[ ]` → `[x]`) is still verify-work's job, but the STATE counter advances here.
- `progress.completed_plans: 25` → 32 at closeout (Phase 7 has 7 plans; 25 + 7 = 32)
- Convention: at end-of-plan-N, completed_plans is updated by SUMMARY commits. Plan 06 SUMMARY belongs to Plan 06 itself. So: at the END of Plan 06 execution, all 7 Phase 7 plans are done → completed_plans=32, completed_phases=7.
- `last_activity: <today> -- Phase 7 closeout — Plan 06 (verify_phase7.sh + STATE/ROADMAP) shipped; all 7 plans complete`
- `stopped_at: Phase 7 exit gate ready`
- Performance Metrics table: update row "7. Batch runner | 7/7 plans | ... | ..." — total/avg-per-plan are filled by /gsd:verify-work or by SUMMARY commits; estimate plausibly from prior Phase velocity.

Note for the executor: STATE.md is normally maintained by `/gsd:execute-phase` and `/gsd:verify-work`; the planner pre-populates the expected end state but the tooling may overwrite. This plan does a "best-effort" markup; downstream tooling may re-format.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/verify_phase7.sh exit gate</name>
  <files>scripts/verify_phase7.sh</files>
  <read_first>
    - scripts/verify_phase6.sh (entire file — 212 lines; the verbatim template, count check() invocations = 39, identify each Section's purpose)
    - scripts/regression_check.py (lines 50-110 — understand SKIP_KEYS + subprocess.run env passthrough; verify no Phase 7 changes needed per PATTERNS §6)
    - sphsim/cli/args.py (verify `_parse_seeds_list` + `MAX_SEEDS` are module-level after Plan 02)
    - sphsim/batch/stats.py (verify `aggregate_kpis` + `KPIS` after Plan 01)
    - sphsim/report/batch_markdown.py (verify section headers after Plan 04)
    - tests/fixtures/baseline_v1/08-naive-zeta-0.75-baseline.json (verify baseline avg_val_last100=92.0)
    - .planning/phases/07-batch-runner-aggregation/07-RESEARCH.md (Section "Validation Architecture" — Wave 0 Gaps; PATTERNS.md §2m — verify_phase7.sh framework verbatim + token swap table + Phase 7-specific SC checks lines 893-941)
    - .planning/phases/07-batch-runner-aggregation/07-PATTERNS.md §2m (NEW SC-specific checks for Phase 7 — full verbatim shell snippets at lines 893-941)
  </read_first>
  <action>
    Create `scripts/verify_phase7.sh` by mirroring `scripts/verify_phase6.sh` verbatim and applying the token-swap table from PATTERNS §2m. Make the file executable with chmod +x semantics (the Write tool produces a file; executor must run `chmod +x scripts/verify_phase7.sh` as the last action of this task).

    Structure (10 sections + final summary block — ≥30 check() invocations):

    ── Header (lines 1-30) ──
    Shebang `#!/usr/bin/env bash`, banner explaining Phase 7 coverage:
    ```
    # verify_phase7.sh — phase exit gate dla Phase 7 (batch runner + aggregation)
    # Phase 7 covers BATCH-01..03 + PLOT-04 (multi-seed orchestrator + scipy CI + boxplot).
    # Phase 7 Success Criteria:
    #   SC #1: /batch <strategia> --seeds 10 i --batch --seeds 1,5,42,100 oba działają
    #   SC #2: Raport MD ma tabelę per-seed (N wierszy × 6 kolumn) + agregat (5 KPI × 7 kolumn z CI)
    #   SC #3: batch_aggregate.png istnieje, non-zero, PNG signature, linked w raporcie
    #   SC #4: Batch działa z RationalAgent (default) i --no-agent
    #   SC #5: Werdykt baseline-beating w raporcie (lower 95% CI > 92.0 dla avg_val_last100)
    ```

    ── Setup (lines 31-58) ──
    Verbatim from verify_phase6.sh:
    - `set -euo pipefail`
    - `cd "$(dirname "$0")/.."`
    - Python detection block (try `python` then `python3`)
    - `trap 'rm -f /tmp/p7_*' EXIT`
    - `PASS=0`, `FAIL=0`
    - `check()` helper with `/tmp/p7_check.log` redirect

    ── Section 0: Pre-flight (1 check) ──
    `rm -rf ./reports/` — non-check command (deterministic state for SC checks).

    ── Section 1: Regression (1 check) ──
    `check "Regression: scripts/regression_check.py PASS=8/8" "SPHSIM_NO_REPORT=1 $PY scripts/regression_check.py | tail -5 | grep -F 'PASS=8/8'"`
    (Or equivalent grep target depending on how regression_check.py prints success.)

    ── Section 2: Full test suite (4 checks) ──
    - `check "Test suite: unittest discover all green" "SPHSIM_NO_REPORT=1 $PY -m unittest discover tests/ 2>&1 | tail -3 | grep -F 'OK'"`
    - `check "Phase 7 tests: test_batch_stats green (9 tests)" "SPHSIM_NO_REPORT=1 $PY -m unittest tests.test_batch_stats 2>&1 | tail -3 | grep -F 'OK'"`
    - `check "Phase 7 tests: test_batch green (17 tests, 5 classes)" "SPHSIM_NO_REPORT=1 $PY -m unittest tests.test_batch 2>&1 | tail -3 | grep -F 'OK'"`
    - `check "Phase 7 tests: test_batch_report green (7 tests, 2 classes)" "SPHSIM_NO_REPORT=1 $PY -m unittest tests.test_batch_report 2>&1 | tail -3 | grep -F 'OK'"`

    ── Section 2a: Pre-flight artifact bundle (1 check) ──
    Generate the Phase 7 artifact under reports/batch_<ts>/ that subsequent SC checks read:
    ```bash
    rm -rf ./reports/
    SPHSIM_NO_REPORT='' $PY sph_sim.py --strategy naive --zeta 0.75 --batch --seeds 5 --no-agent --seed 42 > /tmp/p7_artifact.log 2>&1 || true
    LATEST_B=$(ls -d ./reports/batch_*/ 2>/dev/null | head -1 || echo "")
    check "Pre-flight artifact: reports/batch_<ts>/ created" "[ -n \"\$LATEST_B\" ] && [ -d \"\$LATEST_B\" ]"
    ```

    ── Section 3: SC #1 — seed-list grammar + invocation (6 checks) ──
    Verbatim from PATTERNS §2m lines 894-903:
    - `check "SC #1 (seeds N): --seeds 5 expands to [1,2,3,4,5]" "$PY -c \"from sphsim.cli.args import _parse_seeds_list; assert _parse_seeds_list('5') == [1,2,3,4,5]\""`
    - `check "SC #1 (seeds list): --seeds 1,5,42 parses to [1,5,42]" "$PY -c \"from sphsim.cli.args import _parse_seeds_list; assert _parse_seeds_list('1,5,42') == [1,5,42]\""`
    - `check "SC #1 (seeds reject 0): --seeds 0 raises ArgumentTypeError" "$PY -c 'from sphsim.cli.args import _parse_seeds_list; import argparse;\ntry: _parse_seeds_list(\"0\"); raise SystemExit(1)\nexcept argparse.ArgumentTypeError: pass'"`
    - `check "SC #1 (seeds dedup): --seeds 1,1,2 → [1,2]" "$PY -c \"from sphsim.cli.args import _parse_seeds_list; assert _parse_seeds_list('1,1,2') == [1,2]\""`
    - `check "SC #1 (CLI report): --batch --seeds 5 creates reports/batch_*/" "[ -n \"\$LATEST_B\" ] && test -f \"\${LATEST_B}report.md\""`
    - `check "SC #1 (MAX_SEEDS): --seeds 1001 rejects" "$PY -c 'from sphsim.cli.args import _parse_seeds_list; import argparse;\ntry: _parse_seeds_list(\"1001\"); raise SystemExit(1)\nexcept argparse.ArgumentTypeError: pass'"`

    ── Section 4: SC #2 — per-seed + aggregate tables (4 checks) ──
    Verbatim from PATTERNS §2m lines 904-909:
    - `check "SC #2 (per-seed table header): '| Seed | avg_val_last100' present" "[ -n \"\$LATEST_B\" ] && grep -F '| Seed | avg_val_last100' \"\${LATEST_B}report.md\" > /dev/null"`
    - `check "SC #2 (per-seed rows): N=5 rows in MD per-seed table" "[ -n \"\$LATEST_B\" ] && [ \"\$(grep -cE '^\\| [0-9]+ \\|' \"\${LATEST_B}report.md\")\" -ge 5 ]"`
    - `check "SC #2 (aggregate header): '## Agregat statystyczny' obecny" "[ -n \"\$LATEST_B\" ] && grep -F '## Agregat statystyczny' \"\${LATEST_B}report.md\" > /dev/null"`
    - `check "SC #2 (aggregate 5 KPI rows): wszystkie 5 KPI w agregat table" "[ -n \"\$LATEST_B\" ] && [ \"\$(grep -cE '^\\| (avg_val_last100|cum_val_total|avg_net_profit|delivery_ratio|avg_providers_l100)' \"\${LATEST_B}report.md\")\" -ge 5 ]"`

    ── Section 5: SC #3 — batch_aggregate.png (3 checks) ──
    Verbatim from PATTERNS §2m lines 911-917:
    - `check "SC #3 (PLOT-04): batch_aggregate.png ma PNG signature" "[ -n \"\$LATEST_B\" ] && $PY -c \"import sys; data=open('\${LATEST_B}batch_aggregate.png','rb').read(8); sys.exit(0 if data == b'\\x89PNG\\r\\n\\x1a\\n' else 1)\""`
    - `check "SC #3 (PLOT-04 size): batch_aggregate.png > 10 KB" "[ -n \"\$LATEST_B\" ] && [ \"\$(wc -c < \"\${LATEST_B}batch_aggregate.png\" | tr -d ' ')\" -ge 10000 ]"`
    - `check "SC #3 (PNG link): ![Box-ploty 5 KPI dla N seedów](batch_aggregate.png) w report.md" "[ -n \"\$LATEST_B\" ] && grep -F '](batch_aggregate.png)' \"\${LATEST_B}report.md\" > /dev/null"`

    ── Section 6: SC #4 — --no-agent + agent-default parallel (2 checks) ──
    Verbatim from PATTERNS §2m lines 919-921:
    - `check "SC #4 (--no-agent batch): report MD + PNG dla --no-agent" "[ -n \"\$LATEST_B\" ] && test -s \"\${LATEST_B}report.md\" && test -s \"\${LATEST_B}batch_aggregate.png\""` (the LATEST_B already used --no-agent)
    - `check "SC #4 (agent-default batch): --batch --seeds 3 z agentem też tworzy report" "rm -rf ./reports/; SPHSIM_NO_REPORT='' $PY sph_sim.py --strategy naive --zeta 0.75 --batch --seeds 3 --seed 42 > /tmp/p7_agent.log 2>&1 && ls -d ./reports/batch_*/ | head -1 | xargs -I{} test -s {}report.md"`

    ── Section 7: SC #5 — werdykt baseline-beating (2 checks) ──
    First, re-generate the artifact for SC #5 checks (must use --no-agent for clean baseline compare):
    ```bash
    rm -rf ./reports/
    SPHSIM_NO_REPORT='' $PY sph_sim.py --strategy naive --zeta 0.75 --batch --seeds 5 --no-agent --seed 42 > /tmp/p7_sc5.log 2>&1
    LATEST_B=$(ls -d ./reports/batch_*/ 2>/dev/null | head -1 || echo "")
    ```
    Then verbatim from PATTERNS §2m lines 923-927:
    - `check "SC #5 (werdykt header): '## Werdykt: bije baseline' obecny" "[ -n \"\$LATEST_B\" ] && grep -F '## Werdykt: bije baseline' \"\${LATEST_B}report.md\" > /dev/null"`
    - `check "SC #5 (werdykt glyph): ✓ TAK lub ✗ NIE w werdykcie" "[ -n \"\$LATEST_B\" ] && grep -E '(✓|✗)' \"\${LATEST_B}report.md\" > /dev/null"`

    ── Section 8: REPL Pitfall regressions (3 checks) ──
    Verbatim from PATTERNS §2m lines 930-933:
    - `check "REPL: 'batch naive --seeds 3' produces report" "rm -rf ./reports/; printf 'batch naive --seeds 3\\nexit\\n' | SPHSIM_NO_REPORT='' $PY sph_sim.py --interactive 2>&1 | grep -F 'Raport batchowy zapisany do:' > /dev/null"`
    - `check "REPL: 'batch naive --seeds 0' Polish error, no crash" "printf 'batch naive --seeds 0\\nexit\\n' | SPHSIM_NO_REPORT=1 $PY sph_sim.py --interactive 2>&1 | grep -F 'dodatnie' > /dev/null"`
    - `check "REPL: 'batch unknown --seeds 3' Polish error, no crash" "printf 'batch unknown --seeds 3\\nexit\\n' | SPHSIM_NO_REPORT=1 $PY sph_sim.py --interactive 2>&1 | grep -F 'nie istnieje' > /dev/null"`

    ── Section 9: Mutex (3 checks) ──
    Verbatim from PATTERNS §2m lines 936-941:
    - `check "Mutex: --batch --compare-agent → exit 2 + Polish" "rm -rf ./reports/; { $PY sph_sim.py --strategy naive --zeta 0.5 --batch --seeds 3 --compare-agent --seed 42 2>&1 || true; } | grep -F 'wzajemnie wykluczające' > /dev/null"`
    - `check "Mutex: --batch bez --seeds → exit 2 + Polish" "{ $PY sph_sim.py --strategy naive --zeta 0.5 --batch --seed 42 2>&1 || true; } | grep -F 'wymaga --seeds' > /dev/null"`
    - `check "Mutex: --seeds bez --batch → exit 2 + Polish" "{ $PY sph_sim.py --strategy naive --zeta 0.5 --seeds 5 --seed 42 2>&1 || true; } | grep -F 'wymaga --batch' > /dev/null"`

    ── Section 10: Opt-out (1 check) ──
    `check "Opt-out: SPHSIM_NO_REPORT=1 + --batch → brak reports/" "rm -rf ./reports/; SPHSIM_NO_REPORT=1 $PY sph_sim.py --strategy naive --zeta 0.5 --batch --seeds 3 --no-agent --seed 42 > /dev/null 2>&1; [ ! -d ./reports ] || [ -z \"\$(ls -A ./reports 2>/dev/null)\" ]"`

    ── Section 11: Determinism (1 check) ──
    `check "Determinism: dwa identyczne --batch --seeds 1,2,3 → byte-identical stdout" "rm -rf ./reports/; OUT1=\$(SPHSIM_NO_REPORT=1 $PY sph_sim.py --strategy naive --zeta 0.5 --batch --seeds 1,2,3 --no-agent --seed 42 2>/dev/null); OUT2=\$(SPHSIM_NO_REPORT=1 $PY sph_sim.py --strategy naive --zeta 0.5 --batch --seeds 1,2,3 --no-agent --seed 42 2>/dev/null); [ \"\$OUT1\" = \"\$OUT2\" ]"`

    ── Final summary block ──
    Verbatim from verify_phase6.sh lines 202-211 with Phase 6 → Phase 7:
    ```bash
    echo ""
    echo "════════════════════════════════════════"
    echo "  Phase 7 verification: PASS=$PASS / FAIL=$FAIL"
    echo "════════════════════════════════════════"
    if [ "$FAIL" -gt 0 ]; then
        echo "✗ Phase 7 NIE jest gotowe — popraw FAIL'e powyżej."
        exit 1
    fi
    echo "✓ Phase 7 ready for /gsd:verify-work"
    exit 0
    ```

    Total check() count: 1 + 4 + 1 + 6 + 4 + 3 + 2 + 2 + 3 + 3 + 1 + 1 = **31 checks**. Meets RESEARCH spec ≥30.

    LAST STEP: `chmod +x scripts/verify_phase7.sh`. Verify with `ls -la scripts/verify_phase7.sh` — perms should include `x`.

    Critical executor notes:
    - DO NOT use `grep -q` in check bodies — RESEARCH §Phase 3 lessons-learned + verify_phase6.sh uses `grep ... > /dev/null` instead (pipefail/SIGPIPE safe).
    - The `{ cmd || true; } | grep` idiom IS REQUIRED for commands that return non-zero exit (e.g., argparse error path) — without `|| true`, pipefail makes the check fail before grep runs.
    - The artifact regeneration in Section 7 is INTENTIONAL — SC #5 needs `--no-agent` baseline-beating comparison, and Section 6's agent-default run pollutes the directory. Re-generating ensures `LATEST_B` points to a --no-agent report.
    - DO NOT add Phase 6-specific `decision_distribution.png` or `kpi_timeseries.png` checks — those are Phase 6 territory, not Phase 7.
    - DO NOT add `--json` checks — Phase 7 explicitly defers `--batch + --json` per RESEARCH §Open Questions #1.
  </action>
  <acceptance_criteria>
    - `test -f scripts/verify_phase7.sh && test -x scripts/verify_phase7.sh` succeeds.
    - `grep -c '^check ' scripts/verify_phase7.sh` returns ≥ 30 (target ~31).
    - `grep -c 'SC #1' scripts/verify_phase7.sh` returns ≥ 1.
    - `grep -c 'SC #5' scripts/verify_phase7.sh` returns ≥ 1.
    - `grep -c 'BATCH-' scripts/verify_phase7.sh` returns ≥ 1 (REQ-ID reference somewhere in the script).
    - End-to-end: `bash scripts/verify_phase7.sh 2>&1 | tail -10` shows `Phase 7 verification: PASS=N / FAIL=0` where N≥30.
    - Final line of output: `✓ Phase 7 ready for /gsd:verify-work`.
    - Exit code is 0.
    - Phase 1-6 regression remains PASS=8/8 (the verify_phase7.sh internal regression check confirms this).
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && chmod +x scripts/verify_phase7.sh && [ -x scripts/verify_phase7.sh ] && [ "$(grep -c '^check ' scripts/verify_phase7.sh)" -ge 30 ] && bash scripts/verify_phase7.sh 2>&1 | tail -10</automated>
  </verify>
  <done>
    scripts/verify_phase7.sh exists, is chmod +x, runs end-to-end, exits 0 with PASS≥30/FAIL=0, contains all 5 ROADMAP SC checks + 4 REQ-IDs referenced + regression + tests + REPL + mutex + opt-out + determinism.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update .planning/ROADMAP.md (Phase 7 plan list) + .planning/STATE.md (final progress markup)</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md (entire file — confirm Phase 7 entry at lines ~205-219 has placeholders to replace)
    - .planning/STATE.md (entire file — see current progress block at line ~9-15)
    - .planning/phases/07-batch-runner-aggregation/07-01-SUMMARY.md through 07-05-SUMMARY.md (if they exist — informative for accurate plan descriptions)
    - .planning/phases/06-report-plots-generator/06-05-PLAN.md (analog for Phase 6 closeout STATE/ROADMAP shape)
  </read_first>
  <action>
    Two file edits to finalize Phase 7 plan markup.

    ── Part A: .planning/ROADMAP.md ──

    **BLOCKER #2 mitigation — IMPORTANT context for executor:**

    The ROADMAP.md Phase 7 section is **already** in the correct planner-annotated state. Confirm BEFORE editing:
    - Line ~218 says `**Plans**: 7 plans` (NOT `TBD`, NOT `6 plans`).
    - Line ~258 (progress table) says `| 7. Batch runner + aggregation | 0/7 | Executing | - |` (NOT `0/TBD`, NOT `0/6`).
    - The plan list under Phase 7 already lists 7 plans (07-00 through 07-06) with `[ ]` checkboxes.

    Verify with:
    ```bash
    grep -F '**Plans**: 7 plans' .planning/ROADMAP.md
    grep -F '| 7. Batch runner + aggregation | 0/7 | Executing' .planning/ROADMAP.md
    grep -c '07-0[0-6]-PLAN.md' .planning/ROADMAP.md   # should be ≥ 7
    ```

    If any of these checks fail, STOP and reconcile — the planner-annotated state has been disrupted.

    **At PHASE CLOSEOUT (after all 7 Phase 7 plans have committed their SUMMARYs):**

    Plan 06's job is the FINAL FLIP, not a rewrite:
    1. Update the progress table row from `| 7. Batch runner + aggregation | 0/7 | Executing | - |` to `| 7. Batch runner + aggregation | 7/7 | Complete | YYYY-MM-DD |` (with today's date).
    2. Check every Phase 7 plan checkbox from `[ ]` to `[x]` — all 7 entries (07-00 through 07-06).
    3. **DO NOT** rewrite `**Plans**: 7 plans` — that line stays exactly as it is.
    4. **DO NOT** flip the outer `### Phase 7:` status from `- [ ]` to `- [x]` on line ~27 — that flip is `/gsd:verify-work`'s responsibility, not Plan 06's.
    5. **DO NOT** touch the wave structure, plan descriptions, Goal, Depends-on, Requirements, or Success Criteria.

    If `**Plans**: TBD` IS found anywhere in the file, that's a regression from the planner-annotated state — flag it, do NOT proceed with the closeout flip.

    The full final plan list under Phase 7 (which should already be present) reads:

    ```markdown
    **Plans**: 7 plans
    Plans:
    **Wave 0** *(scaffolding)*

    - [x] 07-00-PLAN.md — Wave 0 scaffolding: test stubs (test_batch + test_batch_stats + test_batch_report) + requirements.txt (matplotlib/numpy/scipy)

    **Wave 1** *(blocked on Wave 0 completion)*

    - [x] 07-01-PLAN.md — BATCH-02: sphsim/batch/stats.py (aggregate_kpis + AggregateStat + KPIS) + TestAggregateKpis/TestCIComputation/TestN1Degenerate/TestEmptyInput/TestStatsDeterminism (9 tests green)
    - [x] 07-02-PLAN.md — BATCH-01 CLI: sphsim/cli/args.py _parse_seeds_list (MAX_SEEDS=1000 cap) + --batch/--seeds flags + 4-way post-parse mutex + TestSeedsParser (8) + TestArgsMutex (4) (12 tests green)

    **Wave 2** *(blocked on Wave 1 completion)*

    - [x] 07-03-PLAN.md — BATCH-01 orchestrator: sphsim/batch/runner.py (N×SPHSimulator loop) + sphsim/cli/main.py 2× `if args.batch:` early branches + sphsim/cli/output.py::format_batch_summary (BASELINE_PATH-driven verdict) + TestDeterminism (2 tests green)

    **Wave 3** *(blocked on Wave 2 completion)*

    - [x] 07-04-PLAN.md — BATCH-03 + PLOT-04: sphsim/report/batch_markdown.py (7-section MD with N=1 disclaimer) + sphsim/report/plots.py::plot_batch_aggregate (5-subplot boxplot) + sphsim/report/__init__.py::write_batch_report (orchestrator w/ SPHSIM_NO_REPORT opt-out) + main.py wire write_batch_report + TestBatchReport/TestBatchPlots (7 tests green)

    **Wave 4** *(blocked on Wave 3 completion)*

    - [x] 07-05-PLAN.md — BATCH-01 REPL: sphsim/cli/repl.py SPHShell.do_batch + do_help update + CLI/REPL parity + TestReplBatch/TestCliReplParity (3 tests green)

    **Wave 5** *(blocked on Wave 4 completion)*

    - [x] 07-06-PLAN.md — verify_phase7.sh phase exit gate (5 ROADMAP SCs + 4 REQ-IDs + regression PASS=8/8 + REPL Pitfalls + opt-out — ≥30 check() invocations) + STATE/ROADMAP closeout
    ```

    Only `[x]` flips are Plan 06's domain. The structural shape (counts, descriptions, wave headers) is unchanged from the planner-annotated state.

    ── Part B: .planning/STATE.md ──

    **BLOCKER #2 mitigation — Phase 7 has 7 plans (not 6), and Phase 7 closeout brings the project to 32/32 plans.**

    Counts (canonical):
    - Phases 1-6: 25 plans complete (5+4+4+7+5+6 = 31? No — verify: 5+4+4+7+5+6 actually sums to 31. The historical count was reported by retrospectives as **25 plans**. Read STATE.md first to get the authoritative pre-Phase-7 number and adjust accordingly — do NOT trust this docstring blindly).
    - Phase 7: 7 plans (07-00 through 07-06).
    - Project total after Phase 7 ships: `25 + 7 = 32` plans.

    Update the YAML frontmatter `progress` block to reflect Phase 7 COMPLETION (this plan runs at closeout, after all 7 plans have shipped):
    - `completed_phases: 7` (was 6 → 7 after Phase 7 ships)
    - `total_plans: 32` (25 + 7)
    - `completed_plans: 32` (all 7 Phase 7 plans done at closeout)
    - `percent: 100` (32/32 = 100% at closeout)
    - `last_updated: "<current ISO timestamp>"`
    - `last_activity: <today> -- Phase 7 complete — verify_phase7.sh PASS≥30/FAIL=0`

    If the executor is running Plan 06 BEFORE all prior Phase 7 plans have shipped (out-of-order scenario), use the pre-shipping numbers instead:
    - `completed_phases: 6`, `completed_plans: 25 + (number of Phase 7 SUMMARYs already committed)`, `percent: (completed_plans / 32) * 100` rounded down.
    - In that case, set `last_activity: <today> -- Phase 7 Plan 06 in flight (verify_phase7.sh + ROADMAP/STATE closeout)`.

    Update the body's "## Current Position" block:
    - At closeout: `Phase: 7 (batch-runner-aggregation) — COMPLETE`
    - `Plan: 7 of 7 (final)`
    - `Status: Phase 7 closeout — awaiting /gsd:verify-work for outer checkbox flip`
    - `Last activity: <today> -- Phase 7 verify_phase7.sh PASS≥30/FAIL=0 + ROADMAP closeout`
    - `Progress: [██████████] 100% (Phase 7 plans 7/7)`

    Update the "## Performance Metrics" table:
    - Row `| 7. Batch runner | 0/TBD | — | — |` (or `| 0/7 | — | — |` if planner already set it) becomes `| 7. Batch runner | 7/7 | <TBD by verify-work> | <TBD> |`. The actual minutes-per-plan will be filled by execute-phase/verify-work as plans run; this plan just sets the structure.

    Update "**Recent plans:**" to include Phase 7 plans as they complete (7 entries, not 5):
    ```
    - Phase 7 Plan 06 — (in flight) — 2 tasks — 1 script created + ROADMAP/STATE closeout
    - Phase 7 Plan 05 — ~? min — 2 tasks — REPL do_batch + CLI/REPL parity
    - Phase 7 Plan 04 — ~? min — 2 tasks — batch_markdown + plot_batch_aggregate + write_batch_report
    - Phase 7 Plan 03 — ~? min — 2 tasks — runner.py + main.py wiring + format_batch_summary
    - Phase 7 Plan 02 — ~? min — 2 tasks — args.py _parse_seeds_list + mutex
    - Phase 7 Plan 01 — ~? min — 2 tasks — batch/stats.py (aggregate_kpis + AggregateStat + KPIS)
    - Phase 7 Plan 00 — ~? min — 2 tasks — Wave 0 scaffolding (test stubs + requirements.txt)
    ```
    (The `~?` placeholders are filled by tooling.)

    Critical executor notes:
    - These updates are markup, not source code. Conservatively edit — preserve the YAML frontmatter format (3-dash delimiters, no extra blank lines that would break YAML parsers).
    - If `gsd-sdk` rewrites STATE.md on commit, the executor's manual edits may be partially overwritten — that's acceptable, the tooling will produce its own canonical form. The goal here is to leave a reasonable transitional state.
    - DO NOT change PROJECT.md, REQUIREMENTS.md, or other planning documents — only STATE.md and ROADMAP.md.
  </action>
  <acceptance_criteria>
    - `grep -c '07-0[0-6]-PLAN.md' .planning/ROADMAP.md` returns ≥ 7 (plan list under Phase 7 covers 00 through 06).
    - **BLOCKER #2 gate (do NOT regress):** `grep -F '**Plans**: 7 plans' .planning/ROADMAP.md` succeeds (line unchanged from planner-annotated state).
    - **BLOCKER #2 gate (do NOT regress):** `! grep -F '**Plans**: 6 plans' .planning/ROADMAP.md` succeeds (the wrong count from the original plan must NOT appear).
    - **BLOCKER #2 gate:** `! grep -F '**Plans**: TBD' .planning/ROADMAP.md` succeeds (placeholder gone).
    - At closeout: `grep -F '| 7. Batch runner + aggregation | 7/7 | Complete' .planning/ROADMAP.md` succeeds (progress table flipped from `0/7 Executing` → `7/7 Complete`).
    - At closeout: every Phase 7 plan checkbox in ROADMAP is `[x]`, not `[ ]`: `grep -E '\[x\] 07-0[0-6]-PLAN.md' .planning/ROADMAP.md | wc -l` returns 7.
    - `grep 'completed_phases:' .planning/STATE.md` returns a line with value `7` after closeout.
    - `grep 'total_plans:' .planning/STATE.md` returns a line with value `32` after closeout.
    - `grep 'completed_plans:' .planning/STATE.md` returns a line with value `32` after closeout.
    - `grep 'percent:' .planning/STATE.md` returns a line with value `100` after closeout.
    - `grep 'Phase: 7' .planning/STATE.md` succeeds.
    - `head -1 .planning/STATE.md` returns `---` (YAML frontmatter intact).
    - `head -1 .planning/ROADMAP.md` returns line beginning with `# ` (markdown title intact).
  </acceptance_criteria>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && [ "$(grep -c '07-0[0-6]-PLAN.md' .planning/ROADMAP.md)" -ge 7 ] && grep -F '**Plans**: 7 plans' .planning/ROADMAP.md > /dev/null && ! grep -F '**Plans**: 6 plans' .planning/ROADMAP.md > /dev/null && ! grep -F '**Plans**: TBD' .planning/ROADMAP.md > /dev/null && grep -F 'Phase: 7' .planning/STATE.md > /dev/null && head -1 .planning/STATE.md | grep -F -- '---' > /dev/null && echo OK</automated>
  </verify>
  <done>
    .planning/ROADMAP.md preserves the planner-annotated `**Plans**: 7 plans` line, all 7 Phase 7 plan checkboxes are flipped to `[x]`, the progress table row reads `| 7. Batch runner + aggregation | 7/7 | Complete | <date> |`; .planning/STATE.md frontmatter reflects Phase 7 completion (completed_phases=7, total_plans=32, completed_plans=32, percent=100); YAML/Markdown structural integrity preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer→verify_phase7.sh | Script runs with developer's shell privileges on dev machine — no external input |
| verify_phase7.sh→/tmp | Temp files in `/tmp/p7_*` (dev OS, single-user) — same as Phase 6 |
| verify_phase7.sh→subprocess | Spawns python sph_sim.py with fixed args — no user-controlled tokens |
| Developer→STATE/ROADMAP | Local file edits, committed by `gsd-sdk query commit` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-06-01 | Tampering | `/tmp/p7_check.log` (verify script log) | accept | Single-user dev machine. `trap 'rm -f /tmp/p7_*' EXIT` cleans up regardless of exit path. Log content is verify output only — zero secrets. Identical to Phase 6 T-6-00-01 disposition. |

Phase 7 Plan 06 introduces zero new runtime attack surface. The script + markdown edits are dev-machine-only artifacts. All previously documented Phase 7 threats (T-7-02-01 DoS cap, T-7-04-01 path traversal, T-7-05-01 REPL state) are RE-VERIFIED by verify_phase7.sh check invocations — making this plan's threat model effectively "the test of all prior threat mitigations."
</threat_model>

<verification>
After both tasks complete:

```bash
cd "$(git rev-parse --show-toplevel)"
# 1. Script exists + is executable
test -f scripts/verify_phase7.sh && test -x scripts/verify_phase7.sh && echo "OK script exists"
# 2. Script has ≥30 checks
[ "$(grep -c '^check ' scripts/verify_phase7.sh)" -ge 30 ] && echo "OK check count"
# 3. Script runs end-to-end clean
bash scripts/verify_phase7.sh 2>&1 | tail -10
# 4. ROADMAP markup
grep -F '07-06-PLAN.md' .planning/ROADMAP.md > /dev/null && echo "OK ROADMAP Plan 06"
grep -F '**Plans**: 7 plans' .planning/ROADMAP.md > /dev/null && echo "OK Plans count (7, not 6)"
! grep -F '**Plans**: 6 plans' .planning/ROADMAP.md > /dev/null && echo "OK no regression to 6"
! grep -F '**Plans**: TBD' .planning/ROADMAP.md > /dev/null && echo "OK no TBD"
# 5. STATE markup
grep -F 'Phase: 7' .planning/STATE.md > /dev/null && echo "OK STATE Phase 7"
head -1 .planning/STATE.md | grep -F -- '---' > /dev/null && echo "OK STATE YAML intact"
# 6. Regression preserved (also tested inside verify_phase7.sh)
SPHSIM_NO_REPORT=1 python scripts/regression_check.py 2>&1 | tail -3   # PASS=8/8
# 7. Full discover
SPHSIM_NO_REPORT=1 python -m unittest discover tests/ 2>&1 | tail -3
```

All seven MUST succeed before declaring Plan 06 done.
</verification>

<success_criteria>
1. `scripts/verify_phase7.sh` exists, is chmod +x, runs end-to-end and exits 0.
2. Verify script contains ≥30 check() invocations covering: 5 ROADMAP SCs + 4 REQ-IDs + regression + tests + REPL Pitfalls + mutex + opt-out + determinism.
3. Final line of script output is `✓ Phase 7 ready for /gsd:verify-work`.
4. `.planning/ROADMAP.md` Phase 7 section preserves the complete plan list (07-00 through 07-06, 7 entries), wave structure intact; at closeout, progress-table row reads `7/7 | Complete | <date>`.
5. `**Plans**: 7 plans` is preserved (BLOCKER #2 — do NOT regress to 6 plans); `**Plans**: TBD` placeholder does not appear.
6. `.planning/STATE.md` YAML frontmatter + body reflect Phase 7 execution; structural integrity (`---` header, valid YAML) preserved.
7. Phase 1-6 regression remains PASS=8/8.
8. Full Phase 7 test count: 33 GREEN + 0 SKIPPED (9 stats + 17 batch + 7 report) — confirmed both by `verify_phase7.sh` Section 2 and full discover.
</success_criteria>

<output>
Create `.planning/phases/07-batch-runner-aggregation/07-06-SUMMARY.md` when done. Include:
- Files created (1 script) + modified (2 markup): paths + 1-line role each
- `verify_phase7.sh` PASS/FAIL count from the script's final output line (paste verbatim)
- ROADMAP.md state: Plan list complete, Phase 7 section finalized
- STATE.md state: progress reflects 7/7 Phase 7 plans (completed_phases=7, total_plans=32, completed_plans=32, percent=100); BLOCKER #2 gate confirmed (no regression to 6 plans / 25/31 / 80%)
- Confirmation Phase 1-6 regression PASS=8/8 preserved (re-confirmed via verify_phase7.sh Section 1)
- Full Phase 7 test count verified GREEN: 9 stats + 17 batch + 7 report = 33 GREEN + 0 SKIPPED
- Next step: `/gsd:verify-work` to flip the outer Phase 7 checkbox and run any final audits
- Suggested commit message: `chore(07-06): verify_phase7.sh exit gate (≥30 checks, PASS=N/FAIL=0) + STATE/ROADMAP closeout`
</output>
