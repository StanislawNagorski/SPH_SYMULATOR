---
phase: 02-interactive-cli-shell
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sphsim/cli/args.py
autonomous: true
requirements:
  - CLI-01

must_haves:
  truths:
    - "argparse w sphsim/cli/args.py udostępnia flagę --interactive (boolean store_true), dostępną jako args.interactive (D-27)"
    - "--interactive i --strategy są w add_mutually_exclusive_group(required=True) — dokładnie jedna musi być podana (D-27/D-28)"
    - "Wywołanie `python sph_sim.py --interactive --strategy naive` kończy się argparse error (mutex violation)"
    - "Wywołanie `python sph_sim.py` (bez żadnej flagi) kończy się argparse error (required mutex)"
    - "Wywołanie `python sph_sim.py --strategy naive --zeta 0.5` nadal parsuje się poprawnie (backwards compat — Phase 1 regression nie złamana, D-28)"
    - "Docstring w sphsim/cli/args.py ma 'Autorzy: Stanisław Nagórski, Mikołaj Rutkowski' zamiast 'Autor: Mikołaj Rutkowski' (D-23 housekeeping)"
    - "Reszta argparse setup (--zeta, --max_phase, --probs, --s_target, --expected_P, --nU, --nSUS, --K1, --T, --kappa, --alpha, --seed, --json, --verbose) NIE jest zmieniona — nazwy, typy i defaulty identyczne"
  artifacts:
    - path: "sphsim/cli/args.py"
      provides: "parse_args() z mutex group --interactive | --strategy + docstring update"
      contains: "add_mutually_exclusive_group"
  key_links:
    - from: "sphsim/cli/args.py mutex group"
      to: "sphsim/cli/main.py (Plan 03 doda gałąź `if args.interactive: run_repl(); return`)"
      via: "args.interactive boolean attribute"
      pattern: "interactive.*store_true|store_true.*interactive"
---

<objective>
Dodać flagę `--interactive` do `sphsim/cli/args.py` w `add_mutually_exclusive_group(required=True)` razem z `--strategy`. Wymusza dwa wyraźne tryby uruchomienia: REPL ALBO one-shot. Plus drobny housekeeping update w docstringu (D-23).

Purpose: Spełnia CLI-01 (`--interactive` jako entry-point flag). Tworzy fundament pod Plan 03 (`sphsim/cli/main.py` doda wczesną gałąź `if args.interactive: run_repl(); return`). Mutex jest celowy — nie ma sensu mieszać REPL i one-shot w jednej inwokacji.

Output: `sphsim/cli/args.py` z mutex group, zaktualizowany docstring, zachowana backwards compat dla 8 Phase 1 fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-interactive-cli-shell/02-CONTEXT.md
@sphsim/cli/args.py
@scripts/regression_check.py
</context>

<interfaces>
<!-- Current argparse contract in sphsim/cli/args.py lines 32-57 (executor must preserve all of this) -->

Existing add_argument calls that MUST remain identical:
- `--zeta`        type=float, default=0.5
- `--max_phase`   type=int,   default=3
- `--probs`       type=str,   default='0.9,0.7,0.5,0.3,0.0'
- `--s_target`    type=int,   default=10
- `--expected_P` type=float, default=100.0
- `--nU`         type=int,   default=DEFAULT_NU
- `--nSUS`       type=int,   default=DEFAULT_NSUS
- `--K1`         type=float, default=DEFAULT_K1
- `--T`          type=int,   default=DEFAULT_T
- `--kappa`      type=float, default=DEFAULT_KAPPA
- `--alpha`      type=float, default=DEFAULT_ALPHA
- `--seed`       type=int,   default=42
- `--json`       action='store_true'
- `--verbose`    action='store_true'

`--strategy` MUST move from top-level `required=True` into a mutex group as the SECOND member.

Mutex group contract (D-27/D-28):
```python
mutex = p.add_mutually_exclusive_group(required=True)
mutex.add_argument('--interactive', action='store_true', help='Uruchom tryb interaktywny (REPL)')
mutex.add_argument('--strategy', choices=list(STRATEGIES.keys()),
                   help='Strategia: ' + ', '.join(STRATEGIES.keys()))
```

Note: `--strategy` keeps its `choices=` and `help=` but LOSES `required=True` (mutex group's required=True takes over).

Phase 1 regression invocations (from scripts/generate_baseline.py INVOCATIONS list — must all still parse):
- `--strategy naive --zeta 0.5`
- `--strategy threshold --max_phase 3`
- `--strategy phase_prob` (default probs)
- `--strategy incentive --expected_P 100`
- `--strategy adaptive --s_target 10`
- `--strategy naive --zeta 0.4 --nU 200 --nSUS 20 --K1 120 --T 1000`
- `--strategy phase_prob --probs 1.0,0.8,0.6,0.2,0.0 --kappa 0.5 --alpha 0`
- `--strategy naive --zeta 0.75`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add --interactive in mutex group + update docstring</name>
  <files>sphsim/cli/args.py</files>

  <read_first>
    - sphsim/cli/args.py (current state — full file, 57 lines)
    - .planning/phases/02-interactive-cli-shell/02-CONTEXT.md (re-read D-23, D-27, D-28 in full)
    - scripts/regression_check.py (understand backwards-compat oracle)
    - scripts/generate_baseline.py (INVOCATIONS list — 8 fixtures all use --strategy)
  </read_first>

  <action>
  Per D-23: Update the module docstring `Autor: Mikołaj Rutkowski` (line 4) to `Autorzy: Stanisław Nagórski, Mikołaj Rutkowski` (plural, both authors). Single-line change inside the existing `"""..."""` block. Do not modify any other line of the docstring.

  Per D-27/D-28: Refactor the argparse `--strategy` setup. Currently (line 38) it's:
  ```
  p.add_argument('--strategy', required=True, choices=list(STRATEGIES.keys()), help='Strategia: ' + ', '.join(STRATEGIES.keys()))
  ```
  Replace with a `add_mutually_exclusive_group(required=True)`. The group has TWO members in this order: `--interactive` first (boolean `action='store_true'`, help in Polish `'Uruchom tryb interaktywny (REPL)'`), then `--strategy` (keeps `choices=list(STRATEGIES.keys())` and the existing `help=` string but REMOVES `required=True` because the mutex group is required).

  Place the mutex group at the same location where `--strategy` currently lives (between description epilog setup and the strategy-parameter args block — i.e., before the `# Parametry strategii` comment on line 40).

  Do NOT touch any other add_argument calls. Do NOT change the order of remaining args. Do NOT introduce new imports (argparse and STRATEGIES are already imported).

  Confirm by inspection: parse_args() still returns a Namespace with fields matching all existing fields PLUS `interactive` (bool, default False when --strategy used).
  </action>

  <verify>
    <automated>python -c "import sphsim.cli.args; ns = sphsim.cli.args.parse_args.__wrapped__() if hasattr(sphsim.cli.args.parse_args, '__wrapped__') else None; print('module loads')"</automated>
    <automated>python -c "import subprocess, sys; r = subprocess.run([sys.executable, 'sph_sim.py', '--interactive', '--strategy', 'naive'], capture_output=True, text=True); assert r.returncode != 0 and ('not allowed' in r.stderr or 'mutually exclusive' in r.stderr.lower()), f'expected mutex error, got: {r.stderr}'; print('mutex OK')"</automated>
    <automated>python -c "import subprocess, sys; r = subprocess.run([sys.executable, 'sph_sim.py'], capture_output=True, text=True); assert r.returncode != 0 and ('required' in r.stderr.lower() or 'one of' in r.stderr.lower()), f'expected required-mutex error, got: {r.stderr}'; print('required mutex OK')"</automated>
    <automated>python sph_sim.py --strategy naive --zeta 0.5 --seed 42 --json > /dev/null && echo 'backwards compat OK'</automated>
    <automated>python scripts/regression_check.py</automated>
    <automated>grep -c 'Autorzy: Stanisław Nagórski, Mikołaj Rutkowski' sphsim/cli/args.py</automated>
    <automated>grep -c 'add_mutually_exclusive_group' sphsim/cli/args.py</automated>
    <automated>grep -c "'--interactive'" sphsim/cli/args.py</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'Autorzy: Stanisław Nagórski, Mikołaj Rutkowski' sphsim/cli/args.py` == 1 (D-23)
    - `grep -c 'Autor: Mikołaj Rutkowski' sphsim/cli/args.py` == 0 (singular form replaced)
    - `grep -c 'add_mutually_exclusive_group(required=True)' sphsim/cli/args.py` == 1 (D-27)
    - `grep -c "'--interactive'" sphsim/cli/args.py` >= 1
    - `grep -c "store_true.*interactive\\|interactive.*store_true" sphsim/cli/args.py` >= 1 (boolean flag)
    - `grep -c "'--strategy', required=True" sphsim/cli/args.py` == 0 (required moved to mutex group)
    - `python sph_sim.py --interactive --strategy naive 2>&1; test $? -ne 0` exits non-zero with mutex error
    - `python sph_sim.py 2>&1; test $? -ne 0` exits non-zero with required-one-of error
    - `python sph_sim.py --strategy naive --zeta 0.5 --seed 42 --json` exits 0 and prints valid JSON
    - `python scripts/regression_check.py` exits 0 (CLI-04 backwards compat preserved — D-28)
    - `python -c "from sphsim.cli.args import parse_args; import sys; sys.argv = ['x', '--strategy', 'naive']; ns = parse_args(); assert ns.interactive is False and ns.strategy == 'naive'"` exits 0
  </acceptance_criteria>

  <done>
  `sphsim/cli/args.py` exposes `--interactive` in a required mutex group with `--strategy`. Docstring lists both authors. All 8 Phase 1 baseline invocations still parse and run identically (regression_check.py exits 0). Plan 03 can now wire `args.interactive` to `run_repl()` in `sphsim/cli/main.py`.
  </done>
</task>

</tasks>

<verification>
- argparse mutex group `--interactive | --strategy` is `required=True`.
- `--interactive` is boolean store_true; default False when omitted.
- `--strategy` keeps its `choices=` validation and Polish `help=` text.
- Docstring header lists both authors (Stanisław Nagórski, Mikołaj Rutkowski).
- Phase 1 regression check (`scripts/regression_check.py`) passes (8/8 fixtures).
- No other CLI args modified.
</verification>

<success_criteria>
- `python sph_sim.py --interactive` parses cleanly (args.interactive=True, args.strategy=None). [Plan 03 wires the run_repl() branch.]
- `python sph_sim.py --strategy X` parses cleanly (args.interactive=False).
- Either flag combined with the other → argparse mutex error.
- Neither flag → argparse required-one-of error.
- `scripts/regression_check.py` exits 0.
</success_criteria>

<output>
Create `.planning/phases/02-interactive-cli-shell/02-02-SUMMARY.md` when done. Record diff summary (3 lines changed: docstring, mutex group creation, strategy moved into group) and regression_check.py exit code.
</output>
