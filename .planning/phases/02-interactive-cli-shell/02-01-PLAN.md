---
phase: 02-interactive-cli-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sphsim/strategies/naive.py
  - sphsim/strategies/threshold.py
  - sphsim/strategies/phase_prob.py
  - sphsim/strategies/incentive.py
  - sphsim/strategies/adaptive.py
autonomous: true
requirements:
  - STRAT-01
  - STRAT-02

must_haves:
  truths:
    - "Każdy z 5 plików sphsim/strategies/<name>.py eksportuje stałą STRATEGY_META obok funkcji strategy_<name> (D-24)"
    - "STRATEGY_META jest dict z dokładnie 3 kluczami: description, params, baseline_kpi (D-24)"
    - "STRATEGY_META['description'] to jednozdaniowy opis po polsku (D-24, D-29 — używany w tabeli 'strategies')"
    - "STRATEGY_META['params'] to lista krotek 4-elementowych (name: str, type: type, default: Any, description: str) (D-25)"
    - "Tylko naive ma baseline_kpi z avg_val_last100 = 92.0 (float, nie string); 4 pozostałe strategie mają baseline_kpi = None (D-26)"
    - "Defaultowe wartości i nazwy parametrów w STRATEGY_META mirrują argparse z sphsim/cli/args.py (D-25 invariant — testowane w Planie 04)"
    - "Funkcje strategy_<name> NIE są modyfikowane — tylko dodanie stałej top-level (Phase 1 verbatim zachowany)"
    - "scripts/regression_check.py nadal zwraca exit 0 po edycji (zero zmiany behawioralnej w logice strategii)"
  artifacts:
    - path: "sphsim/strategies/naive.py"
      provides: "STRATEGY_META z baseline_kpi naive --zeta 0.75 → avg_val_last100 = 92.0"
      contains: "STRATEGY_META = {"
    - path: "sphsim/strategies/threshold.py"
      provides: "STRATEGY_META z params=[('max_phase', int, 3, ...)], baseline_kpi=None"
      contains: "STRATEGY_META = {"
    - path: "sphsim/strategies/phase_prob.py"
      provides: "STRATEGY_META z params=[('probs', str, '0.9,0.7,0.5,0.3,0.0', ...)], baseline_kpi=None"
      contains: "STRATEGY_META = {"
    - path: "sphsim/strategies/incentive.py"
      provides: "STRATEGY_META z params=[('expected_P', float, 100.0, ...)], baseline_kpi=None"
      contains: "STRATEGY_META = {"
    - path: "sphsim/strategies/adaptive.py"
      provides: "STRATEGY_META z params=[('s_target', int, 10, ...)], baseline_kpi=None"
      contains: "STRATEGY_META = {"
  key_links:
    - from: "sphsim/strategies/<name>.py"
      to: "sphsim/cli/args.py argparse defaults"
      via: "STRATEGY_META['params'] mirror nazw/typów/defaultów"
      pattern: "STRATEGY_META\\s*=\\s*\\{"
    - from: "sphsim/strategies/naive.py"
      to: "PROJECT.md baseline naive --zeta 0.75 → avg_val=92"
      via: "STRATEGY_META['baseline_kpi']['avg_val_last100'] = 92.0"
      pattern: "avg_val_last100.*92\\.0"
---

<objective>
Dodać stałą `STRATEGY_META` do każdego z 5 plików strategii zgodnie z kontraktem D-24/D-25/D-26. Metadata mieszka koło funkcji strategii (single source of truth), używana przez REPL w Plan 3 (`do_strategies`, `do_strategy <nazwa>`) i przez invariant test w Plan 4.

Purpose: Spełnia STRAT-01 (lista strategii z opisem) i STRAT-02 (szczegóły strategii: parametry + baseline KPI). Lokalność metadanych = Phase 3 (custom loader) będzie używać tego samego kontraktu bez modyfikacji.

Output: 5 plików z dodaną stałą `STRATEGY_META`. Funkcje strategy_* nietknięte. Backwards compat (regression_check.py) zachowany.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-interactive-cli-shell/02-CONTEXT.md
@sphsim/strategies/__init__.py
@sphsim/cli/args.py
</context>

<interfaces>
<!-- STRATEGY_META contract from D-24/D-25/D-26 - executor uses these directly -->

Shape (Python dict, top-level constant in each strategy file):

```python
STRATEGY_META = {
    'description': str,            # jednozdaniowy opis po polsku
    'params': list,                # [(name: str, type: type, default: Any, description: str), ...]
    'baseline_kpi': dict | None,   # None dla 4 strategii, dict tylko dla naive (D-26)
}
```

Argparse defaults (źródło prawdy dla `params`, z sphsim/cli/args.py lines 41-46):
- `--zeta`        type=float, default=0.5
- `--max_phase`   type=int,   default=3
- `--probs`       type=str,   default='0.9,0.7,0.5,0.3,0.0'
- `--s_target`    type=int,   default=10
- `--expected_P` type=float, default=100.0

Existing argparse `help=` strings (use as `description` field in params, in Polish):
- zeta:       'Frakcja COMMIT (0..1)'
- max_phase:  'Max faza COMMIT'
- probs:      'P(COMMIT) per faza, po przecinku'
- s_target:   'Próg SUS'
- expected_P: 'Oczek. płatność'

Strategy descriptions (single-sentence Polish, derived from existing one-line docstring in each file's header comment, must match D-29 table content):
- naive:       'COMMIT z prawdopodobieństwem zeta'
- threshold:   'COMMIT tylko dla faz <= max_phase'
- phase_prob:  'COMMIT z P(commitów) per faza'
- incentive:   'COMMIT gdy E[zysk_netto] > 0'
- adaptive:    'COMMIT zależnie od poziomu bufora SUS'
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add STRATEGY_META to all 5 strategy files</name>
  <files>sphsim/strategies/naive.py, sphsim/strategies/threshold.py, sphsim/strategies/phase_prob.py, sphsim/strategies/incentive.py, sphsim/strategies/adaptive.py</files>

  <read_first>
    - sphsim/strategies/naive.py (current state — 9 lines, function only)
    - sphsim/strategies/threshold.py (current state — 8 lines, function only)
    - sphsim/strategies/phase_prob.py (current state — 12 lines, function only)
    - sphsim/strategies/incentive.py (current state — 18 lines, function only)
    - sphsim/strategies/adaptive.py (current state — 19 lines, function only)
    - sphsim/cli/args.py (lines 41-46 — argparse defaults SOURCE OF TRUTH for params)
    - .planning/phases/02-interactive-cli-shell/02-CONTEXT.md (re-read D-24, D-25, D-26 in full)
    - .planning/PROJECT.md (baseline `naive --zeta 0.75 → avg_val=92` reference for D-26)
  </read_first>

  <action>
  Per D-24: In each of the 5 strategy files, add a top-level constant `STRATEGY_META` immediately AFTER the existing `def strategy_<name>(...)` function (do NOT modify the function body, signature, comments, or imports). Each `STRATEGY_META` is a dict with exactly 3 keys: `'description'`, `'params'`, `'baseline_kpi'`.

  Per D-25: The `'params'` value MUST be a list of 4-tuples `(name, type, default, description)` where `type` is the actual Python class (`float`, `int`, `str` — not a string). Each tuple's `name`/`type`/`default` MUST mirror argparse from `sphsim/cli/args.py` exactly (the invariant tested by Plan 04). The `description` field is a Polish phrase.

  Per file:
  - `sphsim/strategies/naive.py`: description = 'COMMIT z prawdopodobieństwem zeta'; params = [('zeta', float, 0.5, 'Frakcja COMMIT (0..1)')]; baseline_kpi per D-26 = {'invocation': 'naive --zeta 0.75', 'avg_val_last100': 92.0, 'source': 'PROJECT.md / v1.0 results'} (avg_val_last100 MUST be float 92.0, not string).
  - `sphsim/strategies/threshold.py`: description = 'COMMIT tylko dla faz <= max_phase'; params = [('max_phase', int, 3, 'Max faza COMMIT')]; baseline_kpi = None.
  - `sphsim/strategies/phase_prob.py`: description = 'COMMIT z P(commitów) per faza'; params = [('probs', str, '0.9,0.7,0.5,0.3,0.0', 'P(COMMIT) per faza, po przecinku')]; baseline_kpi = None.
  - `sphsim/strategies/incentive.py`: description = 'COMMIT gdy E[zysk_netto] > 0'; params = [('expected_P', float, 100.0, 'Oczek. płatność')]; baseline_kpi = None.
  - `sphsim/strategies/adaptive.py`: description = 'COMMIT zależnie od poziomu bufora SUS'; params = [('s_target', int, 10, 'Próg SUS')]; baseline_kpi = None.

  Reference D-29 table content in 02-CONTEXT.md when writing `description` — values MUST match (single source of truth for both the `strategies` command output and per-strategy detail view).

  Do NOT add a docstring around STRATEGY_META, do NOT import anything new (`type` references like `float`/`int`/`str` are builtins, no `from typing import` needed). Do NOT touch `sphsim/strategies/__init__.py` (registry stays unchanged).
  </action>

  <verify>
    <automated>python -c "from sphsim.strategies.naive import STRATEGY_META as M; assert set(M.keys()) == {'description','params','baseline_kpi'}; assert M['baseline_kpi']['avg_val_last100'] == 92.0; assert isinstance(M['baseline_kpi']['avg_val_last100'], float); assert M['params'][0] == ('zeta', float, 0.5, 'Frakcja COMMIT (0..1)'); print('naive OK')"</automated>
    <automated>python -c "from sphsim.strategies.threshold import STRATEGY_META as M; assert M['baseline_kpi'] is None; assert M['params'][0] == ('max_phase', int, 3, 'Max faza COMMIT'); print('threshold OK')"</automated>
    <automated>python -c "from sphsim.strategies.phase_prob import STRATEGY_META as M; assert M['baseline_kpi'] is None; assert M['params'][0] == ('probs', str, '0.9,0.7,0.5,0.3,0.0', 'P(COMMIT) per faza, po przecinku'); print('phase_prob OK')"</automated>
    <automated>python -c "from sphsim.strategies.incentive import STRATEGY_META as M; assert M['baseline_kpi'] is None; assert M['params'][0] == ('expected_P', float, 100.0, 'Oczek. płatność'); print('incentive OK')"</automated>
    <automated>python -c "from sphsim.strategies.adaptive import STRATEGY_META as M; assert M['baseline_kpi'] is None; assert M['params'][0] == ('s_target', int, 10, 'Próg SUS'); print('adaptive OK')"</automated>
    <automated>python scripts/regression_check.py</automated>
  </verify>

  <acceptance_criteria>
    - `grep -v '^#' sphsim/strategies/naive.py | grep -c 'STRATEGY_META = {'` == 1
    - `grep -v '^#' sphsim/strategies/threshold.py | grep -c 'STRATEGY_META = {'` == 1
    - `grep -v '^#' sphsim/strategies/phase_prob.py | grep -c 'STRATEGY_META = {'` == 1
    - `grep -v '^#' sphsim/strategies/incentive.py | grep -c 'STRATEGY_META = {'` == 1
    - `grep -v '^#' sphsim/strategies/adaptive.py | grep -c 'STRATEGY_META = {'` == 1
    - `python -c "from sphsim.strategies.naive import STRATEGY_META; assert STRATEGY_META['description'] == 'COMMIT z prawdopodobieństwem zeta'"` exits 0
    - `python -c "from sphsim.strategies.naive import STRATEGY_META; assert STRATEGY_META['baseline_kpi']['avg_val_last100'] == 92.0 and isinstance(STRATEGY_META['baseline_kpi']['avg_val_last100'], float)"` exits 0
    - For each of {threshold, phase_prob, incentive, adaptive}: `python -c "from sphsim.strategies.<name> import STRATEGY_META; assert STRATEGY_META['baseline_kpi'] is None"` exits 0
    - For each strategy: `python -c "from sphsim.strategies.<name> import STRATEGY_META; p = STRATEGY_META['params'][0]; assert len(p) == 4 and isinstance(p[0], str) and isinstance(p[1], type) and isinstance(p[3], str)"` exits 0
    - `python scripts/regression_check.py` exits 0 (Phase 1 backwards compat — D-28)
    - `python -c "from sphsim.strategies.naive import strategy_naive; print(strategy_naive)"` works (function not removed/renamed)
  </acceptance_criteria>

  <done>
  All 5 strategy files contain a `STRATEGY_META` constant matching the D-24/D-25/D-26 contract. naive has baseline_kpi populated; threshold/phase_prob/incentive/adaptive have baseline_kpi=None. Strategy functions unchanged. Regression check passes.
  </done>
</task>

</tasks>

<verification>
- Each of 5 strategy files has `STRATEGY_META` constant with 3 required keys.
- naive: `baseline_kpi['avg_val_last100']` is float `92.0` (D-26 — value from PROJECT.md baseline).
- Other 4 strategies: `baseline_kpi is None` (D-26 — no synthetic baselines).
- `params` entries are 4-tuples with actual Python `type` (not strings).
- `params` entries match argparse defaults in `sphsim/cli/args.py` (D-25 invariant — Plan 04 will codify as automated test).
- Phase 1 regression suite (`scripts/regression_check.py`) still passes (CLI-04 backwards compat preserved).
</verification>

<success_criteria>
- 5 of 5 strategy files have STRATEGY_META exported as top-level constant.
- All keys/types match D-24/D-25/D-26 contract verbatim.
- `python scripts/regression_check.py` exits 0.
- Plan 03 (REPL) can import `STRATEGY_META` from each module via `importlib.import_module(f'sphsim.strategies.{name}')`.
- Plan 04 (invariant test) can compare `STRATEGY_META['params']` with argparse `add_argument` calls.
</success_criteria>

<output>
Create `.planning/phases/02-interactive-cli-shell/02-01-SUMMARY.md` when done. Record which strategies had STRATEGY_META added and confirm regression_check.py exit code.
</output>
