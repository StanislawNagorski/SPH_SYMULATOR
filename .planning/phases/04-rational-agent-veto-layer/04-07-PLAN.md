---
phase: 04-rational-agent-veto-layer
plan: 07
type: execute
wave: 5
depends_on: [04-01, 04-02, 04-03, 04-04, 04-05, 04-06]
files_modified:
  - scripts/verify_phase4.sh
autonomous: true
requirements: [AGENT-01, AGENT-02, AGENT-03, AGENT-04, AGENT-05]

must_haves:
  truths:
    - "scripts/verify_phase4.sh istnieje, jest executable (chmod +x), uruchamiany z dowolnego cwd"
    - "Skrypt weryfikuje wszystkie 5 ROADMAP SC dla Phase 4 + regression + test suite + Phase 2/3 invariants"
    - "Skrypt exit 0 gdy wszystkie checks PASS, exit 1 gdy jakikolwiek FAIL"
    - "Skrypt mirror'uje strukturę verify_phase3.sh (set -euo pipefail, check() helper, PASS/FAIL counter, sumaryczny komunikat)"
    - "Skrypt empirycznie weryfikuje SC #5: dla `naive --zeta 0.95 --compare-agent`, comparison.agent_helps == true (high COMMIT-rate naive → wiele veto candidates gdy E[zysk]<0 → with-agent ma mniej failure costs → wyższy avg_net_profit)"
  artifacts:
    - path: "scripts/verify_phase4.sh"
      provides: "Phase 4 exit gate — re-runnable po każdej zmianie w sphsim/ jako pre-flight przed merge"
      contains: "#!/usr/bin/env bash"
      min_lines: 100
  key_links:
    - from: "scripts/verify_phase4.sh"
      to: "scripts/regression_check.py"
      via: "subprocess invocation jako pierwszy check"
      pattern: "regression_check.py"
    - from: "scripts/verify_phase4.sh"
      to: "tests/test_agent.py"
      via: "python -m unittest tests.test_agent"
      pattern: "unittest tests.test_agent"
    - from: "scripts/verify_phase4.sh SC #5 check"
      to: "sph_sim.py --compare-agent"
      via: "subprocess + jq | python json parse + agent_helps assertion"
      pattern: "agent_helps"
---

<objective>
Dostarczyć Phase 4 exit gate — wykonalny skrypt bash weryfikujący wszystkie 5 ROADMAP Success Criteria dla Phase 4 + regression backwards compat + full test suite + Phase 2/3 invariants. Mirror'uje pattern z verify_phase3.sh.

Purpose: Bez verify_phase4.sh nie ma single command'y dla `/gsd:verify-work` ani pre-flight check'a. Skrypt jest pojedynczym oracle prawdy "Phase 4 ready for merge / verification" — zwraca exit 0 GDY i tylko GDY wszystkie 5 SC + regression + 32+ testy zielone. SC #5 wymaga **empirycznej** weryfikacji (agent_helps==true dla demo scenario `naive --zeta 0.95` — wysoki COMMIT rate naive strategy gwarantuje veto candidates per D-56 idempotency caveat dla incentive).
Output: 1 nowy plik wykonalny scripts/verify_phase4.sh (~150 linii, ~15-20 sekcji check'ów).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-rational-agent-veto-layer/04-CONTEXT.md
@.planning/phases/04-rational-agent-veto-layer/04-PATTERNS.md
@scripts/verify_phase3.sh
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/verify_phase4.sh — phase exit gate (10+ sekcji checków)</name>
  <files>scripts/verify_phase4.sh</files>
  <read_first>
    - scripts/verify_phase3.sh (~170 linii) — wzorzec template do paste-and-modify: shebang, set -euo pipefail, cd do project root, interpreter detection (python vs python3), trap cleanup, check() helper z PASS/FAIL counter, sekcje numerowane "── N. Tytuł ──"
    - .planning/ROADMAP.md sekcja "Phase 4: Rational Agent veto layer" — 5 ROADMAP Success Criteria (SC #1 default-wrap, SC #2 --no-agent, SC #3 veto_per_phase w human+JSON, SC #4 --compare-agent delta KPI, SC #5 demo scenario agent_helps)
    - .planning/phases/04-rational-agent-veto-layer/04-CONTEXT.md `<specifics>` "Demo scenario dla SC #5" — UWAGA: CONTEXT.md wspomina `incentive --expected_P 30 --compare-agent` ale D-56 stipuluje że incentive+wrapper jest IDEMPOTENT (n_vetoed==0 → delta≈0 → assertion `agent_helps==true` fails randomly). Per WRN-03 (checker review) używamy `naive --zeta 0.95 --compare-agent` — wysoki COMMIT rate naive strategy gwarantuje veto candidates gdy E[zysk]<0, więc with-agent ma mierzalnie wyższy avg_net_profit. Verify script jest authoritative nad CONTEXT.md tutaj.
  </read_first>
  <action>
    Utwórz scripts/verify_phase4.sh (chmod +x później). Paste-and-modify z verify_phase3.sh:
    
    **Sekcja 1 — Regression backwards compat (Phase 1 CLI-04 contract):**
    - check: `python scripts/regression_check.py` exit 0 (PASS: 8/8)
    
    **Sekcja 2 — Test suite (Phase 2/3 invariants + Phase 4 new):**
    - check: `python -m unittest tests.test_strategy_meta_consistency` (Phase 2 invariant)
    - check: `python -m unittest tests.test_loader` (Phase 3 — 21 cases)
    - check: `python -m unittest tests.test_agent` (Phase 4 — 10+ cases)
    - check: `python -m unittest discover tests` (full discover — 32+ total)
    
    **Sekcja 3 — SC #1: agent default-wrapped (bez --no-agent):**
    - check: `python sph_sim.py --strategy naive --zeta 0.5 --seed 42 --json | tail -n +1` — parse JSON, assert `agent_enabled==true`
    - check: ten sam dla `--strategy incentive --expected_P 30 --seed 42 --json` (assert agent_enabled==true)
    
    **Sekcja 4 — SC #2: --no-agent escape hatch:**
    - check: `python sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 --json | tail -n +1` — parse JSON, assert `agent_enabled==false` i `n_vetoed_total==0`
    - check: `python sph_sim.py --strategy incentive --expected_P 100 --no-agent --seed 42 --json` — assert agent_enabled==false
    
    **Sekcja 5 — SC #3: veto_per_phase w JSON i human-readable:**
    - check: `python sph_sim.py --strategy phase_prob --probs 1.0,1.0,1.0,1.0,0.0 --seed 42 --json | tail -n +1` — parse JSON, assert `veto_per_phase` jest dict, `n_vetoed_total` jest int (≥ 0). Dla `phi=DEFAULT_PHI` i tej strategii spodziewane n_vetoed_total > 0 bo phase_prob często rekomenduje COMMIT przy net<0.
    - check: ten sam JSON, assert pole `agent_enabled` istnieje
    - check (human-readable veto section): `python sph_sim.py --strategy phase_prob --probs 1.0,1.0,1.0,1.0,0.0 --seed 42 2>/dev/null | grep 'VETO przez RationalAgent' > /dev/null` (sekcja widoczna gdy n_vetoed > 0)
    
    **Sekcja 6 — SC #4: --compare-agent delta KPI table:**
    - check: `python sph_sim.py --strategy incentive --expected_P 30 --compare-agent --seed 42 --json | tail -n +1` — parse JSON, assert `'comparison' in result`, `'with_agent' in result['comparison']`, `'without_agent' in result['comparison']`, `'delta' in result['comparison']`, `'agent_helps' in result['comparison']`
    - check (human-readable compare table): `python sph_sim.py --strategy incentive --expected_P 30 --compare-agent --seed 42 2>/dev/null | grep -E '✓ TAK|✗ NIE' > /dev/null` (werdykt widoczny)
    - check (REPL): `printf 'compare incentive expected_P=30\nexit\n' | python sph_sim.py --interactive 2>&1 | grep -E '✓ TAK|✗ NIE' > /dev/null` (REPL compare działa)
    
    **Sekcja 7 — SC #5: empirical demo scenario — agent_helps == true:**
    - check: `python sph_sim.py --strategy naive --zeta 0.95 --compare-agent --seed 42 --json | tail -n +1 | python -c "import json, sys; raw=sys.stdin.read(); d=json.loads(raw[raw.find('{'):]); assert d['comparison']['agent_helps'] == True, f'SC#5 FAILED: agent_helps should be True for naive --zeta 0.95, got {d[\"comparison\"][\"agent_helps\"]}; delta={d[\"comparison\"][\"delta\"]}'"` — to JEST empiryczny dowód że wrapper poprawia outcome strategii naive (high COMMIT rate → wiele veto candidates → with-agent uwalnia od failure costs). UWAGA: NIE używać `incentive --expected_P 30` bo per D-56 jest idempotent (n_vetoed==0, delta≈0, assertion zawodzi losowo).
    
    **Sekcja 8 — Argparse mutex enforcement (D-60):**
    - check: `{ python sph_sim.py --strategy naive --zeta 0.5 --compare-agent --no-agent 2>&1 || true; } | grep 'wykluczające' > /dev/null` (Polish error visible)
    - check: `{ python sph_sim.py --interactive --compare-agent 2>&1 || true; } | grep -E 'interactive|wykluczające|nie działa' > /dev/null`
    
    **Sekcja 9 — REPL 7 komend (Phase 4 dodaje compare):**
    - check: `printf 'help\nexit\n' | python sph_sim.py --interactive 2>&1 | grep 'compare <nazwa>' > /dev/null`
    
    **Sekcja 10 — Custom strategy + agent (D-58, Phase 3 carry-forward):**
    - check: `python sph_sim.py --custom examples/custom_strategy_template.py --seed 42 --json | tail -n +1` — parse JSON, assert agent_enabled==true (agent default-on dla custom too — D-58)
    - check: `python sph_sim.py --custom examples/custom_strategy_template.py --compare-agent --seed 42 --json | tail -n +1` — assert `comparison` block obecny
    
    **Sekcja 11 — Summary:**
    - Format z verify_phase3.sh:166-170 (PASS=$PASS / FAIL=$FAIL), exit 0 / exit 1 z polskim komunikatem `✓ Phase 4 ready for /gsd:verify-work` lub `✗ Phase 4 NIE jest gotowe — popraw FAIL'e powyżej.`
    
    **Boilerplate** (verbatim z verify_phase3.sh): shebang `#!/usr/bin/env bash`, `set -euo pipefail`, `cd "$(dirname "$0")/.."`, interpreter detection (`if command -v python ...`), `trap 'rm -f /tmp/p4_*' EXIT`, PASS=0/FAIL=0, `check()` function. Komunikaty po polsku. Każda sekcja prefixed `── N. Tytuł ──`. Używać `grep > /dev/null` zamiast `grep -q` (D-52 z Phase 3 — SIGPIPE/pipefail safety). Używać `{ cmd || true; } | grep` dla komend które mogą exit !=0 (jak argparse errors).
    
    Po zapisaniu pliku, ustaw `chmod +x scripts/verify_phase4.sh`.
  </action>
  <verify>
    <automated>cd /Users/stanislawnagorski/Documents/STUDIA/sem4/ekonometria\ 2 && chmod +x scripts/verify_phase4.sh && bash scripts/verify_phase4.sh 2>&1 | tail -20 && bash scripts/verify_phase4.sh; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `test -f scripts/verify_phase4.sh`
    - Source: `test -x scripts/verify_phase4.sh` (executable bit set)
    - Source: `head -1 scripts/verify_phase4.sh | grep '^#!/usr/bin/env bash'` ≥ 1
    - Source: `grep -c 'set -euo pipefail' scripts/verify_phase4.sh` ≥ 1
    - Source: `grep -c 'check ' scripts/verify_phase4.sh` ≥ 15 (15+ check invocations — pełne pokrycie SC #1-5 + invariants + regression)
    - Source: `grep -c 'SC #' scripts/verify_phase4.sh` ≥ 5 (komentarze cytujące SC #1, SC #2, SC #3, SC #4, SC #5)
    - Source: `grep -c 'agent_helps' scripts/verify_phase4.sh` ≥ 1 (SC #5 empirical check)
    - Source: `grep -c 'regression_check.py' scripts/verify_phase4.sh` ≥ 1
    - Source: `grep -c 'tests.test_agent' scripts/verify_phase4.sh` ≥ 1
    - Source: `grep -c 'comparison' scripts/verify_phase4.sh` ≥ 2 (JSON i CLI compare checks)
    - Source: `grep -cE '✓|✗' scripts/verify_phase4.sh` ≥ 2 (final summary verdict markers)
    - Behavior: `bash scripts/verify_phase4.sh` exit 0 (PASS=15+ / FAIL=0)
    - Behavior: skrypt wykonuje się w < 90s na typowym laptopie (subprocess Sequence: regression ~30s + tests ~20s + ~10 CLI invocations × 2-5s = ~60-90s total)
  </acceptance_criteria>
  <done>
    scripts/verify_phase4.sh istnieje, jest executable, mirror'uje pattern z verify_phase3.sh, weryfikuje wszystkie 5 ROADMAP SC + regression + test suite + Phase 2/3 invariants + mutex + REPL + custom integration. Skrypt zwraca exit 0 gdy wszystko zielone. Phase 4 ready for `/gsd:verify-work`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify_phase4.sh → all sphsim subsystems | Skrypt jest meta-check'iem — odpala każdy subsystem przez subprocess; nie modyfikuje plików (read-only). |
| /tmp/p4_* temporary files | trap cleanup na EXIT — nie ma residual state'u po awarii. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-24 | DoS | verify_phase4.sh full suite może trwać do 90s | accept | Akceptowalne dla pre-flight check; nie blokuje normalnego development flow. |
| T-04-25 | Repudiation | False-positive PASS (test przechodzi mimo wadliwego kodu) | mitigate | Każda SC jest weryfikowana przez OBSERWOWALNE behavior (CLI output, JSON keys, regression bit-match), nie przez self-asserting code. SC #5 to empirical computational result (agent_helps==true). |
| T-04-26 | Tampering | Skrypt cd "$(dirname "$0")/.." — może działać z wrong cwd | mitigate | Pattern verbatim z verify_phase3.sh:27 — testowany, safe. |
| T-04-SC | Tampering | Bash injection przez argparse errors | n/a | Wszystkie sub-commands w `cmd` argumencie check() są literal stringi (brak interpolation z user input); $(dirname) jest bash builtin, safe. |
</threat_model>

<verification>
- `bash scripts/verify_phase4.sh` exit 0 z PASS ≥ 15, FAIL == 0
- Wszystkie 5 ROADMAP SC dla Phase 4 zweryfikowane empirycznie
- Regression backwards compat (CLI-04) intact
- 32+ testów pass (22 pre-existing + 10+ Phase 4 nowe)
- SC #5 (agent_helps for naive --zeta 0.95) zwraca true — empiryczne potwierdzenie że wrapper poprawia outcome high-COMMIT-rate strategii (incentive jest idempotent per D-56 i nie nadaje się jako demo)
</verification>

<success_criteria>
- scripts/verify_phase4.sh istnieje, executable, ≥ 100 linii
- Skrypt zwraca exit 0 gdy wszystkie checks PASS
- Skrypt zwraca exit 1 z czytelnym podsumowaniem gdy jakikolwiek FAIL (mirror verify_phase3.sh:158-168)
- Wszystkie 5 ROADMAP Success Criteria dla Phase 4 weryfikowane (SC #1 default-wrap, SC #2 --no-agent, SC #3 veto_per_phase, SC #4 --compare-agent, SC #5 empirical agent_helps==true dla demo scenario)
- Mutex enforcement (D-60) weryfikowane
- REPL 7-mą komendę (compare) weryfikowane
- Custom strategy + agent integration weryfikowane (D-58 carry-forward z Phase 3)
- Phase 4 ready dla `/gsd:verify-work` flow
</success_criteria>

<output>
Create `.planning/phases/04-rational-agent-veto-layer/04-07-SUMMARY.md` po ukończeniu.
</output>
