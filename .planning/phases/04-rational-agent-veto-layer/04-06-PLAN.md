---
phase: 04-rational-agent-veto-layer
plan: 06
type: tdd
wave: 4
depends_on: [04-01, 04-02, 04-03, 04-04, 04-05]
files_modified:
  - tests/test_agent.py
  - scripts/regression_check.py
autonomous: true
requirements: [AGENT-01, AGENT-02, AGENT-03, AGENT-04, AGENT-05]

must_haves:
  truths:
    - "tests/test_agent.py istnieje i zawiera ≥ 10 unit/integration test cases pokrywających wrapper logic + CLI integration"
    - "Wszystkie testy w tests/test_agent.py przechodzą (`python -m unittest tests.test_agent` exit 0)"
    - "scripts/regression_check.py exit 0 — 8/8 baseline_v1 fixtures match (Strategia B z D-67: deep_diff skipuje 3 nowe klucze: veto_per_phase, n_vetoed_total, agent_enabled — supersedes D-59 'patchuj --no-agent w INVOCATIONS')"
    - "Wszystkie 22+ pre-existing testy nadal pass (`python -m unittest discover tests` exit 0)"
    - "Strategia B z D-67 udokumentowana w regression_check.py komentarzem (np. '# Phase 4 D-67: deep_diff skipuje 3 nowe klucze wprowadzone przez agent veto layer')"
  artifacts:
    - path: "tests/test_agent.py"
      provides: "10+ test cases pokrywających wrap_with_agent + CLI integration"
      contains: "class TestWrapWithAgent"
      min_lines: 200
    - path: "scripts/regression_check.py"
      provides: "deep_diff rozszerzony o SKIP_KEYS list zawierający 3 nowe klucze Phase 4 (veto_per_phase, n_vetoed_total, agent_enabled) — Strategia B z D-67"
      contains: "# Phase 4"
  key_links:
    - from: "tests/test_agent.py"
      to: "sphsim.agent.wrap_with_agent"
      via: "from sphsim.agent.rational import wrap_with_agent"
      pattern: "wrap_with_agent"
    - from: "tests/test_agent.py integration tests"
      to: "subprocess.run([python, 'sph_sim.py', ...])"
      via: "subprocess + json.loads(stdout)"
      pattern: "subprocess.run"
    - from: "scripts/regression_check.py deep_diff"
      to: "3 nowe klucze JSON metrics (veto_per_phase, n_vetoed_total, agent_enabled)"
      via: "SKIP_KEYS list filtering before equality check"
      pattern: "SKIP_KEYS"
---

<objective>
Dostarczyć pełen pokrycie testowe Phase 4 oraz zabezpieczyć backwards compat CLI-04: (1) `tests/test_agent.py` z 10+ test cases (wrapper unit + CLI integration); (2) rozszerzenie `scripts/regression_check.py` o SKIP_KEYS filtering (Strategia B z D-67 — fixtures niezmienialne, deep_diff ignoruje 3 nowe klucze wprowadzone w Phase 4: veto_per_phase, n_vetoed_total, agent_enabled); (3) regression_check.py exit 0.

Purpose: Phase 4 wprowadza 3 nowe klucze do JSON metrics (veto_per_phase, n_vetoed_total, agent_enabled). Istniejące fixtures baseline_v1 (8 plików) NIE mają tych kluczy — to oracle dla v1.0 zachowania. CLI-04 (backwards compat) wymaga że istniejące pola (avg_val_last100, cum_val_total, ...) są bit-identyczne; nowe klucze MUSZĄ być ignorowane przy compare. Per D-67 wybieramy Strategię B (CONTEXT D-67 explicit preference: 'Claude's Discretion na rzecz prostszej implementacji bez regenu fixtures'). Bez SKIP_KEYS regression check failuje na 'KEY MISSING w expected'. Bez tests/test_agent.py, brak gwarancji że wrapper jest poprawny dla wszystkich edge cases (D-55, D-56, D-57).
Output: 1 nowy plik (tests/test_agent.py — 200+ linii) + 1 zmodyfikowany skrypt (scripts/regression_check.py — dodanie SKIP_KEYS list i filter w deep_diff). scripts/generate_baseline.py POZOSTAJE niezmieniony — Strategia B nie wymaga zmian w INVOCATIONS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-rational-agent-veto-layer/04-CONTEXT.md
@.planning/phases/04-rational-agent-veto-layer/04-PATTERNS.md
@tests/test_loader.py
@scripts/generate_baseline.py
@scripts/regression_check.py
@sphsim/agent/rational.py
@sphsim/core/device.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/test_agent.py — 10+ unit/integration test cases</name>
  <files>tests/test_agent.py</files>
  <read_first>
    - tests/test_loader.py (396 linii) — wzorzec testów stdlib-only unittest z setUp/tearDown, polish docstrings, subprocess integration (jeśli potrzeba)
    - sphsim/agent/rational.py — wrapper signature i behavior (po Planie 02)
    - sphsim/core/device.py — Device z n_vetoed + veto_phase_stats (po Planie 01)
    - .planning/phases/04-rational-agent-veto-layer/04-PATTERNS.md sekcja "### 9. tests/test_agent.py" — module docstring template, sys.path bootstrap, Device fixture helper, stub strategy_fn pattern, subprocess integration pattern z scripts/regression_check.py:79-99
    - .planning/phases/04-rational-agent-veto-layer/04-CONTEXT.md Claude's Discretion "Test coverage" (10 test cases: 8 unit + 2 integration)
    - sphsim/strategies/incentive.py — dla D-56 idempotency test (incentive + wrapper)
  </read_first>
  <behavior>
    - Test: 10+ test cases pokrywające: (1) wrapper passthrough dla ABSTAIN, (2) brak veto gdy E[zysk]>0, (3) veto gdy E[zysk]<0, (4) n_vetoed inkrementuje a n_abstain NIE, (5) total_h==0 fallback (D-55), (6) phi[idx]>=1.0 → veto (D-57), (7) idx>=len(phi) → veto (D-57), (8) incentive+wrapper idempotent (D-56), (9) `--compare-agent` JSON ma comparison block (integration test), (10) `--no-agent` daje n_vetoed_total=0 (integration test)
    - Test: stdlib only (unittest + subprocess + json + os + sys + tempfile — match test_loader.py)
    - Test: `python -m unittest tests.test_agent -v` exit 0 z minimum 10 passing tests
    - Test: `python -m unittest discover tests` exit 0 z 32+ tests total (22 pre-existing + 10+ nowe)
  </behavior>
  <action>
    Per 04-PATTERNS.md §9 i 04-CONTEXT.md Claude's Discretion "Test coverage": utwórz tests/test_agent.py. Struktura: (1) **Module docstring** (po polsku, lista 10 test cases zgodna z 04-PATTERNS.md §9 template, info "Stdlib only"). (2) **sys.path bootstrap** (verbatim z test_loader.py:33-36). (3) **Imports**: unittest, subprocess, json, os, sys, tempfile + `from sphsim.agent.rational import wrap_with_agent` + `from sphsim.core.device import Device` + `from sphsim.strategies.incentive import strategy_incentive`. (4) **Stałe**: `PROJECT_ROOT = ...`, `MONOLITH = PROJECT_ROOT / 'sph_sim.py'` (jak w regression_check.py). (5) **Helper `_make_device(phase, status='UP')`** zwracający `Device(id=0, phase=phase, status=status)`. (6) **Class `TestWrapWithAgent(unittest.TestCase)`** z 8 unit tests: (a) `test_abstain_passthrough` — stub zwracający 'ABSTAIN', wrapper zwraca 'ABSTAIN', n_vetoed pozostaje 0. (b) `test_commit_positive_E_passthrough` — stub COMMIT, expected_P=1000.0 (duże), assert wrapper zwraca 'COMMIT', n_vetoed=0. (c) `test_commit_negative_E_veto` — stub COMMIT, expected_P=0.01 (małe), assert wrapper zwraca 'VETO', n_vetoed=1, veto_phase_stats={1:1}. (d) `test_n_vetoed_increments_not_n_abstain` — explicit check że po VETO `dev.n_vetoed==1` i `dev.n_abstain==0` (rozróżnione kategorie). (e) `test_total_h_zero_fallback_D55` — l=[0,0,0,0], expected_P=1000.0 (duże), assert brak crash + jakaś sensowna decision (COMMIT lub VETO — kluczowe że NIE rzuca ZeroDivisionError). (f) `test_phi_one_guard_D57` — phi[idx]>=1.0, assert wrapper zwraca 'VETO' bez obliczeń exp_pay. (g) `test_idx_out_of_range_guard_D57` — dev.phase=10, len(phi)=5, assert wrapper zwraca 'VETO'. (h) `test_incentive_plus_wrapper_idempotent_D56` — wrap_with_agent(strategy_incentive, 100.0) na realistycznym Device, expected_P=100.0; oczekiwane: gdy incentive zwraca COMMIT, agent również passthrough (no veto) — sprawdź n_vetoed==0 po wielu wywołaniach z tym samym input'em. (7) **Class `TestCLIIntegration(unittest.TestCase)`** z 2 integration tests: (i) `test_compare_agent_json_has_comparison_block` — subprocess.run python sph_sim.py --strategy incentive --expected_P 30 --compare-agent --seed 42 --json, parse JSON, assert `'comparison' in result` i `result['comparison']['agent_helps'] in (True, False)`. (j) `test_no_agent_zero_vetoes` — subprocess.run python sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 --json, parse JSON, assert `result['metrics']['n_vetoed_total'] == 0` i `result['metrics']['agent_enabled'] == False`. (8) **`if __name__ == '__main__': unittest.main()`** na końcu. Polish docstrings dla każdej metody testowej. Verbose assertion messages (`msg=f"..."`). Stdlib only.
  </action>
  <verify>
    <automated>cd /Users/stanislawnagorski/Documents/STUDIA/sem4/ekonometria\ 2 && python -m unittest tests.test_agent 2>&1; rc=$?; test $rc -eq 0 && python -m unittest discover tests 2>&1; rc2=$?; test $rc2 -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Source: `test -f tests/test_agent.py`
    - Source: `wc -l tests/test_agent.py | awk '{print $1}'` ≥ 150 (10+ tests, polish docstrings, helper functions — realistic minimum)
    - Source: `grep -v '^#' tests/test_agent.py | grep -cE '^    def test_'` ≥ 10
    - Source: `grep -v '^#' tests/test_agent.py | grep -c 'wrap_with_agent'` ≥ 5
    - Source: `grep -v '^#' tests/test_agent.py | grep -c 'subprocess.run'` ≥ 2 (CLI integration tests)
    - Source: `grep -v '^#' tests/test_agent.py | grep -c "'comparison'"` ≥ 1 (integration test ix)
    - Source: `grep -v '^#' tests/test_agent.py | grep -cE "n_vetoed|n_abstain"` ≥ 2 (n_vetoed_increments test)
    - Source: `grep -v '^#' tests/test_agent.py | grep -cE 'D-5[3567]|D-66|D-67'` ≥ 1 (decision refs in docstrings)
    - Behavior: `python -m unittest tests.test_agent` exits 0
    - Behavior: `python -m unittest tests.test_agent -v 2>&1 | grep -cE '^test_.*\.\.\. ok$'` ≥ 10
    - Behavior: `python -m unittest discover tests` exits 0 (nie łamiemy pre-existing testów)
  </acceptance_criteria>
  <done>
    tests/test_agent.py istnieje, zawiera 10+ test cases (8 unit + 2 integration) pokrywających D-55/D-56/D-57/D-63/D-65/D-66/D-67. Wszystkie testy przechodzą. Pre-existing test suite (test_loader, test_strategy_meta_consistency) nadal działa. Pokrycie wystarczające dla Phase 4 exit gate w Planie 07.
  </done>
</task>

<task type="auto">
  <name>Task 2: scripts/regression_check.py — Strategia B z D-67 (SKIP_KEYS filtering)</name>
  <files>scripts/regression_check.py</files>
  <read_first>
    - scripts/regression_check.py — istniejąca funkcja deep_diff (linie ~36-76) z exact-key comparison; metoda dispatch INVOCATIONS importowanego z generate_baseline.py
    - scripts/generate_baseline.py — INVOCATIONS list (8 entries) i COMMANDS_HUMAN list — **POZOSTAJE NIEZMIENIONA** w Strategii B (generate_baseline nie wymaga edycji)
    - tests/fixtures/baseline_v1/*.json — 8 fixtures z v1.0 monolitu; struktura `{strategy, strategy_params, env, metrics: {...stare klucze...}}` BEZ kluczy veto_per_phase / n_vetoed_total / agent_enabled
    - .planning/phases/04-rational-agent-veto-layer/04-CONTEXT.md D-67 — explicit Claude's Discretion: "na rzecz prostszej implementacji bez regenu fixtures" → Strategia B preferred per CONTEXT
    - .planning/phases/04-rational-agent-veto-layer/04-PATTERNS.md sekcja "### 10. scripts/regression_check.py" — SKIP_KEYS pattern reference
  </read_first>
  <action>
    Per D-67 (CONTEXT.md explicit preference: "Claude's Discretion na rzecz prostszej implementacji bez regenu fixtures") implementuj wyłącznie Strategię B. scripts/generate_baseline.py NIE jest edytowany — INVOCATIONS pozostają bez --no-agent (agent jest default-on po Phase 4 i fixtures są oracle dla v1.0 zachowania = bez agenta wynik powinien być identyczny w istniejących kluczach metrics).

    Konkretne kroki w scripts/regression_check.py:

    (1) **Dodaj SKIP_KEYS const** na początku pliku (po importach, przed funkcją deep_diff): 
    ```
    # Phase 4 D-67: trzy nowe klucze w metrics są ignorowane przy compare z baseline_v1 fixtures —
    # fixtures są oracle dla v1.0 zachowania i nie zawierają tych pól. Pola obecne tylko w actual output.
    SKIP_KEYS = ('veto_per_phase', 'n_vetoed_total', 'agent_enabled')
    ```

    (2) **Zmodyfikuj deep_diff** (lub helper który iteruje po kluczach metrics): w pętli porównywania kluczy z `actual['metrics']` vs `expected['metrics']`, dodaj guard `if key in SKIP_KEYS: continue` PRZED jakimkolwiek check'iem (zarówno "KEY MISSING w expected" jak i "MISMATCH w wartości"). Najprostszy patch: na początku rekursywnego deep_diff (lub w iteracji `for key in (set(actual.keys()) | set(expected.keys()))`), wstaw `if key in SKIP_KEYS: continue`. Jeśli deep_diff jest generic (nie wie o "metrics"), zastosuj filtering wyłącznie gdy current path == 'metrics' (zachowanie precyzyjne) — albo prościej: SKIP_KEYS jest aplikowane wszędzie (te nazwy nie kolidują z innymi polami JSON). Wybór precyzji zależy od istniejącej struktury deep_diff (decyduje implementator przy edycji).

    (3) **Dodaj komentarz dokumentujący decyzję** nad SKIP_KEYS:
    ```
    # Phase 4 D-67 (Strategia B): zamiast regenerować baseline_v1/*.json fixtures lub dodawać --no-agent
    # do INVOCATIONS, ignorujemy 3 nowe klucze (wprowadzone w Phase 4 jako agent veto layer) przy
    # compare z fixtures. To zachowuje semantykę "fixtures są oracle dla istniejących pól metrics"
    # bez konieczności regeneracji ani touch'owania generate_baseline.py.
    ```

    (4) **Brak zmian w generate_baseline.py** — explicit non-action. NIE dodawaj --no-agent do INVOCATIONS. NIE generuj fixtures od nowa. NIE modyfikuj COMMANDS_HUMAN.

    (5) **Weryfikacja przy implementacji**: po edycji uruchom `python scripts/regression_check.py` — oczekiwany exit 0 (PASS: 8/8). Jeśli FAIL z innym powodem niż 3 nowe klucze (np. któraś z istniejących wartości avg_val_last100 et al. się rozjechała), to wskazuje BUG w wrapperze (Plan 02) lub simulator branchach (Plan 01) — agent default-on nie powinien zmieniać tych metryk dla strategii które nie zwracają COMMIT (lub gdy zwracają COMMIT i E[zysk]>0 → passthrough, brak veto, identyczne zachowanie). W tym przypadku odeślij blocker do plan-revision dla Plan 01/02.
  </action>
  <verify>
    <automated>cd /Users/stanislawnagorski/Documents/STUDIA/sem4/ekonometria\ 2 && python -c "
import scripts.regression_check as rc
assert hasattr(rc, 'SKIP_KEYS'), 'SKIP_KEYS const missing'
assert set(rc.SKIP_KEYS) >= {'veto_per_phase', 'n_vetoed_total', 'agent_enabled'}, f'SKIP_KEYS should contain 3 Phase 4 keys, got {rc.SKIP_KEYS}'
print('OK — SKIP_KEYS const present with 3 Phase 4 keys')
" && python scripts/regression_check.py 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source: `test -f scripts/regression_check.py`
    - Source: `grep -c 'SKIP_KEYS' scripts/regression_check.py` ≥ 2 (definicja + użycie w deep_diff)
    - Source: `grep -c 'veto_per_phase' scripts/regression_check.py` ≥ 1 (w SKIP_KEYS const)
    - Source: `grep -c 'n_vetoed_total' scripts/regression_check.py` ≥ 1
    - Source: `grep -c 'agent_enabled' scripts/regression_check.py` ≥ 1
    - Source: `grep -c '# Phase 4' scripts/regression_check.py` ≥ 1 (komentarz dokumentujący Strategię B z D-67)
    - Source: `grep -c -- '--no-agent' scripts/generate_baseline.py` == 0 (generate_baseline.py NIE jest tknięty — Strategia B)
    - Behavior: `python -c "import scripts.regression_check as rc; assert 'veto_per_phase' in rc.SKIP_KEYS"` exits 0
    - Behavior: `python scripts/regression_check.py` exit code 0 (PASS: 8/8). Jeśli exit code 1 i komunikat nie wskazuje na 3 nowe klucze — to bug w Planie 01/02, eskalować do plan-revision.
    - Behavior: `python -m unittest discover tests` exit 0 (regression check nie łamie istniejących testów)
  </acceptance_criteria>
  <done>
    scripts/regression_check.py ma const SKIP_KEYS = ('veto_per_phase', 'n_vetoed_total', 'agent_enabled') i deep_diff ignoruje te klucze przy compare. scripts/generate_baseline.py POZOSTAJE niezmieniony (per Strategia B). Regression check zwraca exit 0 (PASS: 8/8). CLI-04 backwards compat utrzymany — istniejące pola metrics bit-identyczne, nowe pola Phase 4 ignorowane przy compare. Plan 07 verify gate może bezpiecznie polegać na `python scripts/regression_check.py`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subprocess.run python sph_sim.py | Integration tests w test_agent.py uruchamiają CLI — args z list literal, NIE shell injection (subprocess.run([...], shell=False) by default) |
| fixtures vs current output | regression_check.py używa deep_diff exact equality z SKIP_KEYS filtering — 3 nowe klucze Phase 4 (veto_per_phase, n_vetoed_total, agent_enabled) są ignorowane; pozostałe pola metrics (avg_val_last100, cum_val_total, ...) MUSZĄ być bit-identyczne (D-67 Strategia B) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-21 | Tampering | tests/test_agent.py modyfikuje sys.modules przez import | accept | Stdlib unittest izoluje per-test stan; brak współdzielonego state'u poprzez moduły. |
| T-04-22 | Repudiation | Backwards compat CLI-04 broken | mitigate | Strategia B z D-67: SKIP_KEYS filtering w regression_check.py ignoruje 3 nowe pola; istniejące pola metrics MUSZĄ być bit-identyczne (każda inna różnica = FAIL). Jeśli wrapper jest poprawnie no-op dla strategii niezgłaszających COMMIT lub gdy E[zysk]>0 (passthrough), istniejące metryki nie zmieniają się i regression check exit 0. |
| T-04-23 | DoS | Integration tests uruchamiają subprocess 2x | accept | Każdy test trwa kilka sekund (T=2000 default); 10 testów total ~30s. Akceptowalne dla pre-commit. |
| T-04-SC | Tampering | scripts/* nie dodają zależności | n/a | Stdlib only (PROJECT.md constraint); Package Legitimacy Gate inapplicable. |
</threat_model>

<verification>
- `python -m unittest tests.test_agent -v` exit 0 z minimum 10 tests passing
- `python -m unittest discover tests` exit 0 z 32+ tests total
- `python scripts/regression_check.py` exit 0 (PASS: 8/8)
- 04-CONTEXT.md SC #1-5 readyness sprawdzone w Planie 07 verify gate
</verification>

<success_criteria>
- tests/test_agent.py istnieje z 10+ test cases pokrywających D-55/D-56/D-57/D-63/D-65/D-66/D-67
- Wszystkie nowe testy passing (`python -m unittest tests.test_agent` exit 0)
- Pre-existing 22+ tests nadal passing (test_loader, test_strategy_meta_consistency)
- scripts/regression_check.py ma SKIP_KEYS const zawierający 3 nowe klucze Phase 4 (`veto_per_phase`, `n_vetoed_total`, `agent_enabled`) — Strategia B z D-67
- scripts/generate_baseline.py POZOSTAJE niezmieniony (Strategia B nie wymaga zmian w INVOCATIONS)
- `python scripts/regression_check.py` exit 0 — fixtures match po wykluczeniu SKIP_KEYS dla wszystkich 8 baseline invocations
- CLI-04 backwards compat formally weryfikowane
</success_criteria>

<output>
Create `.planning/phases/04-rational-agent-veto-layer/04-06-SUMMARY.md` po ukończeniu.
</output>
