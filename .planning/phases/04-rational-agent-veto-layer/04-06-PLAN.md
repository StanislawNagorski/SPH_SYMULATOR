---
phase: 04-rational-agent-veto-layer
plan: 06
type: tdd
wave: 4
depends_on: [04-01, 04-02, 04-03, 04-04, 04-05]
files_modified:
  - tests/test_agent.py
  - scripts/generate_baseline.py
  - scripts/regression_check.py
autonomous: true
requirements: [AGENT-01, AGENT-02, AGENT-03, AGENT-04, AGENT-05]

must_haves:
  truths:
    - "tests/test_agent.py istnieje i zawiera ≥ 10 unit/integration test cases pokrywających wrapper logic + CLI integration"
    - "Wszystkie testy w tests/test_agent.py przechodzą (`python -m unittest tests.test_agent` exit 0)"
    - "scripts/regression_check.py exit 0 — 8/8 baseline_v1 fixtures match (po dodaniu --no-agent do INVOCATIONS lub po skip-keys logiki)"
    - "Wszystkie 22+ pre-existing testy nadal pass (`python -m unittest discover tests` exit 0)"
    - "Strategia D-67 udokumentowana w samym pliku regression_check.py lub generate_baseline.py (komentarz wskazujący: 'Phase 4 — używamy --no-agent w INVOCATIONS dla bit-exact fixtures' lub 'Phase 4 — deep_diff skipuje 3 nowe klucze')"
  artifacts:
    - path: "tests/test_agent.py"
      provides: "10+ test cases pokrywających wrap_with_agent + CLI integration"
      contains: "class TestWrapWithAgent"
      min_lines: 200
    - path: "scripts/generate_baseline.py"
      provides: "INVOCATIONS z --no-agent dodanym do każdej z 8 inwokacji (Strategia A z D-67)"
      contains: "'--no-agent'"
    - path: "scripts/regression_check.py"
      provides: "Albo bez zmian (Strategia A — generate_baseline.py niesie wszystkie zmiany), albo z deep_diff skip 3 nowych kluczy (Strategia B)"
      contains: "# Phase 4"
  key_links:
    - from: "tests/test_agent.py"
      to: "sphsim.agent.wrap_with_agent"
      via: "from sphsim.agent.rational import wrap_with_agent"
      pattern: "wrap_with_agent"
    - from: "tests/test_agent.py integration tests"
      to: "subprocess.run([python, 'sph_sim.py', ...])"
      via: "subprocess + json.loads(stdout)"
      pattern: "subprocess.run"
    - from: "scripts/generate_baseline.py INVOCATIONS"
      to: "scripts/regression_check.py"
      via: "from generate_baseline import INVOCATIONS (DRY single source of truth)"
      pattern: "from generate_baseline import"
---

<objective>
Dostarczyć pełen pokrycie testowe Phase 4 oraz zabezpieczyć backwards compat CLI-04: (1) `tests/test_agent.py` z 10+ test cases (wrapper unit + CLI integration); (2) aktualizacja `scripts/generate_baseline.py` INVOCATIONS o `--no-agent` (Strategia A z D-67 — fixtures bit-identyczne; alternatywnie Strategia B: deep_diff skipuje 3 nowe klucze — Claude's Discretion przy implementacji); (3) regression_check.py exit 0.

Purpose: Phase 4 wprowadza nowe domyślne zachowanie (agent default-on) które ZMIENIA output JSON dla każdej inwokacji bez --no-agent. CLI-04 (backwards compat z v1.0) wymaga że `python sph_sim.py --strategy naive --zeta 0.5 --no-agent` daje bit-identyczne wyniki vs v1.0. Bez aktualizacji generate_baseline.py, regression check failuje. Bez tests/test_agent.py, brak gwarancji że wrapper jest poprawny dla wszystkich edge cases (D-55, D-56, D-57).
Output: 1 nowy plik (tests/test_agent.py — 200+ linii) + 1 zmodyfikowany skrypt (scripts/generate_baseline.py — 8 tuple updates) + opcjonalnie zmodyfikowany regression_check.py (jeśli Strategia B).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-rational-agent-veto-layer/04-CONTEXT.md
@.planning/phases/04-rational-agent-veto-layer/04-PATTERNS.md
@tests/test_loader.py
@scripts/generate_baseline.py
@scripts/regression_check.py
@sphsim/agent/rational.py
@sphsim/core/device.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/test_agent.py — 10+ unit/integration test cases</name>
  <files>tests/test_agent.py</files>
  <read_first>
    - tests/test_loader.py (396 linii) — wzorzec testów stdlib-only unittest z setUp/tearDown, polish docstrings, subprocess integration (jeśli potrzeba)
    - sphsim/agent/rational.py — wrapper signature i behavior (po Planie 02)
    - sphsim/core/device.py — Device z n_vetoed + veto_phase_stats (po Planie 01)
    - .planning/phases/04-rational-agent-veto-layer/04-PATTERNS.md sekcja "### 9. tests/test_agent.py" — module docstring template, sys.path bootstrap, Device fixture helper, stub strategy_fn pattern, subprocess integration pattern z scripts/regression_check.py:79-99
    - .planning/phases/04-rational-agent-veto-layer/04-CONTEXT.md Claude's Discretion "Test coverage" (10 test cases: 8 unit + 2 integration)
    - sphsim/strategies/incentive.py — dla D-56 idempotency test (incentive + wrapper)
  </read_first>
  <behavior>
    - Test: 10+ test cases pokrywające: (1) wrapper passthrough dla ABSTAIN, (2) brak veto gdy E[zysk]>0, (3) veto gdy E[zysk]<0, (4) n_vetoed inkrementuje a n_abstain NIE, (5) total_h==0 fallback (D-55), (6) phi[idx]>=1.0 → veto (D-57), (7) idx>=len(phi) → veto (D-57), (8) incentive+wrapper idempotent (D-56), (9) `--compare-agent` JSON ma comparison block (integration test), (10) `--no-agent` daje n_vetoed_total=0 (integration test)
    - Test: stdlib only (unittest + subprocess + json + os + sys + tempfile — match test_loader.py)
    - Test: `python -m unittest tests.test_agent -v` exit 0 z minimum 10 passing tests
    - Test: `python -m unittest discover tests` exit 0 z 32+ tests total (22 pre-existing + 10+ nowe)
  </behavior>
  <action>
    Per 04-PATTERNS.md §9 i 04-CONTEXT.md Claude's Discretion "Test coverage": utwórz tests/test_agent.py. Struktura: (1) **Module docstring** (po polsku, lista 10 test cases zgodna z 04-PATTERNS.md §9 template, info "Stdlib only"). (2) **sys.path bootstrap** (verbatim z test_loader.py:33-36). (3) **Imports**: unittest, subprocess, json, os, sys, tempfile + `from sphsim.agent.rational import wrap_with_agent` + `from sphsim.core.device import Device` + `from sphsim.strategies.incentive import strategy_incentive`. (4) **Stałe**: `PROJECT_ROOT = ...`, `MONOLITH = PROJECT_ROOT / 'sph_sim.py'` (jak w regression_check.py). (5) **Helper `_make_device(phase, status='UP')`** zwracający `Device(id=0, phase=phase, status=status)`. (6) **Class `TestWrapWithAgent(unittest.TestCase)`** z 8 unit tests: (a) `test_abstain_passthrough` — stub zwracający 'ABSTAIN', wrapper zwraca 'ABSTAIN', n_vetoed pozostaje 0. (b) `test_commit_positive_E_passthrough` — stub COMMIT, expected_P=1000.0 (duże), assert wrapper zwraca 'COMMIT', n_vetoed=0. (c) `test_commit_negative_E_veto` — stub COMMIT, expected_P=0.01 (małe), assert wrapper zwraca 'VETO', n_vetoed=1, veto_phase_stats={1:1}. (d) `test_n_vetoed_increments_not_n_abstain` — explicit check że po VETO `dev.n_vetoed==1` i `dev.n_abstain==0` (rozróżnione kategorie). (e) `test_total_h_zero_fallback_D55` — l=[0,0,0,0], expected_P=1000.0 (duże), assert brak crash + jakaś sensowna decision (COMMIT lub VETO — kluczowe że NIE rzuca ZeroDivisionError). (f) `test_phi_one_guard_D57` — phi[idx]>=1.0, assert wrapper zwraca 'VETO' bez obliczeń exp_pay. (g) `test_idx_out_of_range_guard_D57` — dev.phase=10, len(phi)=5, assert wrapper zwraca 'VETO'. (h) `test_incentive_plus_wrapper_idempotent_D56` — wrap_with_agent(strategy_incentive, 100.0) na realistycznym Device, expected_P=100.0; oczekiwane: gdy incentive zwraca COMMIT, agent również passthrough (no veto) — sprawdź n_vetoed==0 po wielu wywołaniach z tym samym input'em. (7) **Class `TestCLIIntegration(unittest.TestCase)`** z 2 integration tests: (i) `test_compare_agent_json_has_comparison_block` — subprocess.run python sph_sim.py --strategy incentive --expected_P 30 --compare-agent --seed 42 --json, parse JSON, assert `'comparison' in result` i `result['comparison']['agent_helps'] in (True, False)`. (j) `test_no_agent_zero_vetoes` — subprocess.run python sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 --json, parse JSON, assert `result['metrics']['n_vetoed_total'] == 0` i `result['metrics']['agent_enabled'] == False`. (8) **`if __name__ == '__main__': unittest.main()`** na końcu. Polish docstrings dla każdej metody testowej. Verbose assertion messages (`msg=f"..."`). Stdlib only.
  </action>
  <verify>
    <automated>cd /Users/stanislawnagorski/Documents/STUDIA/sem4/ekonometria\ 2 && python -m unittest tests.test_agent -v 2>&1 | tail -30 && python -m unittest tests.test_agent 2>&1 | tail -1 | grep -E '^OK$|^FAILED' && python -m unittest discover tests 2>&1 | tail -1 | grep -E '^OK$'</automated>
  </verify>
  <acceptance_criteria>
    - Source: `test -f tests/test_agent.py`
    - Source: `wc -l tests/test_agent.py | awk '{print $1}'` ≥ 150 (10+ tests, polish docstrings, helper functions — realistic minimum)
    - Source: `grep -v '^#' tests/test_agent.py | grep -cE '^    def test_'` ≥ 10
    - Source: `grep -v '^#' tests/test_agent.py | grep -c 'wrap_with_agent'` ≥ 5
    - Source: `grep -v '^#' tests/test_agent.py | grep -c 'subprocess.run'` ≥ 2 (CLI integration tests)
    - Source: `grep -v '^#' tests/test_agent.py | grep -c "'comparison'"` ≥ 1 (integration test ix)
    - Source: `grep -v '^#' tests/test_agent.py | grep -cE "n_vetoed|n_abstain"` ≥ 2 (n_vetoed_increments test)
    - Source: `grep -v '^#' tests/test_agent.py | grep -cE 'D-5[3567]|D-66|D-67'` ≥ 1 (decision refs in docstrings)
    - Behavior: `python -m unittest tests.test_agent` exits 0
    - Behavior: `python -m unittest tests.test_agent -v 2>&1 | grep -cE '^test_.*\.\.\. ok$'` ≥ 10
    - Behavior: `python -m unittest discover tests` exits 0 (nie łamiemy pre-existing testów)
  </acceptance_criteria>
  <done>
    tests/test_agent.py istnieje, zawiera 10+ test cases (8 unit + 2 integration) pokrywających D-55/D-56/D-57/D-63/D-65/D-66/D-67. Wszystkie testy przechodzą. Pre-existing test suite (test_loader, test_strategy_meta_consistency) nadal działa. Pokrycie wystarczające dla Phase 4 exit gate w Planie 07.
  </done>
</task>

<task type="auto">
  <name>Task 2: scripts/generate_baseline.py — dodaj --no-agent do 8 INVOCATIONS (Strategia A z D-67) + regression_check.py weryfikacja</name>
  <files>scripts/generate_baseline.py, scripts/regression_check.py</files>
  <read_first>
    - scripts/generate_baseline.py — istniejące INVOCATIONS list (linie 32-51) + COMMANDS_HUMAN list (linie 54-63) — DRY single source of truth dla regression_check.py
    - scripts/regression_check.py — importuje INVOCATIONS z generate_baseline (linia 33). Bez zmian w regression_check.py gdy Strategia A; minimalna zmiana gdy Strategia B.
    - .planning/phases/04-rational-agent-veto-layer/04-PATTERNS.md sekcja "### 10. scripts/regression_check.py (MOD via generate_baseline.py)" — Phase 4 modification: dodaj '--no-agent' do każdej z 8 tuple INVOCATIONS oraz COMMANDS_HUMAN
    - .planning/phases/04-rational-agent-veto-layer/04-CONTEXT.md D-59 (Phase 4 mechanism: --no-agent escape hatch do trybu v1.0; fixtures niezmienialne) + D-67 (Strategia A: --no-agent w INVOCATIONS, fixtures bit-identyczne; Strategia B: deep_diff skip 3 nowych kluczy — Claude's Discretion)
    - tests/fixtures/baseline_v1/*.json — istniejące 8 fixtures wygenerowanych z v1.0 monolitu
  </read_first>
  <action>
    **Preferowana Strategia A (D-67, 04-PATTERNS.md §10):** Edytuj scripts/generate_baseline.py: (1) W INVOCATIONS list (linie 32-51) dodaj `'--no-agent'` na końcu każdej z 8 args list. Format: po obecnym ostatnim arg w każdej krotce, dopisz `, '--no-agent'`. Per 04-PATTERNS.md §10 dokładny target shape: każdy element to `(slug, [..., '--no-agent'])`. (2) W COMMANDS_HUMAN list (linie 54-63) dodaj `' --no-agent'` PRZED `' --seed 42 --json'` w każdym z 8 stringów. (3) **NIE generuj fixtures od nowa** — D-59 stipuluje że fixtures pozostają niezmienione (są oracle dla "v1.0 / Phase 1 surowy simulator"). Strategia A działa BO `--no-agent` ⇒ agent_enabled:false ⇒ wrapper nie aplikowany ⇒ output JSON powinien być bit-identyczny z v1.0 (zawierając dodatkowo 3 nowe pola w metrics: veto_per_phase:{}, n_vetoed_total:0, agent_enabled:false). UWAGA: jeśli istniejące fixtures NIE mają tych 3 nowych pól, regression deep_diff (regression_check.py:36-76) zwróci "KEY MISSING w actual" — to mówi że fixtures wymagają patcha (jednorazowy: dodaj `{"veto_per_phase": {}, "n_vetoed_total": 0, "agent_enabled": false}` do bloku metrics w każdym z 8 plików fixtures). **Decyzja przy implementacji:** Spróbuj Strategia A; jeśli regression fails wyłącznie na 3 nowych kluczach, zastosuj fixture patch (one-time `for f in tests/fixtures/baseline_v1/*.json: ...` aktualizacja przez Python). Albo (Strategia B fallback): wzbogać `deep_diff` w regression_check.py o opcjonalny skip-keys list z 3 kluczami — komentarz `# Phase 4: nowe klucze wprowadzone w fixturze są ignorowane przy compare (D-67 alternative)`. Wybierz wariant który działa po pierwszej próbie regression_check. (4) Dodaj komentarz w generate_baseline.py wskazujący że Phase 4 wprowadza `--no-agent` jako escape hatch do trybu v1.0 (D-59): np. nad listą INVOCATIONS dopisać `# Phase 4 D-59: --no-agent dodane do każdej inwokacji — agent jest default-on od Phase 4, ale fixtures są oracle dla v1.0 surowego symulatora.`
  </action>
  <verify>
    <automated>cd /Users/stanislawnagorski/Documents/STUDIA/sem4/ekonometria\ 2 && python -c "
from scripts.generate_baseline import INVOCATIONS, COMMANDS_HUMAN
assert len(INVOCATIONS) == 8, f'INVOCATIONS should have 8 entries, got {len(INVOCATIONS)}'
for slug, args in INVOCATIONS:
    assert '--no-agent' in args, f'Slug {slug} missing --no-agent in args: {args}'
for cmd in COMMANDS_HUMAN:
    assert '--no-agent' in cmd, f'COMMANDS_HUMAN entry missing --no-agent: {cmd}'
print(f'OK — wszystkie {len(INVOCATIONS)} INVOCATIONS i {len(COMMANDS_HUMAN)} COMMANDS_HUMAN mają --no-agent')
" && python scripts/regression_check.py 2>&1 | tail -10
</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "'--no-agent'" scripts/generate_baseline.py` ≥ 8 (8 INVOCATIONS dostały flagę)
    - Source: `grep -c -- '--no-agent' scripts/generate_baseline.py` ≥ 16 (8 w INVOCATIONS + 8 w COMMANDS_HUMAN)
    - Source: `grep -c 'Phase 4' scripts/generate_baseline.py` ≥ 1 (komentarz dokumentujący)
    - Behavior: `python -c "from scripts.generate_baseline import INVOCATIONS; assert all('--no-agent' in args for _, args in INVOCATIONS)"` exits 0
    - Behavior: `python scripts/regression_check.py` exit code 0 (PASS: 8/8). Jeśli exit code 1 — przedyskutuj wybrany wariant (Strategia A z fixtures patch vs Strategia B z deep_diff skip-keys) i zaaplikuj poprawkę; finalne uruchomienie MUSI być exit 0.
    - Behavior: `python -m unittest discover tests` exit 0 (regression w generate_baseline nie łamie istniejących testów)
  </acceptance_criteria>
  <done>
    Wszystkie 8 inwokacji w INVOCATIONS i COMMANDS_HUMAN zawierają --no-agent. Regression check zwraca exit 0 (PASS: 8/8). CLI-04 backwards compat utrzymany — v1.0 zachowanie reprodukowalne przez `--no-agent` flag. Plan 07 verify gate może bezpiecznie polegać na `python scripts/regression_check.py`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subprocess.run python sph_sim.py | Integration tests w test_agent.py uruchamiają CLI — args z list literal, NIE shell injection (subprocess.run([...], shell=False) by default) |
| fixtures vs current output | regression_check.py używa deep_diff exact equality — JEDNA różnica w jednym kluczu = FAIL; D-67 Strategia A/B oboje wymagają że BAZA (avg_val_last100, cum_val_total, ...) bit-identyczna |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-21 | Tampering | tests/test_agent.py modyfikuje sys.modules przez import | accept | Stdlib unittest izoluje per-test stan; brak współdzielonego state'u poprzez moduły. |
| T-04-22 | Repudiation | Backwards compat CLI-04 broken | mitigate | Strategia A z D-67 zapewnia bit-identyczne fixtures matching (po opcjonalnym one-time fixture patch dodającym 3 nowe klucze); regression_check.py exit 0 jest hard gate. |
| T-04-23 | DoS | Integration tests uruchamiają subprocess 2x | accept | Każdy test trwa kilka sekund (T=2000 default); 10 testów total ~30s. Akceptowalne dla pre-commit. |
| T-04-SC | Tampering | scripts/* nie dodają zależności | n/a | Stdlib only (PROJECT.md constraint); Package Legitimacy Gate inapplicable. |
</threat_model>

<verification>
- `python -m unittest tests.test_agent -v` exit 0 z minimum 10 tests passing
- `python -m unittest discover tests` exit 0 z 32+ tests total
- `python scripts/regression_check.py` exit 0 (PASS: 8/8)
- 04-CONTEXT.md SC #1-5 readyness sprawdzone w Planie 07 verify gate
</verification>

<success_criteria>
- tests/test_agent.py istnieje z 10+ test cases pokrywających D-55/D-56/D-57/D-63/D-65/D-66/D-67
- Wszystkie nowe testy passing (`python -m unittest tests.test_agent` exit 0)
- Pre-existing 22+ tests nadal passing (test_loader, test_strategy_meta_consistency)
- scripts/generate_baseline.py INVOCATIONS i COMMANDS_HUMAN mają --no-agent w każdej z 8 inwokacji
- `python scripts/regression_check.py` exit 0 — bit-identyczne fixtures match dla wszystkich 8 baseline invocations
- CLI-04 backwards compat formally weryfikowane
</success_criteria>

<output>
Create `.planning/phases/04-rational-agent-veto-layer/04-06-SUMMARY.md` po ukończeniu.
</output>
