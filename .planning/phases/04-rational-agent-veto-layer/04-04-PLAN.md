---
phase: 04-rational-agent-veto-layer
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - sphsim/cli/args.py
  - sphsim/cli/main.py
autonomous: true
requirements: [AGENT-01, AGENT-03, AGENT-05]

must_haves:
  truths:
    - "argparse rozpoznaje flagi --no-agent i --compare-agent (boolean store_true, poza mutex --interactive|--strategy|--custom)"
    - "argparse error gdy --compare-agent i --no-agent jednocześnie (Polish error: 'Flagi --compare-agent i --no-agent są wzajemnie wykluczające')"
    - "argparse error gdy --compare-agent i --interactive jednocześnie (D-60)"
    - "main.py opakowuje strategy_fn w wrap_with_agent przed budową SPHSimulator gdy not args.no_agent (oba branches: built-in i --custom)"
    - "main.py uruchamia run_compare(...) gdy args.compare_agent — 2x SPHSimulator (z agentem i bez), agregacja delta KPI, output przez format_human/format_json z res zawierającym 'comparison' block"
    - "help text dla --expected_P aktualizowany: '[incentive|agent] Oczek. płatność (def 100.0)'"
  artifacts:
    - path: "sphsim/cli/args.py"
      provides: "--no-agent i --compare-agent flags + post-parse mutex check"
      contains: "--no-agent"
    - path: "sphsim/cli/main.py"
      provides: "wrap_with_agent integration (oba branches) + run_compare function"
      contains: "wrap_with_agent"
  key_links:
    - from: "sphsim/cli/main.py"
      to: "sphsim.agent.wrap_with_agent"
      via: "import wrap_with_agent + conditional wrap przed SPHSimulator()"
      pattern: "from sphsim.agent import wrap_with_agent"
    - from: "sphsim/cli/args.py"
      to: "p.error('Polish msg')"
      via: "argparse hard error przy mutex violation"
      pattern: "p\\.error"
    - from: "main.py run_compare"
      to: "format_compare via format_human (comparison branch)"
      via: "res = {'comparison': {...}}"
      pattern: "'comparison'"
---

<objective>
Wpięcie wrappera RationalAgent (Plan 02) do CLI one-shot path: (1) flagi `--no-agent` i `--compare-agent` w argparse z post-parse mutex enforcement, (2) `main()` opakowuje strategy_fn w `wrap_with_agent(...)` przed budową SPHSimulator (oba branches: built-in `--strategy` i custom `--custom`) gdy `not args.no_agent`, (3) nowa funkcja `run_compare(args, raw_strategy_fn, name, params, K1)` uruchamia symulator dwukrotnie i zwraca dict z `'comparison'` blokiem dla format_compare.

Purpose: AGENT-01 (wrapper default-on), AGENT-03 (--no-agent flag), AGENT-05 (--compare-agent delta KPI) wymagają tej integracji. Bez wpięcia, Plan 02 wrapper jest unused; bez run_compare, --compare-agent nie ma backend. Backwards compat CLI-04: --no-agent jest dokumentowanym escape hatch'em do trybu v1.0 (D-59).
Output: Zmodyfikowane sphsim/cli/args.py (+2 flagi + mutex check) i sphsim/cli/main.py (wrap call w obu branches + run_compare function).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-rational-agent-veto-layer/04-CONTEXT.md
@.planning/phases/04-rational-agent-veto-layer/04-PATTERNS.md
@sphsim/cli/args.py
@sphsim/cli/main.py
@sphsim/cli/output.py

<interfaces>
Po Planie 02, sphsim.agent eksportuje:
```python
from sphsim.agent import wrap_with_agent
# wrap_with_agent(strategy_fn: Callable, expected_P: float) -> Callable (closure z 8-arg sygnaturą)
```

Po Planie 03, sphsim.cli.output eksportuje:
```python
from sphsim.cli.output import format_human, format_json, format_compare
# format_human(args, res, K1, verbose) — z guardem `if 'comparison' in res: return format_compare(...)`
# format_json(args, res, params, K1) — z dispatch metrics vs comparison + agent_enabled
# format_compare(args, comp, K1) — 5×3 delta table
```

run_compare zwraca dict zgodny z format_compare API:
```python
{
    'comparison': {
        'with_agent': {<full metrics z res sim_with>},
        'without_agent': {<full metrics z res sim_without>},
        'delta': {'avg_val_last100': float, 'cum_val_total': float, 'avg_net_profit': float, 'delivery_ratio': float, 'avg_providers_l100': float},
        'agent_helps': bool,  # res_with['avg_net_profit'] > res_without['avg_net_profit']
    }
}
```

(D-67: ten dict zostaje passed do format_human → format_human wykrywa 'comparison' w res → wywołuje format_compare; format_json detektuje to samo → zwraca JSON z top-level 'comparison' zamiast 'metrics'.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sphsim/cli/args.py — --no-agent + --compare-agent + mutex check</name>
  <files>sphsim/cli/args.py</files>
  <read_first>
    - sphsim/cli/args.py — istniejący argparse z mutex group (linie 38-44: --interactive|--strategy|--custom) + boolean flags --json/--verbose (linie 63-64) + --expected_P (linia 51) + return p.parse_args() (linia 65)
    - .planning/phases/04-rational-agent-veto-layer/04-PATTERNS.md sekcja "### 5. sphsim/cli/args.py" — wzorzec boolean-flag (--json/--verbose) + post-parse mutex check z `p.error()`
    - .planning/phases/04-rational-agent-veto-layer/04-CONTEXT.md D-58 (--no-agent boolean store_true, default=False = agent włączony) + D-60 (--compare-agent boolean store_true; mutex z --no-agent i --interactive; argparse error z polskim komunikatem) + D-54 (--expected_P help text update '[incentive|agent] Oczek. płatność (def 100.0)')
  </read_first>
  <behavior>
    - Test: `python sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 --json` exit 0 (flaga rozpoznana, argparse OK)
    - Test: `python sph_sim.py --strategy naive --zeta 0.5 --compare-agent --seed 42 --json` exit 0 (flaga rozpoznana)
    - Test: `python sph_sim.py --strategy naive --zeta 0.5 --compare-agent --no-agent` exit 2 z polskim komunikatem zawierającym '--compare-agent' i '--no-agent' i 'wykluczające'
    - Test: `python sph_sim.py --interactive --compare-agent` exit 2 z polskim komunikatem o niekompatybilności --compare-agent z --interactive
    - Test: `python sph_sim.py --strategy naive --zeta 0.5` (bez flag) — args.no_agent == False i args.compare_agent == False (defaults)
    - Test: `python sph_sim.py --help 2>&1 | grep -- '--no-agent'` zwraca linię z opisem po polsku
    - Test: `python sph_sim.py --help 2>&1 | grep -- '--compare-agent'` zwraca linię z opisem po polsku
    - Test: help text dla --expected_P zaktualizowany do '[incentive|agent] Oczek. płatność (def 100.0)' lub bardzo bliskie wariant
  </behavior>
  <action>
    Per 04-PATTERNS.md §5 i 04-CONTEXT.md D-58/D-60/D-54: (1) **Refaktor return parse_args()** — obecna funkcja `parse_args()` kończy się `return p.parse_args()` (linia 65). Zmień na: `args = p.parse_args()` + 2 post-parse mutex checks + `return args`. (2) **Dodaj 2 boolean flags POZA mutex group** (po linii 64 `--verbose`): `p.add_argument('--no-agent', action='store_true', help='Wyłącz RationalAgent (surowa strategia, bez veto)')` i `p.add_argument('--compare-agent', action='store_true', help='Uruchom 2x: z agentem i bez — tabela delta KPI')`. (3) **Post-parse mutex check #1** (D-60): `if args.compare_agent and args.no_agent: p.error("Flagi --compare-agent i --no-agent są wzajemnie wykluczające.")` (4) **Post-parse mutex check #2** (D-60): `if args.compare_agent and args.interactive: p.error("Flaga --compare-agent nie działa w trybie --interactive.")` (5) **Update --expected_P help text** (linia 51) per D-54 (`'[incentive|agent] Oczek. płatność (def 100.0)'`). Polskie komunikaty z polskimi znakami diakrytycznymi (Phase 3 D-44 precedent). Brak nowych importów (`p.error()` używa istniejącego `p` z funkcji). args.no_agent (Python kebab→snake) i args.compare_agent (argparse default conversion).
  </action>
  <verify>
    <automated>cd /Users/stanislawnagorski/Documents/STUDIA/sem4/ekonometria\ 2 && python -c "
import subprocess, sys

# Test 1: --no-agent recognized
r = subprocess.run([sys.executable, 'sph_sim.py', '--strategy', 'naive', '--zeta', '0.5', '--no-agent', '--seed', '42', '--json'],
                   capture_output=True, text=True)
assert r.returncode == 0, f'--no-agent should succeed, stderr={r.stderr[:300]}'

# Test 2: --compare-agent + --no-agent rejected with Polish message
r = subprocess.run([sys.executable, 'sph_sim.py', '--strategy', 'naive', '--zeta', '0.5', '--compare-agent', '--no-agent'],
                   capture_output=True, text=True)
assert r.returncode != 0, f'mutex --compare-agent + --no-agent should fail, got rc={r.returncode}'
assert 'wykluczające' in r.stderr or 'wykluczajace' in r.stderr, f'Polish error msg missing, stderr={r.stderr[:300]}'

# Test 3: --interactive + --compare-agent rejected
r = subprocess.run([sys.executable, 'sph_sim.py', '--interactive', '--compare-agent'],
                   capture_output=True, text=True)
assert r.returncode != 0, f'--interactive + --compare-agent should fail, got rc={r.returncode}'

# Test 4: --help shows new flags
r = subprocess.run([sys.executable, 'sph_sim.py', '--help'], capture_output=True, text=True)
assert '--no-agent' in r.stdout, '--no-agent missing in --help'
assert '--compare-agent' in r.stdout, '--compare-agent missing in --help'

print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -v '^#' sphsim/cli/args.py | grep -c "'--no-agent'"` ≥ 1
    - Source: `grep -v '^#' sphsim/cli/args.py | grep -c "'--compare-agent'"` ≥ 1
    - Source: `grep -v '^#' sphsim/cli/args.py | grep -c 'p.error'` ≥ 2 (oba mutex checks)
    - Source: `grep -v '^#' sphsim/cli/args.py | grep -c 'wykluczające'` ≥ 1
    - Source: `grep -v '^#' sphsim/cli/args.py | grep -c "incentive|agent"` ≥ 1 (--expected_P help text update)
    - Behavior: verify command (4 inline test cases) exits 0
    - Behavior: `python sph_sim.py --strategy naive --no-agent --seed 42 --json 2>/dev/null | tail -n +1 | python -c "import json,sys; raw=sys.stdin.read(); d=json.loads(raw[raw.find('{'):]); assert d['metrics']['agent_enabled'] == False"` — JSON ma agent_enabled=false dla --no-agent (Plan 03 dostarcza tę logikę)
    - Behavior: `python sph_sim.py --help 2>&1 | grep -- '[incentive|agent]'` ≥ 1 wiersz (--expected_P help update)
  </acceptance_criteria>
  <done>
    args.py rozpoznaje --no-agent i --compare-agent jako boolean flags (default False, poza mutex group). Mutex enforced przez post-parse p.error() z polskim komunikatem. --expected_P help text mówi że flaga jest reuse'owana przez agent. Main.py może czytać args.no_agent i args.compare_agent.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sphsim/cli/main.py — wrap + run_compare</name>
  <files>sphsim/cli/main.py</files>
  <read_first>
    - sphsim/cli/main.py — istniejący main() z dwoma branches: --custom (linie 21-53) i built-in (linie 54-72) oraz wczesna --interactive ścieżka (linie 11-14)
    - .planning/phases/04-rational-agent-veto-layer/04-PATTERNS.md sekcja "### 7. sphsim/cli/main.py" — wrap_with_agent injection patterns (oba branches) + run_compare skeleton
    - .planning/phases/04-rational-agent-veto-layer/04-CONTEXT.md D-58 (default-on agent) + D-60 (--compare-agent z --strategy lub --custom) + Claude's Discretion "compare-agent interakcja z seed" (same seed dla obu run'ów, deterministic) + `<code_context>` §4 (run_compare sketch z 5 KPI delta)
    - sphsim/agent/__init__.py (Plan 02 output) — wrap_with_agent eksport
    - sphsim/cli/output.py (Plan 03 output) — format_human z comparison branch + format_json z dispatch + format_compare
  </read_first>
  <behavior>
    - Test: `python sph_sim.py --strategy naive --zeta 0.5 --seed 42 --json` (bez --no-agent) — JSON pokazuje `agent_enabled: true`; jeśli strategia kiedykolwiek COMMIT przy phi=1.0 lub net<0, n_vetoed_total > 0
    - Test: `python sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 --json` — JSON pokazuje `agent_enabled: false` i `n_vetoed_total: 0`
    - Test: `python sph_sim.py --strategy incentive --expected_P 30 --compare-agent --seed 42 --json` — JSON top-level zawiera klucz 'comparison' z polami with_agent, without_agent, delta, agent_helps
    - Test: `python sph_sim.py --strategy incentive --expected_P 30 --compare-agent --seed 42` (bez --json) — stdout zawiera tabelę delta KPI z werdyktem '✓ TAK' lub '✗ NIE'
    - Test: `python sph_sim.py --custom examples/custom_strategy_template.py --seed 42 --json` (bez --no-agent) — agent default-on dla custom też (D-58)
    - Test: `python sph_sim.py --custom examples/custom_strategy_template.py --compare-agent --seed 42 --json` — comparison block dla custom strategii
    - Test: --compare-agent z --no-agent zatrzymuje się w argparse error (Task 1)
    - Test: regression check po wszystkich planach (Plan 06) — wszystkie 8 fixture invocations z dodanym --no-agent dają bit-identyczne (lub z 3 nowymi kluczami zignorowanymi) wyniki vs v1.0 fixtures
  </behavior>
  <action>
    Per 04-PATTERNS.md §7 i 04-CONTEXT.md `<code_context>` §4: (1) **Top-of-file import** (na początku main.py, obok istniejących importów): `from sphsim.agent import wrap_with_agent`. (2) **Custom branch wrap** (PRZED budową SPHSimulator dla --custom, czyli przed obecną linią 42 `sim = SPHSimulator(...)`): wstaw `if not args.no_agent: strategy_fn = wrap_with_agent(strategy_fn, args.expected_P)`. (3) **Built-in branch wrap** (PRZED linią 60 `sim = SPHSimulator(...)` dla --strategy): rozdziel obecną konstrukcję `strategy_fn=STRATEGIES[args.strategy]` na osobne kroki: `strategy_fn = STRATEGIES[args.strategy]; if not args.no_agent: strategy_fn = wrap_with_agent(strategy_fn, args.expected_P)`, następnie użyj nowej zmiennej w `SPHSimulator(...strategy_fn=strategy_fn,...)`. (4) **--compare-agent branch** (NOWY, w obu ścieżkach: custom i built-in) — PRZED standardową ścieżką `sim = SPHSimulator(...)`, dodaj guard: `if args.compare_agent: res = run_compare(args, strategy_fn=raw_strategy_fn, name=name, params=params, K1=K1); print(format_json(args, res, params, K1) if args.json else format_human(args, res, K1, args.verbose)); return`. Klucz: `raw_strategy_fn` to **NIEopakowany** strategy_fn (gdy compare_agent jest True, NIE robimy wrap na step (2)/(3) — zostawiamy raw dla run_compare, które samo wrap'uje dwukrotnie). (5) **Nowa funkcja `run_compare`** zdefiniowana ABOVE `main()` (top-level): sygnatura `def run_compare(args, raw_strategy_fn, name, params, K1):`. Wewnątrz: zbuduj `common = dict(nU=args.nU, nSUS=args.nSUS, K0=DEFAULT_K0, K1=K1, F=DEFAULT_F, T=args.T, kappa=args.kappa, alpha=args.alpha, phi=DEFAULT_PHI, rho=DEFAULT_RHO, params=params, seed=args.seed)`. Stwórz `sim_with = SPHSimulator(strategy_fn=wrap_with_agent(raw_strategy_fn, args.expected_P), **common)` i `res_with = sim_with.run()`. Stwórz `sim_without = SPHSimulator(strategy_fn=raw_strategy_fn, **common)` i `res_without = sim_without.run()`. Zbuduj `KPIS = ['avg_val_last100', 'cum_val_total', 'avg_net_profit', 'delivery_ratio', 'avg_providers_l100']`. Zwróć `{'comparison': {'with_agent': {k: v for k, v in res_with.items() if k not in ('history', 'devices')}, 'without_agent': {k: v for k, v in res_without.items() if k not in ('history', 'devices')}, 'delta': {k: res_with[k] - res_without[k] for k in KPIS}, 'agent_helps': res_with['avg_net_profit'] > res_without['avg_net_profit']}}`. (6) **Critical: zmień strukturę main()** tak żeby compare_agent branch był rozpoznany w obu ścieżkach (custom i built-in) — najprostsza implementacja: ZAMIAST inline w obu branches, dodaj jeden global guard na końcu obu branches (po wyznaczeniu `strategy_fn`, `name`, `params`, `K1`): `if args.compare_agent: res = run_compare(args, strategy_fn, name, params, K1); print(format_json(args, res, params, K1) if args.json else format_human(args, res, K1, args.verbose)); return`. UWAGA: gdy compare_agent jest True, raw_strategy_fn musi być NIEopakowany — zachowaj kopię PRZED step (2)/(3) wrap, np. `raw_strategy_fn = strategy_fn; if not args.no_agent: strategy_fn = wrap_with_agent(...)`. W compare_agent ścieżce użyj `raw_strategy_fn`. (7) Dla --custom branch: `name = name` (z load_custom return), dla built-in: `name = args.strategy`. (8) Polish komunikaty zero w main.py (matematyczna integracja, brak UI — komunikaty są w format_human/format_compare z Planu 03).
  </action>
  <verify>
    <automated>cd /Users/stanislawnagorski/Documents/STUDIA/sem4/ekonometria\ 2 && python -c "
import subprocess, sys, json

# Test 1: default-on agent (bez --no-agent) — agent_enabled = true
r = subprocess.run([sys.executable, 'sph_sim.py', '--strategy', 'naive', '--zeta', '0.5', '--seed', '42', '--json'],
                   capture_output=True, text=True, check=True)
raw = r.stdout
d = json.loads(raw[raw.find('{'):])
assert d['metrics']['agent_enabled'] == True, f'agent should be enabled by default, got {d[\"metrics\"][\"agent_enabled\"]}'

# Test 2: --no-agent → agent_enabled false + n_vetoed_total 0
r = subprocess.run([sys.executable, 'sph_sim.py', '--strategy', 'naive', '--zeta', '0.5', '--no-agent', '--seed', '42', '--json'],
                   capture_output=True, text=True, check=True)
raw = r.stdout
d = json.loads(raw[raw.find('{'):])
assert d['metrics']['agent_enabled'] == False
assert d['metrics']['n_vetoed_total'] == 0

# Test 3: --compare-agent → JSON ma comparison block
r = subprocess.run([sys.executable, 'sph_sim.py', '--strategy', 'incentive', '--expected_P', '30', '--compare-agent', '--seed', '42', '--json'],
                   capture_output=True, text=True, check=True)
raw = r.stdout
d = json.loads(raw[raw.find('{'):])
assert 'comparison' in d, f'comparison missing in top-level: {list(d.keys())}'
comp = d['comparison']
assert 'with_agent' in comp and 'without_agent' in comp and 'delta' in comp and 'agent_helps' in comp
assert isinstance(comp['agent_helps'], bool)

# Test 4: --compare-agent human output ma werdykt
r = subprocess.run([sys.executable, 'sph_sim.py', '--strategy', 'incentive', '--expected_P', '30', '--compare-agent', '--seed', '42'],
                   capture_output=True, text=True, check=True)
assert '✓' in r.stdout or '✗' in r.stdout or 'TAK' in r.stdout or 'NIE' in r.stdout, f'Verdict marker missing in human output: {r.stdout[:500]}'

# Test 5: --custom z agentem default-on
r = subprocess.run([sys.executable, 'sph_sim.py', '--custom', 'examples/custom_strategy_template.py', '--seed', '42', '--json'],
                   capture_output=True, text=True, check=True)
raw = r.stdout
d = json.loads(raw[raw.find('{'):])
assert d['metrics']['agent_enabled'] == True, 'agent should be default-on for --custom too (D-58)'

print('OK — 5 path testów wired')
"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -v '^#' sphsim/cli/main.py | grep -c "from sphsim.agent import wrap_with_agent"` ≥ 1
    - Source: `grep -v '^#' sphsim/cli/main.py | grep -c "wrap_with_agent(strategy_fn"` ≥ 2 (jeden dla custom branch, jeden dla built-in)
    - Source: `grep -v '^#' sphsim/cli/main.py | grep -c "args.no_agent"` ≥ 2
    - Source: `grep -v '^#' sphsim/cli/main.py | grep -c "args.compare_agent"` ≥ 1
    - Source: `grep -v '^#' sphsim/cli/main.py | grep -c "^def run_compare"` ≥ 1
    - Source: `grep -v '^#' sphsim/cli/main.py | grep -c "agent_helps"` ≥ 1
    - Source: `grep -v '^#' sphsim/cli/main.py | grep -c "'comparison'"` ≥ 1
    - Behavior: verify command (5 inline path tests) exits 0
    - Behavior: `python sph_sim.py --strategy incentive --expected_P 30 --compare-agent --seed 42 --json 2>/dev/null | tail -n +1 | python -c "import json,sys; raw=sys.stdin.read(); d=json.loads(raw[raw.find('{'):]); assert d['comparison']['agent_helps'] in (True, False)"` exits 0 (SC #5 readyness — finalna weryfikacja w Planie 07)
  </acceptance_criteria>
  <done>
    main.py wpina wrap_with_agent w obu branches (built-in + custom) gdy not args.no_agent. Nowa funkcja run_compare uruchamia simulator 2x i zwraca dict z 'comparison' block, który format_human/format_json dispatchuje na format_compare. AGENT-01 + AGENT-03 + AGENT-05 (CLI part) zaimplementowane.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| argparse args → main() | args.no_agent i args.compare_agent są boolean z argparse, default False — wrap zawsze gdy False; mutex enforced w args.py Task 1 |
| run_compare → 2x SPHSimulator | Każdy SPHSimulator ma własny `random.seed(args.seed)` w __init__ — deterministic, ale dwa wywołania z tym samym seedem dają identyczne państwo początkowe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Tampering | wrap_with_agent na nieowiniętym strategy_fn (compare branch) | mitigate | run_compare zachowuje raw_strategy_fn jako oddzielną zmienną PRZED conditional wrap (D-58 + Claude's Discretion); double-wrapping NIE występuje. |
| T-04-14 | Repudiation | Backwards compat CLI-04 | mitigate | `python sph_sim.py --strategy naive --zeta 0.5 --no-agent` MUSI dać bit-identyczny output JSON jak v1.0 (regression check w Planie 06 weryfikuje). |
| T-04-15 | DoS | --compare-agent uruchamia 2x simulator | accept | Użytkownik świadomie wybiera --compare-agent (2x cost); domyślny T=2000 daje ~kilka sekund total nawet bez optymalizacji. Brak confirmation prompt (D-Deferred). |
| T-04-16 | Spoofing | args.expected_P używane przez wrapper niezależnie od strategy | accept | Per D-54 to jest celowo single source of truth — flag --expected_P 100 zarówno dla incentive jak i agent. |
| T-04-SC | Tampering | npm/pip/cargo installs | n/a | Phase 4 nie dodaje zależności (PROJECT.md "Python 3.7+ stdlib only"); brak package install — Package Legitimacy Gate inapplicable. |
</threat_model>

<verification>
- Verify commands (Task 1 + Task 2 = 9 inline path tests) exits 0
- `python -m unittest discover tests` 22+ pre-existing testy pass (nie łamiemy)
- `python sph_sim.py --strategy naive --zeta 0.75 --seed 42 --json` (default agent ON) różni się od fixture (oczekiwane — fixture był v1.0 bez agenta)
- `python sph_sim.py --strategy naive --zeta 0.75 --no-agent --seed 42 --json` daje wyniki bit-identyczne z fixture (po Planie 06 fixtures patch / lub po Planie 06 regression skip 3 nowych kluczy)
</verification>

<success_criteria>
- Flagi --no-agent i --compare-agent rozpoznane przez argparse (boolean store_true, default False)
- Mutex enforced: --compare-agent + --no-agent → exit 2 z polskim komunikatem
- Mutex enforced: --compare-agent + --interactive → exit 2 z polskim komunikatem
- Help dla --expected_P: '[incentive|agent] Oczek. płatność (def 100.0)' lub bliskie równoznaczne
- Default-on agent: bez --no-agent, args.no_agent==False i wrap_with_agent jest aplikowany w obu branches
- --custom branch też dostaje wrap (D-58)
- --compare-agent uruchamia run_compare(...) zwracające dict z 'comparison' block
- Output format_human / format_json wykrywa 'comparison' i renderuje delta table (Plan 03 dostarcza format_compare i dispatch)
- SC #5 readyness: `--strategy incentive --expected_P 30 --compare-agent --seed 42 --json` zwraca JSON z `comparison.agent_helps: bool` — w Planie 07 verify gate to weryfikuje finalnie
</success_criteria>

<output>
Create `.planning/phases/04-rational-agent-veto-layer/04-04-SUMMARY.md` po ukończeniu.
</output>
