---
phase: 04-rational-agent-veto-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sphsim/core/device.py
  - sphsim/core/simulator.py
autonomous: true
requirements: [AGENT-04]

must_haves:
  truths:
    - "Device dataclass udostępnia pole licznikowe n_vetoed (default 0) — D-63"
    - "Device.__post_init__ inicjalizuje pusty dict veto_phase_stats — D-63"
    - "Simulator rozpoznaje 3-stanowy decision 'COMMIT' | 'ABSTAIN' | 'VETO' — D-65"
    - "VETO ścieżka NIE inkrementuje n_abstain — tylko status='DOWN', down_left=1 — D-65"
    - "simulator.run() zwraca veto_per_phase: dict i n_vetoed_total: int w dict wynikowym — D-64"
  artifacts:
    - path: "sphsim/core/device.py"
      provides: "Device z polem n_vetoed:int=0 i dict veto_phase_stats"
      contains: "n_vetoed: int = 0"
    - path: "sphsim/core/simulator.py"
      provides: "3-state decision branch + veto_per_phase aggregation"
      contains: "elif decision == 'VETO':"
  key_links:
    - from: "sphsim/core/simulator.py"
      to: "sphsim/core/device.py"
      via: "dev.veto_phase_stats.items() iteration"
      pattern: "dev\\.veto_phase_stats"
    - from: "sphsim/core/simulator.py return dict"
      to: "downstream output formatters"
      via: "'veto_per_phase' and 'n_vetoed_total' keys"
      pattern: "'veto_per_phase'.*'n_vetoed_total'"
---

<objective>
Rozszerzyć model danych (Device) i orchestrator (Simulator) o 3-stanowy interfejs decyzji COMMIT/ABSTAIN/VETO oraz infrastrukturę zliczania veto per faza — fundament dla wrappera RationalAgent (Plan 02) i wszystkich downstream formatterów (Plan 03+).

Purpose: Bez tych pól w Device i bez VETO-branch w simulator.run() wrapper z Planu 02 nie ma gdzie zapisać wyników veta, a output (Plan 03) nie ma czego renderować. Wave-1 fundament dla całej fazy.
Output: Zmodyfikowane sphsim/core/device.py (+1 pole + 1 dict) oraz sphsim/core/simulator.py (3-stanowy branch + agregacja paralleled to ic_per_phase).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rational-agent-veto-layer/04-CONTEXT.md
@.planning/phases/04-rational-agent-veto-layer/04-PATTERNS.md
@sphsim/core/device.py
@sphsim/core/simulator.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Device — dodaj n_vetoed + veto_phase_stats</name>
  <files>sphsim/core/device.py</files>
  <read_first>
    - sphsim/core/device.py — istniejący dataclass z 4 polami licznikowymi (n_commit, n_abstain, n_delivered, n_failed) + __post_init__ inicjalizujący phase_stats={}
    - .planning/phases/04-rational-agent-veto-layer/04-PATTERNS.md sekcja "### 3. sphsim/core/device.py (MOD — add n_vetoed + veto_phase_stats)" — analog "Existing counter pattern (device.py:17-20)" i "Existing __post_init__ pattern (device.py:22-24)"
    - .planning/phases/04-rational-agent-veto-layer/04-CONTEXT.md D-63 (n_vetoed jako 5-te pole licznikowe paralelnie do n_commit/n_abstain/n_delivered/n_failed; veto_phase_stats jako dict w __post_init__ paralelnie do phase_stats)
  </read_first>
  <behavior>
    - Test (manual import): `from sphsim.core.device import Device; d = Device(id=0, phase=1, status='UP'); assert d.n_vetoed == 0 and d.veto_phase_stats == {}`
    - Test: nowy field n_vetoed:int=0 jest pozycyjnie PO n_failed (zachowanie istniejącego kwargs-only stylu konstrukcji w simulator.py:20,23 — wstawienie nie łamie żadnego call site)
    - Test: dict veto_phase_stats utworzony w __post_init__ (NIE jako dataclass field z default_factory — match istniejącego stylu phase_stats)
  </behavior>
  <action>
    Per D-63 (04-CONTEXT.md) dodaj jedno pole `n_vetoed: int = 0` do dataclass Device w sphsim/core/device.py — wstaw je BEZPOŚREDNIO PO linii `n_failed: int = 0` (currently line 20). W metodzie `__post_init__` dodaj jedną linię inicjalizującą pusty dict: `self.veto_phase_stats = {}` (komentarz: `# {phase: count} — Phase 4 D-64`) PO istniejącej `self.phase_stats = {}`. Match style: dataclass field z literal default (NIE field(default_factory=dict)) jak istniejące pola licznikowe; dict w __post_init__ jak istniejące phase_stats. Brak nowych importów, brak nowych metod (wrapper z Planu 02 będzie bezpośrednio mutował dev.n_vetoed i dev.veto_phase_stats[ph] — analogicznie do tego, jak record_commit/record_delivery/record_failure mutują phase_stats).
  </action>
  <verify>
    <automated>python -c "from sphsim.core.device import Device; d = Device(id=0, phase=1, status='UP'); assert d.n_vetoed == 0, f'n_vetoed should be 0, got {d.n_vetoed}'; assert d.veto_phase_stats == {}, f'veto_phase_stats should be empty dict, got {d.veto_phase_stats!r}'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -v '^#' sphsim/core/device.py | grep -c 'n_vetoed: int = 0'` ≥ 1
    - Source: `grep -v '^#' sphsim/core/device.py | grep -c 'self.veto_phase_stats = {}'` ≥ 1
    - Behavior: `python -c "from sphsim.core.device import Device; d = Device(id=0, phase=1, status='UP'); assert d.n_vetoed == 0 and d.veto_phase_stats == {}"` exits 0
    - Behavior: `python -m unittest tests.test_strategy_meta_consistency` exits 0 (sprawdza że dataclass nie został zepsuty)
    - Source: `python -c "from dataclasses import fields; from sphsim.core.device import Device; names = [f.name for f in fields(Device)]; assert 'n_vetoed' in names, f'n_vetoed missing in {names}'"` exits 0
  </acceptance_criteria>
  <done>
    Device ma 6-te pole licznikowe n_vetoed (default 0) oraz dict veto_phase_stats utworzony w __post_init__. Istniejący test tests/test_strategy_meta_consistency.py nadal przechodzi. Plan 02 (wrapper) może mutować te pola.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Simulator — VETO branch (3-state interface) + veto_per_phase aggregation</name>
  <files>sphsim/core/simulator.py</files>
  <read_first>
    - sphsim/core/simulator.py — istniejący `if decision == 'COMMIT': ... else: dev.n_abstain += 1; dev.status = 'DOWN'; dev.down_left = 1` (linie 49-72) + agregacja ic_per_phase (linie 113-138) + return dict (linie 140-150)
    - .planning/phases/04-rational-agent-veto-layer/04-PATTERNS.md sekcja "### 4. sphsim/core/simulator.py (MOD — VETO guard + veto_per_phase aggregation)" — pattern modyfikacji ABSTAIN branch + parallel aggregation template
    - .planning/phases/04-rational-agent-veto-layer/04-CONTEXT.md D-65 (Claude's Discretion (a): 3-stanowy interface, VETO ma identyczną mechanikę DOWN jak ABSTAIN ale BEZ inkrementacji n_abstain) + D-64 (agregacja veto_per_phase paralelnie do ic_per_phase)
    - sphsim/core/device.py po Task 1 (musi już mieć n_vetoed + veto_phase_stats)
  </read_first>
  <behavior>
    - Test: strategy_fn zwracająca 'VETO' powoduje że simulator NIE inkrementuje n_abstain ale ustawia status='DOWN' i down_left=1
    - Test: strategy_fn zwracająca 'ABSTAIN' (lub nieznany string) nadal inkrementuje n_abstain (backwards compat)
    - Test: simulator.run() zwraca dict z kluczami 'veto_per_phase' (dict) i 'n_vetoed_total' (int)
    - Test: gdy żadne urządzenie nie ma wpisu w veto_phase_stats, simulator.run() zwraca veto_per_phase={} i n_vetoed_total=0 (D-67 backwards compat z agentem wyłączonym)
    - Test: gdy device ma veto_phase_stats={1:3, 2:5}, agregacja zwraca veto_per_phase z {1:3, 2:5} (lub sumy gdy wielu urządzeń) i n_vetoed_total=8
  </behavior>
  <action>
    Per D-65 (04-CONTEXT.md Claude's Discretion (a)) i 04-PATTERNS.md §4 zmień konstrukcję decyzji w pętli run() na 3-stanową: zachowaj istniejący blok `if decision == 'COMMIT':` (simulator.py:49-68) bez zmian; zmień obecne `else:` (simulator.py:69-72) na sekwencję `elif decision == 'VETO': dev.status = 'DOWN'; dev.down_left = 1` (komentarz wyjaśniający że n_vetoed jest inkrementowane w wrapperze PRZED return 'VETO' — D-63) oraz `else: # 'ABSTAIN' lub nieznany decision` zachowujący istniejące 3 linie (n_abstain++, status='DOWN', down_left=1). Per D-64 i 04-PATTERNS.md §4 dodaj agregację veto_per_phase BEZPOŚREDNIO PO bloku agregacji ic_results (po linii 138), PRZED return dict (linia 140): pętla po self.devices i po dev.veto_phase_stats.items() sumująca counts do dict veto_per_phase = {} oraz scalar n_vetoed_total. Następnie rozszerz return dict (simulator.py:140-150) o dwa nowe klucze: `'veto_per_phase': veto_per_phase,` i `'n_vetoed_total': n_vetoed_total,` — wstaw je BEZPOŚREDNIO PRZED `'history':` (zachowuje konwencję history/devices na końcu). Brak nowych importów. Brak zmian w istniejących polach return dict (CLI-04 backwards compat — istniejące klucze nieruszone).
  </action>
  <verify>
    <automated>python -c "
from sphsim.core.simulator import SPHSimulator
from sphsim.config import DEFAULT_PHI, DEFAULT_RHO
# Stub strategy: zwraca 'VETO' dla phase=2, 'ABSTAIN' dla phase=3, 'COMMIT' dla phase=1
def stub(dev, l, s, phi, kappa, rho, h, p):
    if dev.phase == 2:
        dev.n_vetoed += 1
        dev.veto_phase_stats[2] = dev.veto_phase_stats.get(2, 0) + 1
        return 'VETO'
    if dev.phase == 3:
        return 'ABSTAIN'
    return 'COMMIT'
sim = SPHSimulator(nU=50, nSUS=20, K0=100.0, K1=120.0, F=5, T=20, kappa=1.0, alpha=1.0,
                   phi=DEFAULT_PHI, rho=DEFAULT_RHO, strategy_fn=stub, params={}, seed=42)
res = sim.run()
assert 'veto_per_phase' in res, f'veto_per_phase missing in result keys: {list(res.keys())}'
assert 'n_vetoed_total' in res, 'n_vetoed_total missing'
assert isinstance(res['veto_per_phase'], dict), type(res['veto_per_phase'])
assert isinstance(res['n_vetoed_total'], int), type(res['n_vetoed_total'])
assert res['n_vetoed_total'] >= 0
total_vetos = sum(d.n_vetoed for d in res['devices'])
assert res['n_vetoed_total'] == total_vetos, f'n_vetoed_total {res[\"n_vetoed_total\"]} != sum {total_vetos}'
print('OK', res['n_vetoed_total'], 'vetos total')
"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -v '^#' sphsim/core/simulator.py | grep -c "elif decision == 'VETO'"` ≥ 1
    - Source: `grep -v '^#' sphsim/core/simulator.py | grep -c "'veto_per_phase'"` ≥ 2 (jedna linia w return dict, jedna w pętli agregacji)
    - Source: `grep -v '^#' sphsim/core/simulator.py | grep -c "'n_vetoed_total'"` ≥ 2
    - Source: `grep -v '^#' sphsim/core/simulator.py | grep -c 'dev.veto_phase_stats.items()'` ≥ 1
    - Behavior: stub strategy zwracająca 'VETO' powoduje że `dev.n_abstain` zostaje 0 a `dev.n_vetoed` rośnie (verify command pokazuje to przez asercję sum(d.n_vetoed) == n_vetoed_total)
    - Test: `python scripts/regression_check.py` zwraca exit code 1 LUB exit code 0 z różnicą TYLKO w nowych kluczach (veto_per_phase, n_vetoed_total) — istniejące klucze (avg_val_last100, cum_val_total, avg_net_profit, delivery_ratio, avg_providers_l100, sus_final, ic_per_phase) MUSZĄ być bit-identyczne (sprawdzenie ostateczne w Planie 06; tutaj tylko ważne że NIE dodajemy zmian semantycznych do COMMIT/ABSTAIN ścieżki)
  </acceptance_criteria>
  <done>
    simulator.run() ma 3-stanowy guard (COMMIT / VETO / ABSTAIN-lub-default) i zwraca veto_per_phase oraz n_vetoed_total. Wrapper z Planu 02 może zwracać 'VETO' i polegać na bookkeeping w simulator. Output (Plan 03) ma dane do renderowania.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| strategy_fn → simulator | Wrapper (Plan 02) zwraca string decision — simulator musi traktować nieznane wartości jako ABSTAIN (default branch) żeby błędna custom strategia nie wywróciła pętli |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Device dataclass mutable default | mitigate | veto_phase_stats inicjalizowany w __post_init__ (nie field(default=) ani default_factory na poziomie klasy) — każdy Device ma własną instancję dict, brak współdzielonej referencji między urządzeniami (D-63) |
| T-04-02 | DoS | simulator.run() pętla po veto_phase_stats | accept | Złożoność O(nU × F) liniowa, F=5, nU≤500 w defaultach — nieosiągalne dla zwykłego użycia |
| T-04-03 | Repudiation | Backwards compat CLI-04 (return dict nowe klucze) | mitigate | Nowe klucze veto_per_phase i n_vetoed_total są DODAWANE do return dict, istniejące klucze niemodyfikowane (D-67); regression_check.py z Planu 06 weryfikuje exact-match istniejących pól |
| T-04-04 | Spoofing | Nieznana wartość decision (np. typo 'COMIT') | mitigate | else branch po elif VETO traktuje wszystko inne jako ABSTAIN — failsafe, brak crash, brak undefined behavior |
</threat_model>

<verification>
Po obu taskach:
- `python -m unittest discover tests` → 22+ testy passing (nie łamiemy istniejących)
- `python sph_sim.py --strategy naive --zeta 0.5 --seed 42 --json | tail -n +1 | python -c "import json,sys; d=json.load(sys.stdin); assert 'veto_per_phase' in d['metrics'] and 'n_vetoed_total' in d['metrics']; print('NEW KEYS OK')"` — nowe klucze obecne w JSON outputie nawet bez agenta (n_vetoed_total=0, veto_per_phase={})
</verification>

<success_criteria>
- Device ma 6-te pole licznikowe n_vetoed:int=0 (default) widoczne w `dataclasses.fields(Device)`
- Device.__post_init__ inicjalizuje veto_phase_stats={} (per-instance, nie shared)
- simulator.run() rozpoznaje 'VETO' jako 3-state member: stan DOWN+down_left=1 ale BEZ n_abstain++
- simulator.run() return dict zawiera 'veto_per_phase':dict i 'n_vetoed_total':int (zawsze, nawet gdy agent wyłączony — wartości puste)
- Istniejący test tests/test_strategy_meta_consistency.py przechodzi
- Istniejący test tests/test_loader.py przechodzi (signature contract custom strategy nieruszony)
</success_criteria>

<output>
Create `.planning/phases/04-rational-agent-veto-layer/04-01-SUMMARY.md` po ukończeniu.
</output>
