---
phase: 04-rational-agent-veto-layer
plan: 05
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - sphsim/cli/repl.py
autonomous: true
requirements: [AGENT-01, AGENT-05]

must_haves:
  truths:
    - "SPHShell ma nową komendę do_compare obsługującą 'compare <nazwa> [k=v ...]' bez prefiksu '/'"
    - "do_compare uruchamia SPHSimulator 2x (z wrap_with_agent i bez), zwraca dict 'comparison' i renderuje przez format_human / format_compare"
    - "do_run domyślnie opakowuje strategie w wrap_with_agent (D-58 — agent default-on w REPL)"
    - "do_help wyświetla nową linię opisującą 'compare <nazwa>' po polsku"
    - "Module docstring REPL aktualizowany na '7 komend' (z 6 → 7)"
    - "Class docstring SPHShell aktualizowany"
  artifacts:
    - path: "sphsim/cli/repl.py"
      provides: "SPHShell z 7 komendami (help, exit, strategies, strategy, custom, run, compare), do_run wrap'uje agentem default-on"
      contains: "def do_compare"
  key_links:
    - from: "sphsim/cli/repl.py do_run"
      to: "sphsim.agent.wrap_with_agent"
      via: "import + wrap przed SPHSimulator"
      pattern: "wrap_with_agent"
    - from: "sphsim/cli/repl.py do_compare"
      to: "sphsim.cli.output.format_human (comparison branch) lub format_compare"
      via: "res = {'comparison': {...}}; print(format_human(args, res, K1, False))"
      pattern: "'comparison'"
---

<objective>
Wpięcie wrappera RationalAgent do REPL (`sphsim/cli/repl.py`): (1) `do_run` domyślnie opakowuje strategie w `wrap_with_agent(..., params.get('expected_P', DEFAULT_K0))` — agent default-on w REPL (D-58); (2) nowa komenda `do_compare <nazwa> [k=v ...]` uruchamia simulator 2x (z agentem i bez) i renderuje tabelę delta KPI przez format_compare; (3) `do_help` aktualizowany o linię opisującą `compare`; (4) Module/class docstring update (6 → 7 komend).

Purpose: AGENT-05 wymaga że `/compare <strategia>` (D-17 → `compare <strategia>` bez slasha w REPL) pokazuje delta KPI. D-58 określa że agent jest default-on również w REPL. Bez tego planu, REPL nie ma trybu compare; bez wrap'a w do_run, REPL pokazuje surową strategię (sprzeczne z AGENT-01).
Output: Zmodyfikowany sphsim/cli/repl.py — 1 nowy import (wrap_with_agent), 1 nowa metoda (do_compare), 1 zmodyfikowana metoda (do_run wrap), 1 zaktualizowana metoda (do_help), zaktualizowane docstringi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-rational-agent-veto-layer/04-CONTEXT.md
@.planning/phases/04-rational-agent-veto-layer/04-PATTERNS.md
@sphsim/cli/repl.py
@sphsim/cli/output.py
@sphsim/agent/__init__.py
@sphsim/config.py

<interfaces>
Po Planie 02:
```python
from sphsim.agent import wrap_with_agent
# wrap_with_agent(strategy_fn, expected_P) -> wrapped_fn (8-arg closure)
```

Po Planie 03:
```python
from sphsim.cli.output import format_human, format_compare
# format_human dispatcher: gdy res ma 'comparison', wraca format_compare(args, res['comparison'], K1)
```

Z istniejącego repl.py (Phase 3):
- `do_run(self, arg)` linie 172-215 — wzorzec template
- `STRATEGIES`, `BUILTIN_STRATEGIES`, `parse_params_from_meta` już zaimportowane
- `DEFAULT_K0` już zaimportowane z sphsim.config

Format do_compare wynik dict (zgodny z format_human comparison branch):
```python
res_combined = {'comparison': {
    'with_agent': {<metrics dict bez history/devices>},
    'without_agent': {<metrics dict bez history/devices>},
    'delta': {'avg_val_last100': float, 'cum_val_total': float, 'avg_net_profit': float, 'delivery_ratio': float, 'avg_providers_l100': float},
    'agent_helps': bool,
}}
fake_args = argparse.Namespace(strategy=name, nU=..., nSUS=..., T=..., kappa=..., alpha=..., verbose=False, no_agent=False)
print(format_human(fake_args, res_combined, DEFAULT_K1, False))
```

(KRYTYCZNE: fake_args MUSI mieć atrybut `no_agent` żeby format_json przy ewentualnym fall-back nie crashował — Plan 03 Task 2 polega na `not args.no_agent`. Tutaj fake_args dostaje `no_agent=False`.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: do_run wrap + do_help update + docstring bump</name>
  <files>sphsim/cli/repl.py</files>
  <read_first>
    - sphsim/cli/repl.py — istniejący do_run (linie 172-215), do_help (linie 58-66), module docstring (linie 1-16), class docstring (linia 52)
    - .planning/phases/04-rational-agent-veto-layer/04-PATTERNS.md sekcja "### 8. sphsim/cli/repl.py" — do_run modification (wrap z params.get('expected_P', DEFAULT_K0)) + do_help append linia compare + docstring bump
    - .planning/phases/04-rational-agent-veto-layer/04-CONTEXT.md D-58 (REPL run zawsze z agentem; --no-agent w REPL deferred do Phase 5) + D-61 (do_compare bez slasha, do_help update) + D-54 (expected_P z params dict)
  </read_first>
  <behavior>
    - Test: `printf 'run incentive expected_P=30\nexit\n' | python sph_sim.py --interactive` — output zawiera sekcję METRYKI; jeśli strategy zwróci jakikolwiek COMMIT przy net<0, sekcja "VETO przez RationalAgent" pojawi się (Plan 03 dostarcza renderowanie; Plan 01 dostarcza dane)
    - Test: `printf 'help\nexit\n' | python sph_sim.py --interactive` — output zawiera linię `compare <nazwa> [k=v ...]` z polskim opisem
    - Test: module docstring zawiera frazę "7 komend" (lub "(Phase 4 dodaje compare)")
    - Test: `do_run` używa `wrap_with_agent(STRATEGIES[name], params.get('expected_P', DEFAULT_K0))` przed budową SPHSimulator
    - Test: po `run naive zeta=0.75`, JSON metrics (gdy `--json` było obecne — n/a w REPL, ale tu test logiki w wybrane miejsca) lub po prostu format_human pokazuje że agent działa (sekcja VETO renderuje się gdy n_vetoed_total>0)
  </behavior>
  <action>
    Per 04-PATTERNS.md §8 i 04-CONTEXT.md D-58/D-61: (1) **Top-of-file import** (po istniejącym `from sphsim.cli.output import format_human`): dodaj `from sphsim.agent import wrap_with_agent`. (2) **Modify do_run** (linie 172-215) — w bloku konstruującym SPHSimulator (linia 202-207) zmień `strategy_fn=STRATEGIES[name]` na konstrukcję `wrap_with_agent(STRATEGIES[name], params.get('expected_P', DEFAULT_K0))`. Kompletna linia: `strategy_fn=wrap_with_agent(STRATEGIES[name], params.get('expected_P', DEFAULT_K0))`. Brak innych zmian w do_run — REPL run jest zawsze z agentem default-on (per D-58 + Claude's Discretion w 04-CONTEXT.md). (3) **Modify fake_args** (linia 211-214) — dodaj atrybut `no_agent=False` do `argparse.Namespace(...)` (zgodne z interfejsem format_json Plan 03 Task 2, mimo że tu używamy format_human — defensive consistency). (4) **Modify do_help** (linie 58-66) — DODAJ jedną linię PO istniejącej linii `run <nazwa> [k=v ...]` (verbatim z 04-PATTERNS.md §8): `print("  compare <nazwa> [k=v ...]       — Porównaj strategię z i bez RationalAgent (delta KPI).")`. (5) **Update module docstring** (linie 1-16) — zmień "6 komend bez prefiksu '/'" na "7 komend bez prefiksu '/'" w linii 3; dodaj 7-ty bullet w bulleted list (po `run <nazwa>` bullet, linia ~9): `  - compare <nazwa> [k=v ...]    — porównaj strategię z/bez RationalAgent (D-61, AGENT-05)`. (6) **Update class docstring** (linia 52) — zmień "4 komendy bez slasha" na "7 komend bez slasha" (per 04-PATTERNS.md note: code already has 6 commands post-Phase 3; Phase 4 fixes do 7 in passing). Polish UI strings — match istniejący styl (em-dash separator, padding kolumn). Brak zmian w `do_exit`, `do_strategies`, `do_strategy`, `do_custom`, `do_EOF`, `default`.
  </action>
  <verify>
    <automated>cd /Users/stanislawnagorski/Documents/STUDIA/sem4/ekonometria\ 2 && python -c "
import subprocess, sys

# Test 1: do_help shows compare line
r = subprocess.run([sys.executable, 'sph_sim.py', '--interactive'],
                   input='help\nexit\n', capture_output=True, text=True, timeout=30)
assert 'compare' in r.stdout.lower(), f'compare command not in help: {r.stdout[:1500]}'
assert 'RationalAgent' in r.stdout or 'delta KPI' in r.stdout or 'porównaj' in r.stdout.lower() or 'porownaj' in r.stdout.lower(), f'compare description missing: {r.stdout[:1500]}'

# Test 2: do_run agent default-on (agent działa — choć VETO section może nie pojawić się dla naive)
# Po prostu sprawdzamy że REPL run nie crashuje po wpięciu wrap'a
r = subprocess.run([sys.executable, 'sph_sim.py', '--interactive'],
                   input='run naive zeta=0.5\nexit\n', capture_output=True, text=True, timeout=60)
assert r.returncode == 0 or r.returncode is None, f'REPL crashed, rc={r.returncode}, stderr={r.stderr[:500]}'
assert 'NAIVE' in r.stdout or 'Strategia: NAIVE' in r.stdout, f'REPL run output missing: {r.stdout[-2000:]}'

print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -v '^#' sphsim/cli/repl.py | grep -c 'from sphsim.agent import wrap_with_agent'` ≥ 1
    - Source: `grep -v '^#' sphsim/cli/repl.py | grep -c 'wrap_with_agent(STRATEGIES\[name\]'` ≥ 1 (do_run wrap)
    - Source: `grep -v '^#' sphsim/cli/repl.py | grep -c 'compare <nazwa>'` ≥ 1 (do_help linia)
    - Source: `grep -v '^#' sphsim/cli/repl.py | grep -c 'no_agent=False'` ≥ 1 (fake_args defensive consistency)
    - Source: `grep -cE '7 komend|7 commands' sphsim/cli/repl.py` ≥ 1 (docstring bump)
    - Behavior: verify command (2 inline path tests) exits 0
    - Behavior: `printf 'help\nexit\n' | python sph_sim.py --interactive 2>&1 | grep -c 'compare'` ≥ 1
  </acceptance_criteria>
  <done>
    do_run opakowuje strategy w wrap_with_agent (default-on per D-58). do_help wyświetla linię opisującą `compare`. Docstring REPL aktualizowany do 7 komend. fake_args ma `no_agent=False` dla defensive compatibility z output.py format_json (gdyby kiedyś użyto).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: do_compare — 2x SPHSimulator + delta render</name>
  <files>sphsim/cli/repl.py</files>
  <read_first>
    - sphsim/cli/repl.py (po Task 1) — do_run jako template paste-and-modify (linie 172-215)
    - .planning/phases/04-rational-agent-veto-layer/04-PATTERNS.md sekcja "### 8. sphsim/cli/repl.py" — do_compare skeleton (paralelnie do do_run)
    - .planning/phases/04-rational-agent-veto-layer/04-CONTEXT.md D-61 (REPL compare nazwy bez slasha; składnia identyczna do run; działa dla built-in i custom strategii) + Claude's Discretion `compare-agent interakcja z --seed` (jeden seed dla obu run'ów) + `<code_context>` §5 (do_compare sketch)
  </read_first>
  <behavior>
    - Test: `printf 'compare\nexit\n' | python sph_sim.py --interactive` — output zawiera polski hint "Użycie: compare <nazwa> [param=wartość ...]" (D-42 verbatim style)
    - Test: `printf 'compare nieistniejaca\nexit\n' | python sph_sim.py --interactive` — output zawiera "Strategia 'nieistniejaca' nie istnieje. Dostępne: ..." (D-31 style live STRATEGIES list)
    - Test: `printf 'compare incentive expected_P=30\nexit\n' | python sph_sim.py --interactive` — output zawiera sekcję compare (5 wierszy KPI: avg_val_last100, cum_val_total, avg_net_profit, delivery_ratio, avg_providers_l100) + werdykt '✓ TAK' lub '✗ NIE'
    - Test: `printf 'custom examples/custom_strategy_template.py\ncompare custom_strategy_template\nexit\n' | python sph_sim.py --interactive` — compare działa dla custom strategii (D-50 dispatch namespace działa identycznie)
    - Test: do_compare używa tego samego `seed=42` dla obu run'ów (deterministic — Claude's Discretion)
    - Test: do_compare oblicza `agent_helps` na podstawie avg_net_profit (with > without)
  </behavior>
  <action>
    Per 04-PATTERNS.md §8 i 04-CONTEXT.md D-61 + `<code_context>` §5: dodaj nową metodę `do_compare(self, arg)` w klasie SPHShell — wstaw PO `do_run` (po linii 215) PRZED `default(self, line)` (linia 218). Sygnatura: `def do_compare(self, arg):`. Docstring: `"""Porównaj strategię z i bez RationalAgent: compare <nazwa> [param=wartość ...]"""`. Logika (verbatim paralelnie do do_run linii 173-200): (1) `tokens = arg.split()`. (2) `if not tokens: print("Użycie: compare <nazwa> [param=wartość ...]. Wpisz 'strategies' żeby zobaczyć dostępne."); return`. (3) `name, *kv_tokens = tokens`. (4) `if name not in STRATEGIES: print(f"Strategia '{name}' nie istnieje. Dostępne: {', '.join(STRATEGIES.keys())}."); return`. (5) Dispatch namespace per D-50: `ns = 'sphsim.strategies' if name in BUILTIN_STRATEGIES else 'sphsim.custom'`; `mod = importlib.import_module(f'{ns}.{name}')`; `meta = mod.STRATEGY_META`. (6) `try: params = parse_params_from_meta(kv_tokens, meta, name)` z `except LoaderError as e: print(e.args[0]); return`. (7) `raw_strategy_fn = STRATEGIES[name]`; `expected_P = params.get('expected_P', DEFAULT_K0)`. (8) **Build common kwargs**: `common = dict(nU=DEFAULT_NU, nSUS=DEFAULT_NSUS, K0=DEFAULT_K0, K1=DEFAULT_K1, F=DEFAULT_F, T=DEFAULT_T, kappa=DEFAULT_KAPPA, alpha=DEFAULT_ALPHA, phi=DEFAULT_PHI, rho=DEFAULT_RHO, params=params, seed=42)`. (9) **Run with agent**: `sim_with = SPHSimulator(strategy_fn=wrap_with_agent(raw_strategy_fn, expected_P), **common)`; `res_with = sim_with.run()`. (10) **Run without agent**: `sim_without = SPHSimulator(strategy_fn=raw_strategy_fn, **common)`; `res_without = sim_without.run()`. (11) **Build comparison dict**: `KPIS = ['avg_val_last100', 'cum_val_total', 'avg_net_profit', 'delivery_ratio', 'avg_providers_l100']`; `comparison_block = {'with_agent': {k: v for k, v in res_with.items() if k not in ('history', 'devices')}, 'without_agent': {k: v for k, v in res_without.items() if k not in ('history', 'devices')}, 'delta': {k: res_with[k] - res_without[k] for k in KPIS}, 'agent_helps': res_with['avg_net_profit'] > res_without['avg_net_profit']}`. (12) **Render via format_human dispatcher**: `res_combined = {'comparison': comparison_block}`; `fake_args = argparse.Namespace(strategy=name, nU=DEFAULT_NU, nSUS=DEFAULT_NSUS, T=DEFAULT_T, kappa=DEFAULT_KAPPA, alpha=DEFAULT_ALPHA, verbose=False, no_agent=False)`; `print(format_human(fake_args, res_combined, DEFAULT_K1, False))`. Polish UI strings (komunikaty error po polsku). Identyfikatory angielskie.
  </action>
  <verify>
    <automated>cd /Users/stanislawnagorski/Documents/STUDIA/sem4/ekonometria\ 2 && python -c "
import subprocess, sys

# Test 1: compare bez argumentów — polski help
r = subprocess.run([sys.executable, 'sph_sim.py', '--interactive'],
                   input='compare\nexit\n', capture_output=True, text=True, timeout=30)
assert ('Użycie: compare' in r.stdout) or ('Uzycie: compare' in r.stdout), f'help message missing: {r.stdout[:500]}'

# Test 2: compare nieistniejąca → polski error
r = subprocess.run([sys.executable, 'sph_sim.py', '--interactive'],
                   input='compare absolutely_not_a_real_strategy\nexit\n', capture_output=True, text=True, timeout=30)
assert 'nie istnieje' in r.stdout, f'unknown strategy error missing: {r.stdout[:500]}'

# Test 3: compare incentive expected_P=30 → tabela delta + werdykt
r = subprocess.run([sys.executable, 'sph_sim.py', '--interactive'],
                   input='compare incentive expected_P=30\nexit\n', capture_output=True, text=True, timeout=120)
assert r.returncode == 0 or r.returncode is None, f'REPL crashed, rc={r.returncode}, stderr={r.stderr[:500]}'
out = r.stdout
assert 'avg_net_profit' in out or 'net_profit' in out.lower(), f'KPI row missing: {out[-2000:]}'
assert '✓' in out or '✗' in out or 'TAK' in out or 'NIE' in out, f'verdict marker missing: {out[-2000:]}'

# Test 4: compare custom strategy
r = subprocess.run([sys.executable, 'sph_sim.py', '--interactive'],
                   input='custom examples/custom_strategy_template.py\ncompare custom_strategy_template\nexit\n',
                   capture_output=True, text=True, timeout=120)
assert r.returncode == 0 or r.returncode is None
out = r.stdout
assert 'avg_net_profit' in out or 'agent_helps' in out or '✓' in out or '✗' in out, f'compare custom failed: {out[-2000:]}'

print('OK — 4 inline test cases for do_compare')
"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -v '^#' sphsim/cli/repl.py | grep -c '^    def do_compare'` ≥ 1
    - Source: `grep -v '^#' sphsim/cli/repl.py | grep -c "sim_with = SPHSimulator"` ≥ 1
    - Source: `grep -v '^#' sphsim/cli/repl.py | grep -c "sim_without = SPHSimulator"` ≥ 1
    - Source: `grep -v '^#' sphsim/cli/repl.py | grep -c "'comparison'"` ≥ 1
    - Source: `grep -v '^#' sphsim/cli/repl.py | grep -c "agent_helps"` ≥ 1
    - Source: `grep -v '^#' sphsim/cli/repl.py | grep -c "wrap_with_agent(raw_strategy_fn"` ≥ 1
    - Source: `grep -v '^#' sphsim/cli/repl.py | grep -c "Użycie: compare"` ≥ 1
    - Behavior: verify command (4 inline test cases) exits 0
    - Behavior: `printf 'compare incentive expected_P=30\nexit\n' | python sph_sim.py --interactive 2>&1 | grep -cE 'avg_net_profit|net_profit'` ≥ 1
    - Behavior: `printf 'compare incentive expected_P=30\nexit\n' | python sph_sim.py --interactive 2>&1 | grep -cE '✓|✗|TAK|NIE'` ≥ 1 (werdykt)
  </acceptance_criteria>
  <done>
    SPHShell ma do_compare jako paralelnie do do_run. Działa dla built-in i custom strategii. Uruchamia simulator 2x z tym samym seed=42, oblicza delta KPI i agent_helps, renderuje przez format_human → format_compare (Plan 03). AGENT-05 REPL część zaimplementowana.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REPL user input → do_compare | arg jest stringiem; split() i parse_params_from_meta sprawdza typy; brak shell injection bo brak subprocess wywołań w do_compare |
| do_compare → 2x SPHSimulator (sim_with, sim_without) | Każdy SPHSimulator ma własny `random.seed(42)` w __init__ — deterministic; seedy nie kolidują |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Tampering | wrap_with_agent na strategii custom z malformed expected_P param | mitigate | parse_params_from_meta wykonuje typed conversion (Phase 3 D-47 layer 4) — gdy custom STRATEGY_META nie deklaruje 'expected_P', params.get('expected_P', DEFAULT_K0) bezpiecznie używa default. |
| T-04-18 | DoS | compare uruchamia 2x simulator w REPL session | accept | Domyślne T=2000, nU=300 — ~kilka sekund per run; akceptowalne dla użytkownika REPL'a. |
| T-04-19 | Repudiation | Backwards compat — Phase 3 D-44 mutex (--interactive | --strategy | --custom) | mitigate | Phase 4 NIE dodaje compare do mutex (compare jest komendą REPL, NIE CLI flagą). --compare-agent z Planu 04 ma mutex z --interactive (T-04-13 mitigation), więc REPL i one-shot compare są rozdzielne. |
| T-04-20 | Spoofing | fake_args.no_agent vs args.no_agent (CLI flag) | mitigate | fake_args.Namespace ma `no_agent=False` dla defensive consistency — format_json (Plan 03) nie crashuje gdyby kiedyś użyto format_json z fake_args w REPL. |
</threat_model>

<verification>
- Verify commands Task 1 + Task 2 (6 inline test cases) exits 0
- `python -m unittest discover tests` 22+ pre-existing testy pass
- `printf 'help\nexit\n' | python sph_sim.py --interactive` shows 7 commands (help, exit, strategies, strategy, custom, run, compare)
- `printf 'compare incentive expected_P=30\nexit\n' | python sph_sim.py --interactive` produces delta table z werdyktem
</verification>

<success_criteria>
- SPHShell ma do_compare jako 7-mą komendę
- do_compare działa dla built-in (`compare incentive`) i custom (`compare custom_strategy_template` po wcześniejszym `custom <path>`)
- do_run domyślnie opakowuje strategy w wrap_with_agent (D-58 — agent default-on w REPL)
- do_help wyświetla linię `compare <nazwa> [k=v ...] — Porównaj strategię z i bez RationalAgent (delta KPI).`
- Module docstring i class docstring zaktualizowane (6 → 7 komend)
- AGENT-01 (agent default-on) + AGENT-05 (compare delta KPI) zaimplementowane dla REPL
</success_criteria>

<output>
Create `.planning/phases/04-rational-agent-veto-layer/04-05-SUMMARY.md` po ukończeniu.
</output>
