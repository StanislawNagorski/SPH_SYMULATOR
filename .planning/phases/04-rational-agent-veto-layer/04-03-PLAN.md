---
phase: 04-rational-agent-veto-layer
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - sphsim/cli/output.py
autonomous: true
requirements: [AGENT-04, AGENT-05]

must_haves:
  truths:
    - "format_human renderuje sekcję 'VETO przez RationalAgent' gdy n_vetoed_total > 0 — D-66"
    - "format_human renderuje tabelę delta KPI (5 KPI × 3 kolumny with/without/Δ) gdy res zawiera klucz 'comparison' — D-62"
    - "format_json dodaje top-level klucze veto_per_phase, n_vetoed_total, agent_enabled do bloku metrics — D-67"
    - "format_json przy obecnym 'comparison' w res zastępuje 'metrics' blokiem 'comparison' (D-67)"
    - "agent_enabled = not args.no_agent (boolean — D-67)"
    - "Werdykt 'agent_helps' w compare table opiera się na delta avg_net_profit (with > without)"
  artifacts:
    - path: "sphsim/cli/output.py"
      provides: "format_human z sekcją VETO + format_compare + format_json z nowymi polami"
      contains: "VETO przez RationalAgent"
  key_links:
    - from: "format_human"
      to: "res['veto_per_phase'], res['n_vetoed_total']"
      via: "res.get('veto_per_phase', {})"
      pattern: "res\\.get\\('veto_per_phase'"
    - from: "format_human compare path"
      to: "res['comparison']"
      via: "if 'comparison' in res branch"
      pattern: "'comparison'.*res"
    - from: "format_json"
      to: "args.no_agent"
      via: "'agent_enabled': not args.no_agent"
      pattern: "agent_enabled.*not args.no_agent"
---

<objective>
Rozszerzyć sphsim/cli/output.py o (1) sekcję "VETO przez RationalAgent" w format_human (po sekcji IC), (2) funkcję format_compare renderującą tabelę delta KPI 5×3 dla trybu --compare-agent, (3) integrację comparison block w format_human z dispatch przez obecność klucza 'comparison' w res, (4) nowe top-level klucze w format_json (veto_per_phase, n_vetoed_total, agent_enabled) — backwards compat zachowane.

Purpose: Plan 04 (CLI args + main.py wrap + run_compare) i Plan 05 (REPL do_compare) potrzebują gotowych formatterów żeby wywoływać `format_human(args, res, K1, verbose)` z res zawierającym dane veto i opcjonalnie comparison block. JSON output musi mieć stabilną strukturę z nowymi polami nawet gdy agent wyłączony (D-67 — CLI-04 backwards compat).
Output: Zmodyfikowane sphsim/cli/output.py z nową sekcją VETO + nową funkcją format_compare + extended format_json. Public API output.py rozszerzone o format_compare (eksportowany przez import w Planach 04/05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-rational-agent-veto-layer/04-CONTEXT.md
@.planning/phases/04-rational-agent-veto-layer/04-PATTERNS.md
@sphsim/cli/output.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: format_human — sekcja VETO + comparison branch</name>
  <files>sphsim/cli/output.py</files>
  <read_first>
    - sphsim/cli/output.py — istniejący format_human (linie 16-64) z sekcją METRYKI (20-31) i sekcją IC per-phase (38-54)
    - .planning/phases/04-rational-agent-veto-layer/04-PATTERNS.md sekcja "### 6. sphsim/cli/output.py" — VETO section template (paste-and-modify z IC section structure) + comparison branch
    - .planning/phases/04-rational-agent-veto-layer/04-CONTEXT.md D-66 (sekcja VETO po sekcji IC; tabela Faza | COMMIT zgłoszone | VETO | % zaweto'wanych; total summary line) + D-62 (compare table format 5 KPI × 3 kolumny + werdykt agent_helps)
    - .planning/phases/04-rational-agent-veto-layer/04-CONTEXT.md Claude's Discretion "Lokalizacja sekcji VETO w format_human" (po sekcji IC, przed verbose block)
  </read_first>
  <behavior>
    - Test: gdy res={'comparison': {...}}, format_human zwraca string z tabelą 5 KPI × 3 kolumny i werdyktem '✓ TAK' / '✗ NIE' — pomija standardowe METRYKI/IC/VETO bloki
    - Test: gdy res nie zawiera 'comparison' ale ma n_vetoed_total=0, format_human zwraca string BEZ sekcji VETO (gracefully pomija — czysty output dla --no-agent)
    - Test: gdy res ma n_vetoed_total>0, format_human zwraca string zawierający 'VETO przez RationalAgent' i wiersze per-faza z pct values
    - Test: format_human z verbose=True nadal działa (verbose block po sekcji VETO, nie przerywany)
    - Test: sekcja VETO renderuje wszystkie fazy gdzie istnieje COMMIT lub VETO (union of veto_pp.keys() ∪ ic.keys())
    - Test: total summary line 'Łącznie zaweto'wano: N COMMIT-ów z M zgłoszonych (X.X%)' obecny gdy n_vetoed_total>0
  </behavior>
  <action>
    Per 04-PATTERNS.md §6 i 04-CONTEXT.md D-66/D-62: (1) **Comparison branch** — na PIERWSZEJ linii ciała format_human (po `lines = []`) dodaj guard: `if 'comparison' in res: return format_compare(args, res['comparison'], K1)`. To krótka ścieżka która ominie standardowy METRYKI/IC/VETO output i zwróci tabelę delta. (2) **Sekcja VETO** — dodaj BEZPOŚREDNIO PO sekcji IC (po linii 54 `lines.append(f"  Zgodność motywacyjna: {verdict}")`), PRZED `if verbose:` (linia 56). Pattern z 04-PATTERNS.md §6: pobierz `veto_pp = res.get('veto_per_phase', {})` i `n_vetoed = res.get('n_vetoed_total', 0)`; wewnątrz `if n_vetoed > 0:` dodaj sekcję z polskim header `"\n  VETO przez RationalAgent — rekomendacje COMMIT odrzucone per faza:"`, separator, kolumny `'Faza':>6, 'COMMIT zgłoszone':>18, 'VETO':>8, '% zaweto':>10`, pętlę po `sorted(set(list(veto_pp.keys()) + list(ic.keys())))` gdzie `commits = ic.get(ph, {}).get('commits', 0)`, `vetos = veto_pp.get(ph, 0)`, `total = commits + vetos`, `pct = (vetos/total*100) if total > 0 else 0`. Po pętli linia podsumowująca: `"Łącznie zaweto'wano: {n_vetoed} COMMIT-ów z {total_committed} zgłoszonych ({pct_total:.1f}%)"`. (3) **NIE renderuj sekcji VETO gdy n_vetoed=0** — gracefully clean output dla --no-agent. (4) Polish UI strings z PROJECT.md constraint. Identyfikatory ('veto_per_phase', 'n_vetoed_total') angielskie.
  </action>
  <verify>
    <automated>python -c "
import argparse
from sphsim.cli.output import format_human

# Test 1: res z n_vetoed_total=0 → no VETO section
res_no_veto = {
    'avg_val_last100': 92.0, 'cum_val_total': 9200.0, 'avg_net_profit': 1.0,
    'delivery_ratio': 0.5, 'avg_providers_l100': 45.0, 'sus_final': 5,
    'ic_per_phase': {}, 'history': {}, 'devices': [],
    'veto_per_phase': {}, 'n_vetoed_total': 0,
}
fake_args = argparse.Namespace(strategy='naive', nU=300, nSUS=30, T=2000, kappa=1.0, alpha=1.0, verbose=False)
out = format_human(fake_args, res_no_veto, 100.0, False)
assert 'VETO przez RationalAgent' not in out, f'Should NOT contain VETO section when n_vetoed=0'

# Test 2: res z n_vetoed_total>0 → VETO section present
res_with_veto = dict(res_no_veto)
res_with_veto['veto_per_phase'] = {1: 12, 2: 45}
res_with_veto['n_vetoed_total'] = 57
res_with_veto['ic_per_phase'] = {1: {'commits': 245, 'deliveries': 200, 'failures': 10, 'avg_earning_per_commit': 5.0, 'avg_cost_per_commit': 1.0, 'avg_net_per_commit': 4.0, 'delivery_rate': 0.8, 'ic_satisfied': True}}
out = format_human(fake_args, res_with_veto, 100.0, False)
assert 'VETO przez RationalAgent' in out, 'VETO section missing'
assert \"Łącznie zaweto'wano\" in out, 'Polish diacritics required per project convention (Phase 3 D-44 precedent)'
print('OK — sekcja VETO conditional rendering działa')
"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -v '^#' sphsim/cli/output.py | grep -c "VETO przez RationalAgent"` ≥ 1
    - Source: `grep -v '^#' sphsim/cli/output.py | grep -c "res.get('veto_per_phase'"` ≥ 1
    - Source: `grep -v '^#' sphsim/cli/output.py | grep -c "res.get('n_vetoed_total'"` ≥ 1
    - Source: `grep -v '^#' sphsim/cli/output.py | grep -c "'comparison' in res"` ≥ 1 (comparison branch)
    - Source: `grep -v '^#' sphsim/cli/output.py | grep -c "Łącznie zaweto"` ≥ 1
    - Behavior: verify command (2 inline asercje) exits 0
    - Behavior: `python sph_sim.py --strategy naive --zeta 0.5 --seed 42 2>/dev/null | grep -c 'VETO przez RationalAgent'` == 0 (przed wired Plan 04 — agent jeszcze nie podpięty; po Planie 04 stanie się >= 1)
  </acceptance_criteria>
  <done>
    format_human ma sekcję VETO conditional na n_vetoed_total>0 (no-op dla --no-agent — czysty output) oraz comparison branch na res['comparison']. Plan 04/05 mogą wywołać format_human zarówno w trybie single-run jak i compare.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: format_compare — nowa funkcja + format_json extension</name>
  <files>sphsim/cli/output.py</files>
  <read_first>
    - sphsim/cli/output.py (po Task 1) — istniejące format_human (z guardem `if 'comparison' in res: return format_compare(...)`) + format_json (linie 5-13)
    - .planning/phases/04-rational-agent-veto-layer/04-PATTERNS.md sekcja "### 6. sphsim/cli/output.py" — `format_compare` skeleton (no perfect analog; tabela 5 KPI × 3 col) + format_json extension pattern
    - .planning/phases/04-rational-agent-veto-layer/04-CONTEXT.md D-62 (compare table format 5 KPI × 3 col; werdykt agent_helps na avg_net_profit) + D-67 (format_json dodaje 3 nowe klucze: veto_per_phase, n_vetoed_total, agent_enabled; gdy comparison w res — zastępuje metrics blokiem comparison) + Claude's Discretion "Verdict logic dla agent_helps"
  </read_first>
  <behavior>
    - Test: `format_compare(args, comp, K1)` zwraca string zawierający 5 wierszy KPI: avg_val_last100, cum_val_total, avg_net_profit, delivery_ratio, avg_providers_l100
    - Test: format_compare zwraca string z werdyktem '✓ TAK' gdy comp['agent_helps'] is True, '✗ NIE' gdy False
    - Test: format_compare używa polskich nagłówków kolumn: 'with-agent', 'without', 'Δ (with-no)' (lub równoznacznie z 04-CONTEXT.md D-62 ASCII art)
    - Test: format_json z args.no_agent=False zwraca JSON gdzie `metrics.agent_enabled == true`
    - Test: format_json z args.no_agent=True zwraca JSON gdzie `metrics.agent_enabled == false`
    - Test: gdy res zawiera klucz 'comparison', format_json zwraca JSON z top-level 'comparison' zamiast 'metrics' (D-67)
    - Test: gdy res NIE zawiera 'comparison', format_json zachowuje istniejącą strukturę (strategy, strategy_params, env, metrics) — backwards compat
  </behavior>
  <action>
    (1) **Nowa funkcja `format_compare(args, comp, K1)`** w output.py — wstaw PRZED `format_human` lub PO `format_human` (Claude's Discretion). Per 04-PATTERNS.md §6 i 04-CONTEXT.md D-62: zbierz `with_, without_, delta = comp['with_agent'], comp['without_agent'], comp['delta']`. Zdefiniuj listę 5 KPI z format spec: `[('avg_val_last100', '{:>10.2f}'), ('cum_val_total', '{:>10.1f}'), ('avg_net_profit', '{:>10.4f}'), ('delivery_ratio', '{:>10.2%}'), ('avg_providers_l100', '{:>10.2f}')]`. Buduj listę linii: header z polskim opisem ('PORÓWNANIE STRATEGII z/bez RationalAgent'), tabela ASCII z box-drawing chars (analogicznie do D-62 ASCII art lub prościej `'-'*62` separators jak w format_human), 5 wierszy KPI z trzema kolumnami (with/without/Δ), separator i ostatnia linia z werdyktem `verdict = '✓ TAK' if comp['agent_helps'] else '✗ NIE'` + tekst `f"Veto'wano: {n} COMMIT-ów; with-agent bije without-agent: {verdict}"`. Werdykt na podstawie avg_net_profit per D-62 + Claude's Discretion. Zwróć string przez '\n'.join(lines). (2) **format_json extension** (modyfikacja istniejącej funkcji linii 5-13): wewnątrz funkcji rozszerz blok metrics o `agent_enabled` field — modify the `'metrics': {...}` line do: `'metrics': {**{k: v for k, v in res.items() if k not in ('history', 'devices')}, 'agent_enabled': not args.no_agent}`. Dodaj guard PRZED `return json.dumps`: `if 'comparison' in res:` → zamień `out['metrics']` na `out['comparison']` (usuń klucz metrics, dodaj comparison) — D-67 semantics. Backwards compat: bez --compare-agent, JSON nadal ma `metrics` z istniejącymi kluczami PLUS 3 nowe (veto_per_phase, n_vetoed_total, agent_enabled). Sort_keys=True ZACHOWANE (z istniejącego json.dumps(out, indent=2) — sprawdź czy sort_keys jest tam; jeśli nie, NIE dodawaj). Polish UI strings (werdykt, header tabeli, kolumna 'with-agent' / 'bez agenta' / 'Δ'). Identyfikatory angielskie (agent_enabled, with_agent, without_agent, delta, comparison).
  </action>
  <verify>
    <automated>python -c "
import argparse, json
from sphsim.cli.output import format_compare, format_json

# Test 1: format_compare produces 5-KPI delta table
comp = {
    'with_agent': {'avg_val_last100': 92.0, 'cum_val_total': 9200.0, 'avg_net_profit': 12.4, 'delivery_ratio': 0.88, 'avg_providers_l100': 45.2, 'n_vetoed_total': 87},
    'without_agent': {'avg_val_last100': 85.3, 'cum_val_total': 8530.0, 'avg_net_profit': -3.2, 'delivery_ratio': 0.625, 'avg_providers_l100': 38.7, 'n_vetoed_total': 0},
    'delta': {'avg_val_last100': 6.7, 'cum_val_total': 670.0, 'avg_net_profit': 15.6, 'delivery_ratio': 0.255, 'avg_providers_l100': 6.5},
    'agent_helps': True,
}
fake_args = argparse.Namespace(strategy='incentive', nU=300, nSUS=30, T=2000, kappa=1.0, alpha=1.0, verbose=False)
table = format_compare(fake_args, comp, 100.0)
assert 'avg_val_last100' in table or 'val_last100' in table.lower(), 'avg_val_last100 row missing'
assert 'avg_net_profit' in table or 'net_profit' in table.lower(), 'avg_net_profit row missing'
assert 'delivery_ratio' in table or 'delivery' in table.lower(), 'delivery_ratio row missing'
assert '✓' in table or 'TAK' in table, 'verdict marker missing'

# Test 2: format_json z agent_enabled
res_no_agent = {
    'avg_val_last100': 92.0, 'cum_val_total': 9200.0, 'avg_net_profit': 1.0,
    'delivery_ratio': 0.5, 'avg_providers_l100': 45.0, 'sus_final': 5,
    'ic_per_phase': {}, 'history': {}, 'devices': [],
    'veto_per_phase': {}, 'n_vetoed_total': 0,
}
fake_args_off = argparse.Namespace(strategy='naive', nU=300, nSUS=30, T=2000, kappa=1.0, alpha=1.0, no_agent=True)
js = json.loads(format_json(fake_args_off, res_no_agent, {}, 100.0))
assert js['metrics']['agent_enabled'] == False, f'agent_enabled should be false for --no-agent, got {js[\"metrics\"][\"agent_enabled\"]}'
assert 'veto_per_phase' in js['metrics'], 'veto_per_phase missing in metrics'
assert js['metrics']['n_vetoed_total'] == 0, 'n_vetoed_total should be 0'

# Test 3: comparison block w JSON
res_compare = {'comparison': comp}
fake_args_cmp = argparse.Namespace(strategy='incentive', nU=300, nSUS=30, T=2000, kappa=1.0, alpha=1.0, no_agent=False)
js_cmp = json.loads(format_json(fake_args_cmp, res_compare, {}, 100.0))
assert 'comparison' in js_cmp, f'comparison key missing in JSON: {list(js_cmp.keys())}'
assert js_cmp['comparison']['agent_helps'] == True

print('OK — format_compare + format_json extensions działa')
"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -v '^#' sphsim/cli/output.py | grep -c '^def format_compare'` ≥ 1
    - Source: `grep -v '^#' sphsim/cli/output.py | grep -c "'agent_enabled'"` ≥ 1
    - Source: `grep -v '^#' sphsim/cli/output.py | grep -c "not args.no_agent"` ≥ 1
    - Source: `grep -v '^#' sphsim/cli/output.py | grep -c "'comparison' in res"` ≥ 1 (w format_json LUB format_human z Task 1 — wymaga obu)
    - Source: `grep -v '^#' sphsim/cli/output.py | grep -cE "'avg_val_last100'|avg_val_last100"` ≥ 1 (KPI w format_compare)
    - Source: `grep -v '^#' sphsim/cli/output.py | grep -cE "agent_helps|TAK"` ≥ 1
    - Behavior: verify command (3 inline test cases) exits 0
    - Behavior: `python sph_sim.py --strategy naive --zeta 0.5 --seed 42 --json 2>/dev/null | tail -n +2 | python -c "import json,sys; d=json.load(sys.stdin); assert 'agent_enabled' in d['metrics'], f'agent_enabled missing in metrics: {list(d[\"metrics\"].keys())}'; print('OK')"` — przed Planem 04 args.no_agent może nie istnieć, więc ten check ostatecznie pójdzie po Planie 04 (tutaj akceptujemy oba: pass jeśli zaimplementowane, lub `AttributeError` na args.no_agent gdy flagi jeszcze nie ma)
  </acceptance_criteria>
  <done>
    sphsim/cli/output.py eksportuje 3 publiczne funkcje: format_human (z VETO section + comparison branch), format_compare (nowa, 5×3 delta table z werdyktem agent_helps), format_json (rozszerzony o agent_enabled + auto-pass-through veto_per_phase i n_vetoed_total + comparison branch). Plan 04 (main.py) i Plan 05 (repl.py) mogą zaimportować format_compare.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| simulator.run() dict → output formatters | Formattery używają `res.get(key, default)` zamiast `res[key]` dla nowych pól — zapobiega KeyError gdy res z legacy ścieżki nie ma veto_per_phase |
| args.no_agent → format_json | format_json używa `not args.no_agent` — wymagana obecność atrybutu po Planie 04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Repudiation | JSON backwards compat (CLI-04) | mitigate | Sekcja VETO gracefully pomijana gdy n_vetoed=0; format_json zachowuje istniejące klucze (avg_val_last100 etc.) bit-identycznie; tylko DODAJE nowe klucze (D-67). Regression check w Planie 06 weryfikuje exact-match istniejących pól. |
| T-04-10 | DoS | format_compare ASCII rendering | accept | Czyste string building, O(1) — 5 wierszy. |
| T-04-11 | Information disclosure | agent_helps werdykt w JSON | accept | Boolean true/false na podstawie avg_net_profit — żadne wrażliwe dane, czysta dydaktyka. |
| T-04-12 | Spoofing | args bez atrybutu no_agent (np. REPL fake_args) | mitigate | Plan 05 (REPL do_compare) MUSI dodać `no_agent=False` do fake_args.Namespace; verify gate weryfikuje że format_json nie crashuje dla typowych REPL fake_args |
</threat_model>

<verification>
- Verify command Task 1 i Task 2 (5 inline asercji łącznie) exits 0
- `python -m unittest discover tests` 22+ pre-existing testy pass
- `python sph_sim.py --strategy naive --zeta 0.5 --seed 42 --json 2>/dev/null | tail -n +1 | python -c "import json,sys; raw=sys.stdin.read(); d=json.loads(raw[raw.find('{'):]); assert 'veto_per_phase' in d['metrics'] and 'n_vetoed_total' in d['metrics']"` — JSON ma nowe klucze (po Planie 01 + ten Plan)
</verification>

<success_criteria>
- format_human zawiera sekcję 'VETO przez RationalAgent' gdy n_vetoed_total > 0
- format_human zawiera early branch `if 'comparison' in res: return format_compare(...)` dla --compare-agent
- format_compare zwraca string z 5 KPI × 3 kolumn (with/without/Δ) + werdykt agent_helps
- format_json dodaje veto_per_phase, n_vetoed_total, agent_enabled do bloku metrics
- format_json przy comparison w res zastępuje metrics blokiem comparison (D-67)
- Istniejące pola JSON (avg_val_last100 etc.) NIE zmienione (CLI-04 backwards compat — egzekwowane przez regression check w Planie 06)
</success_criteria>

<output>
Create `.planning/phases/04-rational-agent-veto-layer/04-03-SUMMARY.md` po ukończeniu.
</output>
