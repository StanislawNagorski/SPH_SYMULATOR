---
plan_id: 03-01
phase: 3
wave: 1
depends_on: []
requirements: [STRAT-03, STRAT-04]
files_modified:
  - sphsim/strategies/__init__.py
  - sphsim/strategies/loader.py
  - tests/test_loader.py
autonomous: true
must_haves:
  truths:
    - "load_custom('<path>.py') zwraca (basename, fn, meta) dla poprawnego pliku custom strategii"
    - "D-34: Nazwa strategii (klucz w STRATEGIES) = basename pliku bez `.py` — load_custom robi `os.path.splitext(os.path.basename(abspath))[0]`; jeden source of truth dla user'a"
    - "D-36: Loader akceptuje dowolną ścieżkę (absolutną i relatywną) po `os.path.expanduser` + `os.path.abspath`; brak whitelist katalogów (projekt lokalny akademicki); bezpieczeństwo przez banner D-45, nie przez sandbox"
    - "Każdy z 4 błędów walidacji D-47 (import, brak funkcji/non-callable, zła sygnatura, malformed meta) podnosi LoaderError z polskim komunikatem zawierającym konkret (nazwa, ścieżka, oczekiwana sygnatura)"
    - "Kolizja basename z BUILTIN_STRATEGIES (D-49) podnosi LoaderError 'Nazwa X koliduje z wbudowaną strategią. Zmień nazwę pliku.'"
    - "Drugie wywołanie load_custom na tym samym pliku (D-38) ładuje świeży kod ze zmienionej zawartości (fresh spec_from_file_location, NIE importlib.reload)"
    - "Po failed exec_module sys.modules['sphsim.custom.<basename>'] NIE zostaje jako zombie (Pitfall #2 cleanup)"
    - "Strategia NIE zostaje zarejestrowana w STRATEGIES przy żadnym z 4 błędów (fail-fast — D-46: rejestrację robi wywołujący)"
    - "parse_params_from_meta robi typed conversion z STRATEGY_META['params'] (D-39, D-40), odrzuca nieznane klucze z polskim komunikatem 'Nieznany parametr X. Dostępne: ...'"
    - "D-43 graceful parser: token bez `=` (np. literówka `zeta 0.7`) → ostrzeżenie `Pominięto token 'zeta' — oczekiwany format key=value.` na stdout; loader kontynuuje, używa default dla brakującego parametru"
  artifacts:
    - path: "sphsim/strategies/loader.py"
      provides: "load_custom + parse_params_from_meta + LoaderError + EXPECTED_PARAMS"
      contains: "def load_custom("
    - path: "sphsim/strategies/__init__.py"
      provides: "BUILTIN_STRATEGIES frozenset snapshot Phase 1 keys"
      contains: "BUILTIN_STRATEGIES = frozenset("
    - path: "tests/test_loader.py"
      provides: "19+ unit tests pokrywających 4 warstwy D-47 + D-49 collision + D-38 reload + D-39/D-40 param parser"
      contains: "class TestLoader(unittest.TestCase)"
  key_links:
    - from: "sphsim/strategies/loader.py"
      to: "sphsim/strategies/__init__.py BUILTIN_STRATEGIES"
      via: "from sphsim.strategies import BUILTIN_STRATEGIES (collision check)"
      pattern: "import BUILTIN_STRATEGIES"
    - from: "tests/test_loader.py"
      to: "sphsim.strategies.loader"
      via: "from sphsim.strategies.loader import load_custom, parse_params_from_meta, LoaderError"
      pattern: "from sphsim.strategies.loader import"
---

<objective>
Zbuduj samodzielny moduł loadera custom strategii (Phase 3 D-46) plus stałą BUILTIN_STRATEGIES (D-49) plus pełne pokrycie testowe loadera (19 przypadków per VALIDATION.md). Plan jest izolowany od CLI/REPL — tworzymy pure functions + tests, żeby kolejne fale (CLI w Plan 02, REPL w Plan 03) mogły je używać bez ryzyka regresu.

Purpose: D-46 wymaga że loader jest **pure** (zwraca tuple, nie modyfikuje STRATEGIES) — testowalny w izolacji. D-47 wymaga 4-warstwowej walidacji z polskimi komunikatami; bez dedykowanego pliku testów (D-25 z Phase 2 jest invariant'em meta↔argparse, nie loadera) loader byłby weryfikowany tylko ręcznie. D-49 wprowadza `BUILTIN_STRATEGIES` jako frozenset snapshot zanim runtime'owo zarejestrujemy custom — ta stała jest też używana w Plan 02 (argparse choices) i Plan 03 (do_strategies dispatch namespace), więc tworzymy ją tutaj raz, na samym dnie.

Output: 3 pliki — 1 nowy moduł, 1 zmodyfikowany __init__.py (1 linia dodana), 1 nowy plik testów. Po tej fali `python -m unittest tests.test_loader` musi zwrócić 19/19 pass. Regression `python scripts/regression_check.py` ma nadal zwracać 8/8 (CHANGE w __init__.py to czysty add stałej — nie zmienia STRATEGIES dict).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-custom-strategy-loader/03-CONTEXT.md
@.planning/phases/03-custom-strategy-loader/03-RESEARCH.md
@.planning/phases/03-custom-strategy-loader/03-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md

<interfaces>
<!-- Kontrakty exportowane przez moduł loadera — kluczowe dla Plan 02 (CLI) i Plan 03 (REPL). -->

Eksport `sphsim/strategies/loader.py`:
- `class LoaderError(Exception)` — wszystkie polskie komunikaty błędów w `args[0]`
- `EXPECTED_PARAMS = ('dev', 'l', 's', 'phi', 'kappa', 'rho', 'h', 'p')` — krotka 8 nazw
- `load_custom(path: str) -> tuple[str, callable, dict]` — zwraca (basename, fn, meta); banner stdout PRZED exec; rejestrację robi wywołujący (D-46)
- `parse_params_from_meta(tokens: list[str], meta: dict, strategy_name: str) -> dict` — typed conversion z `STRATEGY_META['params']`; token bez `=` → print warning, nie crash; nieznany key → LoaderError; bad ValueError przy conwersji → LoaderError

Eksport `sphsim/strategies/__init__.py` (DODATEK, istniejący STRATEGIES dict nietykany):
- `BUILTIN_STRATEGIES: frozenset[str]` — snapshot 5 kluczy Phase 1

Sygnatura strategii (verbatim z PROMPT_DLA_AGENTA.txt + Phase 1 D-03):
```python
def strategy_<basename>(dev, l, s, phi, kappa, rho, h, p) -> str:  # 'COMMIT' | 'ABSTAIN'
```

Schema `STRATEGY_META` (verbatim z Phase 2 D-25):
```python
STRATEGY_META = {
    'description': str,
    'params': list[tuple[str, type, Any, str]],  # (name, type_callable, default, polish_description)
    'baseline_kpi': dict | None,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <id>3-01-01</id>
  <name>Task 3-01-01: Dodaj BUILTIN_STRATEGIES frozenset do sphsim/strategies/__init__.py</name>
  <read_first>
    - sphsim/strategies/__init__.py (current state: 19 linii, STRATEGIES dict z 5 kluczami)
    - .planning/phases/03-custom-strategy-loader/03-CONTEXT.md (D-49 spec)
    - .planning/phases/03-custom-strategy-loader/03-RESEARCH.md (Example 2: __init__.py DELTA)
    - .planning/phases/03-custom-strategy-loader/03-PATTERNS.md (sphsim/strategies/__init__.py MOD section)
    - .planning/codebase/CONVENTIONS.md (Constants — SCREAMING_SNAKE_CASE)
  </read_first>
  <action>
    Otwórz `sphsim/strategies/__init__.py` i ZAcHOWAJ wszystko od linii 1 do 19 BEZ zmian (`STRATEGIES` dict musi pozostać jako mutable global — Phase 3 rejestruje custom runtime'owo). Po istniejącej linii 19 (zamknięcie STRATEGIES) dodaj jedną pustą linię, polski komentarz wyjaśniający (D-49: snapshot zanim custom load się stanie), i jedną nową stałą `BUILTIN_STRATEGIES = frozenset(STRATEGIES.keys())`. NIE rób refaktoringu istniejącego kodu. NIE rusz `StrategyFn` aliasu (linia 11). Stała musi być `SCREAMING_SNAKE_CASE` (CONVENTIONS.md). Komentarz polski (PROJECT.md): wymień że to snapshot Phase 1 dla collision detection (D-49) i dispatch namespace w `do_strategies` (D-50 dla Plan 03).
  </action>
  <acceptance_criteria>
    - Source: `python -c "from sphsim.strategies import BUILTIN_STRATEGIES; assert isinstance(BUILTIN_STRATEGIES, frozenset); assert BUILTIN_STRATEGIES == frozenset(['naive', 'threshold', 'phase_prob', 'incentive', 'adaptive'])"` exits 0
    - Source: `grep -c "BUILTIN_STRATEGIES = frozenset" sphsim/strategies/__init__.py` returns 1
    - Behavior: `python -c "from sphsim.strategies import STRATEGIES; assert set(STRATEGIES.keys()) == {'naive','threshold','phase_prob','incentive','adaptive'}"` exits 0 (STRATEGIES dict nienaruszone)
    - Regression: `python scripts/regression_check.py` exits 0 (8/8 fixtures pass — invariant Phase 1)
    - Invariant: `python -m unittest tests.test_strategy_meta_consistency` exits 0 (Phase 2 invariant nadal trzyma)
  </acceptance_criteria>
  <automated>python -c "from sphsim.strategies import STRATEGIES, BUILTIN_STRATEGIES; assert isinstance(BUILTIN_STRATEGIES, frozenset) and BUILTIN_STRATEGIES == frozenset(STRATEGIES.keys()) == frozenset(['naive','threshold','phase_prob','incentive','adaptive'])" && python scripts/regression_check.py && python -m unittest tests.test_strategy_meta_consistency</automated>
  <done>BUILTIN_STRATEGIES istnieje jako frozenset 5 Phase 1 kluczy; STRATEGIES dict nietknięte; regression 8/8; invariant pass.</done>
</task>

<task type="auto" tdd="true">
  <id>3-01-02</id>
  <name>Task 3-01-02: Zaimplementuj sphsim/strategies/loader.py z LoaderError + load_custom + parse_params_from_meta</name>
  <read_first>
    - sphsim/strategies/__init__.py (już zaktualizowany w 3-01-01 — BUILTIN_STRATEGIES dostępne)
    - sphsim/strategies/threshold.py (kanoniczny shape strategii + STRATEGY_META — wzór dla template i walidacji)
    - sphsim/strategies/naive.py (alternatywny analog STRATEGY_META z baseline_kpi nie-None)
    - sphsim/cli/repl.py (linie 1-21 — module docstring + imports pattern do skopiowania)
    - .planning/phases/03-custom-strategy-loader/03-CONTEXT.md (D-34..D-49)
    - .planning/phases/03-custom-strategy-loader/03-RESEARCH.md (Example 1: pełny ~100-linijny skeleton loader.py — interfejs identyfikatorów do replikacji; Pattern 1/2/3; Common Pitfalls #1-#5)
    - .planning/phases/03-custom-strategy-loader/03-PATTERNS.md (loader.py module docstring + imports + exception class + EXPECTED_PARAMS + spec_from_file_location + error message format patterns)
    - .planning/codebase/CONVENTIONS.md (Naming + Polish strings)
    - PROMPT_DLA_AGENTA.txt (kontrakt sygnatury strategii — 8 nazw w EXPECTED_PARAMS)
  </read_first>
  <behavior>
    - load_custom('valid_strategy.py') gdy plik ma `strategy_<basename>(dev,l,s,phi,kappa,rho,h,p)` + valid STRATEGY_META → return (basename, fn, meta), drukuje banner `[OSTRZEŻENIE] Ładuję arbitralny kod Pythona z: <abspath>` na stdout PRZED exec_module
    - load_custom('/nonexistent.py') → raise LoaderError z message `Plik nie istnieje: /nonexistent.py` (Pitfall #4)
    - load_custom('file.txt') → raise LoaderError z message zawierającym `nie wygląda na plik Pythona` (Pitfall #3 — spec is None)
    - load_custom('plik_z_syntax_error.py') → raise LoaderError z message zawierającym `Błąd podczas importu` i `SyntaxError` (D-47 layer 1)
    - load_custom z plikiem bez `strategy_<basename>` function → raise LoaderError z message zawierającym `Brak funkcji 'strategy_<basename>'` i `Oczekiwana sygnatura: strategy_<basename>(dev, l, s, phi, kappa, rho, h, p) -> str.` (D-47 layer 2)
    - load_custom gdy `strategy_<basename> = 42` (non-callable) → raise LoaderError zawierający `Brak funkcji 'strategy_<basename>'` (D-47 layer 2, callable check PRZED signature — Pitfall #5)
    - load_custom z `def strategy_X(dev, x, y, z)` → raise LoaderError zawierający `Oczekiwana: (dev, l, s, phi, kappa, rho, h, p)` (D-47 layer 3)
    - load_custom z `def strategy_X(*args)` → return tuple (wrapper escape — Pattern 2)
    - load_custom z brakującym `STRATEGY_META` → raise LoaderError zawierający `STRATEGY_META` (D-47 layer 4)
    - load_custom z `STRATEGY_META = "string"` → raise LoaderError zawierający `musi być dict`
    - load_custom z `STRATEGY_META = {}` (brak `description`/`params`/`baseline_kpi`) → raise LoaderError zawierający `brakuje klucza`
    - load_custom z basename collision (np. plik nazwany `naive.py`) → raise LoaderError zawierający `koliduje z wbudowaną` (D-49)
    - load_custom drugie wywołanie na tej samej ścieżce po edycji pliku → zwraca nową wersję meta (D-38 — fresh spec, NIE importlib.reload — Pitfall #1)
    - load_custom z plikiem rzucającym Exception na top-level (`raise ZeroDivisionError('init')`) → raise LoaderError I `sys.modules['sphsim.custom.<basename>']` NIE istnieje po failed call (Pitfall #2)
    - parse_params_from_meta(['zeta=0.75', 'max_phase=4'], meta, 'fake') gdy meta ma `('zeta', float, ...)` i `('max_phase', int, ...)` → return {'zeta': 0.75, 'max_phase': 4} (D-40 typed conversion; type identity float i int zachowane)
    - parse_params_from_meta(['foo=1'], meta_without_foo, 'fake') → raise LoaderError zawierający `Nieznany parametr 'foo'` i `Dostępne:`
    - parse_params_from_meta(['zeta=0.75x'], meta_with_float_zeta, 'fake') → raise LoaderError zawierający `Nie można skonwertować` i `'zeta'`
    - parse_params_from_meta(['zeta'], meta_with_zeta, 'fake') → token bez `=` drukuje warning `Pominięto token 'zeta' — oczekiwany format key=value.` na stdout, returns default value dla zeta (D-43 graceful)
    - parse_params_from_meta([], meta_with_defaults, 'fake') → return dict z wartościami default z meta (D-40)
    - parse_params_from_meta(['json_key=k=v'], meta_with_json_key_str, 'fake') → `{'json_key': 'k=v'}` (D-39 split na PIERWSZY `=`)
  </behavior>
  <action>
    Utwórz nowy plik `sphsim/strategies/loader.py` (~120 linii target). Module docstring po polsku (skopiuj styl z `sphsim/cli/repl.py` linie 1-14): wymień że loader jest pure (D-46), eksportuje `load_custom`, `parse_params_from_meta`, `LoaderError`, że re-load jest manual fresh-spec (Pitfall #1 — NIE używaj `importlib.reload`), że banner idzie na stdout PRZED exec_module (D-45). Stdlib only — importy: `importlib.util`, `inspect`, `os`, `sys`.

    Eksportuj `EXPECTED_PARAMS = ('dev', 'l', 's', 'phi', 'kappa', 'rho', 'h', 'p')` jako module-level constant (SCREAMING_SNAKE_CASE per CONVENTIONS.md).

    Zdefiniuj `class LoaderError(Exception): pass` z polskim docstring "Dowolny problem podczas ładowania custom strategii (D-48). Wywołujący wyciąga e.args[0] jako polski one-liner."

    Zaimplementuj `load_custom(path: str)` w kolejności KROK PO KROKU (kolejność krytyczna — Pitfall #5 mówi że callable check MUSI być przed signature check):
    1) **Path resolve (D-36):** `abspath = os.path.abspath(os.path.expanduser(path))`. Jeśli `not os.path.exists(abspath)` → `raise LoaderError(f"Plik nie istnieje: {abspath}")` (Pitfall #4 — bez tego user dostałby angielskie FileNotFoundError z exec_module).
    2) **Name extract + collision check (D-34, D-49):** `basename = os.path.splitext(os.path.basename(abspath))[0]`. Import wewnątrz funkcji: `from sphsim.strategies import BUILTIN_STRATEGIES`. Jeśli `basename in BUILTIN_STRATEGIES` → `raise LoaderError(f"Nazwa '{basename}' koliduje z wbudowaną strategią. Zmień nazwę pliku.")`.
    3) **Banner (D-45):** `print(f"[OSTRZEŻENIE] Ładuję arbitralny kod Pythona z: {abspath}")` — stdout (default print target), PRZED jakimkolwiek `spec_from_file_location`. Banner musi się pojawić nawet gdy `exec_module` później wybuchnie (user musi wiedzieć skąd kod).
    4) **Spec + exec (D-47 layer 1, Pitfall #2/#3):** `full_name = f'sphsim.custom.{basename}'`. `spec = importlib.util.spec_from_file_location(full_name, abspath)`. Jeśli `spec is None or spec.loader is None` → `raise LoaderError(f"Ścieżka {abspath} nie wygląda na plik Pythona (.py).")` (Pitfall #3 — `.txt` zwraca None). `mod = importlib.util.module_from_spec(spec)`. `sys.modules[full_name] = mod` PRZED `exec_module` (circular import safety). W try: `spec.loader.exec_module(mod)`; w except Exception as e: `sys.modules.pop(full_name, None)` (Pitfall #2 — cleanup zombie) i `raise LoaderError(f"Błąd podczas importu pliku {abspath}: {type(e).__name__}: {e}")`.
    5) **Function exists + callable (D-47 layer 2, Pitfall #5):** `fn_name = f'strategy_{basename}'`. `fn = getattr(mod, fn_name, None)`. Jeśli `not callable(fn)` → `raise LoaderError(f"Brak funkcji '{fn_name}' w pliku {abspath}. Oczekiwana sygnatura: {fn_name}({', '.join(EXPECTED_PARAMS)}) -> str.")`. **KRYTYCZNE:** callable check MUSI być PRZED signature check — `inspect.signature(NotCallable())` rzuca `TypeError`, nie `LoaderError`.
    6) **Signature exact + var-pos escape (D-47 layer 3, Pattern 2):** `sig = inspect.signature(fn)`. `params = list(sig.parameters.values())`. `has_var_pos = any(p.kind == inspect.Parameter.VAR_POSITIONAL for p in params)`. Jeśli `has_var_pos`: pomiń sprawdzanie (wrapper escape — `*args` legit dla decorators). Inaczej: `actual_names = tuple(p.name for p in params[:8])`. Jeśli `actual_names != EXPECTED_PARAMS` → `raise LoaderError(f"Funkcja '{fn_name}' ma sygnaturę {sig}. Oczekiwana: ({', '.join(EXPECTED_PARAMS)}).")`.
    7) **STRATEGY_META validation (D-47 layer 4):** wydzielony helper `_validate_meta(meta, abspath)` (private — leading underscore). Checks (kolejność): `meta = getattr(mod, 'STRATEGY_META', None)`; jeśli `not isinstance(meta, dict)` → `raise LoaderError(f"STRATEGY_META w pliku {abspath} musi być dict, otrzymano {type(meta).__name__}.")`. Dla każdego z `('description', 'params', 'baseline_kpi')`: jeśli brak → `raise LoaderError(f"STRATEGY_META w pliku {abspath} brakuje klucza '{required}'.")`. `isinstance(meta['description'], str)`; `isinstance(meta['params'], list)`. Dla każdego `tup` w `meta['params']` z indeksem `i`: musi być `tuple` o `len==4`, inaczej `LoaderError(f"STRATEGY_META['params'][{i}] w {abspath} musi być krotką 4-elementową (name, type, default, description), otrzymano {tup!r}.")`. Unpack `(pname, ptype, _default, pdesc)`: `pname` musi być str, `ptype` callable, `pdesc` str (inaczej `LoaderError`). `meta['baseline_kpi']` musi być None lub dict.
    8) **Return:** `return basename, fn, meta`. Loader NIE robi `STRATEGIES[basename] = fn` (D-46 — rejestracja w wywołującym).

    Zaimplementuj `parse_params_from_meta(tokens: list, meta: dict, strategy_name: str) -> dict` (D-39, D-40, D-43):
    1) `declared = {name: (ptype, default) for name, ptype, default, _ in meta['params']}`
    2) `out = {name: default for name, (_ptype, default) in declared.items()}` — populate defaults
    3) Buduj listę `warnings = []`. Dla każdego `tok` w `tokens`: jeśli `'=' not in tok` → `warnings.append(f"Pominięto token '{tok}' — oczekiwany format key=value.")` i `continue` (D-43 graceful). Inaczej `key, raw_value = tok.split('=', 1)` (PIERWSZY `=`, D-39 — wartość może zawierać `=`).
    4) Jeśli `key not in declared` → `available = ', '.join(declared.keys()) if declared else '(brak)'`; `raise LoaderError(f"Nieznany parametr '{key}' dla strategii '{strategy_name}'. Dostępne: {available}.")`.
    5) `ptype, _default = declared[key]`. W try: `out[key] = ptype(raw_value)`; w except (ValueError, TypeError): `raise LoaderError(f"Nie można skonwertować '{raw_value}' na {ptype.__name__} dla parametru '{key}'.")`.
    6) Po pętli: `for w in warnings: print(w)`. Return `out`.

    Wszystkie identyfikatory (LoaderError, load_custom, parse_params_from_meta, EXPECTED_PARAMS) po angielsku. Wszystkie komunikaty błędów + komentarze + docstring po polsku (PROJECT.md constraint).
  </action>
  <acceptance_criteria>
    - Source: `grep -c "class LoaderError" sphsim/strategies/loader.py` returns 1
    - Source: `grep -c "def load_custom" sphsim/strategies/loader.py` returns 1
    - Source: `grep -c "def parse_params_from_meta" sphsim/strategies/loader.py` returns 1
    - Source: `grep -c "EXPECTED_PARAMS = " sphsim/strategies/loader.py` returns 1 (8-element tuple)
    - Source: `grep -c "importlib.reload" sphsim/strategies/loader.py` returns 0 (Pitfall #1 — nigdy nie używamy reload)
    - Source: `grep -c "sys.modules.pop" sphsim/strategies/loader.py` returns at least 1 (Pitfall #2 cleanup)
    - Behavior: `python -c "from sphsim.strategies.loader import load_custom, parse_params_from_meta, LoaderError, EXPECTED_PARAMS; assert EXPECTED_PARAMS == ('dev','l','s','phi','kappa','rho','h','p')"` exits 0
    - Behavior: smoke test happy path `python -c "import tempfile, os; f=tempfile.NamedTemporaryFile('w', suffix='.py', delete=False); f.write(\"def strategy_\" + os.path.basename(f.name)[:-3] + \"(dev,l,s,phi,kappa,rho,h,p): return 'COMMIT'\\nSTRATEGY_META={'description':'t','params':[],'baseline_kpi':None}\\n\"); f.close(); from sphsim.strategies.loader import load_custom; n,fn,m = load_custom(f.name); assert callable(fn); assert m['description']=='t'"` exits 0 with stdout containing `[OSTRZEŻENIE]`
    - Regression: `python scripts/regression_check.py` exits 0 (loader.py dodanie nie zmienia żadnego built-in code path)
    - Invariant: `python -m unittest tests.test_strategy_meta_consistency` exits 0
  </acceptance_criteria>
  <automated>python -c "from sphsim.strategies.loader import load_custom, parse_params_from_meta, LoaderError, EXPECTED_PARAMS; assert EXPECTED_PARAMS == ('dev','l','s','phi','kappa','rho','h','p') and isinstance(LoaderError('x'), Exception)" && python scripts/regression_check.py && python -m unittest tests.test_strategy_meta_consistency</automated>
  <done>loader.py istnieje, eksportuje load_custom + parse_params_from_meta + LoaderError + EXPECTED_PARAMS; brak `importlib.reload`; zawiera `sys.modules.pop` cleanup; smoke happy path działa; regression i invariant zielone.</done>
</task>

<task type="auto" tdd="true">
  <id>3-01-03</id>
  <name>Task 3-01-03: Stwórz tests/test_loader.py z 19 przypadkami pokrywającymi 4 warstwy D-47 + D-38 reload + D-49 collision + parser params</name>
  <read_first>
    - sphsim/strategies/loader.py (właśnie stworzony w 3-01-02 — sprawdź dokładne komunikaty błędów)
    - tests/test_strategy_meta_consistency.py (linie 1-30 — analog dla module docstring + sys.path bootstrap; linie 119-128 — assertEqual z descriptive msg= pattern; linie 171-172 — `if __name__ == '__main__': unittest.main()`)
    - .planning/phases/03-custom-strategy-loader/03-CONTEXT.md (D-47/D-49 specs)
    - .planning/phases/03-custom-strategy-loader/03-RESEARCH.md (Example 7+: pełen test_loader.py skeleton — 19 cases ready to copy patterns from; Validation Architecture section)
    - .planning/phases/03-custom-strategy-loader/03-PATTERNS.md (tests/test_loader.py section — setUp/tearDown helpers + assertRaises pattern)
    - .planning/phases/03-custom-strategy-loader/03-VALIDATION.md (19-case test taxonomy)
  </read_first>
  <behavior>
    19 testów (klasa `TestLoader(unittest.TestCase)`), każdy izolowany przez `setUp/tearDown` z `tempfile.mkdtemp` + cleanup `sphsim.custom.*` z `sys.modules` + restore `STRATEGIES.keys()` do snapshotu pre-test:
    - `test_happy_path_loads_validates_returns` → load_custom('valid.py') zwraca (basename, callable, dict z description='test')
    - `test_syntax_error_in_user_file` → plik z `def broken(:` rzuca LoaderError z `Błąd podczas importu` i `SyntaxError` w message
    - `test_path_not_exists` → load_custom('/tmp/nope.py') rzuca LoaderError z `Plik nie istnieje`
    - `test_rejects_non_py_extension` → plik `.txt` rzuca LoaderError (nie wygląda na plik Pythona)
    - `test_missing_function` → plik bez `strategy_<basename>` rzuca LoaderError z `Brak funkcji 'strategy_no_fn'` i `dev, l, s, phi, kappa, rho, h, p`
    - `test_non_callable_attribute` → `strategy_not_callable = 42` (int) rzuca LoaderError z `Brak funkcji 'strategy_not_callable'`
    - `test_wrong_signature` → `def strategy_wrong_sig(dev, x, y, z)` rzuca LoaderError z `Oczekiwana: (dev, l, s, phi, kappa, rho, h, p)`
    - `test_var_positional_accepted` → `def strategy_var_pos(*args)` → load_custom succeeds, return ('var_pos', fn, meta)
    - `test_missing_meta` → plik bez STRATEGY_META rzuca LoaderError z `STRATEGY_META`
    - `test_malformed_meta` → `STRATEGY_META = "not a dict"` rzuca LoaderError z `musi być dict`
    - `test_meta_missing_keys` → `STRATEGY_META = {'description': 'x'}` (brak params + baseline_kpi) rzuca LoaderError z `brakuje klucza`
    - `test_meta_params_not_list` → `STRATEGY_META = {'description': 'x', 'params': 'not list', 'baseline_kpi': None}` rzuca LoaderError
    - `test_meta_param_tuple_wrong_arity` → params zawierające 3-tuple zamiast 4-tuple rzuca LoaderError z `krotką 4-elementową`
    - `test_builtin_name_collision` → plik nazwany `naive.py` (zaisting builtin) rzuca LoaderError z `koliduje z wbudowaną`
    - `test_reload_picks_up_changes` → load_custom('x.py') zwraca meta v1; nadpisz plik z v2 (z `time.sleep(1.1)` dla mtime change na ext4/HFS+); drugie load_custom zwraca meta v2 (D-38 — fresh spec)
    - `test_failed_load_cleans_sys_modules` → plik z `raise ZeroDivisionError('init')` na top-level: load_custom rzuca LoaderError I `'sphsim.custom.boom' not in sys.modules` po failed call (Pitfall #2)
    - `test_param_typed_from_meta` → parse_params_from_meta(['zeta=0.75', 'max_phase=4'], meta_with_both, 'fake') zwraca `{'zeta': 0.75, 'max_phase': 4}` z type identity float/int
    - `test_param_unknown` → parse_params_from_meta(['foo=1'], meta_without_foo, 'fake') rzuca LoaderError z `Nieznany parametr 'foo'` i `Dostępne:`
    - `test_param_conversion_error` → parse_params_from_meta(['zeta=0.75x'], meta_with_float_zeta, 'fake') rzuca LoaderError z `Nie można skonwertować`
    - `test_param_malformed_token_warns` → parse_params_from_meta(['zeta'], meta, 'fake') (token bez `=`) NIE rzuca, returns defaults, stdout zawiera `Pominięto token 'zeta'` (capture stdout via `io.StringIO` + `contextlib.redirect_stdout`)
    - `test_param_value_contains_equals` → parse_params_from_meta(['k=a=b'], meta_with_k_str, 'fake') zwraca `{'k': 'a=b'}` (D-39 — split na PIERWSZY `=`)
  </behavior>
  <action>
    Utwórz nowy plik `tests/test_loader.py`. Module docstring po polsku (skopiuj styl z `tests/test_strategy_meta_consistency.py` linie 1-15) wymieniający: pokrywa 4 warstwy D-47 + D-49 collision + D-38 reload + parse_params_from_meta; stdlib only (`unittest + tempfile + textwrap + os + sys + time + shutil + io + contextlib`); referencje D-XX.

    Bootstrap sys.path verbatim z `tests/test_strategy_meta_consistency.py` linie 23-29 (`_PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), os.pardir)); if _PROJECT_ROOT not in sys.path: sys.path.insert(0, _PROJECT_ROOT)`).

    Importy: `import io, os, shutil, sys, tempfile, textwrap, time, unittest` plus `from contextlib import redirect_stdout` plus `from sphsim.strategies.loader import load_custom, parse_params_from_meta, LoaderError` plus `from sphsim.strategies import STRATEGIES, BUILTIN_STRATEGIES`.

    Klasa `TestLoader(unittest.TestCase)` z polskim docstring "4-warstwowa walidacja loadera + reload + collision + param parser (D-46/D-47/D-49/D-39/D-40/D-43).".

    `setUp(self)`: tworzy `self.tmpdir = tempfile.mkdtemp()` + zapisuje snapshot `self._snapshot_strategies = set(STRATEGIES.keys())` (do cleanup'u jeśli który test by zarejestrował coś).

    `tearDown(self)`: `shutil.rmtree(self.tmpdir, ignore_errors=True)`; cleanup `for k in list(STRATEGIES.keys()): if k not in self._snapshot_strategies: del STRATEGIES[k]`; cleanup `for k in list(sys.modules.keys()): if k.startswith('sphsim.custom.'): del sys.modules[k]`.

    Helper `_write(self, name, content)`: `path = os.path.join(self.tmpdir, name); with open(path, 'w', encoding='utf-8') as f: f.write(textwrap.dedent(content)); return path` (dedent pozwala na czytelne wielolinijkowe content w testach z indentacją).

    Helper `_write_valid(self, basename)`: tworzy minimal valid strategy plik `{basename}.py` z `def strategy_{basename}(dev,l,s,phi,kappa,rho,h,p): return 'COMMIT'` + `STRATEGY_META = {'description':'test', 'params':[], 'baseline_kpi':None}`. Używany w testach gdzie szczegóły walidacji nie są pointem.

    Napisz **19 metod testowych** dokładnie według behavior block powyżej. Każdy test używa `with self.assertRaises(LoaderError) as cm:` + `self.assertIn(<polski substring>, cm.exception.args[0], msg=f'LoaderError msg should mention X, got: {cm.exception.args[0]!r}')`. Assertions używają descriptive `msg=` (zob. `tests/test_strategy_meta_consistency.py` linie 121-128).

    Dla `test_param_malformed_token_warns`: użyj `buf = io.StringIO(); with redirect_stdout(buf): result = parse_params_from_meta(['zeta'], meta, 'fake'); self.assertIn("Pominięto token 'zeta'", buf.getvalue())`.

    Dla `test_reload_picks_up_changes`: między dwoma write'ami użyj `time.sleep(1.1)` (mtime resolution na HFS+/ext4 to ~1s). Sprawdź `meta1['description'] == 'v1'` i `meta2['description'] == 'v2'` po drugim load_custom.

    Plik kończy się verbatim: `if __name__ == '__main__':\n    unittest.main()`.

    Czas wykonania całego suite'u: cel <5 sec (VALIDATION.md Sampling Rate).
  </action>
  <acceptance_criteria>
    - Source: `grep -c "class TestLoader" tests/test_loader.py` returns 1
    - Source: `grep -E "def test_" tests/test_loader.py | wc -l` returns at least 19
    - Source: `grep -c "from sphsim.strategies.loader import" tests/test_loader.py` returns 1
    - Source: `grep -c "shutil.rmtree" tests/test_loader.py` returns at least 1 (tearDown cleanup)
    - Behavior: `python -m unittest tests.test_loader -v` exits 0 with all 19+ tests passing
    - Behavior: `python -m unittest tests.test_loader -v 2>&1 | grep -c "... ok"` returns at least 19
    - Behavior: `python -m unittest tests.test_loader.TestLoader.test_happy_path_loads_validates_returns` exits 0 in <2 sec
    - Behavior: `python -m unittest tests.test_loader.TestLoader.test_failed_load_cleans_sys_modules` exits 0 (Pitfall #2 zombie cleanup verified)
    - Behavior: `python -m unittest tests.test_loader.TestLoader.test_reload_picks_up_changes` exits 0 (Pitfall #1 reload via fresh spec verified)
    - Behavior: `python -m unittest tests.test_loader.TestLoader.test_builtin_name_collision` exits 0 (D-49 collision verified)
    - Full suite: `python -m unittest discover tests -v` exits 0 (test_loader + test_strategy_meta_consistency both green)
    - Regression: `python scripts/regression_check.py` exits 0
    - Runtime: `time python -m unittest tests.test_loader` reports real time <8 sec (loader test 19 cases ~2s + sleep 1.1s in reload test)
  </acceptance_criteria>
  <automated>python -m unittest tests.test_loader -v && python -m unittest discover tests && python scripts/regression_check.py</automated>
  <done>tests/test_loader.py istnieje, zawiera 19+ test methods, wszystkie zielone; pełny test discover green; regression 8/8.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-supplied path → loader | Plik `.py` użytkownika dostarczony jako string ścieżki przez CLI `--custom` (Plan 02) lub REPL `custom` (Plan 03). Wykonywany przez `spec.loader.exec_module(mod)`. |
| Loader → STRATEGIES registry | Loader celowo NIE wstawia do `STRATEGIES` (D-46); ten boundary jest "pure boundary" — wywołujący (Plan 02/03) musi świadomie wykonać side effect. |
| Loader → sys.modules | Loader rejestruje moduł w `sys.modules['sphsim.custom.<basename>']` PRZED exec_module (circular import safety); po failed exec robi `sys.modules.pop(full_name, None)` (Pitfall #2 — zero zombie). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering / Elevation | `spec.loader.exec_module(mod)` w `load_custom` — wykonuje arbitralny Python | ACCEPTED-WITH-WARNING | D-45 banner `[OSTRZEŻENIE] Ładuję arbitralny kod Pythona z: <abspath>` drukowany PRZED exec (banner widoczny nawet gdy exec wybuchnie); template (Plan 04) ma polski docstring "lokalny projekt akademicki"; ASVS L1 wystarcza (nie hosted service per PROJECT.md). Sandboxowanie explicit out-of-scope w CONTEXT.md `<out_of_scope>`. |
| T-3-02 | Tampering / Information Disclosure | sys.modules pollution / namespace bleed między built-in a custom | MITIGATED | Loader używa PRIVATE namespace `sphsim.custom.<basename>` (D-46) — nigdy nie ładuje do `sphsim.strategies.<basename>` gdzie żyją built-in. Collision detection (D-49): `if basename in BUILTIN_STRATEGIES: raise LoaderError` zanim spec_from_file_location. Test: `test_builtin_name_collision`. |
| T-3-03 | Tampering | Failed-import zombies w sys.modules (Pitfall #2 z RESEARCH) | MITIGATED | `sys.modules.pop(full_name, None)` w `except` branch `exec_module`. Test: `test_failed_load_cleans_sys_modules`. Bez tej mitigacji następne `load_custom` na tej samej nazwie dostałoby corrupted module obj. |
| T-3-04 | Spoofing | Signature spoofing via *args/**kwargs wrappers omijające positional-arg validation (D-47 layer 3) | ACCEPTED (low) | D-47 layer 3 explicit pozwala VAR_POSITIONAL escape (legit wrapper pattern z decorators). Layer 4 STRATEGY_META validation nadal wymagana — wrapper bez meta failuje. Test: `test_var_positional_accepted` + `test_missing_meta`. |
| T-3-05 | Information Disclosure | Path traversal / unintended file load — user podaje dowolną ścieżkę | ACCEPTED-WITH-BANNER | D-36 explicit nie używa whitelist (projekt lokalny akademicki). Banner D-45 wyświetla absolutną ścieżkę (po `os.path.abspath(os.path.expanduser(path))`), więc user widzi skąd kod przychodzi. Pitfall #4 — explicit `os.path.exists` check daje polski "Plik nie istnieje" zamiast angielskiego FileNotFoundError. |
| T-3-SC | Tampering | npm/pip/cargo dependency confusion | N/A | Phase 3 jest STDLIB-ONLY (RESEARCH.md Package Legitimacy Audit pusty). Brak nowych instalacji = brak `[ASSUMED]/[SUS]` packages = brak checkpoint'u. Pierwsza zewnętrzna zależność dopiero w Phase 6 (matplotlib). |

**ASVS level:** 1 (projekt akademicki lokalny per PROJECT.md; nie hosted service).
**Block on:** żaden — wszystkie threats ACCEPTED-with-mitigation lub MITIGATED.
</threat_model>

<verification>
- [ ] `BUILTIN_STRATEGIES` jest frozenset 5 kluczy Phase 1 — verified by inline python -c
- [ ] `load_custom` zwraca tuple `(basename, fn, meta)` dla valid pliku — verified by smoke + happy path test
- [ ] 4 warstwy D-47 walidacji rzucają LoaderError z polskim message zawierającym konkret — verified by 11 dedicated test methods
- [ ] D-38 reload przez fresh spec (NIE importlib.reload) — verified by `test_reload_picks_up_changes` + `grep -c "importlib.reload" sphsim/strategies/loader.py` returns 0
- [ ] D-49 collision z BUILTIN_STRATEGIES → LoaderError — verified by `test_builtin_name_collision`
- [ ] Failed exec_module cleanup z sys.modules — verified by `test_failed_load_cleans_sys_modules`
- [ ] parse_params_from_meta robi typed conversion z meta + odrzuca nieznane + ostrzega o malformed tokens — verified by 5 dedicated tests
- [ ] Regression 8/8 nadal pass — verified by `python scripts/regression_check.py`
- [ ] Phase 2 invariant nadal pass — verified by `python -m unittest tests.test_strategy_meta_consistency`
- [ ] Pełne test discover green — verified by `python -m unittest discover tests`
</verification>

<success_criteria>
- `python -m unittest tests.test_loader` exits 0 (19/19 pass)
- `python -m unittest discover tests` exits 0 (test_loader + test_strategy_meta_consistency)
- `python scripts/regression_check.py` exits 0 (8/8 backwards compat)
- `python -c "from sphsim.strategies import BUILTIN_STRATEGIES, STRATEGIES; assert BUILTIN_STRATEGIES == frozenset(STRATEGIES.keys()) == frozenset(['naive','threshold','phase_prob','incentive','adaptive'])"` exits 0
- `python -c "from sphsim.strategies.loader import load_custom, parse_params_from_meta, LoaderError, EXPECTED_PARAMS; assert EXPECTED_PARAMS == ('dev','l','s','phi','kappa','rho','h','p')"` exits 0
- `grep -c "importlib.reload" sphsim/strategies/loader.py` returns 0 (Pitfall #1)
- `grep -c "sys.modules.pop" sphsim/strategies/loader.py` returns >=1 (Pitfall #2)
- Plan 02 i Plan 03 mogą importować `load_custom`, `parse_params_from_meta`, `LoaderError`, `BUILTIN_STRATEGIES` bez modyfikacji tego planu
</success_criteria>

<output>
Create `.planning/phases/03-custom-strategy-loader/03-01-SUMMARY.md` when done.
</output>
