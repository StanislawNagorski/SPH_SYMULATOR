---
plan_id: 03-04
phase: 3
wave: 3
depends_on: [03-02, 03-03]
requirements: [STRAT-05]
files_modified:
  - examples/custom_strategy_template.py
  - scripts/verify_phase3.sh
autonomous: true
must_haves:
  truths:
    - "Plik examples/custom_strategy_template.py istnieje, kompiluje się bez ostrzeżeń (py_compile), zawiera polski docstring header tłumaczący do czego służy, polskie komentarze inline opisujące argumenty dev/l/s/phi/kappa/rho/h/p + wartości zwrotne 'COMMIT'/'ABSTAIN' (SC #3, STRAT-05)"
    - "D-52 examples/ jako katalog projektu: `examples/custom_strategy_template.py` to JEDYNY plik tutaj — brak `examples/__init__.py` (nie pakiet, tylko storage), brak `examples/.gitignore` (user trzyma swoje strategie poza repo, np. `~/my-strats/`); template jest commitowany jako kanoniczny starter"
    - "Template ma `def strategy_custom_strategy_template(dev, l, s, phi, kappa, rho, h, p)` (D-35 nazwa = strategy_<basename>) + STRATEGY_META z description (polski), params [('max_phase', int, 4, 'Maksymalna faza dla COMMIT')], baseline_kpi=None (D-51)"
    - "`python sph_sim.py --custom examples/custom_strategy_template.py --seed 42 --json` uruchamia się bez błędu, drukuje banner, drukuje deterministyczny JSON z `strategy: custom_strategy_template` i wszystkimi 6 KPI (SC #3 acceptance check)"
    - "Komenda REPL `custom examples/custom_strategy_template.py` ładuje template i pokazuje go w `strategies` z suffixem ` [custom]`, następnie `run custom_strategy_template max_phase=3` uruchamia symulację (SC #1, #4)"
    - "Wszystkie 5 ROADMAP Phase 3 Success Criteria są weryfikowane przez verify_phase3.sh: SC #1 CLI+REPL custom load; SC #2 polskie error messages z konkretem; SC #3 template kompiluje + uruchamia + daje wyniki; SC #4 [custom] suffix w strategies; SC #5 banner pre-import"
    - "Regression 8/8 backwards compat (Phase 1 contract) + Phase 2 invariant + Plan 01 loader 19/19 tests + pełny test discover są częścią verify_phase3.sh phase exit gate"
  artifacts:
    - path: "examples/custom_strategy_template.py"
      provides: "Kanoniczny template custom strategii — polski docstring + polskie komentarze + STRATEGY_META schema"
      contains: "def strategy_custom_strategy_template"
    - path: "scripts/verify_phase3.sh"
      provides: "Phase 3 exit gate — 5 ROADMAP SCs + regression + invariant + loader tests"
      contains: "#!/usr/bin/env bash"
  key_links:
    - from: "examples/custom_strategy_template.py"
      to: "loader.load_custom (Plan 01) + CLI main.py args.custom branch (Plan 02) + REPL do_custom (Plan 03)"
      via: "wszystkie 3 entry pointy muszą działać out-of-the-box dla template'a"
      pattern: "load_custom"
    - from: "scripts/verify_phase3.sh"
      to: "regression_check.py + unittest discover + sph_sim.py --custom + sph_sim.py --interactive"
      via: "shell script wywołuje wszystkie 4 weryfikatory; exit 0 gdy wszystkie pass"
      pattern: "regression_check\\|unittest\\|sph_sim"
---

<objective>
Dostarcz pliki acceptance Phase 3: kanoniczny template `examples/custom_strategy_template.py` (D-51, STRAT-05) plus phase exit gate `scripts/verify_phase3.sh` weryfikujący wszystkie 5 ROADMAP Success Criteria razem z regressionem i invariantem. To jest ostatnia fala — depends on Plan 02 (CLI musi działać) ORAZ Plan 03 (REPL musi działać), bo template testowany jest przez OBIE ścieżki.

Purpose: D-51 + STRAT-05 wymagają konkretnego pliku `examples/custom_strategy_template.py` z polskimi komentarzami który **MUSI** uruchamiać się przez `python sph_sim.py --custom examples/custom_strategy_template.py --seed 42 --json` deterministycznie (SC #3 acceptance check). Bez tego user nie ma startera — REQUIREMENTS.md mówi explicit "projekt zawiera przykładowy szablon". `verify_phase3.sh` jest paraleluzeem do istniejącego `scripts/verify_phase1.sh` z Phase 1 — zachowuje spójność stylu phase gates'ów w projekcie i daje jeden command do gating całej Phase 3 przed merge'em.

Output: 1 nowy plik template + 1 nowy executable shell script. Po tej fali `bash scripts/verify_phase3.sh` musi zwrócić exit 0 — to jest finalne gate dla Phase 3 (przed `/gsd:verify-work`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-custom-strategy-loader/03-CONTEXT.md
@.planning/phases/03-custom-strategy-loader/03-RESEARCH.md
@.planning/phases/03-custom-strategy-loader/03-PATTERNS.md
@.planning/phases/03-custom-strategy-loader/03-01-PLAN.md
@.planning/phases/03-custom-strategy-loader/03-02-PLAN.md
@.planning/phases/03-custom-strategy-loader/03-03-PLAN.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Wszystkie kontrakty z poprzednich planów zakończonych (Wave 1 + Wave 2): -->

```
# Plan 01 dostarczył:
sphsim/strategies/loader.py — load_custom + parse_params_from_meta + LoaderError + EXPECTED_PARAMS
sphsim/strategies/__init__.py — STRATEGIES + BUILTIN_STRATEGIES
tests/test_loader.py — 19+ tests, all green

# Plan 02 dostarczył:
sphsim/cli/args.py — mutex 3-człony (interactive | strategy | custom) + --param append
sphsim/cli/main.py — early branch args.custom: load + register + simulate + format

# Plan 03 dostarczył:
sphsim/cli/repl.py — SPHShell z 6 metodami (help, exit, strategies, strategy, custom, run);
                     do_strategies dispatch ns + [custom] suffix; do_strategy dispatch ns

# Phase 1/2 contracts (must NOT break):
scripts/regression_check.py — 8/8 fixtures must stay green
tests/test_strategy_meta_consistency.py — Phase 2 invariant
sphsim/core/simulator.py — interface NIE modyfikowany
sphsim/cli/output.py — format_human / format_json NIE modyfikowane
```

```
# Phase 3 ROADMAP Success Criteria (verifiers w verify_phase3.sh):
SC #1: `--custom` (CLI) + `custom` (REPL) ładują plik .py przez importlib
SC #2: Loader sprawdza obecność funkcji i komunikuje błędy z konkretem po polsku
SC #3: examples/custom_strategy_template.py istnieje, polskie komentarze, kompiluje się bez ostrzeżeń, ładuje przez loader, daje wyniki
SC #4: Custom strategia w `strategies` z suffixem [custom]
SC #5: Loader komunikuje że wykonuje arbitralny Python (banner D-45)
```

```
# scripts/verify_phase1.sh — istniejący wzorzec (czytaj BEFORE pisania verify_phase3.sh)
# Layout (bash strict mode + sectional checks + final summary)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <id>3-04-01</id>
  <name>Task 3-04-01: Utwórz examples/custom_strategy_template.py (D-51, STRAT-05)</name>
  <read_first>
    - sphsim/strategies/threshold.py (D-51 explicit: "alias threshold z innym defaultem" — wzór funkcji)
    - sphsim/strategies/naive.py (analog STRATEGY_META z baseline_kpi)
    - .planning/phases/03-custom-strategy-loader/03-CONTEXT.md (D-51 + D-52 spec)
    - .planning/phases/03-custom-strategy-loader/03-RESEARCH.md (Example 6: pełen template ~46 linii z polskim docstring + argument explanations + STRATEGY_META)
    - .planning/phases/03-custom-strategy-loader/03-PATTERNS.md (examples/custom_strategy_template.py — strategy function body + STRATEGY_META patterns verbatim do skopiowania)
    - PROMPT_DLA_AGENTA.txt (kontrakt sygnatury — 8 argumentów do udokumentowania w komentarzach)
  </read_first>
  <action>
    Sprawdź czy `examples/` directory istnieje — jeśli nie, utwórz: `mkdir -p examples` (D-52: `examples/` jest katalogiem projektu, JEDYNY plik to template; brak `__init__.py`, brak `.gitignore`).

    Utwórz nowy plik `examples/custom_strategy_template.py`. Target ~40-50 linii (D-51 ~30-50).

    **Struktura pliku (kolejność krytyczna dla py_compile + ładowności):**

    1) **Header docstring** (multi-line `"""..."""`) — polski, ~10 linii. Zawiera:
       - Krótkie zdanie do czego służy ("Szablon custom strategii dla SPH Symulatora (Phase 3).")
       - Instrukcja: "Skopiuj ten plik gdziekolwiek (np. ~/moje-strategie/my_strat.py), zmień nazwę pliku i nazwę funkcji na strategy_<nowy_basename>, uruchom przez:"
       - Polecenie przykładowe: `python sph_sim.py --custom <ścieżka>.py --param max_phase=3 --seed 42`
       - Reguła nazewnictwa (D-34 + D-35): "Nazwa funkcji MUSI być `strategy_<basename_pliku_bez_py>` — np. dla `my_strat.py` → `strategy_my_strat`."
       - Krótka nota bezpieczeństwa: "Uwaga: loader wykonuje ten plik jak każdy moduł Pythona. Projekt akademicki lokalny — bez sandboxa."

    2) **Komentarz blok argumentów funkcji strategii** (polski, ~10 linii — verbatim z PROMPT_DLA_AGENTA.txt + Phase 1 D-03; format: jedna linia per argument):
       ```
       # Argumenty funkcji strategii (DOKŁADNIE w tej kolejności, nazwy jak w sygnaturze):
       #   dev    — bieżące urządzenie (obiekt Device z polami: id, phase 1..F-1, status 'UP'/'DOWN')
       #   l      — list[int] — liczba dostawców per faza z POPRZEDNIEGO cyklu, długość F-1
       #   s      — int — bieżąca zajętość bufora SUS (0..nSUS)
       #   phi    — list[float] — P(awarii) per faza, długość F
       #   kappa  — float — koszt commit (dostarczenia usługi)
       #   rho    — list[float] — koszt awarii per faza, długość F
       #   h      — callable(int) -> float — funkcja wagi i^alpha
       #   p      — dict — params strategii (z --param k=v lub k=v w REPL)
       #
       # Wartość zwrotna: literal string 'COMMIT' albo 'ABSTAIN' (NIE enum, NIE bool).
       ```

    3) **Definicja funkcji strategii** (D-35: nazwa = `strategy_custom_strategy_template`; D-51: alias `threshold` z defaultem 4):
       ```python
       def strategy_custom_strategy_template(dev, l, s, phi, kappa, rho, h, p):
           # Guard: tylko urządzenia UP podejmują decyzję
           if dev.status != 'UP':
               return 'ABSTAIN'

           # Czytamy parametr 'max_phase' — przekazany przez --param max_phase=N lub default 4
           max_phase = int(p.get('max_phase', 4))

           # Prosta reguła: COMMIT dla wczesnych faz, ABSTAIN dla późnych
           # (alias `threshold` z innym defaultem — pokazuje jak iść własną drogą)
           return 'COMMIT' if dev.phase <= max_phase else 'ABSTAIN'
       ```

    4) **STRATEGY_META kontrakt** (D-47 layer 4 wymaga — bez tego loader rzuci LoaderError):
       ```python
       # Metadane strategii — wymagany kontrakt (loader D-47 layer 4)
       # Klucze: 'description' (str), 'params' (list[tuple-4]), 'baseline_kpi' (dict|None)
       STRATEGY_META = {
           'description': 'Szablon: COMMIT dla faz <= max_phase (przykład dydaktyczny)',
           'params': [
               # (nazwa, typ_callable, wartość_domyślna, opis_polski)
               ('max_phase', int, 4, 'Maksymalna faza dla COMMIT'),
           ],
           'baseline_kpi': None,  # opcjonalne — gdy znasz baseline, podaj dict z 'invocation' i 'avg_val_last100'
       }
       ```

    Polski w **wszystkich** komentarzach + docstring + description string. Identyfikatory (`strategy_custom_strategy_template`, `STRATEGY_META`, `dev`, `l`, `s`, `phi`, `kappa`, `rho`, `h`, `p`, `max_phase`) po angielsku (zgodne z Phase 1/2 spójność).

    **Krytyczne wymaganie**: plik musi się skompilować przez `python -m py_compile examples/custom_strategy_template.py` bez ostrzeżeń (SC #3). Po `compileall` lub `py_compile`, sprawdź `.pyc` w `__pycache__/` (gdyby się pojawił).

    Komendę acceptance: `python sph_sim.py --custom examples/custom_strategy_template.py --seed 42 --json` musi zwrócić exit 0 + valid JSON na stdout (po pierwszej linii banneru) zawierający `"strategy": "custom_strategy_template"` (D-51 acceptance check). Wynik musi być deterministyczny dla seed=42 (powtarzalny przy ponownych uruchomieniach).

    Test po REPL też: `printf "custom examples/custom_strategy_template.py\nstrategies\nrun custom_strategy_template max_phase=3\nexit\n" | python sph_sim.py --interactive` musi działać i zawierać "custom_strategy_template" z `[custom]` suffix w `strategies` outputcie.
  </action>
  <acceptance_criteria>
    - Source: `ls examples/custom_strategy_template.py` exits 0 (plik istnieje)
    - Source: `ls examples/__init__.py 2>/dev/null; test $? -ne 0` (D-52 — NIE pakiet, brak __init__.py)
    - Source: `grep -c "def strategy_custom_strategy_template(dev, l, s, phi, kappa, rho, h, p)" examples/custom_strategy_template.py` returns 1
    - Source: `grep -c "STRATEGY_META = {" examples/custom_strategy_template.py` returns 1
    - Source: `grep -c "'max_phase', int, 4" examples/custom_strategy_template.py` returns 1
    - Source: `grep -c "Szablon" examples/custom_strategy_template.py` returns at least 1 (polski docstring)
    - Source (no English comments by accident): `grep -E "^\s*#\s+(The|This|To|For)" examples/custom_strategy_template.py | grep -v "TODO" | wc -l` returns 0 (komentarze nie zaczynają się od typowych angielskich słów)
    - Compile: `python -m py_compile examples/custom_strategy_template.py` exits 0 (SC #3 — kompiluje bez ostrzeżeń)
    - Compile-Wall: `python -W all -m py_compile examples/custom_strategy_template.py 2>&1; echo $?` outputs "0" (no warnings)
    - Behavior (loader unit): `python -c "from sphsim.strategies.loader import load_custom; n, fn, m = load_custom('examples/custom_strategy_template.py'); assert n == 'custom_strategy_template'; assert callable(fn); assert m['description']"` exits 0
    - Behavior (CLI acceptance — SC #3): `python sph_sim.py --custom examples/custom_strategy_template.py --seed 42 --json > /tmp/template_out.txt 2>/dev/null && tail -n +2 /tmp/template_out.txt | python -c "import json, sys; d = json.loads(sys.stdin.read()); assert d['strategy'] == 'custom_strategy_template'; assert 'avg_val_last100' in d['metrics']"` exits 0
    - Behavior (banner present — SC #5): `python sph_sim.py --custom examples/custom_strategy_template.py --seed 42 --json 2>/dev/null | head -1 | grep -q "^\[OSTRZEŻENIE\] Ładuję arbitralny kod Pythona z:.*examples/custom_strategy_template.py$"`
    - Behavior (determinism — SC #3): two runs produce identical JSON: `python sph_sim.py --custom examples/custom_strategy_template.py --seed 42 --json 2>/dev/null | tail -n +2 > /tmp/run1.json && python sph_sim.py --custom examples/custom_strategy_template.py --seed 42 --json 2>/dev/null | tail -n +2 > /tmp/run2.json && diff /tmp/run1.json /tmp/run2.json` exits 0
    - Behavior (REPL flow — SC #1, #4): `printf "custom examples/custom_strategy_template.py\nstrategies\nrun custom_strategy_template max_phase=3\nexit\n" | python sph_sim.py --interactive 2>&1 | grep -q "custom_strategy_template.*\[custom\]"` matches
    - Behavior (parametry działają): `python sph_sim.py --custom examples/custom_strategy_template.py --param max_phase=2 --seed 42 --json 2>/dev/null | tail -n +2 > /tmp/mp2.json && python sph_sim.py --custom examples/custom_strategy_template.py --param max_phase=4 --seed 42 --json 2>/dev/null | tail -n +2 > /tmp/mp4.json && ! diff -q /tmp/mp2.json /tmp/mp4.json > /dev/null` (różne max_phase → różne KPI; D-39/D-40 param flow działa end-to-end)
    - Regression: `python scripts/regression_check.py` exits 0
    - Tests: `python -m unittest discover tests` exits 0
  </acceptance_criteria>
  <automated>python -m py_compile examples/custom_strategy_template.py && python -c "from sphsim.strategies.loader import load_custom; n, fn, m = load_custom('examples/custom_strategy_template.py'); assert n == 'custom_strategy_template' and callable(fn) and m['description']" && python sph_sim.py --custom examples/custom_strategy_template.py --seed 42 --json > /tmp/t_out.txt 2>/dev/null && head -1 /tmp/t_out.txt | grep -q "^\[OSTRZEŻENIE\]" && tail -n +2 /tmp/t_out.txt | python -c "import json, sys; d = json.loads(sys.stdin.read()); assert d['strategy'] == 'custom_strategy_template' and 'avg_val_last100' in d['metrics']" && python scripts/regression_check.py && python -m unittest discover tests</automated>
  <done>Template istnieje, kompiluje, ładuje się przez loader, działa CLI+REPL, parametry zmieniają wynik deterministycznie; banner widoczny; regression + invariant + loader tests zielone.</done>
</task>

<task type="auto">
  <id>3-04-02</id>
  <name>Task 3-04-02: Utwórz scripts/verify_phase3.sh — Phase 3 exit gate weryfikujący 5 ROADMAP SCs + regression + invariant</name>
  <read_first>
    - scripts/verify_phase1.sh (jeśli istnieje — wzorzec stylu phase gate w projekcie; sprawdź `ls scripts/verify_phase*.sh`)
    - scripts/regression_check.py (źródło prawdy CLI-04 backwards compat)
    - tests/test_loader.py (Plan 01 — `python -m unittest tests.test_loader` musi pass)
    - tests/test_strategy_meta_consistency.py (Phase 2 — `python -m unittest tests.test_strategy_meta_consistency` musi pass)
    - examples/custom_strategy_template.py (Plan 04 task 01 — acceptance smoke target)
    - .planning/ROADMAP.md (Phase 3 — 5 Success Criteria)
    - .planning/phases/03-custom-strategy-loader/03-RESEARCH.md (Validation Architecture: phase gate command examples; acceptance commands per SC)
    - .planning/phases/03-custom-strategy-loader/03-VALIDATION.md (Test Infrastructure full suite command)
  </read_first>
  <action>
    Sprawdź czy `scripts/verify_phase1.sh` istnieje (`ls scripts/verify_phase1.sh 2>/dev/null`). Jeśli istnieje — czytaj go (Read tool) i adaptuj strukturę do Phase 3. Jeśli nie istnieje — utwórz bez analogi, styl ustalony poniżej.

    Utwórz `scripts/verify_phase3.sh`. Po utworzeniu zrób executable: `chmod +x scripts/verify_phase3.sh`.

    **Struktura skryptu (bash strict mode + sectional checks + final summary):**

    1) **Shebang + strict mode:**
       ```
       #!/usr/bin/env bash
       set -euo pipefail
       ```

    2) **Konfiguracja:**
       - `cd "$(dirname "$0")/.."` (zmień na project root, niezależnie od miejsca uruchomienia)
       - `PASS=0`, `FAIL=0`
       - Funkcja helper `check()` przyjmująca opis + komendę, drukująca `[PASS]` lub `[FAIL]` z opisem i inkrementująca licznik.

    3) **Sekcja 1 — Regression (CLI-04 backwards compat, Phase 1 contract):**
       ```
       echo "── 1. Regression backwards compat (Phase 1 contract) ──"
       check "Regression: 8/8 baseline_v1 fixtures" "python scripts/regression_check.py"
       ```

    4) **Sekcja 2 — Phase 2 invariant + Plan 01 loader tests:**
       ```
       echo "── 2. Invariant + loader unit tests ──"
       check "Phase 2 invariant: STRATEGY_META ↔ argparse" "python -m unittest tests.test_strategy_meta_consistency"
       check "Phase 3 loader: 19+ unit cases" "python -m unittest tests.test_loader"
       check "Full test discover" "python -m unittest discover tests"
       ```

    5) **Sekcja 3 — SC #3 Template acceptance:**
       ```
       echo "── 3. SC #3: examples/custom_strategy_template.py ──"
       check "Template kompiluje bez ostrzeżeń" "python -W all -m py_compile examples/custom_strategy_template.py"
       check "Template ładuje się przez loader" "python -c \"from sphsim.strategies.loader import load_custom; n,fn,m = load_custom('examples/custom_strategy_template.py'); assert n == 'custom_strategy_template' and callable(fn)\""
       check "Template uruchamia się przez CLI --custom" "python sph_sim.py --custom examples/custom_strategy_template.py --seed 42 --json > /tmp/p3_template.txt 2>/dev/null"
       check "Template output to valid JSON z strategy=custom_strategy_template" "tail -n +2 /tmp/p3_template.txt | python -c \"import json, sys; d = json.loads(sys.stdin.read()); assert d['strategy'] == 'custom_strategy_template' and 'avg_val_last100' in d['metrics']\""
       ```

    6) **Sekcja 4 — SC #5 Banner pre-import:**
       ```
       echo "── 4. SC #5: banner [OSTRZEŻENIE] pre-import ──"
       check "Banner widoczny PRZED JSON outputem" "python sph_sim.py --custom examples/custom_strategy_template.py --seed 42 --json 2>/dev/null | head -1 | grep -q '^\\[OSTRZEŻENIE\\] Ładuję arbitralny kod Pythona z:.*custom_strategy_template.py$'"
       ```

    7) **Sekcja 5 — SC #2 Loader error messages (polskie z konkretem):**
       ```
       echo "── 5. SC #2: polskie error messages z konkretem ──"
       check "Loader rzuca polski błąd dla nieistniejącej ścieżki" "python sph_sim.py --custom /tmp/nope_$$.py --seed 42 2>&1 1>/dev/null | grep -q 'Plik nie istnieje'; test \${PIPESTATUS[0]} -eq 1"
       # Stwórz tymczasowy plik z błędną sygnaturą żeby zweryfikować SC #2 layer 3:
       cat > /tmp/p3_bad_sig.py <<'EOF'
       def strategy_p3_bad_sig(dev, x, y):
           return 'COMMIT'
       STRATEGY_META = {'description': 't', 'params': [], 'baseline_kpi': None}
       EOF
       check "Loader rzuca polski błąd dla złej sygnatury" "python sph_sim.py --custom /tmp/p3_bad_sig.py --seed 42 2>&1 1>/dev/null | grep -q 'Oczekiwana: (dev, l, s, phi, kappa, rho, h, p)'"
       rm -f /tmp/p3_bad_sig.py
       ```

    8) **Sekcja 6 — SC #1 + SC #4 REPL flow (custom + strategies [custom] + run):**
       ```
       echo "── 6. SC #1 + SC #4: REPL custom + strategies [custom] + run ──"
       check "REPL ładuje custom + listuje z [custom] suffix" "printf 'custom examples/custom_strategy_template.py\nstrategies\nexit\n' | python sph_sim.py --interactive 2>&1 | grep -q 'custom_strategy_template.*\\[custom\\]'"
       check "REPL uruchamia custom przez run" "printf 'custom examples/custom_strategy_template.py\nrun custom_strategy_template max_phase=3\nexit\n' | python sph_sim.py --interactive 2>&1 | grep -q 'Strategia: CUSTOM_STRATEGY_TEMPLATE'"
       check "REPL reload (D-38) drukuje Przeładowano" "printf 'custom examples/custom_strategy_template.py\ncustom examples/custom_strategy_template.py\nexit\n' | python sph_sim.py --interactive 2>&1 | grep -q 'Przeładowano custom strategię'"
       ```

    9) **Sekcja 7 — SC #1 CLI + determinism:**
       ```
       echo "── 7. SC #1: CLI --custom determinism ──"
       check "Dwa uruchomienia template'a dają identyczny output (seed=42)" "python sph_sim.py --custom examples/custom_strategy_template.py --seed 42 --json 2>/dev/null | tail -n +2 > /tmp/p3_run1.json && python sph_sim.py --custom examples/custom_strategy_template.py --seed 42 --json 2>/dev/null | tail -n +2 > /tmp/p3_run2.json && diff -q /tmp/p3_run1.json /tmp/p3_run2.json"
       check "Param przekazany przez --param zmienia wynik" "python sph_sim.py --custom examples/custom_strategy_template.py --param max_phase=2 --seed 42 --json 2>/dev/null | tail -n +2 > /tmp/p3_mp2.json && python sph_sim.py --custom examples/custom_strategy_template.py --param max_phase=4 --seed 42 --json 2>/dev/null | tail -n +2 > /tmp/p3_mp4.json && ! diff -q /tmp/p3_mp2.json /tmp/p3_mp4.json"
       ```

    10) **Sekcja 8 — Mutex enforcement (D-44):**
        ```
        echo "── 8. D-44: mutex enforcement (interactive | strategy | custom) ──"
        check "Mutex odrzuca --custom + --strategy" "python sph_sim.py --custom foo.py --strategy naive 2>&1 | grep -q 'not allowed with argument'"
        check "Mutex required: brak żadnego trybu → błąd" "python sph_sim.py 2>&1 | grep -q 'one of the arguments'"
        ```

    11) **Final summary:**
        ```
        echo ""
        echo "════════════════════════════════════════"
        echo "  Phase 3 verification: PASS=$PASS / FAIL=$FAIL"
        echo "════════════════════════════════════════"
        if [ "$FAIL" -gt 0 ]; then
            exit 1
        fi
        echo "✓ Phase 3 ready for /gsd:verify-work"
        exit 0
        ```

    Funkcja `check()` implementacja:
    ```bash
    check() {
        local desc="$1"
        local cmd="$2"
        if eval "$cmd" > /tmp/p3_check.log 2>&1; then
            echo "[PASS] $desc"
            PASS=$((PASS + 1))
        else
            echo "[FAIL] $desc"
            echo "       cmd: $cmd"
            echo "       log:"
            sed 's/^/         /' /tmp/p3_check.log | head -20
            FAIL=$((FAIL + 1))
        fi
    }
    ```

    Polskie komunikaty user-facing (sectional headers `── N. <desc> ──`, summary). Identyfikatory bash (PASS, FAIL, check, desc, cmd) po angielsku.

    Po utworzeniu skryptu: `chmod +x scripts/verify_phase3.sh`. Final test: `bash scripts/verify_phase3.sh` musi zwrócić exit 0 z wszystkimi `[PASS]` i `FAIL=0` w summary.
  </action>
  <acceptance_criteria>
    - Source: `ls -l scripts/verify_phase3.sh` shows file exists and is executable (`-rwxr-xr-x` or similar — `x` bit set)
    - Source: `head -1 scripts/verify_phase3.sh | grep -q "^#!/usr/bin/env bash"` matches
    - Source: `grep -c "set -euo pipefail" scripts/verify_phase3.sh` returns 1
    - Source: `grep -c "regression_check.py" scripts/verify_phase3.sh` returns 1
    - Source: `grep -c "test_loader" scripts/verify_phase3.sh` returns at least 1
    - Source: `grep -c "test_strategy_meta_consistency" scripts/verify_phase3.sh` returns at least 1
    - Source: `grep -c "examples/custom_strategy_template.py" scripts/verify_phase3.sh` returns at least 4 (compile + load + CLI + REPL acceptance)
    - Source: `grep -c "\\[OSTRZE" scripts/verify_phase3.sh` returns at least 1 (banner SC #5 check)
    - Source: `grep -c "\\[custom\\]" scripts/verify_phase3.sh` returns at least 1 (suffix SC #4 check)
    - Source: `grep -c "Oczekiwana: (dev, l, s, phi, kappa, rho, h, p)" scripts/verify_phase3.sh` returns at least 1 (SC #2 error message check)
    - Source: `grep -c "Plik nie istnieje" scripts/verify_phase3.sh` returns at least 1 (SC #2 path error check)
    - Source: `grep -c "Przeładowano custom" scripts/verify_phase3.sh` returns at least 1 (D-38 reload check)
    - Source: `grep -c "diff -q\|diff -" scripts/verify_phase3.sh` returns at least 1 (determinism check)
    - Behavior: `bash scripts/verify_phase3.sh` exits 0
    - Behavior: `bash scripts/verify_phase3.sh 2>&1 | grep -c "\\[FAIL\\]"` returns 0
    - Behavior: `bash scripts/verify_phase3.sh 2>&1 | grep -c "\\[PASS\\]"` returns at least 13 (every check group: regression, invariant, loader, discover, compile, load, run, JSON, banner, error path, error sig, REPL custom listing, REPL run, REPL reload, determinism, param-effect, mutex 1, mutex 2 — przynajmniej ~16-18 ale 13 to safe lower bound)
    - Behavior: `bash scripts/verify_phase3.sh 2>&1 | tail -5 | grep -q "Phase 3 ready"` matches
    - Regression (final cross-check): `python scripts/regression_check.py && python -m unittest discover tests && bash scripts/verify_phase3.sh` ALL exit 0
  </acceptance_criteria>
  <automated>chmod +x scripts/verify_phase3.sh && bash scripts/verify_phase3.sh</automated>
  <done>verify_phase3.sh executable, kompletny phase gate weryfikujący wszystkie 5 ROADMAP SCs + regression + invariant; exit 0; "Phase 3 ready" w outputcie.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template plik → loader | Plik kontrolowany przez nas (commitowany), loader go ładuje jak każdy custom — banner + walidacja. |
| verify_phase3.sh → wszystkie modyfikowane komponenty | Shell script wykonuje komendy projektowe; nie wprowadza nowych boundary, tylko weryfikuje istniejące. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering / Elevation | exec_module dla template'a | ACCEPTED-WITH-WARNING | Template jest commitowany przez nas, polski docstring tłumaczy "lokalny projekt akademicki — bez sandboxa" (D-51, T-3-01 z Plan 01 dziedziczone). |
| T-3-12 | Tampering | verify_phase3.sh modyfikuje `/tmp/p3_*` files | MITIGATED (low) | Wszystkie pliki tymczasowe są w `/tmp/p3_*` namespace (prefix wyraźny); `rm -f` w sekcji 5 po użyciu. Brak race condition na single-user dev machine. |
| T-3-13 | Repudiation | Plik tmp `/tmp/p3_template.txt` zostaje po skrypcie | ACCEPTED | `/tmp` jest cleared on reboot na większości systemów; debug info dla developera. Nie security issue. |
| T-3-SC | Tampering | npm/pip/cargo confusion | N/A | STDLIB-ONLY. Bash script używa tylko POSIX utilities. |

**ASVS level:** 1. **Block on:** żaden.
</threat_model>

<verification>
- [ ] `examples/custom_strategy_template.py` istnieje i ładuje się przez wszystkie 3 entry pointy (Plan 01 loader, Plan 02 CLI, Plan 03 REPL) — verified by 03-04-01 acceptance criteria
- [ ] Template ma polski docstring + polskie komentarze + 8 argumentów udokumentowanych — verified by source greps
- [ ] Template kompiluje się bez ostrzeżeń — verified przez `python -W all -m py_compile`
- [ ] CLI acceptance command z D-51 działa deterministycznie — verified przez double-run diff
- [ ] Param max_phase rzeczywiście zmienia wynik — verified przez różny output dla max_phase=2 vs max_phase=4
- [ ] REPL flow custom→strategies→run działa — verified by 03-04-01 acceptance criteria
- [ ] `scripts/verify_phase3.sh` istnieje, executable, exits 0 — verified by 03-04-02 acceptance criteria
- [ ] Wszystkie 5 ROADMAP SCs sprawdzane przez verify_phase3.sh sections 3-7 — verified przez source greps i exit code
- [ ] Regression 8/8 + Phase 2 invariant + Plan 01 loader 19/19 + pełny discover — verified przez sections 1-2
- [ ] Mutex enforcement zachowany — verified przez section 8
</verification>

<success_criteria>
- `bash scripts/verify_phase3.sh` exits 0 i drukuje "Phase 3 ready for /gsd:verify-work"
- Wszystkie 5 ROADMAP Phase 3 Success Criteria są mechanicznie weryfikowane:
  - SC #1 (`--custom`/`custom` ładują przez importlib): sections 3, 6, 7
  - SC #2 (polskie error messages z konkretem): section 5
  - SC #3 (template kompiluje + uruchamia + daje wyniki): section 3
  - SC #4 (custom widoczne w `strategies` z `[custom]` suffix): section 6
  - SC #5 (banner pre-import): section 4
- `python sph_sim.py --custom examples/custom_strategy_template.py --seed 42 --json` daje deterministyczny JSON output (powtarzalny)
- Regression `python scripts/regression_check.py` exits 0 (CLI-04 backwards compat zachowany przez całą Phase 3)
- Pełny `python -m unittest discover tests` exits 0 (Phase 1 + Phase 2 + Phase 3 tests wszystkie zielone)
</success_criteria>

<output>
Create `.planning/phases/03-custom-strategy-loader/03-04-SUMMARY.md` when done.
</output>
