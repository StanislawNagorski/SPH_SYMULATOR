---
plan_id: 03-02
phase: 3
wave: 2
depends_on: [03-01]
requirements: [STRAT-03]
files_modified:
  - sphsim/cli/args.py
  - sphsim/cli/main.py
autonomous: true
must_haves:
  truths:
    - "Komenda `python sph_sim.py --custom <path>.py --seed 42 --json` ładuje plik, drukuje banner [OSTRZEŻENIE] PRZED uruchomieniem strategii, uruchamia SPHSimulator z DEFAULT env params, drukuje deterministyczny JSON output (D-44, D-45, SC #1)"
    - "Mutex group ma teraz 3 człony (--interactive | --strategy | --custom), required=True; user musi wybrać dokładnie jeden (D-44)"
    - "Flaga --strategy używa BUILTIN_STRATEGIES jako choices (NIE STRATEGIES.keys()), więc argparse listuje tylko 5 wbudowanych — custom uruchamia się wyłącznie przez --custom (D-50 świadome ograniczenie one-shot CLI)"
    - "Flaga --param k=v jest repeatable (action='append'), poza mutex, działa tylko z --custom (D-39); gdy podana bez --custom, drukuje graceful warning na stderr i kontynuuje built-in flow"
    - "Loader error (np. SyntaxError, brak funkcji, kolizja z built-in) drukuje polski komunikat na stderr i sys.exit(1) — user widzi konkret bez angielskiego traceback"
    - "Regression 8/8 nadal pass — istniejące inwokacje `--strategy <name>` z fixtures nie są zaburzone (CLI-04 backwards compat, SC #1 ROADMAP Phase 1)"
  artifacts:
    - path: "sphsim/cli/args.py"
      provides: "argparse parser z --custom w mutex + --param poza mutex + --strategy choices na BUILTIN_STRATEGIES"
      contains: "--custom"
    - path: "sphsim/cli/main.py"
      provides: "early branch `if args.custom: ...` po istniejącym `if args.interactive: ...`"
      contains: "if args.custom"
  key_links:
    - from: "sphsim/cli/args.py"
      to: "sphsim/strategies BUILTIN_STRATEGIES"
      via: "import BUILTIN_STRATEGIES + choices=list(BUILTIN_STRATEGIES)"
      pattern: "BUILTIN_STRATEGIES"
    - from: "sphsim/cli/main.py"
      to: "sphsim.strategies.loader.load_custom / parse_params_from_meta / LoaderError"
      via: "import wewnątrz if args.custom branch (lazy — nie obciąża built-in flow)"
      pattern: "from sphsim.strategies.loader import"
    - from: "sphsim/cli/main.py"
      to: "sphsim.core.simulator.SPHSimulator"
      via: "sim = SPHSimulator(strategy_fn=strategy_fn, params=params, ...)"
      pattern: "SPHSimulator\\("
---

<objective>
Wepnij `--custom <path>` jako trzeci człon mutex group w argparse, dodaj repeatable `--param k=v` poza mutex, i podłącz drugą early-branch w `main()` która ładuje plik przez `load_custom`, parsuje params przez `parse_params_from_meta`, buduje `SPHSimulator` z DEFAULT env (Phase 5 doda override) i drukuje wynik. Plan jest izolowany od REPL (Plan 03 robi REPL równolegle, też depends_on [03-01]).

Purpose: D-44 wymaga że `--custom` jest trzecim członem mutex `required=True`, bo CLI musi wymusić wybór jednego trybu (interactive | strategy | custom). D-50 wymaga zmiany `choices` w `--strategy` z `list(STRATEGIES.keys())` na `list(BUILTIN_STRATEGIES)` — bez tego po Phase 3, gdyby kiedyś (testy?) `STRATEGIES` zawierała custom, `argparse --help` listowałby je jako valid choices, ale CLI one-shot parsuje argparse przed loaderem, więc te custom nie istniałyby w czasie parsowania. D-39 repeatable `--param k=v` pozwala użytkownikowi przekazać `--param zeta=0.7 --param max_phase=4` — `action='append'` jest standardowym argparse idiomem.

Output: 2 zmodyfikowane pliki. SC #1 ROADMAP zaspokojone w CLI części (`--custom <path>` ładuje + rejestruje + uruchamia). Regression 8/8 musi pozostać zielone — to twarde acceptance criterion (CLI-04, ROADMAP Phase 1 SC #1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-custom-strategy-loader/03-CONTEXT.md
@.planning/phases/03-custom-strategy-loader/03-RESEARCH.md
@.planning/phases/03-custom-strategy-loader/03-PATTERNS.md
@.planning/phases/03-custom-strategy-loader/03-01-PLAN.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Z Plan 01 (właśnie zakończonego — zielone testy): -->

```python
# sphsim/strategies/__init__.py — istniejące + nowe
STRATEGIES: dict[str, callable]      # mutable runtime registry
BUILTIN_STRATEGIES: frozenset[str]   # snapshot Phase 1 — 5 nazw

# sphsim/strategies/loader.py — moduł stworzony w 03-01
def load_custom(path: str) -> tuple[str, callable, dict]
    # Drukuje banner [OSTRZEŻENIE] na stdout PRZED exec
    # Rzuca LoaderError z polskim komunikatem przy 4 warstwach walidacji
    # Wywołujący MUSI sam zrobić STRATEGIES[name] = fn (D-46 pure loader)

def parse_params_from_meta(tokens: list[str], meta: dict, strategy_name: str) -> dict
    # Token bez `=` → print warning + continue
    # Niezadeklarowany key → raise LoaderError
    # Bad type conversion → raise LoaderError

class LoaderError(Exception): pass
```

```python
# sphsim/core/simulator.py — istniejąca z Phase 1 (NIE modyfikujemy)
SPHSimulator(
    nU=int, nSUS=int, K0=int, K1=float, F=int, T=int,
    kappa=float, alpha=float, phi=list, rho=list,
    strategy_fn=callable, params=dict, seed=int
).run() -> dict
```

```python
# sphsim/cli/output.py — istniejące (NIE modyfikujemy)
format_human(args, res, K1, verbose: bool) -> str
    # args musi mieć attrs: strategy, nU, nSUS, T, kappa, alpha
format_json(args, res, params, K1) -> str
    # args.strategy używane jako 'strategy' field
```

```python
# sphsim/config.py — istniejące DEFAULT_* (NIE modyfikujemy)
DEFAULT_NU, DEFAULT_NSUS, DEFAULT_K0, DEFAULT_K1, DEFAULT_F,
DEFAULT_T, DEFAULT_KAPPA, DEFAULT_ALPHA, DEFAULT_PHI, DEFAULT_RHO
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <id>3-02-01</id>
  <name>Task 3-02-01: Rozszerz sphsim/cli/args.py o --custom (mutex) + --param (poza mutex) + --strategy choices na BUILTIN_STRATEGIES</name>
  <read_first>
    - sphsim/cli/args.py (current state: 61 linii, mutex z --interactive | --strategy + 5 strategy params + 7 env params)
    - sphsim/strategies/__init__.py (właśnie dodane BUILTIN_STRATEGIES)
    - .planning/phases/03-custom-strategy-loader/03-CONTEXT.md (D-39 + D-44 + D-50)
    - .planning/phases/03-custom-strategy-loader/03-RESEARCH.md (Example 3: args.py DELTA; Pitfall #6 backwards compat)
    - .planning/phases/03-custom-strategy-loader/03-PATTERNS.md (sphsim/cli/args.py MODIFY section)
    - .planning/codebase/CONVENTIONS.md (vertical alignment dla add_argument)
  </read_first>
  <action>
    Otwórz `sphsim/cli/args.py`. Edytuj precyzyjnie:

    1) **Linia 28 (import):** zamień `from sphsim.strategies import STRATEGIES` na `from sphsim.strategies import STRATEGIES, BUILTIN_STRATEGIES`. STRATEGIES nadal potrzebne (Plan 02 main.py go używa) ale args.py używa już tylko BUILTIN_STRATEGIES (per D-50).

    2) **Linia 41 (--strategy choices):** zamień `choices=list(STRATEGIES.keys())` na `choices=list(BUILTIN_STRATEGIES)`. **WAŻNE:** zamień TYLKO ten fragment, NIE rusz `help='Strategia: ' + ', '.join(STRATEGIES.keys())` w tej samej linii **na początku** (bo BUILTIN_STRATEGIES jest frozenset bez gwarantowanej kolejności iteracji — ale teoretycznie po Phase 3 początek zawiera same nazwy co Phase 1, więc to safe — JEDNAK dla deterministycznego help output zamień całą linię na: `choices=list(BUILTIN_STRATEGIES), help='Strategia: ' + ', '.join(sorted(BUILTIN_STRATEGIES))`). Sortowanie alphabetyczne dla deterministycznego --help (regression check używa argparse'a, ale `--help` jest cosmetic — nie wpływa na test).

    3) **Po linii 42 (po --strategy, wewnątrz mutex):** dodaj trzeci człon mutex (D-44):
       `mutex.add_argument('--custom', type=str, default=None,\n                       help='Ścieżka do pliku .py z custom strategią')`. Zachowaj 4-spacjowy indent + vertical alignment z dwoma poprzednimi członami mutex (CONVENTIONS.md §Formatting).

    4) **Po linii 49 (po `--expected_P`, przed `# Parametry środowiska`):** dodaj `--param` POZA mutex (D-39, Claude's Discretion `action='append'` default `[]`):
       `p.add_argument('--param', action='append', dest='param', default=[], metavar='K=V',\n                   help='[--custom] Parametr custom strategii, np. --param zeta=0.7 (repeatable)')`.
       Pomiń typ converter — `parse_params_from_meta` w Plan 02 main.py robi typed cast z meta.

    NIE rusz linii 1-26 (docstring CLI usage examples — historyczny doc, Phase 5/6 może dodać `--custom` example). NIE rusz innych `add_argument` lini (5 strategy params + 7 env params + `--seed/--json/--verbose`). NIE zmieniaj `return p.parse_args()` na końcu.

    Polski w `help='...'` (PROJECT.md). Identyfikatory (`--custom`, `--param`, `dest='param'`) po angielsku.

    Po edycji: `python -c "from sphsim.cli.args import parse_args; import sys; sys.argv = ['x', '--custom', 'foo.py', '--param', 'zeta=0.7', '--param', 'max_phase=3']; a = parse_args(); assert a.custom == 'foo.py'; assert a.param == ['zeta=0.7', 'max_phase=3']"` musi działać.
  </action>
  <acceptance_criteria>
    - Source: `grep -c "mutex.add_argument('--custom'" sphsim/cli/args.py` returns 1
    - Source: `grep -c "p.add_argument('--param'" sphsim/cli/args.py` returns 1
    - Source: `grep -c "choices=list(BUILTIN_STRATEGIES)" sphsim/cli/args.py` returns 1
    - Source: `grep -c "from sphsim.strategies import STRATEGIES, BUILTIN_STRATEGIES" sphsim/cli/args.py` returns 1
    - Behavior: `python -c "from sphsim.cli.args import parse_args; import sys; sys.argv = ['x', '--custom', 'foo.py']; a = parse_args(); assert a.custom == 'foo.py' and a.param == [] and a.strategy is None and a.interactive is False"` exits 0
    - Behavior: `python -c "from sphsim.cli.args import parse_args; import sys; sys.argv = ['x', '--custom', 'foo.py', '--param', 'zeta=0.7', '--param', 'max_phase=3']; a = parse_args(); assert a.param == ['zeta=0.7', 'max_phase=3']"` exits 0 (append works)
    - Behavior: `python sph_sim.py --custom foo.py --strategy naive 2>&1 | grep -q "not allowed with argument"` (mutex enforcement — argparse rzuca błąd)
    - Behavior: `python sph_sim.py --help 2>&1 | grep -q "Ścieżka do pliku .py z custom strategią"`
    - Behavior: `python sph_sim.py --help 2>&1 | grep -q "Parametr custom strategii"`
    - Regression: `python scripts/regression_check.py` exits 0 (8/8 fixtures pass — istniejące `--strategy X` zostają niezmienione)
    - Invariant: `python -m unittest tests.test_strategy_meta_consistency` exits 0 (Phase 2 invariant — _capture_parser nadal działa, choices=list(BUILTIN_STRATEGIES) ma te same 5 nazw)
    - Plan 01 tests: `python -m unittest tests.test_loader` exits 0 (loader nadal działa po args.py changes)
  </acceptance_criteria>
  <automated>python -c "from sphsim.cli.args import parse_args; import sys; sys.argv = ['x', '--custom', 'foo.py', '--param', 'zeta=0.7', '--param', 'max_phase=3']; a = parse_args(); assert a.custom == 'foo.py' and a.param == ['zeta=0.7', 'max_phase=3'] and a.strategy is None" && python scripts/regression_check.py && python -m unittest tests.test_strategy_meta_consistency tests.test_loader</automated>
  <done>args.py ma 3-człony mutex + --param append; argparse parsuje --custom + --param poprawnie; mutex wymusza wybór; --help wymienia nowe flagi; regression 8/8 + 2 testy suites zielone.</done>
</task>

<task type="auto">
  <id>3-02-02</id>
  <name>Task 3-02-02: Dodaj early-branch `if args.custom:` w sphsim/cli/main.py — load_custom → parse_params → SPHSimulator → format output</name>
  <read_first>
    - sphsim/cli/main.py (current state: 33 linii — args parse, early branch interactive, K1 norm, params dict, SPHSimulator build, output dispatch)
    - sphsim/strategies/loader.py (Plan 01 — interfejs load_custom + parse_params_from_meta + LoaderError)
    - sphsim/cli/output.py (linie 5-13 format_json — używa args.strategy field; linie 16-64 format_human — używa args.strategy + nU + nSUS + T + kappa + alpha)
    - sphsim/core/simulator.py (signature SPHSimulator __init__ — patrz docstring)
    - sphsim/config.py (DEFAULT_K0, DEFAULT_F, DEFAULT_PHI, DEFAULT_RHO — env constants)
    - .planning/phases/03-custom-strategy-loader/03-CONTEXT.md (D-46 — wywołujący robi STRATEGIES[name] = fn; integration point pseudo-code z 03-CONTEXT.md `<code_context>`)
    - .planning/phases/03-custom-strategy-loader/03-RESEARCH.md (Example 4: main.py DELTA — pełen branch z `args.strategy = name` quick fix dla format_human)
    - .planning/phases/03-custom-strategy-loader/03-PATTERNS.md (sphsim/cli/main.py MODIFY section + simulator construction pattern)
  </read_first>
  <action>
    Otwórz `sphsim/cli/main.py`. Zachowaj linie 1-7 BEZ zmian (imports — może być potrzebne dodanie nowych imports na top, ale w branch'u używamy lazy import dla LoaderError aby nie obciążać normalnego flow).

    Wstaw NOWY kod między istniejącą linią 14 (`return` w bloku `if args.interactive:`) a linią 15 (`K1 = float('inf') ...`).

    **Najpierw dodaj graceful warning** (Claude's Discretion z CONTEXT.md): bezpośrednio po `if args.interactive: ... return` (przed `if args.custom:`):
    ```
    if args.param and not args.custom:
        import sys
        print('Flaga --param ignorowana — działa tylko z --custom.', file=sys.stderr)
    ```
    Nie sys.exit — `--param` bez `--custom` to user typo, nie fatal. Built-in flow leci dalej.

    **Potem dodaj główną early-branch `if args.custom:`** (D-44, D-45, D-46):
    1) Lazy import wewnątrz brancha (nie globalnie — built-in flow nie potrzebuje loader): `import sys` + `from sphsim.strategies import STRATEGIES` + `from sphsim.strategies.loader import load_custom, parse_params_from_meta, LoaderError`.
    2) Try-block: `name, strategy_fn, meta = load_custom(args.custom)` (load_custom drukuje banner na stdout PRZED exec, więc user widzi `[OSTRZEŻENIE] Ładuję arbitralny kod...` przed jakąkolwiek symulacją). W except `LoaderError as e`: `print(e.args[0], file=sys.stderr); sys.exit(1)` (polski one-liner, exit code 1, NIE traceback).
    3) Drugi try-block: `params = parse_params_from_meta(args.param, meta, name)`. W except `LoaderError as e`: `print(e.args[0], file=sys.stderr); sys.exit(1)` (np. `Nieznany parametr 'foo' dla strategii 'my_strat'. Dostępne: max_phase.`).
    4) Rejestracja w wywołującym (D-46): `STRATEGIES[name] = strategy_fn`. Robimy to TUTAJ, nie w loaderze. (W CLI one-shot rejestracja nie ma długoterminowego efektu bo proces kończy się po `return`, ale jest spójne semantyczne z REPL'em w Plan 03.)
    5) K1 norm + simulator build verbatim z istniejącego paternu w main.py linie 15-27, ALE: `strategy_fn=strategy_fn` (już mamy z load_custom) zamiast `STRATEGIES[args.strategy]`. Reszta args (nU, nSUS, T, kappa, alpha, seed) bez zmian — Phase 5 doda override env.
    6) **Quick fix dla format_*** (Research Example 4): `format_human` i `format_json` używają `args.strategy` jako field. Custom flow ma `args.strategy = None` (mutex), więc PRZED format ustaw `args.strategy = name` (np. `args.strategy = 'custom_strategy_template'`). Per D-50 świadome ograniczenie — w one-shot CLI custom nie jest "selectable przez --strategy", tylko `--custom`, ale w outputcie pokażemy `Strategia: CUSTOM_STRATEGY_TEMPLATE`.
    7) Output dispatch verbatim z linii 30-33: `if args.json: print(format_json(args, res, params, K1)) else: print(format_human(args, res, K1, args.verbose))`.
    8) `return` na końcu brancha (nie wpadaj w istniejący built-in flow poniżej).

    NIE rusz linii 15-33 istniejącego built-in flow — to jest fallback dla `--strategy <name>` (mutex required=True gwarantuje że jeden z trzech jest wybrany; jeśli nie `--interactive` i nie `--custom`, to musi być `--strategy`).

    Polski w wszystkich `print(...)` user-facing (banner już w loaderze, error message polski via LoaderError). Identyfikatory po angielsku.

    **Krytyczna kolejność:** import sys MUSI być wewnątrz `if args.param and not args.custom:` BLOCK (lub na górze pliku — Claude wybiera, ale lazy import wewnątrz branch jest spójne ze stylem Phase 1/2).
  </action>
  <acceptance_criteria>
    - Source: `grep -c "if args.custom:" sphsim/cli/main.py` returns 1
    - Source: `grep -c "from sphsim.strategies.loader import" sphsim/cli/main.py` returns 1
    - Source: `grep -c "load_custom(args.custom)" sphsim/cli/main.py` returns 1
    - Source: `grep -c "STRATEGIES\[name\] = strategy_fn" sphsim/cli/main.py` returns 1 (D-46 rejestracja w wywołującym)
    - Source: `grep -c "Flaga --param ignorowana" sphsim/cli/main.py` returns 1 (graceful warning)
    - Source: `grep -c "args.strategy = name" sphsim/cli/main.py` returns at least 1 (quick fix dla format_*)
    - Behavior (smoke happy): create a temp strategy file, then `python sph_sim.py --custom /tmp/smoke_custom.py --seed 42 --json` exits 0 and stdout starts with `[OSTRZEŻENIE] Ładuję arbitralny kod Pythona z: /tmp/smoke_custom.py` and contains valid JSON with `"strategy": "smoke_custom"` (test via: `printf 'def strategy_smoke_custom(dev,l,s,phi,kappa,rho,h,p): return %s\\nSTRATEGY_META={%s}\\n' "'COMMIT' if dev.status=='UP' else 'ABSTAIN'" "'description':'s','params':[],'baseline_kpi':None" > /tmp/smoke_custom.py && python sph_sim.py --custom /tmp/smoke_custom.py --seed 42 --json | python -c "import json, sys; d = json.loads(sys.stdin.read().split('\\n', 1)[1] if sys.stdin.read().startswith('[OSTRZE') else sys.stdin.read()); assert d['strategy'] == 'smoke_custom'"`) — alternatywnie split na linii 1 banneru
    - Behavior (loader error → polish stderr + exit 1): `python sph_sim.py --custom /tmp/nope.py --seed 42 2>&1 1>/dev/null | grep -q "Plik nie istnieje"` AND `python sph_sim.py --custom /tmp/nope.py --seed 42; echo $?` outputs 1
    - Behavior (param without custom warns): `python sph_sim.py --strategy naive --param zeta=0.7 --seed 42 --json 2>&1 1>/dev/null | grep -q "Flaga --param ignorowana"` AND exit code 0 (built-in flow continues)
    - Behavior (banner on stdout, NIE stderr): `python sph_sim.py --custom /tmp/smoke_custom.py --seed 42 --json 2>/dev/null | head -1 | grep -q "^\[OSTRZEŻENIE\]"`
    - Behavior (param error → polish stderr + exit 1): create smoke_custom.py with STRATEGY_META params=[('foo', int, 1, 'x')], then `python sph_sim.py --custom /tmp/smoke_custom.py --param bar=2 --seed 42 2>&1 1>/dev/null | grep -q "Nieznany parametr 'bar'"`
    - Regression: `python scripts/regression_check.py` exits 0 (8/8 — istniejące --strategy <name> path nieruszone)
    - Plan 01: `python -m unittest tests.test_loader` exits 0
    - Invariant: `python -m unittest tests.test_strategy_meta_consistency` exits 0
    - Full discover: `python -m unittest discover tests` exits 0
  </acceptance_criteria>
  <automated>printf "def strategy_smoke_custom(dev,l,s,phi,kappa,rho,h,p): return 'COMMIT' if dev.status=='UP' else 'ABSTAIN'\nSTRATEGY_META = {'description':'smoke','params':[],'baseline_kpi':None}\n" > /tmp/smoke_custom.py && python sph_sim.py --custom /tmp/smoke_custom.py --seed 42 --json > /tmp/smoke_out.txt 2> /tmp/smoke_err.txt && head -1 /tmp/smoke_out.txt | grep -q "^\[OSTRZEŻENIE\]" && tail -n +2 /tmp/smoke_out.txt | python -c "import json, sys; d = json.loads(sys.stdin.read()); assert d['strategy'] == 'smoke_custom', d" && python sph_sim.py --custom /tmp/nope_does_not_exist.py --seed 42 2> /tmp/err2.txt; test $? -eq 1 && grep -q "Plik nie istnieje" /tmp/err2.txt && python scripts/regression_check.py && python -m unittest discover tests</automated>
  <done>main.py ma early branch dla args.custom — load + parse + register + simulate + format; banner na stdout; LoaderError → polski stderr + exit 1; --param bez --custom → warning; regression 8/8; pełny discover zielony.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User CLI argv → argparse | argparse parsuje `--custom <path>` jako string; mutex wymusza wybór (interactive | strategy | custom); `--param k=v` accumulated do listy bez validacji w args.py (validacja w loader.parse_params_from_meta). |
| main.py args.custom branch → loader | Path string przekazany do `load_custom(args.custom)`; loader robi banner + walidację + exec (T-3-01 z Plan 01). |
| main.py → STRATEGIES global | `STRATEGIES[name] = strategy_fn` — D-46 rejestracja w wywołującym. W CLI one-shot żyje tylko do końca procesu; brak persystencji. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering / Elevation | `--custom <path>` → load_custom → exec_module | ACCEPTED-WITH-WARNING | Mitigated w Plan 01 (banner pre-import); Plan 02 to po prostu wire — argparse parsuje string, główna mitigation już w loaderze. CLI dziedziczy mitigation. |
| T-3-06 | Tampering | `--param` injection do parametru built-in (gdyby flow się zmienił i `--param` byłby zastosowany do built-in) | MITIGATED (defense in depth) | Plan 02 explicit: gdy `args.param and not args.custom` → graceful warning + ignore (built-in flow nie konsumuje args.param). Gdyby kiedyś built-in flow zaczęło używać `args.param`, parse_params_from_meta loader by walidował klucze przeciw STRATEGY_META built-in i odrzucał nieznane (D-40). |
| T-3-07 | Repudiation | Brak logging gdy user uruchamia custom strategię | ACCEPTED | Banner `[OSTRZEŻENIE]` na stdout jest "logiem" dla user'a; CLI output (human/json) zawiera `"strategy": name` (po quick fix `args.strategy = name`). Brak persistent audit log — projekt akademicki, out of scope. |
| T-3-08 | DoS | Custom strategia może mieć infinite loop / massive resource use w `strategy_<name>` callable | ACCEPTED | User explicit decyduje uruchomić custom przez `--custom`; banner ostrzega. Brak sandboxa (D-36 explicit accept). Phase 7 batch może to wykryć przez timeout per-seed (out of scope dla Phase 3). |
| T-3-SC | Tampering | npm/pip/cargo dependency confusion | N/A | STDLIB-ONLY (Phase 3 nie dodaje pakietów). |

**ASVS level:** 1. **Block on:** żaden.
</threat_model>

<verification>
- [ ] Mutex group ma 3 człony (--interactive | --strategy | --custom) required=True — verified by `python sph_sim.py 2>&1 | grep -q "one of the arguments"`
- [ ] `--strategy` choices to BUILTIN_STRATEGIES (snapshot), NIE STRATEGIES.keys() — verified przez `grep -c "choices=list(BUILTIN_STRATEGIES)" sphsim/cli/args.py` returns 1
- [ ] `--param k=v` repeatable (action='append') — verified by smoke przez 2x --param
- [ ] `--custom <path>` triggers load + simulate + output flow — verified by smoke happy path test
- [ ] Banner pre-import drukowany na stdout — verified przez stdout/stderr separation w smoke test
- [ ] LoaderError → polski stderr + exit 1 — verified przez test z nieistniejącą ścieżką
- [ ] `--param` bez `--custom` → graceful warning + continue built-in flow — verified przez `--strategy naive --param zeta=0.7`
- [ ] Regression 8/8 backwards compat — verified przez regression_check.py
- [ ] Phase 2 invariant test nadal pass — verified przez test_strategy_meta_consistency
- [ ] Plan 01 loader tests nadal pass — verified przez test_loader
</verification>

<success_criteria>
- `python sph_sim.py --custom <valid_path>.py --seed 42 --json` exits 0, drukuje banner na stdout, drukuje deterministyczny JSON z `"strategy": "<basename>"`
- `python sph_sim.py --custom /tmp/nope.py 2>&1 >/dev/null | grep "Plik nie istnieje"` matches AND exit code is 1
- `python sph_sim.py --strategy naive --param zeta=0.7 --seed 42 --json` exits 0, stderr zawiera `Flaga --param ignorowana`, stdout to valid JSON (built-in flow działa)
- `python sph_sim.py --custom foo.py --strategy naive` argparse error z "not allowed with argument" (mutex enforcement)
- `python scripts/regression_check.py` exits 0 (CLI-04 backwards compat)
- `python -m unittest discover tests` exits 0 (all test suites green)
</success_criteria>

<output>
Create `.planning/phases/03-custom-strategy-loader/03-02-SUMMARY.md` when done.
</output>
