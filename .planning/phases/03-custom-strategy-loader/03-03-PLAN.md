---
plan_id: 03-03
phase: 3
wave: 2
depends_on: [03-01]
requirements: [STRAT-03, STRAT-04]
files_modified:
  - sphsim/cli/repl.py
autonomous: true
must_haves:
  truths:
    - "REPL komenda `custom <ścieżka> [k=v ...]` ładuje custom strategię, rejestruje w STRATEGIES, drukuje `Załadowano custom strategię '<name>'.` (lub `Przeładowano...` przy drugim wywołaniu — D-38)"
    - "D-37 sticky: raz załadowana custom strategia zostaje w STRATEGIES do `exit`; widoczna w `strategies`, wielokrotnie wybieralna przez `run <nazwa>`; brak persystencji do plików — po wyjściu z REPL'a znika (naturalne dla iteracji edit-run-edit)"
    - "D-43 pozycyjne parsing w REPL: `do_custom(arg)` i `do_run(arg)` dzielą raw string przez `arg.split()`; pierwszy token = ścieżka/nazwa, reszta = tokens `k=v`; ścieżki ze spacjami NIE są wspierane; token bez `=` → ostrzeżenie 'Pominięto token... — oczekiwany format key=value.' (graceful, kontynuacja)"
    - "REPL komenda `run <nazwa> [k=v ...]` buduje SPHSimulator z DEFAULT env params, uruchamia symulację, drukuje wynik przez format_human (D-41)"
    - "`run` działa zarówno dla built-in (`run naive zeta=0.7`) jak i dla custom (`run my_strat max_phase=3`) z jednolitym shape (D-41)"
    - "`run` bez argumentu drukuje `Użycie: run <nazwa> [param=wartość ...]. Wpisz 'strategies' żeby zobaczyć dostępne.` (D-42)"
    - "`run nieznana` drukuje `Strategia 'nieznana' nie istnieje. Dostępne: <live STRATEGIES.keys()>.` (D-42; po `custom` custom widoczne w liście)"
    - "`strategies` listuje built-in jak teraz, plus custom z suffixem ` [custom]` (D-50); dispatch namespace `sphsim.strategies.<name>` vs `sphsim.custom.<name>` per BUILTIN_STRATEGIES membership"
    - "`strategy <name>` działa dla built-in (jak Phase 2) i dla custom — dispatch namespace per D-50"
    - "`help` listuje 6 komend (help, exit, strategies, strategy, custom, run) z polskim opisem"
    - "LoaderError w `do_custom`/`do_run` drukuje polski one-liner na stdout (NIE stderr, NIE crash REPL'a), kontrola wraca do prompt'u (D-48)"
  artifacts:
    - path: "sphsim/cli/repl.py"
      provides: "SPHShell rozszerzony o do_custom + do_run; do_strategies + do_strategy + do_help zmodyfikowane do obsługi custom namespace"
      contains: "def do_custom"
  key_links:
    - from: "sphsim/cli/repl.py do_custom"
      to: "sphsim.strategies.loader.load_custom + parse_params_from_meta"
      via: "from sphsim.strategies.loader import (na górze modułu)"
      pattern: "from sphsim.strategies.loader import"
    - from: "sphsim/cli/repl.py do_run"
      to: "sphsim.core.simulator.SPHSimulator + sphsim.cli.output.format_human + sphsim.config.DEFAULT_*"
      via: "imports na górze modułu (lub wewnątrz do_run; Claude wybiera ale top-level preferred dla czytelności)"
      pattern: "SPHSimulator|format_human|DEFAULT_"
    - from: "sphsim/cli/repl.py do_strategies + do_strategy"
      to: "sphsim.strategies.BUILTIN_STRATEGIES"
      via: "import BUILTIN_STRATEGIES + dispatch ns = 'sphsim.strategies' if name in BUILTIN_STRATEGIES else 'sphsim.custom'"
      pattern: "BUILTIN_STRATEGIES"
---

<objective>
Rozszerz `SPHShell(cmd.Cmd)` o dwie nowe metody (`do_custom`, `do_run`) i zmodyfikuj 3 istniejące (`do_help`, `do_strategies`, `do_strategy`) zgodnie z D-41 (Phase 2 deferred — `run` w Phase 3), D-50 (listing z `[custom]` suffix), i dispatch namespace `sphsim.strategies.<name>` vs `sphsim.custom.<name>`. Plan jest izolowany od CLI one-shot (Plan 02 robi to równolegle, też depends_on [03-01]) — modyfikujemy tylko jeden plik.

Purpose: D-41 wymaga komendy `run <nazwa> [k=v ...]` w REPL, bo bez tego custom strategia załadowana przez `custom <path>` byłaby "zawieszona" — widoczna w `strategies`, ale niemożliwa do uruchomienia z REPL'a. D-50 wymaga że `do_strategies` listuje custom z suffixem `[custom]` (SC #4 ROADMAP), inaczej user nie odróżni built-in od własnych. Dispatch namespace jest krytyczny: `do_strategy <name>` w Phase 2 robił `importlib.import_module(f'sphsim.strategies.{name}')` — to nie zadziała dla custom, które żyją w `sphsim.custom.<name>` (D-46 private namespace z Plan 01).

Output: 1 zmodyfikowany plik (`sphsim/cli/repl.py`). Po tej fali REPL ma 6 komend, custom strategie sticky w sesji (D-37), `run` działa jednolicie dla built-in i custom. Regression 8/8 musi pozostać zielone (REPL nie jest częścią regression suite — zmiana w `repl.py` nie wpływa na CLI one-shot path z `--strategy`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-custom-strategy-loader/03-CONTEXT.md
@.planning/phases/03-custom-strategy-loader/03-RESEARCH.md
@.planning/phases/03-custom-strategy-loader/03-PATTERNS.md
@.planning/phases/03-custom-strategy-loader/03-01-PLAN.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Z Plan 01 (zakończonego): -->

```python
# sphsim/strategies/__init__.py
STRATEGIES: dict[str, callable]      # mutable runtime; Plan 03 dodaje custom przez STRATEGIES[name] = fn
BUILTIN_STRATEGIES: frozenset[str]   # snapshot Phase 1 — dispatch namespace

# sphsim/strategies/loader.py
load_custom(path: str) -> tuple[str, callable, dict]      # banner stdout PRZED exec; LoaderError przy 4 warstwach
parse_params_from_meta(tokens, meta, name) -> dict        # typed conversion; LoaderError na unknown key/bad type
class LoaderError(Exception): pass
```

```python
# sphsim/core/simulator.py — istniejący Phase 1
SPHSimulator(nU, nSUS, K0, K1, F, T, kappa, alpha, phi, rho, strategy_fn, params, seed).run() -> dict

# sphsim/cli/output.py — istniejący Phase 2
format_human(args, res, K1, verbose) -> str
    # args wymaga attrs: strategy, nU, nSUS, T, kappa, alpha
    # K1 = float lub float('inf')
    # verbose = bool

# sphsim/config.py — istniejące DEFAULT_*
DEFAULT_NU, DEFAULT_NSUS, DEFAULT_K0, DEFAULT_K1, DEFAULT_F,
DEFAULT_T, DEFAULT_KAPPA, DEFAULT_ALPHA, DEFAULT_PHI, DEFAULT_RHO
```

```python
# sphsim/cli/repl.py — istniejący Phase 2 (do NIE zmieniać semantyki):
class SPHShell(cmd.Cmd):
    intro = INTRO
    prompt = 'sph> '
    def do_help(self, arg)        # OBECNIE: 4 komendy — Plan 03 doda 2 (custom, run)
    def do_exit(self, arg)        # OBECNIE: bez zmian
    def do_EOF(self, arg)         # OBECNIE: bez zmian
    def do_strategies(self, arg)  # OBECNIE: iteruje STRATEGIES, importuje sphsim.strategies.<name>
                                  #          — Plan 03 doda dispatch + [custom] suffix (D-50)
    def do_strategy(self, arg)    # OBECNIE: importuje sphsim.strategies.<name>
                                  #          — Plan 03 doda dispatch
    def default(self, line)       # OBECNIE: bez zmian
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <id>3-03-01</id>
  <name>Task 3-03-01: Dodaj importy + do_custom + do_run do SPHShell w sphsim/cli/repl.py</name>
  <read_first>
    - sphsim/cli/repl.py (current state: 150 linii — SPHShell z 4 do_* metodami)
    - sphsim/strategies/loader.py (Plan 01 — interfejs do importu)
    - sphsim/config.py (linie 4-13 — wszystkie DEFAULT_* constants)
    - sphsim/cli/output.py (linie 16-64 — format_human signature i Namespace pola)
    - .planning/phases/03-custom-strategy-loader/03-CONTEXT.md (D-38, D-41, D-42, D-43, D-46)
    - .planning/phases/03-custom-strategy-loader/03-RESEARCH.md (Example 5: pełne repl.py DELTA z do_custom + do_run; D-42 error messages verbatim; argparse.Namespace dla format_human)
    - .planning/phases/03-custom-strategy-loader/03-PATTERNS.md (sphsim/cli/repl.py MODIFY section — wzorzec do_strategy dla error handling shape do skopiowania)
  </read_first>
  <action>
    Otwórz `sphsim/cli/repl.py`. NIE rusz linii 1-22 (module docstring + imports + STRATEGIES import) **OPRÓCZ**: zaktualizuj docstring linie 1-14 żeby wymienić **6 komend** (dodaj `custom <ścieżka> [k=v ...]` i `run <nazwa> [k=v ...]`). Stdlib only linia (13) zmień na "Stdlib only: cmd + readline + importlib + os + sys + argparse + atexit (D-18, D-19); plugin loader (D-46)".

    **Dodaj nowe importy na top modułu** (po istniejących linii 15-21, BEFORE `from sphsim.strategies import STRATEGIES`):
    ```
    import argparse
    import sys
    ```
    A **PO** linii 21 (po `from sphsim.strategies import STRATEGIES`):
    ```
    from sphsim.strategies import BUILTIN_STRATEGIES
    from sphsim.strategies.loader import load_custom, parse_params_from_meta, LoaderError
    from sphsim.core.simulator import SPHSimulator
    from sphsim.cli.output import format_human
    from sphsim.config import (
        DEFAULT_NU, DEFAULT_NSUS, DEFAULT_K0, DEFAULT_K1, DEFAULT_F,
        DEFAULT_T, DEFAULT_KAPPA, DEFAULT_ALPHA, DEFAULT_PHI, DEFAULT_RHO,
    )
    ```
    Top-level imports preferowane nad lazy: Phase 2 też robi `from sphsim.strategies import STRATEGIES` na top, spójność. REPL już ma overhead startupu (readline, history), kilka extra importów to nic.

    **Dodaj `do_custom(self, arg)` jako nową metodę w klasie SPHShell** (umieść po `do_strategy` linii 78-109, przed `default` linią 112):
    1) Docstring polski: `"""Załaduj custom strategię z pliku .py. Składnia: custom <ścieżka> [param=wartość ...]"""`
    2) `parts = arg.split()` — D-43 pozycyjne parsing; pierwszy token = ścieżka, reszta `k=v`. Ścieżki ze spacjami NIE wspierane (D-43 explicit).
    3) `if not parts:` → `print("Użycie: custom <ścieżka> [param=wartość ...].")` + `return` (analog D-32 z `do_strategy`).
    4) `path, *param_tokens = parts`.
    5) **Reload detection (D-38):** PRZED `load_custom` (bo loader zarejestruje fresh module nadpisując `sys.modules`): `basename_check = os.path.splitext(os.path.basename(os.path.abspath(os.path.expanduser(path))))[0]`. `was_loaded = f'sphsim.custom.{basename_check}' in sys.modules`. Ten check musi się stać PRZED loaderem, bo loader robi `sys.modules[full_name] = mod` overwriting.
    6) Try block: `name, fn, meta = load_custom(path)`. W except `LoaderError as e`: `print(e.args[0]); return` (D-48 polski one-liner na **stdout** — REPL output, UAT widzi w transcript; NIE stderr, NIE crash REPL'a).
    7) Drugi try block: `params = parse_params_from_meta(param_tokens, meta, name)`. W except `LoaderError as e`: `print(e.args[0]); return`.
    8) Rejestracja w wywołującym (D-46): `STRATEGIES[name] = fn`.
    9) Komunikat sukcesu z reload-aware verb: `verb = "Przeładowano" if was_loaded else "Załadowano"`; `print(f"{verb} custom strategię '{name}'.")` (D-38 verbatim).

    **Dodaj `do_run(self, arg)` jako nową metodę** (po `do_custom`, przed `default`):
    1) Docstring polski: `"""Uruchom symulację: run <nazwa> [param=wartość ...]"""`
    2) `tokens = arg.split()`.
    3) `if not tokens:` → `print("Użycie: run <nazwa> [param=wartość ...]. Wpisz 'strategies' żeby zobaczyć dostępne.")` + `return` (D-42 verbatim).
    4) `name, *kv_tokens = tokens`.
    5) `if name not in STRATEGIES:` → `available = ', '.join(STRATEGIES.keys())`; `print(f"Strategia '{name}' nie istnieje. Dostępne: {available}.")` + `return` (D-42 verbatim; live STRATEGIES.keys() — po `custom` custom widoczne).
    6) **Dispatch namespace dla meta lookup (D-50):** `ns = 'sphsim.strategies' if name in BUILTIN_STRATEGIES else 'sphsim.custom'`. `mod = importlib.import_module(f'{ns}.{name}')`. `meta = mod.STRATEGY_META`.
    7) Try block: `params = parse_params_from_meta(kv_tokens, meta, name)`. W except `LoaderError as e`: `print(e.args[0]); return`.
    8) Build SPHSimulator z DEFAULT_* (Phase 5 doda override env): `sim = SPHSimulator(nU=DEFAULT_NU, nSUS=DEFAULT_NSUS, K0=DEFAULT_K0, K1=DEFAULT_K1, F=DEFAULT_F, T=DEFAULT_T, kappa=DEFAULT_KAPPA, alpha=DEFAULT_ALPHA, phi=DEFAULT_PHI, rho=DEFAULT_RHO, strategy_fn=STRATEGIES[name], params=params, seed=42)`. **Seed 42 hardcoded** — to świadome (D-41 mówi env z config.py defaults; Phase 5 doda flagę `--seed` w REPL, Phase 3 nie).
    9) `res = sim.run()`.
    10) **Fabricate args-like Namespace dla format_human** (D-41 — format_human wymaga args.strategy/nU/nSUS/T/kappa/alpha/verbose):
        `fake_args = argparse.Namespace(strategy=name, nU=DEFAULT_NU, nSUS=DEFAULT_NSUS, T=DEFAULT_T, kappa=DEFAULT_KAPPA, alpha=DEFAULT_ALPHA, verbose=False)`.
    11) `print(format_human(fake_args, res, DEFAULT_K1, False))` (D-41 — brak `--json` w REPL; verbose=False).

    Polski we wszystkich user-facing print'ach. Identyfikatory (do_custom, do_run, fake_args) po angielsku.

    NIE rusz `do_help`, `do_strategies`, `do_strategy`, `do_exit`, `do_EOF`, `default` w tym tasku — zrobimy w 3-03-02. NIE rusz `run_repl()` ani `_write_history_silent()`.
  </action>
  <acceptance_criteria>
    - Source: `grep -c "def do_custom" sphsim/cli/repl.py` returns 1
    - Source: `grep -c "def do_run" sphsim/cli/repl.py` returns 1
    - Source: `grep -c "from sphsim.strategies.loader import" sphsim/cli/repl.py` returns 1
    - Source: `grep -c "from sphsim.cli.output import format_human" sphsim/cli/repl.py` returns 1
    - Source: `grep -c "from sphsim.config import" sphsim/cli/repl.py` returns 1
    - Source: `grep -c "Załadowano custom" sphsim/cli/repl.py` returns 1 (D-38 verbatim)
    - Source: `grep -c "Przeładowano custom" sphsim/cli/repl.py` returns 1 (D-38 verbatim)
    - Source: `grep -c "Użycie: run <nazwa>" sphsim/cli/repl.py` returns 1 (D-42 verbatim)
    - Source: `grep -c "Użycie: custom <ścieżka>" sphsim/cli/repl.py` returns 1
    - Source: `grep -c "argparse.Namespace" sphsim/cli/repl.py` returns 1 (fake_args dla format_human)
    - Source: `grep -c "STRATEGIES\[name\] = fn" sphsim/cli/repl.py` returns 1 (D-46 rejestracja w do_custom)
    - Behavior (do_custom happy): create temp strategy, then `printf "custom /tmp/repl_smoke.py\nexit\n" | python sph_sim.py --interactive 2>&1 | grep -q "Załadowano custom strategię 'repl_smoke'"`
    - Behavior (do_custom reload): create temp strategy, then `printf "custom /tmp/repl_smoke.py\ncustom /tmp/repl_smoke.py\nexit\n" | python sph_sim.py --interactive 2>&1 | grep -q "Przeładowano custom strategię"`
    - Behavior (do_run built-in): `printf "run naive zeta=0.7\nexit\n" | python sph_sim.py --interactive 2>&1 | grep -q "SPH SYMULATOR"`
    - Behavior (do_run custom): `printf "custom /tmp/repl_smoke.py\nrun repl_smoke\nexit\n" | python sph_sim.py --interactive 2>&1 | grep -q "Strategia: REPL_SMOKE"`
    - Behavior (do_run no args): `printf "run\nexit\n" | python sph_sim.py --interactive 2>&1 | grep -q "Użycie: run <nazwa>"`
    - Behavior (do_run unknown): `printf "run nieznana\nexit\n" | python sph_sim.py --interactive 2>&1 | grep -q "Strategia 'nieznana' nie istnieje"`
    - Behavior (do_custom unknown path): `printf "custom /tmp/nope_does_not_exist.py\nexit\n" | python sph_sim.py --interactive 2>&1 | grep -q "Plik nie istnieje"` AND REPL nie crashuje (kontynuuje do exit)
    - Regression: `python scripts/regression_check.py` exits 0
    - Tests: `python -m unittest discover tests` exits 0
  </acceptance_criteria>
  <automated>printf "def strategy_repl_smoke(dev,l,s,phi,kappa,rho,h,p): return 'COMMIT' if dev.status=='UP' else 'ABSTAIN'\nSTRATEGY_META = {'description':'r','params':[],'baseline_kpi':None}\n" > /tmp/repl_smoke.py && printf "custom /tmp/repl_smoke.py\ncustom /tmp/repl_smoke.py\nrun repl_smoke\nrun naive zeta=0.7\nrun\nrun nieznana\nexit\n" | python sph_sim.py --interactive > /tmp/repl_out.txt 2>&1 && grep -q "Załadowano custom strategię 'repl_smoke'" /tmp/repl_out.txt && grep -q "Przeładowano custom strategię 'repl_smoke'" /tmp/repl_out.txt && grep -q "Strategia: REPL_SMOKE" /tmp/repl_out.txt && grep -q "Strategia: NAIVE" /tmp/repl_out.txt && grep -q "Użycie: run <nazwa>" /tmp/repl_out.txt && grep -q "Strategia 'nieznana' nie istnieje" /tmp/repl_out.txt && python scripts/regression_check.py && python -m unittest discover tests</automated>
  <done>SPHShell ma do_custom + do_run; load+reload+register; run działa dla built-in i custom z DEFAULT env; error paths drukują polski na stdout bez crash; regression 8/8; tests green.</done>
</task>

<task type="auto">
  <id>3-03-02</id>
  <name>Task 3-03-02: Zmodyfikuj do_help (6 komend), do_strategies ([custom] suffix + dispatch ns), do_strategy (dispatch ns)</name>
  <read_first>
    - sphsim/cli/repl.py (state po 3-03-01 — ma już do_custom/do_run + nowe importy)
    - .planning/phases/03-custom-strategy-loader/03-CONTEXT.md (D-50)
    - .planning/phases/03-custom-strategy-loader/03-RESEARCH.md (Example 5: do_help + do_strategies + do_strategy DELTA z dispatch namespace)
    - .planning/phases/03-custom-strategy-loader/03-PATTERNS.md (sphsim/cli/repl.py MODIFY section — Phase 3 do_strategies + do_help patterns)
    - sphsim/strategies/threshold.py + naive.py (kontrakt STRATEGY_META do referencji format_human / strategy output)
  </read_first>
  <action>
    **Modyfikacja 1 — `do_help` (linie 46-52 obecnie 4 komendy → 6 komend, padding 27 znaków):**
    Zastąp całą metodę:
    ```
    def do_help(self, arg):
        """Wyświetl listę dostępnych komend."""
        print("Dostępne komendy:")
        print("  help                            — Wyświetl tę listę komend.")
        print("  exit                            — Zakończ sesję (alternatywnie Ctrl+D).")
        print("  strategies                      — Wyświetl listę wbudowanych i custom strategii.")
        print("  strategy <nazwa>                — Wyświetl szczegóły strategii (parametry, baseline KPI).")
        print("  custom <ścieżka> [k=v ...]      — Załaduj custom strategię z pliku .py.")
        print("  run <nazwa> [k=v ...]           — Uruchom symulację (built-in lub custom).")
    ```
    Padding zwiększony z 17 (Phase 2) do 32 znaków, bo `custom <ścieżka> [k=v ...]` ma 26 znaków; padding 32 trzyma em-dash w jednej kolumnie (CONVENTIONS.md §Formatting vertical alignment).

    **Modyfikacja 2 — `do_strategies` (linie 68-75 — dodaj dispatch namespace + suffix `[custom]`):**
    Zastąp całą metodę:
    ```
    def do_strategies(self, arg):
        """Wyświetl listę wbudowanych i custom strategii."""
        print("Dostępne strategie:")
        for name in STRATEGIES.keys():
            if name in BUILTIN_STRATEGIES:
                mod = importlib.import_module(f'sphsim.strategies.{name}')
                description = mod.STRATEGY_META['description']
                print(f"  {name:<12}— {description}")
            else:
                mod = importlib.import_module(f'sphsim.custom.{name}')
                description = mod.STRATEGY_META['description']
                print(f"  {name:<12}— {description} [custom]")
    ```
    Dispatch namespace per D-46: built-in żyją w `sphsim.strategies.<name>`, custom w `sphsim.custom.<name>` (private namespace z loadera). Padding 12 znaków zachowany z Phase 2 D-29. Suffix ` [custom]` PO description (z wiodącą spacją).

    **Modyfikacja 3 — `do_strategy` (linie 78-109 — dodaj dispatch namespace):**
    Linie 78-91 (signature + early returns dla brak/nieznana) ZOSTAJĄ bez zmian. Zmień TYLKO linię 94: `mod = importlib.import_module(f'sphsim.strategies.{name}')` na:
    ```
    ns = 'sphsim.strategies' if name in BUILTIN_STRATEGIES else 'sphsim.custom'
    mod = importlib.import_module(f'{ns}.{name}')
    ```
    Reszta metody (description, params, baseline_kpi sections — linie 95-109) ZOSTAJE bez zmian.

    NIE rusz `do_exit`, `do_EOF`, `default`, `_write_history_silent`, `run_repl`, ani modułowych stałych (`HISTORY_FILE`, `INTRO`).

    Polski we wszystkich user-facing print'ach. Identyfikatory (`ns`, `mod`) po angielsku.
  </action>
  <acceptance_criteria>
    - Source: `grep -c "custom <ścieżka> \[k=v ...\]" sphsim/cli/repl.py` returns 1 (do_help line)
    - Source: `grep -c "run <nazwa> \[k=v ...\]" sphsim/cli/repl.py` returns 1 (do_help line)
    - Source: `grep -c "if name in BUILTIN_STRATEGIES:" sphsim/cli/repl.py` returns at least 1 (do_strategies dispatch)
    - Source: `grep -c "sphsim.custom.{name}" sphsim/cli/repl.py` returns at least 1 (do_strategies custom branch)
    - Source: `grep -c "\\[custom\\]" sphsim/cli/repl.py` returns at least 1 (suffix verbatim)
    - Source: `grep -c "ns = 'sphsim.strategies' if name in BUILTIN_STRATEGIES else 'sphsim.custom'" sphsim/cli/repl.py` returns at least 1 (do_strategy dispatch)
    - Behavior (help shows 6 commands): `printf "help\nexit\n" | python sph_sim.py --interactive 2>&1 | grep -c "  " | awk '$1 >= 6 {print "OK"}'` outputs "OK" (6 indented lines w help output)
    - Behavior (help mentions custom + run): `printf "help\nexit\n" | python sph_sim.py --interactive 2>&1 | grep -q "custom <ścieżka>"` AND `printf "help\nexit\n" | python sph_sim.py --interactive 2>&1 | grep -q "run <nazwa>"`
    - Behavior (strategies built-in only, before custom load): `printf "strategies\nexit\n" | python sph_sim.py --interactive 2>&1 | grep -v "\\[custom\\]" | grep -c "  naive\|  threshold\|  phase_prob\|  incentive\|  adaptive"` returns 5
    - Behavior (strategies shows [custom] after custom load): `printf "custom /tmp/repl_smoke.py\nstrategies\nexit\n" | python sph_sim.py --interactive 2>&1 | grep -q "repl_smoke.*\\[custom\\]"`
    - Behavior (strategy <built-in> still works): `printf "strategy naive\nexit\n" | python sph_sim.py --interactive 2>&1 | grep -q "Opis:.*zeta"`
    - Behavior (strategy <custom> works after load): `printf "custom /tmp/repl_smoke.py\nstrategy repl_smoke\nexit\n" | python sph_sim.py --interactive 2>&1 | grep -q "Opis:"`
    - Regression: `python scripts/regression_check.py` exits 0
    - Tests: `python -m unittest discover tests` exits 0
  </acceptance_criteria>
  <automated>printf "def strategy_repl_smoke(dev,l,s,phi,kappa,rho,h,p): return 'COMMIT' if dev.status=='UP' else 'ABSTAIN'\nSTRATEGY_META = {'description':'r','params':[],'baseline_kpi':None}\n" > /tmp/repl_smoke.py && printf "help\nstrategies\ncustom /tmp/repl_smoke.py\nstrategies\nstrategy repl_smoke\nstrategy naive\nexit\n" | python sph_sim.py --interactive > /tmp/repl_mod.txt 2>&1 && grep -q "custom <ścieżka>" /tmp/repl_mod.txt && grep -q "run <nazwa>" /tmp/repl_mod.txt && grep -q "repl_smoke.*\[custom\]" /tmp/repl_mod.txt && grep -q "Opis:.*zeta" /tmp/repl_mod.txt && python scripts/regression_check.py && python -m unittest discover tests</automated>
  <done>do_help listuje 6 komend; do_strategies dispatch ns + [custom] suffix; do_strategy dispatch ns; built-in + custom oba działają w strategy/strategies; regression 8/8; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REPL user input → cmd.Cmd onecmd → do_custom/do_run | Surowy string przekazany przez `cmd.Cmd` parser; `arg.split()` rozdziela na tokens. |
| do_custom → loader | Path string przez `load_custom(path)` — banner + walidacja w loaderze (T-3-01 w Plan 01). |
| do_run → SPHSimulator | Strategy function callable + params dict; SPHSimulator wykonuje strategy function `T*nU` razy. |
| do_custom → STRATEGIES (sticky w sesji per D-37) | Registration `STRATEGIES[name] = fn` żyje do `exit`; brak persystencji do plików. Po REPL exit — znika. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering / Elevation | do_custom → load_custom → exec_module | ACCEPTED-WITH-WARNING | Dziedziczone z Plan 01 (banner pre-import); REPL pokazuje banner w transcript widzialnym dla user'a. |
| T-3-02 | Tampering | sys.modules pollution / namespace bleed | MITIGATED | Dispatch namespace `sphsim.strategies` vs `sphsim.custom` w `do_strategies`/`do_strategy`/`do_run`; collision check w loaderze (Plan 01 D-49) zanim cokolwiek się stanie. |
| T-3-09 | Spoofing | User łata STRATEGIES dict bezpośrednio (np. `python -c "from sphsim.strategies import STRATEGIES; STRATEGIES['fake'] = lambda *a: 'COMMIT'"`) | ACCEPTED | Projekt akademicki — user ma pełną kontrolę nad swoim procesem Pythona. REPL to user-facing interface, nie security boundary. |
| T-3-10 | Information Disclosure | Custom strategia może czytać sąsiednie pliki / env vars w trakcie wykonania | ACCEPTED | Banner D-45 ostrzega; sandbox out of scope (D-36); konsystencja z T-3-01. Phase 7 batch może to wykryć przez file watcher (out of scope dla Phase 3). |
| T-3-11 | DoS | `run` z DEFAULT_T=1000 cykli * nU=250 = 250k iteracji strategy_fn; custom strategy może mieć infinite loop wewnątrz | ACCEPTED | Brak timeout per-cykl (out of scope). User może Ctrl+C — `cmd.Cmd` cmdloop łapie KeyboardInterrupt jako sygnał do prompt'u. |
| T-3-SC | Tampering | npm/pip/cargo confusion | N/A | STDLIB-ONLY. |

**ASVS level:** 1. **Block on:** żaden.
</threat_model>

<verification>
- [ ] REPL ma 6 komend (help, exit, strategies, strategy, custom, run) — verified by `printf "help\nexit\n" | python sph_sim.py --interactive | grep -c "^  "` returns at least 6
- [ ] `custom <path>` ładuje + rejestruje + komunikuje "Załadowano custom strategię 'X'." — verified by smoke
- [ ] Drugie `custom <same_path>` komunikuje "Przeładowano custom strategię 'X'." (D-38) — verified by smoke
- [ ] `run <built-in>` działa — verified by `run naive zeta=0.7`
- [ ] `run <custom>` działa po `custom <path>` — verified by smoke (sticky D-37)
- [ ] `strategies` po `custom` listuje custom z `[custom]` suffix — verified by smoke (D-50, SC #4)
- [ ] `strategy <custom>` działa po `custom` — verified by smoke (dispatch namespace)
- [ ] LoaderError nie crashuje REPL'a — verified przez `custom /nonexistent.py` + kontynuacja do `exit`
- [ ] Banner `[OSTRZEŻENIE]` widoczny w REPL transcript przed "Załadowano..." — verified przez stdout capture
- [ ] Regression 8/8 + invariant + loader tests nadal pass — verified przez full discover
</verification>

<success_criteria>
- `printf "help\nexit\n" | python sph_sim.py --interactive` listuje 6 komend z polskimi opisami
- `printf "custom <valid_path>.py\nstrategies\nrun <basename>\nexit\n" | python sph_sim.py --interactive` ładuje, listuje z `[custom]` suffix, uruchamia symulację (SC #1, #3, #4)
- `printf "custom <valid>.py\ncustom <valid>.py\nexit\n" | python sph_sim.py --interactive` drugi raz drukuje "Przeładowano" (D-38 reload semantic)
- `printf "custom /nonexistent.py\nexit\n" | python sph_sim.py --interactive` drukuje "Plik nie istnieje" + REPL nie crashuje (D-48 fail-safe)
- `printf "run\nrun nieznana\nexit\n" | python sph_sim.py --interactive` drukuje D-42 verbatim error messages
- `python scripts/regression_check.py` exits 0 (REPL changes nie psują CLI one-shot)
- `python -m unittest discover tests` exits 0
</success_criteria>

<output>
Create `.planning/phases/03-custom-strategy-loader/03-03-SUMMARY.md` when done.
</output>
