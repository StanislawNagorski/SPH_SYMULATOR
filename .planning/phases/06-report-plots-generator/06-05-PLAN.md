---
phase: 06-report-plots-generator
plan: 05
type: execute
wave: 4
depends_on: ["06-04"]
files_modified:
  - scripts/regression_check.py
  - scripts/verify_phase6.sh
autonomous: true
requirements: [REPORT-01, REPORT-02, REPORT-03, PLOT-01, PLOT-02, PLOT-03]
must_haves:
  truths:
    - "scripts/regression_check.py PASS 8/8 with env passthrough (SPHSIM_NO_REPORT=1 forwarded to subprocess.run) — no report-pollution during regression run, baseline contract preserved"
    - "scripts/regression_check.py SKIP_KEYS final form has Phase 4 + Phase 5 + Phase 6 entries consolidated; comment block deduplicates the Plan-01 intermediate addition"
    - "scripts/verify_phase6.sh runs end-to-end and exits 0 with PASS≥20 / FAIL=0, covering all 6 ROADMAP Phase 6 SCs + regression + tests + opt-out"
    - "verify_phase6.sh starts with rm -rf ./reports/ pre-flight cleanup so SC#1/3/4/5 checks have deterministic artifact paths"
    - "Exit gate is the single canonical pre-flight before /gsd:verify-work — green = Phase 6 ships"
  artifacts:
    - path: "scripts/regression_check.py"
      provides: "SKIP_KEYS consolidated for Phase 4 + Phase 5 + Phase 6 entries; subprocess env passthrough SPHSIM_NO_REPORT=1"
      contains: "SPHSIM_NO_REPORT"
    - path: "scripts/verify_phase6.sh"
      provides: "All 6 ROADMAP Phase 6 SCs covered as check() invocations + regression + full test suite + opt-out test"
      contains: "check \"SC #"
      min_lines: 150
  key_links:
    - from: "scripts/regression_check.py:run_invocation (or subprocess.run call)"
      to: "env={**os.environ, 'SPHSIM_NO_REPORT': '1'}"
      via: "subprocess.run env keyword argument"
      pattern: "SPHSIM_NO_REPORT.*1"
    - from: "scripts/verify_phase6.sh"
      to: "ROADMAP Phase 6 Success Criteria 1-6"
      via: "explicit check() per SC with grep/python-c assertion bodies"
      pattern: "SC #(1|2|3|4|5|6)"
---

<objective>
Close Phase 6 by (a) extending `scripts/regression_check.py` to pass `SPHSIM_NO_REPORT=1` into every subprocess invocation (so the 8 baseline regression runs do not create 24 unwanted PNG/MD files), consolidating the SKIP_KEYS comment block that Plan 01 introduced as an intermediate, and (b) fleshing out `scripts/verify_phase6.sh` with all check() invocations for the 6 ROADMAP Success Criteria + regression + test suite + opt-out + REPL Pitfall regressions. After this plan the Phase 6 exit gate is the single decision: green means Phase 6 ships.

Purpose: Final wave before `/gsd:verify-work`. Two scripts → one decision: PASS≥20 / FAIL=0 means Phase 6 is shippable. Mirrors Phase 5 Plan 04 structure exactly.

Output: 2 edits in regression_check.py (consolidate SKIP_KEYS comment + add `env=` to subprocess.run); ~20 check() invocations added to verify_phase6.sh covering SC#1-6 + regression + tests + opt-out + REPL Pitfall 2 + REPL Pitfall 6 (defused via fake_args + Plan 04 wiring).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-report-plots-generator/06-RESEARCH.md
@.planning/phases/06-report-plots-generator/06-PATTERNS.md
@.planning/phases/06-report-plots-generator/06-VALIDATION.md
@.planning/phases/06-report-plots-generator/06-04-SUMMARY.md
@scripts/regression_check.py
@scripts/verify_phase6.sh
@scripts/verify_phase5.sh

<interfaces>
regression_check.py state after Plan 01 (Wave 1) — intermediate SKIP_KEYS:
```python
SKIP_KEYS = (
    'veto_per_phase', 'n_vetoed_total', 'agent_enabled',  # Phase 4 D-67 Strategia B
    'K0', 'phi', 'rho', 'seed', 'valuation',              # Phase 5 ENV-03 (D-PH5 SKIP-EXT, mirroring D-67)
    'abstain_per_phase',                                  # Phase 6 PLOT-01 (D-PH6 SKIP-EXT, mirroring D-67)
)
```

Plan 05 leaves the tuple itself UNCHANGED (Plan 01 already added the line). Plan 05's only edit to the SKIP_KEYS area is to consolidate the rationale comment block above it — verify that the Phase 4 + Phase 5 + Phase 6 paragraphs are present and not duplicated. If Plan 01's "Phase 6 (Strategia B...)" paragraph is already correct and non-duplicate, no change needed.

`_with_agent_full` is NOT added to SKIP_KEYS — Plan 04's output.py filter strips it BEFORE format_json serializes, so it never appears in JSON output. Regression check never sees it.

Subprocess env passthrough — Plan 05's substantive edit to regression_check.py (RESEARCH §G.18 + §G.19 + Pitfall 4):

regression_check.py:101 (approximate, in `run_invocation` function):
```python
full_args = [sys.executable, str(MONOLITH), *args, '--no-agent', '--seed', '42', '--json']
try:
    result = subprocess.run(
        full_args,
        cwd=str(PROJECT_ROOT),
        capture_output=True,
        text=True,
        check=True,
    )
```

Plan 05 modifies to:
```python
full_args = [sys.executable, str(MONOLITH), *args, '--no-agent', '--seed', '42', '--json']
try:
    result = subprocess.run(
        full_args,
        cwd=str(PROJECT_ROOT),
        capture_output=True,
        text=True,
        check=True,
        env={**os.environ, 'SPHSIM_NO_REPORT': '1'},   # Phase 6 — opt-out side effects in regression
    )
```

Need to verify `import os` is present at top of regression_check.py (it almost certainly is — used for path operations). If not, add it.

Without this edit, every `python scripts/regression_check.py` run creates 8 × 3 = 24 files in `./reports/`. With it, zero. Plan 05 also adds `rm -rf ./reports/` pre-flight in verify_phase6.sh as belt-and-suspenders.

verify_phase6.sh skeleton state after Plan 00:
- Header docstring listing 6 SCs ✓
- set -euo pipefail, cd, PY detection, trap, PASS/FAIL counters, check() function ✓
- Banner echo + 10 placeholder section banners (Pre-flight + 9 SC slots) marked "Plan 05 owns body"
- PASS=$PASS / FAIL=$FAIL summary block + exit 0/1 ✓

Plan 05 REPLACES the placeholder section banners with full check() invocations.

ROADMAP Phase 6 Success Criteria mapped to check() invocations (verbatim from ROADMAP):

SC #1 — Każde uruchomienie tworzy `./reports/<ts>/` z 3 plikami (report.md + 2 PNG):
- pre-flight `rm -rf ./reports/`
- run sph_sim.py once with SPHSIM_NO_REPORT='' (explicit unset to overcome conftest)
- assert: `ls ./reports/*/report.md | head -1 | xargs -I{} test -s {}` (non-empty)
- assert: `ls ./reports/*/decision_distribution.png | head -1 | xargs -I{} test -s {}` (non-empty)
- assert: `ls ./reports/*/kpi_timeseries.png | head -1 | xargs -I{} test -s {}` (non-empty)

SC #2 — report.md zawiera 6 sekcji + 5 wierszy KPI + baseline row:
- assert: `grep -c '^## ' ./reports/*/report.md` ≥ 6 (filter out comments per PATTERNS guidance: `grep -v '^#' file | grep -c ...` — but here we want H2 lines, so `grep -c '^## '` is the right gate; MD section headers don't count as comments)
- assert: 5 KPI rows present: `grep -E '\| (avg_val_last100|cum_val_total|avg_net_profit|delivery_ratio|avg_providers_l100)' ./reports/*/report.md | wc -l` ≥ 10 (each KPI appears in KPI table + baseline table)
- assert: baseline disclaimer present: `grep -F '08-naive-zeta-0.75-baseline.json' ./reports/*/report.md`

SC #3 — decision_distribution.png i kpi_timeseries.png to PNG-i:
- assert: PNG signature on both files (xxd first 8 bytes)

SC #4 — PNG-i linkowane jako relatywne ścieżki w report.md:
- assert: `grep -F '![Rozkład decyzji per faza](decision_distribution.png)' ./reports/*/report.md`
- assert: `grep -F '![Przebieg KPI w czasie](kpi_timeseries.png)' ./reports/*/report.md`
- assert: NO absolute paths: `! grep -E '\]\(/' ./reports/*/report.md > /dev/null` (negative grep)

SC #5 — --compare-agent dodaje delta KPI section:
- run separately: `rm -rf ./reports/` then `python sph_sim.py --strategy naive --zeta 0.5 --compare-agent --seed 42`
- assert: `grep -F '## Porównanie z RationalAgent' ./reports/*/report.md`
- assert: compare table header: `grep -F '| with-agent | bez agenta |' ./reports/*/report.md`
- assert: werdykt line present: `grep -F '**Werdykt:**' ./reports/*/report.md`

SC #6 — --json stdout zachowuje kompatybilność v1.0 (stdout = czysty JSON):
- run: `SPHSIM_NO_REPORT='' python sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 --json 2>/dev/null | python -c "import json,sys; json.loads(sys.stdin.read())"` exit 0
- regression check `PASS: 8/8` (existing regression flow extended with env passthrough)

Plus regression + test suite + REPL + opt-out gates:
- `python scripts/regression_check.py` exits 0 with `PASS: 8/8` (with env passthrough — no reports/ pollution)
- `python -m unittest discover tests/` exits 0
- `python -m unittest tests.test_report tests.test_plots tests.test_simulator_abstain` exits 0
- REPL Pitfall 2 (Phase 5 D-PH5): `printf 'run naive zeta=0.5\nexit\n' | python sph_sim.py --interactive 2>&1 | grep 'Konfiguracja środowiska'` exits 0 (still defused after Phase 6 wiring)
- REPL Pitfall 6 (Phase 6 — fake_args extended): `printf 'compare naive zeta=0.5\nexit\n' | python sph_sim.py --interactive 2>&1 | grep -F 'Raport porównawczy zapisany do:'` exits 0 (write_report wires in REPL do_compare with correct fake_args)
- Opt-out: `rm -rf ./reports/ && SPHSIM_NO_REPORT=1 python sph_sim.py ... --json > /dev/null && { [ ! -d ./reports ] || [ -z "$(ls -A ./reports)" ]; }` succeeds

Pattern for check() invocations from verify_phase5.sh (post-Plan 04 form):
```
check "DESC" "CMD || ANNOT"
```
Use $PY (the interpreter detected at script top), not literal `python`.
Pipe-safe grep pattern from verify_phase5.sh: `{ cmd || true; } | grep PATTERN > /dev/null`.
JSON assertion via inline python -c: `... | $PY -c \"import json,sys; d=json.load(sys.stdin); assert ...\"`.

For SC#1-5 the "happy path" requires a SINGLE sph_sim.py invocation followed by multiple greps on the same `./reports/<ts>/`. Use this pattern (one Run + Multiple Check approach):
```bash
echo "── 3. SC #1-4 (single-run report): kombinacja Run + 4 grep checks ──"
rm -rf ./reports/
SPHSIM_NO_REPORT='' $PY sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 --json > /dev/null 2>&1
LATEST=$(ls -dt ./reports/*/ | head -1)
[ -n "$LATEST" ] || { echo "[FAIL] preflight: no reports/ dir created"; FAIL=$((FAIL+1)); }
check "SC #1: report.md istnieje + non-empty" "test -s ${LATEST}report.md"
check "SC #1: decision_distribution.png istnieje + non-empty" "test -s ${LATEST}decision_distribution.png"
check "SC #1: kpi_timeseries.png istnieje + non-empty" "test -s ${LATEST}kpi_timeseries.png"
...
```
This minimizes subprocess overhead (1 sph_sim invocation produces artifacts for 5 sub-checks).

Final summary block format — same as verify_phase5.sh (Plan 00 already preserved):
```
echo "════════════════════════════════════════"
echo "  Phase 6 verification: PASS=$PASS / FAIL=$FAIL"
echo "════════════════════════════════════════"
if [ "$FAIL" -gt 0 ]; then
    echo "✗ Phase 6 NIE jest gotowe — popraw FAIL'e powyżej."
    exit 1
fi
echo "✓ Phase 6 ready for /gsd:verify-work"
exit 0
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend scripts/regression_check.py with subprocess env passthrough (SPHSIM_NO_REPORT=1) + consolidate SKIP_KEYS comment block</name>
  <files>scripts/regression_check.py</files>
  <read_first>
    - scripts/regression_check.py (full file — locate SKIP_KEYS area + subprocess.run call site in run_invocation)
    - .planning/phases/06-report-plots-generator/06-RESEARCH.md §G.17, §G.18, §G.19 (env passthrough + SKIP_KEYS evolution)
    - .planning/phases/06-report-plots-generator/06-PATTERNS.md §5 (JSON output extension) + §6 (SKIP_KEYS evolution)
  </read_first>
  <action>
    ── Step 1: Verify SKIP_KEYS state after Plan 01 ──

    Plan 01 added `'abstain_per_phase'` to SKIP_KEYS with a Phase 6 D-PH6 comment. Verify the tuple looks like:
    ```python
    SKIP_KEYS = (
        'veto_per_phase', 'n_vetoed_total', 'agent_enabled',  # Phase 4 D-67 Strategia B
        'K0', 'phi', 'rho', 'seed', 'valuation',              # Phase 5 ENV-03 (D-PH5 SKIP-EXT, mirroring D-67)
        'abstain_per_phase',                                  # Phase 6 PLOT-01 (D-PH6 SKIP-EXT, mirroring D-67)
    )
    ```

    AND the rationale comment block above it includes Phase 4, Phase 5, and Phase 6 paragraphs (Plan 01 added the Phase 6 paragraph). If the comment block is in good shape (3 paragraphs, no duplicates), DO NOT touch — just verify and move on. If Plan 01's intermediate added redundant text, dedupe to this exact 3-paragraph form:
    ```python
    # Phase 4 (Strategia B, D-67): trzy klucze ignorowane (veto_per_phase,
    # n_vetoed_total, agent_enabled) — wprowadzone w Phase 4 jako pola w env/metrics
    # bloku, nieobecne w baseline_v1 fixtures.
    #
    # Phase 5 (Strategia B, mirror Phase 4 D-67): pięć nowych kluczy w env bloku
    # (K0, phi, rho, seed, valuation) ignorowane przy compare z baseline_v1 fixtures —
    # fixtures są oracle dla zachowania v1.0 i nie zawierają tych pól. Pola obecne
    # tylko w actual output po Plan 03 rozszerzeniu format_json env block.
    #
    # Phase 6 (Strategia B, mirror Phase 4 D-67 + Phase 5 D-PH5): nowy klucz
    # 'abstain_per_phase' (dict per-phase ABSTAIN counts) ignorowany przy compare —
    # fixtures baseline_v1 są oracle dla v1.0 i nie zawierają tego pola. Pole obecne
    # tylko w actual output po Plan 01 rozszerzeniu simulator.run() return dict.
    ```

    ── Step 2: Add subprocess env passthrough — PRIMARY edit of Plan 05 ──

    Locate the subprocess.run call in run_invocation (around line 101 per RESEARCH §G.19; verify by grep `subprocess.run` in regression_check.py — there may be only ONE call site).

    Find:
    ```python
    result = subprocess.run(
        full_args,
        cwd=str(PROJECT_ROOT),
        capture_output=True,
        text=True,
        check=True,
    )
    ```

    Replace with:
    ```python
    # Phase 6 — opt-out side effects in regression: SPHSIM_NO_REPORT=1 forwarded
    # to every subprocess so 8 baseline runs nie tworzą 24 plików w ./reports/.
    # RESEARCH §G.18-G.19 + Pitfall 4 mitigation. os.environ inherited so
    # local dev env vars (PATH, PYTHONPATH) propagate normally.
    result = subprocess.run(
        full_args,
        cwd=str(PROJECT_ROOT),
        capture_output=True,
        text=True,
        check=True,
        env={**os.environ, 'SPHSIM_NO_REPORT': '1'},
    )
    ```

    Verify `import os` is present at top of regression_check.py. If not, add it (it almost certainly IS — used for Path operations elsewhere).

    ── Step 3: Sanity check ──

    After the edit:
    - `rm -rf ./reports/`
    - `python scripts/regression_check.py` exits 0 with `PASS: 8/8` in stderr.
    - `ls ./reports/ 2>/dev/null` returns nothing — env passthrough working.
    - `grep -c 'SPHSIM_NO_REPORT' scripts/regression_check.py` returns 1 (the one env injection — comment doesn't count if you used full word).
    - `grep -c 'abstain_per_phase' scripts/regression_check.py` returns 1 (preserved from Plan 01).
    - SKIP_KEYS tuple unchanged; no fixtures touched; backwards compat 100%.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; rm -rf ./reports/ &amp;&amp; python scripts/regression_check.py 2>&amp;1 | tee /tmp/p6_w4_reg.log | grep -E "PASS: 8/8" &amp;&amp; { [ ! -d ./reports ] || [ -z "$(ls -A ./reports 2>/dev/null)" ]; } &amp;&amp; echo "env passthrough OK: regression 8/8 + no pollution" &amp;&amp; grep -c "SPHSIM_NO_REPORT" scripts/regression_check.py | { read n; [ "$n" -ge 1 ] || { echo "SPHSIM_NO_REPORT not found in regression_check.py"; exit 1; }; }</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/regression_check.py` exits 0 with `PASS: 8/8` in stderr.
    - After regression run, `./reports/` directory either does not exist or is empty (no `<ts>/` subdirs).
    - `grep -c 'SPHSIM_NO_REPORT' scripts/regression_check.py` returns ≥ 1 (env injection in subprocess.run).
    - `grep -c "env={\*\*os.environ" scripts/regression_check.py` returns ≥ 1 (env passthrough pattern).
    - `grep -c "'abstain_per_phase'" scripts/regression_check.py` returns 1 (Plan 01 SKIP_KEYS extension preserved).
    - SKIP_KEYS tuple has 9 entries: `python -c "import sys; sys.path.insert(0,'scripts'); from regression_check import SKIP_KEYS; assert len(SKIP_KEYS) == 9, SKIP_KEYS; print(SKIP_KEYS)"` exits 0.
    - `python -m unittest discover tests/` still green (no collateral damage).
    - No `./reports/` pollution after regression run.
  </acceptance_criteria>
  <done>
    regression_check.py forwards SPHSIM_NO_REPORT=1 to every subprocess; PASS 8/8 preserved; zero ./reports/ pollution during regression; SKIP_KEYS tuple intact (9 entries, Plan 01's intermediate consolidated).
  </done>
</task>

<task type="auto">
  <name>Task 2: Flesh out scripts/verify_phase6.sh with all check() invocations covering 6 ROADMAP SCs + regression + tests + REPL + opt-out</name>
  <files>scripts/verify_phase6.sh</files>
  <read_first>
    - scripts/verify_phase6.sh (current state — Plan 00 skeleton with placeholder section banners)
    - scripts/verify_phase5.sh (full file — post-Plan-04 with all 21 SC + regression + REPL checks; analog for syntax)
    - .planning/ROADMAP.md (Phase 6 SC #1-6 verbatim wording)
    - .planning/phases/06-report-plots-generator/06-RESEARCH.md §H (Per-Task Verification Map)
    - .planning/phases/06-report-plots-generator/06-PATTERNS.md §7 (verify-script template + NEW sections to add)
  </read_first>
  <action>
    Edit `scripts/verify_phase6.sh`: REPLACE the placeholder section-banner block (the 10 echo lines from Plan 00 + the "(Wave 0 skeleton — section banners only; Plan 05 fills check() bodies)" line) with the full check() invocation set below. Keep:
    - File header docstring (Plan 00 set the 6 SC list) UNCHANGED.
    - set/cd/PY-detect/trap/PASS-FAIL/check() function definition (Plan 00) UNCHANGED.
    - Final summary block (PASS=$PASS / FAIL=$FAIL + exit 0/1) UNCHANGED.

    Insert these sections in order. Use `$PY` (not literal `python`) and follow the pipe-safe grep pattern `{ ... || true; } | grep PATTERN > /dev/null`. Quote-escape inline python `-c` snippets per verify_phase5.sh:131-145 style.

    ── 0. Pre-flight: clean ./reports/ + sanity-check that LATEST will be populated ──

    ```
    echo ""
    echo "── 0. Pre-flight: cleanup ./reports/ + jeden run sph_sim do produkcji artefaktów ──"
    rm -rf ./reports/
    echo "      reports/ cleaned"

    # Generate the canonical "happy path" artifact bundle once — SC #1-4 reuse it.
    SPHSIM_NO_REPORT='' $PY sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 --json > /tmp/p6_single.json 2> /tmp/p6_single.err || {
        echo "[FAIL] preflight: sph_sim.py single-run nie udało się"
        echo "       stderr:"; sed 's/^/         /' /tmp/p6_single.err
        FAIL=$((FAIL + 1))
    }
    LATEST=$(ls -dt ./reports/*/ 2>/dev/null | head -1)
    if [ -z "$LATEST" ]; then
        echo "[FAIL] preflight: brak ./reports/<ts>/ po sph_sim.py — Plan 04 wire-in może nie działać"
        FAIL=$((FAIL + 1))
    else
        echo "      LATEST=$LATEST"
        PASS=$((PASS + 1))
    fi
    ```

    ── 1. Regression backwards compat (CLI-04 + Phase 6 SKIP_KEYS + env passthrough) ──

    ```
    echo ""
    echo "── 1. Regression backwards compat (CLI-04 z Phase 1, env passthrough Plan 05) ──"
    check "Regression: 8/8 baseline_v1 fixtures (SKIP_KEYS extended for Phase 6)" \
        "$PY scripts/regression_check.py"
    check "Regression: ./reports/ NIE zaśmiecone po regression run (env passthrough OK)" \
        "rm -rf /tmp/p6_pre_reg_state; mv ./reports /tmp/p6_pre_reg_state 2>/dev/null || true; $PY scripts/regression_check.py > /dev/null 2>&1; { [ ! -d ./reports ] || [ -z \"\$(ls -A ./reports 2>/dev/null)\" ]; }; rc=\$?; rm -rf ./reports; mv /tmp/p6_pre_reg_state ./reports 2>/dev/null || true; exit \$rc"
    ```

    ── 2. Full test suite (Phase 1-5 invariants + Phase 6 nowe) ──

    ```
    echo ""
    echo "── 2. Full test suite (Phase 1-5 invariants + Phase 6 nowe) ──"
    check "Unittest discover tests/ — wszystko zielone (Phase 1-6)" \
        "SPHSIM_NO_REPORT=1 $PY -m unittest discover tests"
    check "Phase 6 tests/test_report.py — 14 testów GREEN" \
        "SPHSIM_NO_REPORT=1 $PY -m unittest tests.test_report"
    check "Phase 6 tests/test_plots.py — 6 testów GREEN (Pillow opcjonalny)" \
        "SPHSIM_NO_REPORT=1 $PY -m unittest tests.test_plots"
    check "Phase 6 tests/test_simulator_abstain.py — 3 testy GREEN" \
        "SPHSIM_NO_REPORT=1 $PY -m unittest tests.test_simulator_abstain"
    ```

    ── 3. SC #1 — REPORT-01: 3 pliki w reports/<ts>/ ──

    ```
    echo ""
    echo "── 3. SC #1: REPORT-01 — 3 pliki w reports/<ts>/ ──"
    check "SC #1: report.md istnieje + non-empty" \
        "test -n \"$LATEST\" && test -s \"${LATEST}report.md\""
    check "SC #1: decision_distribution.png istnieje + non-empty" \
        "test -n \"$LATEST\" && test -s \"${LATEST}decision_distribution.png\""
    check "SC #1: kpi_timeseries.png istnieje + non-empty" \
        "test -n \"$LATEST\" && test -s \"${LATEST}kpi_timeseries.png\""
    check "SC #1: katalog ma EXACTLY 3 pliki (no extras)" \
        "test -n \"$LATEST\" && [ \"\$(ls \"$LATEST\" | wc -l | tr -d ' ')\" = '3' ]"
    ```

    ── 4. SC #2 — REPORT-02: 6 sekcji MD + 5 KPI rows + baseline row ──

    ```
    echo ""
    echo "── 4. SC #2: REPORT-02 — 6 sekcji MD + 5 KPI rows + baseline row ──"
    check "SC #2 (sekcje): grep '^## ' znajduje ≥6 nagłówków H2" \
        "test -n \"$LATEST\" && [ \"\$(grep -c '^## ' \"${LATEST}report.md\")\" -ge 6 ]"
    check "SC #2 (KPI): wszystkie 5 KPI keys w MD" \
        "test -n \"$LATEST\" && [ \"\$(grep -cE '\\| (avg_val_last100|cum_val_total|avg_net_profit|delivery_ratio|avg_providers_l100)' \"${LATEST}report.md\")\" -ge 5 ]"
    check "SC #2 (baseline disclaimer): fixture path obecny" \
        "test -n \"$LATEST\" && grep -F '08-naive-zeta-0.75-baseline.json' \"${LATEST}report.md\" > /dev/null"
    check "SC #2 (strategia): args.strategy w sekcji 2" \
        "test -n \"$LATEST\" && grep -F 'Strategia | \`naive\`' \"${LATEST}report.md\" > /dev/null"
    check "SC #2 (konfiguracja): nagłówek '## Konfiguracja środowiska'" \
        "test -n \"$LATEST\" && grep -F '## Konfiguracja środowiska' \"${LATEST}report.md\" > /dev/null"
    ```

    ── 5. SC #3 — PLOT-01/02: PNG signature (walidne PNG) ──

    ```
    echo ""
    echo "── 5. SC #3: PLOT-01/PLOT-02 — PNG signature walidne ──"
    check "SC #3 (PLOT-01): decision_distribution.png ma PNG signature (\\x89PNG)" \
        "test -n \"$LATEST\" && $PY -c \"import sys; data=open('${LATEST}decision_distribution.png','rb').read(8); sys.exit(0 if data == b'\\x89PNG\\r\\n\\x1a\\n' else 1)\""
    check "SC #3 (PLOT-02): kpi_timeseries.png ma PNG signature" \
        "test -n \"$LATEST\" && $PY -c \"import sys; data=open('${LATEST}kpi_timeseries.png','rb').read(8); sys.exit(0 if data == b'\\x89PNG\\r\\n\\x1a\\n' else 1)\""
    check "SC #3 (PLOT-01): decision_distribution.png > 5 KB (matplotlib render real)" \
        "test -n \"$LATEST\" && [ \"\$(stat -f%z \"${LATEST}decision_distribution.png\" 2>/dev/null || stat -c%s \"${LATEST}decision_distribution.png\")\" -ge 5000 ]"
    check "SC #3 (PLOT-02): kpi_timeseries.png > 10 KB (T=1000 line points)" \
        "test -n \"$LATEST\" && [ \"\$(stat -f%z \"${LATEST}kpi_timeseries.png\" 2>/dev/null || stat -c%s \"${LATEST}kpi_timeseries.png\")\" -ge 10000 ]"
    ```

    ── 6. SC #4 — PLOT-03: relatywne MD image links ──

    ```
    echo ""
    echo "── 6. SC #4: PLOT-03 — relatywne MD image links ──"
    check "SC #4 (link 1): ![Rozkład decyzji per faza](decision_distribution.png) obecny" \
        "test -n \"$LATEST\" && grep -F '![Rozkład decyzji per faza](decision_distribution.png)' \"${LATEST}report.md\" > /dev/null"
    check "SC #4 (link 2): ![Przebieg KPI w czasie](kpi_timeseries.png) obecny" \
        "test -n \"$LATEST\" && grep -F '![Przebieg KPI w czasie](kpi_timeseries.png)' \"${LATEST}report.md\" > /dev/null"
    check "SC #4 (negative): brak absolutnych ścieżek w MD links" \
        "test -n \"$LATEST\" && ! grep -E '\\]\\(/' \"${LATEST}report.md\" > /dev/null"
    check "SC #4 (negative): brak http:// w MD links" \
        "test -n \"$LATEST\" && ! grep -E '\\]\\(http' \"${LATEST}report.md\" > /dev/null"
    ```

    ── 7. SC #5 — REPORT-03: --compare-agent dodaje sekcję 7 ──

    ```
    echo ""
    echo "── 7. SC #5: REPORT-03 — --compare-agent dodaje delta KPI section ──"
    rm -rf ./reports/
    SPHSIM_NO_REPORT='' $PY sph_sim.py --strategy naive --zeta 0.5 --compare-agent --seed 42 --json > /tmp/p6_compare.json 2> /tmp/p6_compare.err || true
    LATEST_C=$(ls -dt ./reports/*/ 2>/dev/null | head -1)
    check "SC #5 (preflight): compare-mode utworzyl reports/<ts>/" \
        "test -n \"$LATEST_C\" && test -s \"${LATEST_C}report.md\""
    check "SC #5 (sekcja 7): '## Porównanie z RationalAgent' obecna" \
        "test -n \"$LATEST_C\" && grep -F '## Porównanie z RationalAgent' \"${LATEST_C}report.md\" > /dev/null"
    check "SC #5 (delta table header): '| with-agent | bez agenta |' obecny" \
        "test -n \"$LATEST_C\" && grep -F '| with-agent | bez agenta |' \"${LATEST_C}report.md\" > /dev/null"
    check "SC #5 (werdykt): linia '**Werdykt:**' obecna" \
        "test -n \"$LATEST_C\" && grep -F '**Werdykt:**' \"${LATEST_C}report.md\" > /dev/null"
    check "SC #5 (PNG present): kpi_timeseries.png z compare > 10 KB (with_agent history threading)" \
        "test -n \"$LATEST_C\" && [ \"\$(stat -f%z \"${LATEST_C}kpi_timeseries.png\" 2>/dev/null || stat -c%s \"${LATEST_C}kpi_timeseries.png\")\" -ge 10000 ]"
    ```

    ── 8. SC #6 — JSON stdout cleanliness (Pitfall 3) ──

    ```
    echo ""
    echo "── 8. SC #6: JSON stdout cleanliness (Pitfall 3 — banner na stderr) ──"
    check "SC #6 (--json stdout): pure JSON parseable mimo banner-on-stderr" \
        "rm -rf ./reports/; SPHSIM_NO_REPORT='' $PY sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 --json 2>/dev/null | $PY -c 'import json,sys; json.loads(sys.stdin.read())'"
    check "SC #6 (banner): 'Raport zapisany do:' obecny w stderr (NIE stdout)" \
        "rm -rf ./reports/; SPHSIM_NO_REPORT='' $PY sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 --json 2>&1 1>/dev/null | grep -F 'Raport zapisany do:' > /dev/null"
    check "SC #6 (env keys): JSON output zawiera abstain_per_phase w metrics (Plan 01)" \
        "SPHSIM_NO_REPORT=1 $PY sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 --json | $PY -c \"import json,sys; d=json.load(sys.stdin)['metrics']; assert 'abstain_per_phase' in d, sorted(d.keys()); print('abstain_per_phase keys:', sorted(d['abstain_per_phase'].keys()))\""
    check "SC #6 (no leak): _with_agent_full NIE w compare JSON output" \
        "rm -rf ./reports/; SPHSIM_NO_REPORT=1 $PY sph_sim.py --strategy naive --zeta 0.5 --compare-agent --seed 42 --json | $PY -c \"import json,sys; d=json.load(sys.stdin); assert '_with_agent_full' not in d, sorted(d.keys()); print('private key correctly stripped')\""
    ```

    ── 9. REPL Pitfalls defused (Phase 5 D-PH5 + Phase 6 D-PH6) ──

    ```
    echo ""
    echo "── 9. REPL Pitfall 2 (Phase 5) + Pitfall 6 (Phase 6 fake_args) defused ──"
    check "REPL Pitfall 2: 'run naive zeta=0.5' nie crashe; output zawiera 'Konfiguracja środowiska'" \
        "printf 'run naive zeta=0.5\\nexit\\n' | SPHSIM_NO_REPORT=1 $PY sph_sim.py --interactive 2>&1 | grep 'Konfiguracja środowiska' > /dev/null"
    check "REPL Pitfall 2: 'compare naive zeta=0.5' nie crashe; output zawiera 'PORÓWNANIE'" \
        "printf 'compare naive zeta=0.5\\nexit\\n' | SPHSIM_NO_REPORT=1 $PY sph_sim.py --interactive 2>&1 | grep 'PORÓWNANIE' > /dev/null"
    check "REPL Pitfall 6: 'run naive zeta=0.5' z opt-out NIE generuje pliku ale fake_args ma wszystkie pola" \
        "printf 'run naive zeta=0.5\\nexit\\n' | SPHSIM_NO_REPORT=1 $PY sph_sim.py --interactive 2>&1 | { ! grep 'AttributeError' > /dev/null; }"
    check "REPL Pitfall 6: 'compare naive zeta=0.5' z opt-out NIE crashe na AttributeError" \
        "printf 'compare naive zeta=0.5\\nexit\\n' | SPHSIM_NO_REPORT=1 $PY sph_sim.py --interactive 2>&1 | { ! grep 'AttributeError' > /dev/null; }"
    ```

    ── 10. Opt-out: SPHSIM_NO_REPORT=1 NIE tworzy plików ──

    ```
    echo ""
    echo "── 10. Opt-out: SPHSIM_NO_REPORT=1 → brak side effects ──"
    check "Opt-out (CLI single): NIE tworzy reports/<ts>/ pod SPHSIM_NO_REPORT=1" \
        "rm -rf ./reports; SPHSIM_NO_REPORT=1 $PY sph_sim.py --strategy naive --zeta 0.5 --no-agent --seed 42 --json > /dev/null 2>&1 && { [ ! -d ./reports ] || [ -z \"\$(ls -A ./reports 2>/dev/null)\" ]; }"
    check "Opt-out (CLI compare): NIE tworzy reports/<ts>/ pod SPHSIM_NO_REPORT=1 + --compare-agent" \
        "rm -rf ./reports; SPHSIM_NO_REPORT=1 $PY sph_sim.py --strategy naive --zeta 0.5 --compare-agent --seed 42 --json > /dev/null 2>&1 && { [ ! -d ./reports ] || [ -z \"\$(ls -A ./reports 2>/dev/null)\" ]; }"
    check "Opt-out (regression): scripts/regression_check.py nie zaśmieca reports/" \
        "rm -rf ./reports; $PY scripts/regression_check.py > /dev/null 2>&1 && { [ ! -d ./reports ] || [ -z \"\$(ls -A ./reports 2>/dev/null)\" ]; }"
    ```

    After insertion, total PASS count target: ≥ 20.

    Counting:
    - Pre-flight: 1
    - Section 1: 2
    - Section 2: 4
    - Section 3: 4
    - Section 4: 5
    - Section 5: 4
    - Section 6: 5 (SC#5 sub-checks)
    - Section 7 (SC#6): 4
    - Section 8 (REPL): 4
    - Section 9 (opt-out): 3

    Sum: 1 + 2 + 4 + 4 + 5 + 4 + 5 + 4 + 4 + 3 = 36 checks. Target ≥ 20 — generous margin.

    After saving, ensure file remains executable: `chmod +x scripts/verify_phase6.sh`.

    The check() definitions and PASS/FAIL summary tail (Plan 00) remain unchanged — only the inner section between the `=== Phase 6: Report + plots generator — verification ===` banner and the final `if [ "$FAIL" -gt 0 ]` block is replaced.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; chmod +x scripts/verify_phase6.sh &amp;&amp; rm -rf ./reports/ &amp;&amp; bash scripts/verify_phase6.sh 2>&amp;1 | tee /tmp/p6_verify.out | tail -15 &amp;&amp; grep -E "Phase 6 verification: PASS=([2-9][0-9]|[1-9][0-9]{2,}) / FAIL=0" /tmp/p6_verify.out &amp;&amp; grep -E "Phase 6 ready for /gsd:verify-work" /tmp/p6_verify.out</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/verify_phase6.sh` exits 0.
    - Output contains literal `Phase 6 verification: PASS=N / FAIL=0` where N ≥ 20 (regex `PASS=([2-9][0-9]|[1-9][0-9]{2,}) / FAIL=0`).
    - Output contains literal line `✓ Phase 6 ready for /gsd:verify-work`.
    - `grep -cE '^check ' scripts/verify_phase6.sh` returns ≥ 30 (counting only top-level check invocations — target is 36, must be ≥ 30).
    - All 6 SCs covered: `grep -c 'SC #1' scripts/verify_phase6.sh` ≥ 4; `grep -c 'SC #2' scripts/verify_phase6.sh` ≥ 5; `grep -c 'SC #3' scripts/verify_phase6.sh` ≥ 4; `grep -c 'SC #4' scripts/verify_phase6.sh` ≥ 4; `grep -c 'SC #5' scripts/verify_phase6.sh` ≥ 5; `grep -c 'SC #6' scripts/verify_phase6.sh` ≥ 4.
    - `test -x scripts/verify_phase6.sh` (executable bit preserved).
    - `python scripts/regression_check.py` separately exits 0 with `PASS: 8/8` (independent re-verify of Task 1).
    - No leftover `./reports/` after the full verify run (gate cleans up via section 10 opt-out checks).
    - Section banners match Plan 00 structure: 10 numbered sections (── 0. through ── 10.).
  </acceptance_criteria>
  <done>
    verify_phase6.sh has all 6 ROADMAP SCs + regression + test suite + REPL Pitfall 2/6 + opt-out covered as ≥30 check() invocations; running the script exits 0 with PASS≥20 and FAIL=0; final line states "Phase 6 ready for /gsd:verify-work"; file remains chmod +x.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → verify script | Local exit gate; no external input. Runs developer's own code. |
| Developer → regression_check.py | Same; deep_diff runs only on local fixtures + local subprocess output. |
| env passthrough in subprocess.run | Inherits parent env; only SPHSIM_NO_REPORT=1 is injected. No secret material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-05-FP | Spoofing (false-PASS) | SKIP_KEYS hides a real regression in abstain_per_phase values | accept | abstain_per_phase is NEWLY introduced — no v1.0 baseline to regress against (Strategia B mirror Phase 4 D-67). Per-phase abstain values are asserted in tests/test_simulator_abstain.py — that's the regression net for THIS field. |
| T-6-05-INJ | Injection (shell) | verify_phase6.sh executes user-supplied flags as part of check() commands | accept | All flags in the script are hard-coded literals authored by the developer. No untrusted input crosses any check() boundary. |
| T-6-05-LEAK | Info Disclosure | env passthrough leaks parent shell secrets into subprocess | accept | subprocess.run with env={**os.environ, ...} inherits parent env — same as subprocess defaults if env keyword is OMITTED. No new attack surface; SPHSIM_NO_REPORT=1 is the only injection. |
| T-6-05-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new packages in Plan 05. |
</threat_model>

<verification>
After both tasks complete, run the full gate:

```bash
cd "$(git rev-parse --show-toplevel)"
rm -rf ./reports/
python scripts/regression_check.py                    # PASS 8/8 + no ./reports/ pollution
bash scripts/verify_phase6.sh                          # PASS ≥20 / FAIL=0
SPHSIM_NO_REPORT=1 python -m unittest discover tests/ # full suite OK
{ [ ! -d ./reports ] || [ -z "$(ls -A ./reports 2>/dev/null)" ]; } && echo "FINAL: no pollution after gate"
```

If all four exit 0 and no pollution, Phase 6 is ready for `/gsd:verify-work`.
</verification>

<success_criteria>
1. regression_check.py forwards SPHSIM_NO_REPORT=1 to subprocess; PASS 8/8 preserved; zero ./reports/ pollution during regression.
2. regression_check.py SKIP_KEYS has 9 entries (Phase 4 × 3 + Phase 5 × 5 + Phase 6 × 1); rationale comment block has 3 paragraphs (no duplicates).
3. verify_phase6.sh runs end-to-end and exits 0 with PASS ≥ 20 / FAIL = 0.
4. All 6 ROADMAP Phase 6 SCs explicitly verified by check() invocations.
5. REPL Pitfall 2 (Phase 5 D-PH5) + Pitfall 6 (Phase 6 fake_args coverage) explicitly verified by piped subprocess checks.
6. SPHSIM_NO_REPORT=1 opt-out verified for: CLI single, CLI compare, regression_check.py.
7. Phase 1-5 + Plan 01-04 test suite remains green; no regressions in earlier phases.
8. After full gate run, no ./reports/ pollution in project root.
</success_criteria>

<output>
Create `.planning/phases/06-report-plots-generator/06-05-SUMMARY.md` when done. Include:
- regression_check.py final diff (env passthrough — 1 line added; SKIP_KEYS comment block — 0 lines or dedupe noted)
- Count of check() invocations added to verify_phase6.sh (target ≥30, baseline 36)
- Captured PASS/FAIL counts from a real verify_phase6.sh run
- Confirmation all 6 ROADMAP SCs + REPL Pitfalls 2/6 + opt-out are covered
- Confirmation no ./reports/ pollution after full gate
- Suggested commit message: `feat(06-05): wave 4 — regression env passthrough + verify_phase6.sh exit gate (SC#1-6 + REPL + opt-out)`

After Plan 05's SUMMARY is written, also create `.planning/phases/06-report-plots-generator/06-VERIFICATION.md` (mirror Phase 5 VERIFICATION.md shape) summarizing the full Phase 6 verification state ready for `/gsd:verify-work` handoff.
</output>
