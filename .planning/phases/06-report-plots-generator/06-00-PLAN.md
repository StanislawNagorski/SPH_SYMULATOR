---
phase: 06-report-plots-generator
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_report.py
  - tests/test_plots.py
  - tests/test_simulator_abstain.py
  - tests/conftest.py
  - .gitignore
  - scripts/verify_phase6.sh
autonomous: true
requirements: [REPORT-01, REPORT-02, REPORT-03, PLOT-01, PLOT-02, PLOT-03]
must_haves:
  truths:
    - "tests/test_report.py exists with all REPORT-* test classes as skip-stubs that import without errors"
    - "tests/test_plots.py exists with all PLOT-* test classes as skip-stubs (split from test_report.py so Wave 2 plans 02/03 have zero file overlap)"
    - "tests/test_simulator_abstain.py exists with stub class for the abstain_per_phase data-gap fix (Wave 1 target)"
    - "tests/conftest.py forces SPHSIM_NO_REPORT=1 for every test process so subsequent waves cannot litter ./reports/ during subprocess.run() calls"
    - ".gitignore lists reports/ so accidental local runs do not taint commits"
    - "scripts/verify_phase6.sh exists, is chmod +x, runs end-to-end producing PASS=0 / FAIL=0 on empty body"
    - "Subsequent waves (1-4) have concrete test class names + verify-script slots to GREEN against — zero scavenger-hunt"
  artifacts:
    - path: "tests/test_report.py"
      provides: "Stub container for markdown-side test classes (TestReportFiles, TestReportSections, TestReportCompareMode, TestPlotLinks, TestJsonStdoutClean)"
      contains: "class TestReportFiles"
      min_lines: 80
    - path: "tests/test_plots.py"
      provides: "Stub container for PNG-side test classes (TestPlots, TestPlotDimensions)"
      contains: "class TestPlots"
      min_lines: 40
    - path: "tests/test_simulator_abstain.py"
      provides: "Stub container for abstain_per_phase aggregation tests (TestSimulatorAbstain)"
      contains: "class TestSimulatorAbstain"
      min_lines: 30
    - path: "tests/conftest.py"
      provides: "Session-scoped enforcement of SPHSIM_NO_REPORT=1 for unittest discover runs"
      contains: "SPHSIM_NO_REPORT"
    - path: ".gitignore"
      provides: "reports/ ignored so single-run side effects do not leak into git"
      contains: "reports/"
    - path: "scripts/verify_phase6.sh"
      provides: "Phase-6 exit-gate skeleton with check() helper, /tmp/p6_* trap, PASS/FAIL counters, Polish banners"
      contains: "PASS=$((PASS + 1))"
      min_lines: 55
  key_links:
    - from: "tests/conftest.py"
      to: "os.environ['SPHSIM_NO_REPORT']"
      via: "module-level assignment before any test imports"
      pattern: "SPHSIM_NO_REPORT"
    - from: "scripts/verify_phase6.sh"
      to: "verify_phase5.sh skeleton (verbatim copy with p5_→p6_ + Phase 5→Phase 6 swap)"
      via: "check() helper, set -euo pipefail, trap cleanup"
      pattern: "/tmp/p6_"
---

<objective>
Scaffold the Wave 0 test containers (`tests/test_report.py`, `tests/test_plots.py`, `tests/test_simulator_abstain.py`), the env-var enforcement (`tests/conftest.py`), the gitignore entry (`reports/`), and the exit-gate skeleton (`scripts/verify_phase6.sh`) so Waves 1-4 have concrete file paths, class names, and gate slots to write against. NO Phase 6 feature code lands in this plan — only stubs that import cleanly, skip with explicit Wave-N reasons, and a verify-script that exits 0 on an empty body.

Purpose: Per `06-VALIDATION.md` Wave 0 contract + `06-RESEARCH.md` §M Wave 0 row, Nyquist compliance requires the test scaffolding to exist BEFORE implementation waves run. Two extra discipline points specific to Phase 6 (vs Phase 5 W0):
  (a) Test pollution prophylaxis — without `tests/conftest.py` forcing `SPHSIM_NO_REPORT=1`, every subprocess-based test in `test_args_agent_flags.py`, `test_agent.py`, `test_env.py`, etc. would create a `./reports/<ts>/` directory after Wave 3 lands. `conftest.py` is the cheapest insurance.
  (b) Test-file split to unlock Wave 2 parallelism — Plans 02 (`markdown.py`) and 03 (`plots.py`) MUST execute in parallel (per RESEARCH §M Wave 2). If both wrote into `tests/test_report.py`, executors in separate worktrees would conflict on merge. Wave 0 pre-splits: `tests/test_report.py` (markdown side) vs `tests/test_plots.py` (PNG side).

Output: 6 files created (5 new tests/* + 1 new script). Zero production code. `python -m unittest discover tests/` exits 0 with new skip-stubs counted but full Phase 5 suite still green. `bash scripts/verify_phase6.sh` exits 0 with PASS=0 / FAIL=0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-report-plots-generator/06-RESEARCH.md
@.planning/phases/06-report-plots-generator/06-PATTERNS.md
@.planning/phases/06-report-plots-generator/06-VALIDATION.md
@tests/test_env.py
@tests/test_agent.py
@scripts/verify_phase5.sh
@.gitignore

<interfaces>
Test-class taxonomy locked by 06-VALIDATION.md per-task verification map AND the W2-parallelism split decision:

| Class name              | File                       | Plan that GREENs it | Covers              |
|-------------------------|----------------------------|---------------------|---------------------|
| TestReportFiles         | tests/test_report.py       | 04                  | REPORT-01 (mkdir + collision + env-var opt-out) |
| TestReportSections      | tests/test_report.py       | 02                  | REPORT-02 sections 1-6 |
| TestReportCompareMode   | tests/test_report.py       | 04                  | REPORT-03 section 7 (compare delta) |
| TestPlotLinks           | tests/test_report.py       | 02                  | PLOT-03 (![alt](decision_distribution.png) presence) |
| TestJsonStdoutClean     | tests/test_report.py       | 04                  | SC#6 (--json stdout still parses as JSON; banner on stderr) |
| TestPlots               | tests/test_plots.py        | 03                  | PLOT-01 + PLOT-02 (PNG files exist, non-zero size, valid PNG signature) |
| TestPlotDimensions      | tests/test_plots.py        | 03                  | PLOT-02 (history T=1000 not truncated — verify width via PNG IHDR or os.path.getsize > threshold) |
| TestSimulatorAbstain    | tests/test_simulator_abstain.py | 01             | PLOT-01 data gap (abstain_per_phase aggregation correctness) |

verify_phase6.sh check-slot taxonomy (Plan 05 will FILL these; Plan 00 inserts the section banners only):

| Slot              | Banner section                                              | Plan that fills body |
|-------------------|-------------------------------------------------------------|----------------------|
| 0. Pre-flight     | rm -rf ./reports/ (cleanup for deterministic checks)         | 05                   |
| 1. Regression     | scripts/regression_check.py (SKIP_KEYS + env passthrough)    | 05                   |
| 2. Test suite     | unittest discover tests/                                     | 05                   |
| 3. SC#1 REPORT-01 | ls ./reports/<ts>/{report.md,*.png}                          | 05                   |
| 4. SC#2 REPORT-02 | grep section headers + KPI rows + baseline row              | 05                   |
| 5. SC#3 PLOT-01/02 | PNG file existence + non-zero size                          | 05                   |
| 6. SC#4 PLOT-03   | grep relative MD image links in report.md                    | 05                   |
| 7. SC#5 REPORT-03 | --compare-agent run produces delta-KPI section               | 05                   |
| 8. SC#6 JSON      | --json stdout parses as JSON despite banner on stderr        | 05                   |
| 9. Opt-out        | SPHSIM_NO_REPORT=1 → no reports/ dir created                 | 05                   |

verify_phase5.sh skeleton precedent (scripts/verify_phase5.sh lines 1-65 — RESEARCH §L verbatim) — Plan 00 mirrors this with `p5_` → `p6_` token swap + Phase 5 → Phase 6 swap + the cleanup `trap` namespace bump.

Subprocess-test helper pattern (tests/test_env.py:1-30 — `_PROJECT_ROOT`, `MONOLITH`, `_run_sph`) — re-used verbatim in all 3 new test files for consistency.

conftest.py — first-of-its-kind in this repo (verified: `ls tests/conftest.py` → does not exist). Minimal stdlib-only module-level body: `import os; os.environ['SPHSIM_NO_REPORT'] = '1'`. unittest discovery imports `tests.__init__` (if present) and `tests.conftest` indirectly via test modules — actually unittest does NOT auto-import conftest the way pytest does. RESEARCH §G.18 recommends two approaches: (a) module-level setUp in tests/__init__.py, (b) explicit setUp in each TestCase. SAFER + simpler: use `tests/__init__.py` for the env-var assignment (Python imports `tests.X` → triggers `tests/__init__.py`), AND keep `tests/conftest.py` as a fallback for any future pytest adoption. This plan creates BOTH files but the EFFECTIVE enforcement vehicle for unittest is `tests/__init__.py`. NOTE: `tests/__init__.py` may already exist — if yes, EXTEND it (append os.environ line); if no, CREATE it. Verify state first.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_report.py + tests/test_plots.py + tests/test_simulator_abstain.py stub containers</name>
  <files>tests/test_report.py, tests/test_plots.py, tests/test_simulator_abstain.py</files>
  <read_first>
    - tests/test_env.py (lines 1-30 — header boilerplate: imports, _PROJECT_ROOT, MONOLITH, _run_sph helper)
    - tests/test_agent.py (lines 1-36 — subprocess + Path import pattern; also lines 45-105 for _stub_* + counter assertion style)
    - .planning/phases/06-report-plots-generator/06-VALIDATION.md (Per-Task Verification Map + Requirement → Test Coverage Map)
    - .planning/phases/06-report-plots-generator/06-PATTERNS.md §2 sub-section "tests/test_report.py → analog tests/test_env.py:1-336"
  </read_first>
  <action>
    Create three new test files. ALL three share the same boilerplate header (copy from tests/test_env.py:1-30 with file-specific docstring + the additional `_REPORTS_DIR = PROJECT_ROOT / 'reports'` constant for the report files).

    Common header for each file (verbatim apart from docstring + last constant):
    ```python
    """
    <FILE-SPECIFIC DOCSTRING — see below>
    Stdlib only: unittest + subprocess + json + os + sys + tempfile + pathlib.
    """
    import json, os, subprocess, sys, unittest
    from pathlib import Path

    _PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), os.pardir))
    if _PROJECT_ROOT not in sys.path:
        sys.path.insert(0, _PROJECT_ROOT)

    PROJECT_ROOT = Path(_PROJECT_ROOT)
    MONOLITH = PROJECT_ROOT / 'sph_sim.py'


    def _run_sph(*args, **kwargs):
        """Subprocess helper — uruchamia sph_sim.py z cwd=_PROJECT_ROOT (mirror tests/test_env.py)."""
        env = {**os.environ, 'SPHSIM_NO_REPORT': kwargs.pop('SPHSIM_NO_REPORT', '1')}
        return subprocess.run(
            [sys.executable, 'sph_sim.py', *args],
            cwd=_PROJECT_ROOT,
            capture_output=True,
            text=True,
            env=env,
            **kwargs,
        )
    ```
    Note: the `_run_sph` helper accepts an `SPHSIM_NO_REPORT` kwarg (default `'1'`) so tests that actually need a report (Plan 04 entry-point tests) can pass `SPHSIM_NO_REPORT=''` to disable the opt-out for that one subprocess.

    ── File 1: tests/test_report.py ──

    Docstring: `"Unit i integration tests dla Phase 6 — markdown side (REPORT-01..03, PLOT-03, SC#6). Klasy: TestReportFiles, TestReportSections, TestReportCompareMode, TestPlotLinks, TestJsonStdoutClean."`

    After the common header, define exactly FIVE classes, each `unittest.TestCase` subclass, each with one `test_placeholder(self)` method whose body is `self.skipTest("Wave N — <plan-id> — <one-line scope>")`. Class-level docstrings in Polish naming the SC/requirement they cover.

    Classes in order:

    1. `class TestReportFiles(unittest.TestCase):` — covers REPORT-01.
       Docstring: `"REPORT-01: ./reports/<ts>/ utworzony z 3 plikami; env var SPHSIM_NO_REPORT=1 opt-out; mkdir collision suffiks -N."`
       `def test_placeholder(self): self.skipTest("Wave 3 — Plan 04 — REPORT-01 entry-point + mkdir + opt-out wiring")`

    2. `class TestReportSections(unittest.TestCase):` — covers REPORT-02.
       Docstring: `"REPORT-02: report.md zawiera 6 sekcji H2 (Konfiguracja, Strategia, KPI, Rozkład, Wykresy, Baseline) + 5 wierszy KPI + baseline row."`
       `def test_placeholder(self): self.skipTest("Wave 2 — Plan 02 — markdown.py render_report sekcje 1-6")`

    3. `class TestReportCompareMode(unittest.TestCase):` — covers REPORT-03.
       Docstring: `"REPORT-03: --compare-agent dodaje sekcję 7 z delta KPI (with-agent vs without-agent) + werdykt agent_helps."`
       `def test_placeholder(self): self.skipTest("Wave 3 — Plan 04 — compare-mode wiring + render_compare_section")`

    4. `class TestPlotLinks(unittest.TestCase):` — covers PLOT-03.
       Docstring: `"PLOT-03: report.md zawiera relatywne MD image links ![Rozkład decyzji](decision_distribution.png) + ![Przebieg KPI](kpi_timeseries.png)."`
       `def test_placeholder(self): self.skipTest("Wave 2 — Plan 02 — _render_plots_section emits relative MD links")`

    5. `class TestJsonStdoutClean(unittest.TestCase):` — covers SC#6 (stdout cleanliness despite report-banner).
       Docstring: `"SC#6 (kompatybilność v1.0): --json stdout parsuje się jako JSON nawet gdy report banner trafia na stderr — Pitfall 3 mitigation."`
       `def test_placeholder(self): self.skipTest("Wave 3 — Plan 04 — banner-on-stderr wiring + Plan 05 verify_phase6.sh JSON check")`

    Footer: `if __name__ == '__main__': unittest.main()`.

    ── File 2: tests/test_plots.py ──

    Docstring: `"Unit i integration tests dla Phase 6 — PNG side (PLOT-01, PLOT-02). Klasy: TestPlots, TestPlotDimensions. Wydzielony z test_report.py żeby Plan 02/03 mogły lądować parallel bez konfliktu mergem."`

    After the common header, define exactly TWO classes:

    1. `class TestPlots(unittest.TestCase):` — covers PLOT-01 + PLOT-02 (file existence + non-zero size).
       Docstring: `"PLOT-01/PLOT-02: oba PNG istnieją w katalogu raportu, mają >0 bajtów, mają walidny PNG header (b'\\x89PNG\\r\\n\\x1a\\n')."`
       `def test_placeholder(self): self.skipTest("Wave 2 — Plan 03 — plots.py matplotlib Agg + 2 plot functions + close-figure discipline")`

    2. `class TestPlotDimensions(unittest.TestCase):` — covers PLOT-02 (history not truncated).
       Docstring: `"PLOT-02 detail: kpi_timeseries.png ma dimensions współmierne do figsize=(10,5) dpi=120 (≥1000×500 px). Pillow opcjonalne — fallback to file size >5 KB jako proxy."`
       `def test_placeholder(self): self.skipTest("Wave 2 — Plan 03 — PNG dim probe via Pillow or size threshold")`

    Footer: `if __name__ == '__main__': unittest.main()`.

    ── File 3: tests/test_simulator_abstain.py ──

    Docstring: `"Unit tests dla Phase 6 data gap fix (PLOT-01 input): abstain_per_phase aggregation. Mirror Phase 4 D-64 veto_phase_stats pattern. Klasa: TestSimulatorAbstain."`

    After the common header, define ONE class:

    1. `class TestSimulatorAbstain(unittest.TestCase):`
       Docstring: `"PLOT-01 data gap: Device.abstain_phase_stats per-phase counter + simulator.run() aggregation do result['abstain_per_phase']. Mirror Phase 4 D-64."`
       `def test_placeholder(self): self.skipTest("Wave 1 — Plan 01 — device.py + simulator.py inkrementacja + agregacja")`

    Footer: `if __name__ == '__main__': unittest.main()`.

    All three files MUST be importable without errors (`python -c "import tests.test_report"` etc.), MUST report only `skipped` entries when run via `python -m unittest`, and MUST NOT touch any production code.

    Stdlib-only: no pytest, no third-party imports. Polish docstrings everywhere (Phase 6 Polish-language convention HARD requirement).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; SPHSIM_NO_REPORT=1 python -m unittest tests.test_report tests.test_plots tests.test_simulator_abstain -v 2>&amp;1 | tee /tmp/p6_w0_t1.log | grep -E "OK \(skipped=8\)|skipped=8" || { echo "expected 8 skipped tests, got:"; tail -20 /tmp/p6_w0_t1.log; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - Three new files exist: tests/test_report.py, tests/test_plots.py, tests/test_simulator_abstain.py.
    - `python -c "import tests.test_report; import tests.test_plots; import tests.test_simulator_abstain"` exits 0 with no output.
    - `SPHSIM_NO_REPORT=1 python -m unittest tests.test_report tests.test_plots tests.test_simulator_abstain -v` exits 0; total skipped = 8 (5 + 2 + 1).
    - `grep -cE '^class Test' tests/test_report.py` = 5.
    - `grep -cE '^class Test' tests/test_plots.py` = 2.
    - `grep -cE '^class Test' tests/test_simulator_abstain.py` = 1.
    - All 5 expected class names present in test_report.py via `grep -E '^class (TestReportFiles|TestReportSections|TestReportCompareMode|TestPlotLinks|TestJsonStdoutClean)\b' tests/test_report.py | wc -l` = 5.
    - Both PLOT-side class names present: `grep -E '^class (TestPlots|TestPlotDimensions)\b' tests/test_plots.py | wc -l` = 2.
    - TestSimulatorAbstain present: `grep -c '^class TestSimulatorAbstain' tests/test_simulator_abstain.py` = 1.
    - No `pytest` or non-stdlib imports: `grep -E 'import pytest|from pytest' tests/test_report.py tests/test_plots.py tests/test_simulator_abstain.py | wc -l` = 0.
    - Full Phase 5 test suite still green: `SPHSIM_NO_REPORT=1 python -m unittest discover tests/` exits 0.
  </acceptance_criteria>
  <done>
    Three test files created; 8 total skipped tests across them; all class names from VALIDATION.md per-task map are grep-discoverable; Phase 5 suite unaffected.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tests/conftest.py + ensure tests/__init__.py forces SPHSIM_NO_REPORT=1</name>
  <files>tests/conftest.py, tests/__init__.py</files>
  <read_first>
    - .planning/phases/06-report-plots-generator/06-RESEARCH.md §G.18 (Test pollution + opt-out mechanism)
    - tests/__init__.py (verify if it already exists; ls tests/__init__.py)
  </read_first>
  <action>
    Step 1: Check state of `tests/__init__.py`:
    - If `tests/__init__.py` exists (likely empty per Python package convention), APPEND the env-var assignment.
    - If it does NOT exist, CREATE it.

    Final content of `tests/__init__.py` (whether created or extended):
    ```python
    # Test package — Phase 6 enforces SPHSIM_NO_REPORT=1 globally to prevent
    # ./reports/ pollution during subprocess-based tests (RESEARCH §G.18, Pitfall 4).
    # Module-level assignment runs once when unittest discovery imports tests.X.
    import os
    os.environ.setdefault('SPHSIM_NO_REPORT', '1')
    ```
    (Use `setdefault` not `=` so users can OVERRIDE via shell `SPHSIM_NO_REPORT=` to test the side-effect path explicitly.)

    Step 2: Create `tests/conftest.py` (NEW file) — pytest compatibility safety-net even though current test runner is unittest (RESEARCH §G.18 mentions both pathways). Content:
    ```python
    """Phase 6 — pytest conftest (compatibility safety-net).

    Sets SPHSIM_NO_REPORT=1 for any pytest-based test run so future migration
    away from unittest does not regress on the test-pollution prophylaxis.
    Current test runner is unittest; this file is harmless if pytest is not
    installed (Python simply does not import it).

    See RESEARCH §G.18 Rozwiązanie 1 — Env var (PREFEROWANE).
    """
    import os

    os.environ.setdefault('SPHSIM_NO_REPORT', '1')
    ```

    Both files are minimal (≤10 LoC) and stdlib-only.

    Step 3: Verify enforcement works — after creating these files, ANY `python -m unittest discover tests/` import path will trigger `tests/__init__.py` (because `tests` becomes an importable package), which sets the env var BEFORE any test runs. This means even subprocess-based tests in Phase 5 (test_env.py, test_args_agent_flags.py) that invoke `sph_sim.py` will pass `SPHSIM_NO_REPORT=1` IF they propagate `os.environ` to their subprocess.run() call (verify by reading tests/test_env.py:_run_sph — it passes `**kwargs` without env, so subprocess inherits parent env, which now has SPHSIM_NO_REPORT=1 set by __init__.py. Confirmed safe.)

    No other test files need modification. The new `_run_sph` helper in Task 1 also explicitly passes `env={**os.environ, 'SPHSIM_NO_REPORT': '1'}` for belt-and-suspenders safety.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; rm -rf ./reports/ &amp;&amp; python -m unittest discover tests/ -v > /tmp/p6_w0_t2.log 2>&amp;1; rc=$?; { [ ! -d ./reports ] || [ -z "$(ls -A ./reports 2>/dev/null)" ]; } || { echo "TEST POLLUTION DETECTED: ./reports/ created during test discover"; ls -la ./reports/; exit 1; }; test "$rc" -eq 0 || { echo "test suite failed; rc=$rc; last 30 lines:"; tail -30 /tmp/p6_w0_t2.log; exit 1; }; echo "OK: no test pollution, suite green"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/__init__.py` exists and contains `os.environ.setdefault('SPHSIM_NO_REPORT', '1')`.
    - `tests/conftest.py` exists and contains `os.environ.setdefault('SPHSIM_NO_REPORT', '1')`.
    - `grep -l "SPHSIM_NO_REPORT" tests/__init__.py tests/conftest.py | wc -l` = 2.
    - After `rm -rf ./reports/` followed by `python -m unittest discover tests/`, the `./reports/` directory either does not exist or is empty (no `<ts>/` subdirs created).
    - Full test suite green (exit code 0).
    - `python -c "import tests; import os; assert os.environ.get('SPHSIM_NO_REPORT') == '1', os.environ.get('SPHSIM_NO_REPORT')"` exits 0 (proves importing the package sets the env var).
  </acceptance_criteria>
  <done>
    tests/__init__.py + tests/conftest.py both force SPHSIM_NO_REPORT=1 via setdefault; full test suite runs without creating any ./reports/ subdirs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add reports/ to .gitignore + create scripts/verify_phase6.sh skeleton</name>
  <files>.gitignore, scripts/verify_phase6.sh</files>
  <read_first>
    - .gitignore (current content — confirm reports/ not yet listed)
    - scripts/verify_phase5.sh (full file — verbatim skeleton template)
    - .planning/phases/06-report-plots-generator/06-PATTERNS.md §7 (verify-script template, verbatim block + p5_→p6_ token swap rules)
    - .planning/phases/06-report-plots-generator/06-RESEARCH.md §C.7 (.gitignore impact: reports/ entry required)
  </read_first>
  <action>
    ── Step 1: Extend .gitignore ──

    Read .gitignore. Append a new section AT THE END (preserve all existing entries):
    ```
    # Phase 6 — report side effects (REPORT-01); see RESEARCH §C.7.
    # Każde uruchomienie symulacji tworzy ./reports/<timestamp>/. Lokalnie nie commitujemy.
    reports/
    ```

    Idempotency: if `reports/` is already listed (re-run safety), skip append. Verify with `grep -c '^reports/' .gitignore` before adding.

    ── Step 2: Create scripts/verify_phase6.sh ──

    Copy `scripts/verify_phase5.sh` VERBATIM to `scripts/verify_phase6.sh`, then apply these EXACT token swaps:

    1. Replace all occurrences of `/tmp/p5_` with `/tmp/p6_` (including the `trap 'rm -f /tmp/p5_*' EXIT` line and the `/tmp/p5_check.log` references inside the `check()` body).
    2. Replace all occurrences of `Phase 5` with `Phase 6` (case-sensitive, in comments, banners, summary line, blessing line).
    3. Replace all occurrences of `phase5` (lowercase) with `phase6`.
    4. Replace the header docstring SC list (verify_phase5.sh:5-28 area) with Phase 6 SC list verbatim from ROADMAP (6 SCs):
       ```
       # Phase 6 Success Criteria:
       #   SC #1: Każde uruchomienie symulacji tworzy ./reports/<ts>/ z 3 plikami (report.md + 2 PNG)
       #   SC #2: report.md zawiera sekcje: konfiguracja, strategia, KPI table, rozkład decyzji, baseline
       #   SC #3: decision_distribution.png + kpi_timeseries.png — wykresy renderują się (non-zero PNG)
       #   SC #4: PNG-i linkowane z report.md jako relatywne ścieżki ![](decision_distribution.png)
       #   SC #5: --compare-agent dodaje tabelę delta KPI (with vs without agent)
       #   SC #6: --json output zachowuje kompatybilność v1.0 (stdout = czysty JSON, banner na stderr)
       ```
    5. After the `check()` function definition, REMOVE the entire SC-body block from verify_phase5.sh (lines that contain check "SC #1" ... check "SC #4" ...). Replace with the section-banner-only placeholder block — section headers but NO `check ...` invocations yet (Plan 05 owns those):
       ```
       echo "=== Phase 6: Report + plots generator — verification ==="
       echo "Interpreter: $PY ($($PY --version 2>&1))"
       echo ""

       echo "── 0. Pre-flight: cleanup ./reports/ (Plan 05 owns body) ──"
       echo ""
       echo "── 1. Regression backwards compat (Plan 05 owns body) ──"
       echo ""
       echo "── 2. Full test suite (Plan 05 owns body) ──"
       echo ""
       echo "── 3. SC #1 REPORT-01: 3 pliki w reports/<ts>/ (Plan 05 owns body) ──"
       echo ""
       echo "── 4. SC #2 REPORT-02: sekcje MD + KPI table + baseline row (Plan 05 owns body) ──"
       echo ""
       echo "── 5. SC #3 PLOT-01/02: PNG generation (Plan 05 owns body) ──"
       echo ""
       echo "── 6. SC #4 PLOT-03: relatywne linki w MD (Plan 05 owns body) ──"
       echo ""
       echo "── 7. SC #5 REPORT-03: --compare-agent delta KPI section (Plan 05 owns body) ──"
       echo ""
       echo "── 8. SC #6 JSON stdout cleanliness (Plan 05 owns body) ──"
       echo ""
       echo "── 9. SPHSIM_NO_REPORT=1 opt-out (Plan 05 owns body) ──"
       echo ""
       echo "(Wave 0 skeleton — section banners only; Plan 05 fills check() bodies)"
       echo ""
       ```

    6. Preserve the FINAL summary block VERBATIM (with Phase 5 → Phase 6 wording swap), matching verify_phase5.sh lines 139-149:
       ```
       echo ""
       echo "════════════════════════════════════════"
       echo "  Phase 6 verification: PASS=$PASS / FAIL=$FAIL"
       echo "════════════════════════════════════════"
       if [ "$FAIL" -gt 0 ]; then
           echo "✗ Phase 6 NIE jest gotowe — popraw FAIL'e powyżej."
           exit 1
       fi
       echo "✓ Phase 6 ready for /gsd:verify-work"
       exit 0
       ```

    7. After creation: `chmod +x scripts/verify_phase6.sh`.

    DO NOT add any `check "SC #..." "..."` invocations in this plan — Plan 05 (Wave 4) owns SC body implementation. Wave 0 deliverable is a runnable skeleton that exits 0 with PASS=0 / FAIL=0 and prints all 10 section banners (Pre-flight + 9 SC slots).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; grep -c '^reports/' .gitignore | { read n; [ "$n" -eq 1 ] || { echo ".gitignore should list reports/ exactly once, got $n"; exit 1; }; } &amp;&amp; test -x scripts/verify_phase6.sh &amp;&amp; bash scripts/verify_phase6.sh > /tmp/p6_w0_t3.log 2>&amp;1 &amp;&amp; grep -E "Phase 6 verification: PASS=0 / FAIL=0" /tmp/p6_w0_t3.log &amp;&amp; grep -E "Phase 6 ready for /gsd:verify-work" /tmp/p6_w0_t3.log</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains exactly one line `reports/` (grep -c '^reports/' returns 1).
    - `scripts/verify_phase6.sh` exists and is executable (`test -x scripts/verify_phase6.sh`).
    - `bash scripts/verify_phase6.sh` exits 0.
    - Output contains literal line `Phase 6 verification: PASS=0 / FAIL=0`.
    - Output contains literal line `✓ Phase 6 ready for /gsd:verify-work`.
    - `grep -c 'p5_' scripts/verify_phase6.sh` returns 0 (no leftover Phase 5 prefixes).
    - `grep -c 'Phase 5' scripts/verify_phase6.sh` returns 0 (no leftover Phase 5 mentions).
    - `grep -c 'Phase 6' scripts/verify_phase6.sh` returns ≥ 8 (header SC list + banner + summary + blessing).
    - `grep -cE '^check ' scripts/verify_phase6.sh` returns 0 (zero check invocations — Plan 05 owns those).
    - `grep -c '── [0-9]' scripts/verify_phase6.sh` returns ≥ 9 (at least 9 section banner lines for SC slots 0-9).
    - `python -m unittest discover tests/` still green (no collateral damage).
    - `python scripts/regression_check.py` still PASS 8/8 (no collateral damage).
  </acceptance_criteria>
  <done>
    .gitignore lists reports/; verify_phase6.sh skeleton runs end-to-end, exits 0 with PASS=0/FAIL=0, contains zero `check ` invocations but ≥9 section banners + Phase 6 branding throughout.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer→test infra | No external input; stubs run only locally |
| Developer→verify script | No untrusted input; script runs with developer privileges |
| Developer→.gitignore | Local-only config; no remote attacker can modify pre-commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-00-01 | Tampering | `/tmp/p6_check.log` (verify script log) | accept | Single-user dev machine; `trap 'rm -f /tmp/p6_*' EXIT` cleans up. Log content is verify output only, zero secrets. |
| T-6-00-02 | Info Disclosure | `tests/conftest.py` accidentally logs SPHSIM_NO_REPORT value | accept | Value is hardcoded `'1'` literal; no secret material. |
| T-6-00-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — Phase 6 W0 introduces zero new packages (stdlib only). matplotlib install is Plan 03 territory. |

Phase 6 W0 introduces zero new attack surface (stubs + skeleton + gitignore + env-var defaults). T-6-01 through T-6-05 (path traversal, JSON injection via stdout, etc.) defer to Plans 02-04 where the actual filesystem writes are introduced.
</threat_model>

<verification>
After all three tasks complete:

```bash
cd "$(git rev-parse --show-toplevel)"
rm -rf ./reports/
python -m unittest tests.test_report tests.test_plots tests.test_simulator_abstain -v 2>&1 | tail -3  # 8 skipped, OK
python -m unittest discover tests/ -v 2>&1 | tail -3                                                  # Phase 5 suite still green
{ [ ! -d ./reports ] || [ -z "$(ls -A ./reports 2>/dev/null)" ]; } && echo "NO POLLUTION"             # conftest works
bash scripts/verify_phase6.sh 2>&1 | tail -5                                                          # PASS=0/FAIL=0
python scripts/regression_check.py 2>&1 | tail -3                                                     # 8/8 still
grep -c '^reports/' .gitignore                                                                        # exactly 1
```

All six MUST succeed before declaring Plan 00 done.
</verification>

<success_criteria>
1. `tests/test_report.py` exists with 5 named test classes (TestReportFiles, TestReportSections, TestReportCompareMode, TestPlotLinks, TestJsonStdoutClean), all skip-bodied.
2. `tests/test_plots.py` exists with 2 named test classes (TestPlots, TestPlotDimensions), all skip-bodied — split from test_report.py to unlock Wave 2 parallelism.
3. `tests/test_simulator_abstain.py` exists with 1 named test class (TestSimulatorAbstain), skip-bodied.
4. `tests/__init__.py` + `tests/conftest.py` both set `SPHSIM_NO_REPORT=1` via `os.environ.setdefault`; full discover run does NOT create any `./reports/<ts>/` directories.
5. `.gitignore` lists `reports/` exactly once.
6. `scripts/verify_phase6.sh` exists, is chmod +x, runs end-to-end, exits 0 with PASS=0 / FAIL=0, contains 10 section-banner slots (0 + 9 SC), zero `check ` invocations.
7. Phase 5 test suite (`python -m unittest discover tests/`) remains green — zero collateral damage.
8. Regression check (`python scripts/regression_check.py`) remains green — zero collateral damage.
</success_criteria>

<output>
Create `.planning/phases/06-report-plots-generator/06-00-SUMMARY.md` when done. Include:
- Files created (6): list paths and 1-line role
- Test class taxonomy locked: 8 classes total across 3 files, ready for Waves 1-3 to GREEN
- Confirmation that conftest.py + __init__.py prevent test pollution (empirical: tail of discover run shows no reports/ subdirs)
- Confirmation Phase 5 suite + regression_check.py still pass
- Suggested commit message: `chore(06-00): wave 0 scaffolding — test stubs + conftest opt-out + verify_phase6 skeleton + .gitignore reports/`
</output>
